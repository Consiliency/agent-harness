---
phase_loop_plan_version: 1
phase: FABPUB
roadmap: specs/phase-plans-v10.md
roadmap_sha256: 7f2590bdebf5a892cf0987b67916d2c3b95970b547117b8cbf4adc7c7220838e
automation:
  suite_command:
    - env
    - PYTHONPATH=phase-loop-runtime/src
    - python3
    - -m
    - pytest
    - phase-loop-runtime/tests
    - -q
    - -k
    - fabpub_ or test_convergence_broker_admission or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_convergence_fencing or test_publishing or test_train_prebuilt or test_convergence_train_integration
    - --junitxml=.phase-loop/runs/fabpub-final.junit.xml
---

# FABPUB: Shared Epoch Allocator — Publish Identity

## Context

FABPUB migrates every fresh `publish_committed_branch` admission from caller-stamped `lease_epoch=1` to the broker's single per-repository monotonic allocator. The phase is an independent v10 root and produces `IF-0-FABPUB-1`; FABREADMIT and RESIDUAL remain downstream and must not execute beside this phase because they share publish-identity surfaces.

The roadmap fixes four boundaries. First, `LinearizableAdmissionStore.admit_next` allocates under the existing advisory lock and enforces both values supplied to `make_request(epoch, attempt_id)`. Second, completed-effect identity remains the roadmap-frozen, base-free `publish_committed_branch_idempotency_key(repo, branch, head_sha)`; the `base` question carried by `EC-RESIDUAL-1` is not reopened. Third, approval identity is derived from the transaction's resolved post-commit head and the merge base against its frozen full `base_tip_sha`, never from moving live `HEAD` or a raw base ref after preparation. Fourth, `BrokerService.execute` performs terminal replay first, then requires every fresh or continuing `PUBLISH_COMMITTED_BRANCH` to carry the new epoch-free `PreAdmissionEnvelope` and use `admit_next`; a finalized `AdmissionRequest` remains compatible only for non-publish verbs and already-completed terminal replay. A fresh finalized publish cannot provide transaction identity or commit-stable approval and is rejected before either admission method, broker-intent persistence, or adapter execution.

The live crash hole is also part of this phase. A normal `publish_from_worktree` call currently commits and then enters the broker, but a crash in that gap leaves a clean worktree; the next normal call exits at `nothing_staged` and never reaches `BrokerService.execute`. A checkpoint written only after commit leaves the same unrecoverable kill before that write. FABPUB therefore adds a versioned, metadata-only `PublishTransaction` under the coordinator-owned root and persists its `PREPARED` intent before `git commit`. The intent binds the canonical repo/worktree identity, branch and base name, frozen full `base_tip_sha`, normal/prebuilt mode, pre-commit parent SHA, staged tree SHA, ordered owned paths and staged-diff digest, deterministic commit-message digest and `FABPUB-Intent-ID` trailer, draft/body identity, and envelope authority pre-images/digest. Its deterministic transaction ID is stable before commit. After a normal commit, recovery accepts only a current `HEAD` commit whose full parent SHA, tree SHA, and trailer all match that intent; it can therefore rediscover the exact committed head after a kill before any post-commit checkpoint write, skip staging/commit, and reject descendants, rewritten heads, or caller mismatches. Prebuilt publication uses the same transaction shape with its already-committed full head/tree identity.

The allowed durable state progression is `PREPARED -> COMMITTED_HEAD_RESOLVED -> ADMISSION_DURABLE -> BROKER_INTENT_DURABLE -> ADAPTER_STARTED -> TERMINAL_SEALED`. Every local transition is serialized by the transaction lock and persisted by temp-file write, file fsync, atomic replace, and parent-directory fsync; Git ref update, the admission append, and broker evidence appends remain their authoritative atomic records. Recovery may advance a lagging transaction projection only after exact-identity reconciliation proves the corresponding Git commit, admission record, or broker evidence already exists. The adapter-start marker is durable before the adapter effect: a crash at the preserved broker-intent arm occurs before that marker and may retry, while an `ADAPTER_STARTED` record without terminal evidence is fail-closed as outcome-ambiguous and never re-invokes the adapter. `ABANDONED` and `CONFLICTED` are terminal non-authorizing tombstones. An active mismatch is never overwritten; cleanup can archive a terminal/tombstoned transaction and clear its active pointer only under the same lock and only after the tombstone is durable. A new different-head publish then creates a fresh fully validated intent. Exact terminal records remain replayable so a whole-path retry reaches broker terminal replay instead of `nothing_staged`.

Canonical runner state is `.phase-loop/`. Legacy `.codex/phase-loop/` compatibility files have no planning authority. The tracked roadmap bytes match the frontmatter digest. The panel record at `.phase-loop/reviews/v10-fabpub-plan-panel.json` binds exact plan digest `93aae35552d2e17220e69aae211b35d4ded598c31622579ea9cf26c47b37d8b3`: Grok and Gemini returned `AGREE`, Sol returned `DISAGREE` on the fresh-finalized-publish allocator bypass, and Fable was intentionally deferred. This revision supersedes exactly that rejected compatibility premise while preserving its repaired TDD lifecycle, crash/idempotency, publication-identity, provenance, and lane-isolation contracts. The revised digest must clear the roadmap-required four-seat plan panel before SL-0.

## Interface Freeze Gates

- [ ] IF-0-FABPUB-1 — `LinearizableAdmissionStore.admit_next(make_request, *, attempt_id, precondition) -> AdmissionRecord` holds `admissions.lock` while it checks `epoch_blocked`, policy, prior records, and the in-lock precondition. On an `attempt_id` hit it rebuilds with `make_request(prior.epoch, attempt_id)`, requires `rebuilt.lease_epoch == prior.epoch` and `rebuilt.attempt_id == attempt_id`, rejects any other authority-field mismatch, and returns the original record. On a miss it allocates `(max(record.epoch) if records else 0) + 1`, calls `make_request(epoch, attempt_id)`, requires `request.lease_epoch == epoch` and `request.attempt_id == attempt_id`, and appends plus fsyncs one record. `publish_from_worktree` creates a deterministic coordinator-owned `PublishTransaction` in `PREPARED` before a normal commit, binding immutable repo/branch/base-tip/parent/tree/owned-diff/commit-trailer/mode/presentation/authority identities. A restart resolves `COMMITTED_HEAD_RESOLVED` only when current `HEAD` has the intent's exact parent, tree, and `FABPUB-Intent-ID` trailer; it never re-commits that head and never uses a moving ref. `BrokerService.execute` computes the base-free publish completed-effect key and returns any already-completed terminal-evidence replay before fresh authorization. If replay does not return—including absent evidence or a non-terminal in-flight record—`PUBLISH_COMMITTED_BRANCH` requires an epoch-free `PreAdmissionEnvelope`; a finalized `AdmissionRequest` raises exact `TypeError("fresh publish_committed_branch requires PreAdmissionEnvelope")` before policy classification, either admission method, broker-intent/evidence mutation, or adapter execution. Finalized `AdmissionRequest` retains legacy `admit()` semantics only for non-publish verbs; non-publish envelopes fail before admission. The publish envelope is legal only for `PUBLISH_COMMITTED_BRANCH`, names the exact `COMMITTED_HEAD_RESOLVED` transaction, derives `attempt_id = sha256("publish\\0" + repo + "\\0" + branch + "\\0" + transaction.committed_head_sha)`, derives approval `base_sha = merge-base(transaction.committed_head_sha, transaction.base_tip_sha)` from full frozen SHAs, rebuilds the lease/fence/idempotency fields at the allocated epoch through `admit_next`, and passes only the stored finalized request to the adapter. Thus no fresh publish can bypass broker allocation, transaction identity, or commit-stable approval. The transaction reconciles and durably projects `ADMISSION_DURABLE`, `BROKER_INTENT_DURABLE`, `ADAPTER_STARTED`, and `TERMINAL_SEALED` from the authoritative admission/evidence records; only the pre-`ADAPTER_STARTED` broker-intent state may retry adapter execution. Active mismatch, stale/ref-moved state, or an abandoned/conflicted tombstone fails closed before admission, and cleanup cannot turn it into publish authority. Terminal replay retains the separate base-free `publish_committed_branch_idempotency_key(repo, branch, head_sha)`.

- **Tests-first activation freeze**: SL-0 owns `_fabpub_tdd_guard.py`, the exact selectors, expected nodeid sets/counts, per-node RED anchors, and `PHASE_LOOP_TDD_EXPECT_FABPUB=1`. Before the production marker exists, exact activation runs every new or migrated production-dependent assertion and ordinary unactivated CI skips only the frozen newly added production-dependent nodeids. Migrated existing tests are never skipped: inactive tests-only execution runs their legacy-compatible branch against production-identical `main`, while explicit activation and the final marker run the replacement envelope/transaction branch; an obsolete assertion that a fresh finalized publish succeeds is transition-only and forbidden after activation. The final marker is `phase_loop_runtime.fabpub_capability.FABPUB_CAPABILITY_VERSION == 1`; after its marker-only commit, the same immutable tests run without activation. `xfail`, import/collection failure, and implementation edits to any frozen test/evidence surface are forbidden.
- **Merge-safe default**: Ordinary default CI remains GREEN before the marker. No collection/import failure is permitted. Final verification reruns the reviewed implementation in a fresh process at the exact candidate HEAD and reruns the landed result in a fresh process at the exact landed SHA.

## Lane Index & Dependencies

SL-0 — Governed default-green tests-only contract and chronology reducer
  Depends on: (none)
  Blocks: SL-1, SL-2, SL-3
  Parallel-safe: no

SL-1 — Shared allocator and enforced equalities
  Depends on: SL-0
  Blocks: SL-2, SL-4
  Parallel-safe: no

SL-2 — Broker fresh-publish gate and recoverable publish transaction
  Depends on: SL-0, SL-1
  Blocks: SL-3, SL-4
  Parallel-safe: no

SL-3 — Live train authority handoff and authentic resume
  Depends on: SL-0, SL-2
  Blocks: SL-4
  Parallel-safe: no

SL-4 — Documentation, capability activation, chronology, and verification reducer
  Depends on: SL-0, SL-1, SL-2, SL-3
  Blocks: (none)
  Parallel-safe: no

## Lanes

### SL-0 — Governed default-green tests-only contract and chronology reducer

- **Scope**: Land the complete FABPUB falsifier set, its deterministic test-owned activation guard, and its executable evidence/chronology reducer as one tests-and-verification-only commit that is RED only under explicit activation and remains green in ordinary default-main CI.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/fabpub_tdd_chronology.py`, `phase-loop-runtime/tests/_fabpub_tdd_guard.py`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py`, `phase-loop-runtime/tests/test_convergence_broker_admission.py`, `phase-loop-runtime/tests/test_convergence_broker_verbs.py`, `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py`, `phase-loop-runtime/tests/test_convergence_broker_credsep.py`, `phase-loop-runtime/tests/test_convergence_live_enable.py`, `phase-loop-runtime/tests/test_convergence_fencing.py`, `phase-loop-runtime/tests/test_publishing.py`, `phase-loop-runtime/tests/test_train_prebuilt.py`, `phase-loop-runtime/tests/test_convergence_train_integration.py`
- **Interfaces provided**: `FABPUB immutable test digest`, `PHASE_LOOP_TDD_EXPECT_FABPUB=1`, `FABPUB_NEW_PRODUCTION_NODEIDS`, `FABPUB_MIGRATED_NODEIDS`, `FABPUB_RED_NODEIDS`, `FABPUB_EXPECTED_NODEIDS`, frozen nodeid counts/selectors/RED anchors, `fabpub_tdd_chronology executable`, `fresh-publish envelope gate`, `finalized-admission compatibility matrix`
- **Interfaces consumed**: `plan-frozen FABPUB contract` (pre-existing), `AdmissionRequest` (pre-existing), `BrokerRequest` (pre-existing), `LinearizableAdmissionStore` (pre-existing), `BrokerService` (pre-existing), `publish_from_worktree` (pre-existing), `_default_build_admission` (pre-existing)
- **Parallel-safe**: no; the exact test/guard/reducer digest must clear the governed four-seat test panel and land as one default-green tests-only commit before any production writer starts.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-0-T1` | `test` | (none) | SL-0 owned set | exact `FABPUB_RED_NODEIDS` | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_ or test_convergence_broker_admission or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_convergence_fencing or test_publishing or test_train_prebuilt or test_convergence_train_integration" --junitxml=.phase-loop/runs/fabpub-red.junit.xml` exits exactly 1; every frozen RED nodeid fails its intended assertion anchor |
| `FABPUB-0-T2` | `test` | `FABPUB-0-T1` | SL-0 owned set plus all pre-existing tests | guard/default compatibility | with activation absent and marker absent, the same focused command plus the ordinary `-m "not dotfiles_integration"` suite are GREEN; only `FABPUB_NEW_PRODUCTION_NODEIDS` skip and no pre-existing test is disabled |
| `FABPUB-0-T3` | `impl` | `FABPUB-0-T1`, `FABPUB-0-T2` | SL-0 owned set only | none beyond SL-0 | Commit only tests, `_fabpub_tdd_guard.py`, and the verification-only `phase_loop_runtime.fabpub_tdd_chronology` module; no production capability, marker, doc, or other path is legal |
| `FABPUB-0-T4` | `verify` | `FABPUB-0-T3` | Git topology and evidence | chronology/default/RED audit | `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode tests-only` |
| `FABPUB-0-T5` | `verify` | `FABPUB-0-T4` | exact tests-only digest | four-seat governed test panel | Panel and persist the test-tree, guard, selector, nodeid/count, anchor-map, raw RED, RED JUnit, and default-green JUnit digests before merge |

- **Tasks**:
  - test: Add test-owned `_fabpub_tdd_guard.py`. Exact activation is only `PHASE_LOOP_TDD_EXPECT_FABPUB=1`; otherwise capability is active only when importing `phase_loop_runtime.fabpub_capability` yields exact `FABPUB_CAPABILITY_VERSION == 1`. A missing marker module is handled without collection failure. Freeze literal `FABPUB_RED_SELECTOR` and final selector plus exact `FABPUB_NEW_PRODUCTION_NODEIDS`, `FABPUB_MIGRATED_NODEIDS`, `FABPUB_RED_NODEIDS`, `FABPUB_EXPECTED_NODEIDS`, their independently collected integer counts, `FABPUB_RED_ANCHORS`, and the one skip reason. Guard-control tests never skip. Only each exact newly added production-dependent nodeid may use per-test `skipif` while inactive; there is no module-level skip and no `xfail`. The exact new node `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter` belongs to `FABPUB_NEW_PRODUCTION_NODEIDS`, `FABPUB_RED_NODEIDS`, and `FABPUB_EXPECTED_NODEIDS`; it contributes exactly one to each corresponding count and to the inactive default-skip count. No total count from digest `93aae35552d2e17220e69aae211b35d4ded598c31622579ea9cf26c47b37d8b3` may be copied because that plan predates a materialized immutable test tree: collect the revised T tree and freeze the honest totals.
  - test: New production-dependent tests delay new-symbol imports/probes until after the guard and fail under activation with `AssertionError` carrying their unique frozen RED anchor, never an import/collection error. Existing nodeids whose fresh publish currently supplies finalized `AdmissionRequest` are enumerated exactly in `FABPUB_MIGRATED_NODEIDS` and are never skipped. With marker and explicit activation absent they run only their unchanged legacy-compatible branch and stay GREEN against production-identical `main`; with explicit activation or the final marker they run the replacement `PreAdmissionEnvelope` plus transaction branch. Any assertion that a fresh finalized publish is accepted exists only in the inactive transition branch and must not execute after activation; valid lower adapter, non-publish, and terminal-replay assertions remain positive controls.
  - test: Add independently runnable `test_fabpub_allocator_*`, `test_fabpub_broker_*`, `test_fabpub_intent_*`, `test_fabpub_checkpoint_*`, `test_fabpub_transaction_*`, `test_fabpub_dual_path_*`, `test_fabpub_train_handoff_*`, `test_fabpub_train_resume_*`, `test_fabpub_faithful_retry_*`, `test_fabpub_terminal_replay_*`, `test_fabpub_different_head_*`, `test_fabpub_conflicting_resume_*`, `test_fabpub_genuine_resume_*`, and `test_fabpub_changelog_*` cases. Assert every mutation's live source anchor before applying it and retain GREEN reachability controls.
  - test: Freeze `test_fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter` as the allocator-bypass falsifier. Against an empty admission/evidence store and a test-local supported publish contract, submit a finalized `AdmissionRequest` and assert the exact `TypeError`, both `admit` and `admit_next` call counts zero, `admission_store.replay() == ()`, no broker-intent/evidence append, and adapter call count zero. Its mutation audit first asserts the exact production guard/error literal anchor, disables only that guard, and reruns the node; the observable must become one caller-epoch admission plus one adapter call, proving the path was entered. In the same node, run the positive controls before the rejecting arm: (a) a finalized request for a test-local supported non-publish verb still reaches legacy `admit()` and its adapter once, (b) a manually seeded completed terminal publish record replays through a finalized request with neither admission method nor adapter called, and (c) a fresh envelope publish reaches `admit_next` and the adapter with the broker-allocated epoch. These controls prove the guard is publish/freshness-specific rather than a blanket request rejection or dead seam.
  - test: Add normal and prebuilt whole-path crash tests that enter `run_train`/`publish_from_worktree`. The mandatory normal sequence durably creates `PREPARED`, lets the real `git commit` return success, kills at `after_git_commit_success_before_committed_checkpoint`, asserts the worktree is clean and only the pre-commit intent exists, and restarts. Recovery must match the commit's exact parent/tree/trailer, persist `COMMITTED_HEAD_RESOLVED`, skip staging and commit, and enter the real `BrokerService.execute`. Continue through `after_broker_intent_before_adapter_started`; the final restart dedups one admission, executes the adapter exactly once total, and seals broker/transaction evidence. Preserve `after_committed_checkpoint_before_broker_execute`, post-terminal replay, faithful-envelope, conflict, stale/rewritten/descendant, different-head, tombstone/cleanup, ambiguity, and prebuilt controls.
  - test: Migrate every BrokerService-level fresh publish in `test_convergence_broker_verbs.py`, `test_convergence_broker_revocation_race.py`, `test_convergence_broker_credsep.py`, and `test_convergence_live_enable.py` to an envelope plus exact transaction fixture in the activated/final branch; revocation race injection moves from legacy `admit` to `admit_next`, and `_AdmitAll`-style service fakes grow the allocator surface instead of preserving fresh publish through `admit()`. Migrate `test_publishing.py`, `test_train_prebuilt.py`, and `test_convergence_train_integration.py` expectations from the finalized train handoff to the envelope/transaction handoff under the same immutable transition rule. Direct `GitHubBrokerAdapter.execute` tests continue to receive finalized requests because the broker has already allocated by that boundary. Finalized BrokerService compatibility remains only for non-publish verbs and already-completed terminal replay.
  - test: The activated RED run uses the tests-only tree on the production-identical pre-implementation base, captures exact argv/exit code plus unabridged stdout/stderr as raw evidence, and emits JUnit. The reducer requires every `FABPUB_RED_NODEIDS` testcase to be present once, failed (not passed/skipped/errored), and matched to its unique intended assertion anchor in both structured failure data and raw output. It rejects pytest exit codes other than 1, missing/duplicate/unexpected RED nodeids, collection errors, `xfail`, or a vacuous selector.
  - test: The unactivated tests-only run emits separate JUnit. The reducer requires the skip count to equal exactly `len(FABPUB_NEW_PRODUCTION_NODEIDS)` and the skipped nodeids to equal that set byte-for-byte—no migrated or other phase nodeid may skip. Every migrated/control nodeid runs its inactive transition branch and passes, every pre-existing nodeid remains collected with no new guard skip, and the ordinary compatible suite remains GREEN. The preactivation activated JUnit and final default JUnit each require every `FABPUB_EXPECTED_NODEIDS` entry exactly once and zero skips/errors/xfails.
  - test: Keep `phase_loop_runtime.fabpub_tdd_chronology` stdlib-only and independent of production modules changed by FABPUB. It audits the guard/JUnit/raw evidence, exact digests, and the lifecycle SHAs described in Execution Notes, and prints deterministic JSON. It accepts runner-stamped metadata or the exact frozen environment names, never a moving/raw base ref.
  - impl: Land only SL-0 after the revised plan digest and the exact test/evidence digest each clear Fable 5, GPT-5.6 Sol, Gemini 3.6 Flash, and Grok 4.5. The implementation base must contain that landed commit; SL-1 through SL-4 cannot edit tests, guard, reducer, selectors, expected nodeids/counts, RED anchors, or retained evidence.
  - verify: `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode tests-only`

### SL-1 — Shared allocator and enforced equalities

- **Scope**: Add the one in-lock allocator and make its epoch, attempt, policy, revocation, precondition, dedup, and conflict guarantees fail closed.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/admission.py`
- **Interfaces provided**: `LinearizableAdmissionStore.admit_next`, `make_request(epoch, attempt_id) contract`, `attempt_id authority comparison`
- **Interfaces consumed**: `FABPUB immutable test digest`, `FABPUB stage selectors`, `AdmissionRequest` (pre-existing), `AdmissionRecord` (pre-existing), `BrokerAdmissionPolicy` (pre-existing)
- **Parallel-safe**: no; this is the sole allocator writer and its implementation base must already contain SL-0.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-1-T1` | `test` | (none) | `admission.py` behavior only | immutable SL-0 allocator selector | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator"` is RED immediately before the edit |
| `FABPUB-1-T2` | `impl` | `FABPUB-1-T1` | `admission.py` | none | Implement `admit_next` without changing `admit` |
| `FABPUB-1-T3` | `verify` | `FABPUB-1-T2` | `admission.py` behavior only | immutable SL-0 allocator selector | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator"` is GREEN |

- **Tasks**:
  - test: Run only activated `fabpub_allocator` cases before and after the edit; the marker is still absent, and later broker, train, and CHANGELOG tests are deliberately not selected in this lane.
  - impl: Implement `admit_next(make_request, *, attempt_id, precondition)` under the existing flock; preserve fail-closed `policy is None`, policy denial, and `epoch_blocked` checks on fresh and dedup paths; rebuild and compare a dedup hit; allocate only after the in-lock precondition; enforce both equalities; retain append-only fsync persistence and the legacy `admit()` surface.
  - verify: `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator"`

### SL-2 — Broker fresh-publish gate and recoverable publish transaction

- **Scope**: Freeze terminal replay before a mandatory fresh-publish envelope gate, allocate every fresh publish through `admit_next`, and close the commit/checkpoint crash hole with a coordinator-owned transaction whose durable intent precedes commit.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/convergence/contracts.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/verbs.py`, `phase-loop-runtime/src/phase_loop_runtime/publishing.py`
- **Interfaces provided**: `PreAdmissionEnvelope`, `PublishIntent`, `PublishCheckpoint`, `PublishTransactionState`, `PublishTransactionStore`, `publish_attempt_id(repo, branch, head_sha)`, `fresh publish requires PreAdmissionEnvelope`, `non-publish finalized AdmissionRequest compatibility path`, `completed terminal replay compatibility path`, `publish_from_worktree transaction recovery route`, `base-free publish_committed_branch_idempotency_key(repo, branch, head_sha)`
- **Interfaces consumed**: `FABPUB immutable test digest`, `FABPUB stage selectors`, `LinearizableAdmissionStore.admit_next`, `make_request(epoch, attempt_id) contract`, `FencedAdmissionFactory` (pre-existing), `BrokerEvidenceStore` (pre-existing), `BrokerProviderAdapter` (pre-existing)
- **Parallel-safe**: no; the request union, service dispatch, transaction validation, and publish resume route move together after SL-1.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-2-T1` | `test` | (none) | SL-2 behavior only | immutable broker/intent/checkpoint/transaction/dual-path selectors | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_broker or fabpub_intent or fabpub_checkpoint or fabpub_transaction or fabpub_dual_path"` is RED immediately before the edit |
| `FABPUB-2-T2` | `impl` | `FABPUB-2-T1` | SL-2 owned production files | none | Implement the request union, service branch, and recoverable transaction state machine |
| `FABPUB-2-T3` | `verify` | `FABPUB-2-T2` | SL-2 plus finalized compatibility callers | immutable SL-0 files only | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_publishing) and not fabpub_train_handoff and not fabpub_train_resume and not fabpub_changelog"` is GREEN |

- **Tasks**:
  - test: Run the activated SL-2 RED selector first, including the exact fresh-finalized-publish rejection node and its mutation/positive controls, then the activated GREEN command that includes all migrated direct `BrokerService.execute` callers/fakes in `test_convergence_broker_verbs.py`, `test_convergence_broker_revocation_race.py`, `test_convergence_broker_credsep.py`, and `test_convergence_live_enable.py`. The marker is still absent. Do not run the SL-3 authentic train-resume or SL-4 CHANGELOG selectors here.
  - impl: Add an epoch-free `PreAdmissionEnvelope` carrying train/node/action identity, roadmap/effective-code/dependency/verification pre-images, expected-version predicate, authority scope, and deterministic operation identity, but no epoch, attempt ID, fence token, approval digest, or fencing idempotency key. Permit it only on `PUBLISH_COMMITTED_BRANCH`.
  - impl: Widen `BrokerRequest.admission` only to `AdmissionRequest | PreAdmissionEnvelope`. In `BrokerService.execute`, compute the publish completed-effect key and return an already-completed terminal replay before inspecting that union. Immediately after replay, require `PreAdmissionEnvelope` for every remaining `PUBLISH_COMMITTED_BRANCH`; exact `TypeError("fresh publish_committed_branch requires PreAdmissionEnvelope")` leaves the admission log, broker evidence, and adapter untouched. Keep the following request-shape dispatch explicit and mutation-testable: envelope routes to `admit_next`, finalized request routes to `admit()`, and the preceding guard makes that finalized branch non-publish-only. Disabling only the anchored guard must therefore reproduce the legacy caller-epoch publish bypass and be killed by the frozen falsifier. Every non-publish envelope fails before admission. A fresh publish envelope must name the exact `COMMITTED_HEAD_RESOLVED` transaction, resolve approval from `merge-base(committed_head_sha, base_tip_sha)`, build commit-stable approval, call `admit_next`, replace the envelope with the stored finalized request, and only then record broker intent/call the adapter. An unresolved full SHA or transaction/authority mismatch blocks; current `HEAD`, moving `origin/<base>`, caller epoch, and caller approval are never substitutes after `PREPARED`.
  - impl: Keep `publish_committed_branch_idempotency_key(repo, branch, head_sha)` byte-for-byte base-free and epoch-free as required by `EC-FABPUB-4`; do not fold `base`, `attempt_id`, `lease_epoch`, or randomness into the completed-effect key.
  - impl: Add versioned metadata-only `PublishIntent`/`PublishCheckpoint` payloads and a `PublishTransactionStore` under an explicit coordinator-owned `checkpoint_root`, never under the published repo. For normal mode, stage and audit first; resolve full `parent_head_sha` and `base_tip_sha`; compute `staged_tree_sha`, staged-diff digest, deterministic commit bytes containing one `FABPUB-Intent-ID` trailer, presentation digests, and the envelope authority digest; derive the transaction ID from those immutable fields; and persist `PREPARED` before invoking `git commit`. Prebuilt mode persists an equivalent intent with its full committed head/tree before broker entry. Serialization, lock ownership, compare-and-transition, temp write, file fsync, atomic replace, and parent fsync are part of the frozen contract.
  - impl: Resolve `PREPARED` before any new staging. A normal intent becomes `COMMITTED_HEAD_RESOLVED` only when current branch `HEAD` is the unique commit with exactly the recorded parent SHA, tree SHA, and transaction trailer; store that full head and derive `attempt_id` from it. If `HEAD == parent_head_sha` and the audited staged tree still matches, a pre-commit crash may continue the one commit; if the matching commit already exists, never commit again. A descendant, rewrite, missing/multiple marker, repo/branch/base-tip/mode/path/digest/authority mismatch, or clean `HEAD == parent_head_sha` is a typed conflict/stale intent before admission and cannot fall through to `nothing_staged` or start a new transaction.
  - impl: Enforce only `PREPARED -> COMMITTED_HEAD_RESOLVED -> ADMISSION_DURABLE -> BROKER_INTENT_DURABLE -> ADAPTER_STARTED -> TERMINAL_SEALED`, plus terminal `ABANDONED`/`CONFLICTED`. After each authoritative Git/admission/evidence fsync, atomically project the matching transaction state and identity digest. Recovery of a projection lag replays the authoritative stores and advances only on exact matches. Persist `ADAPTER_STARTED` immediately before the adapter call; `BROKER_INTENT_DURABLE` without that marker may resume, while `ADAPTER_STARTED` without terminal evidence records/refuses as outcome-ambiguous and never invokes the adapter again. Seal terminal broker state/evidence reference into `TERMINAL_SEALED` only after broker terminal evidence is durable.
  - impl: Preserve the finalized-admission API only for non-publish BrokerService callers and the finalized request passed from broker to provider adapters. A completed terminal publish replay remains shape-agnostic because it returns before authorization and performs no admission or adapter call. An envelope requires `checkpoint_root`; absence fails closed. Exact active input resumes; active mismatches never overwrite the record. Cleanup first writes and fsyncs an `ABANDONED` or `CONFLICTED` tombstone, then archives/clears the active pointer under the same lock; those states never authorize admission or effect. Only a terminal/tombstoned prior transaction may be superseded by a freshly validated different-head intent. Retain sealed transaction/evidence identity for exact terminal replay.
  - verify: `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_publishing) and not fabpub_train_handoff and not fabpub_train_resume and not fabpub_changelog"`

### SL-3 — Live train authority handoff and authentic resume

- **Scope**: Make normal and prebuilt train publication supply epoch-free authority and the transaction root, then prove the live route recovers an exact commit from pre-commit intent and resumes through the idempotent broker path.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/convergence/fencing.py`, `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`
- **Interfaces provided**: `_default_build_admission -> PreAdmissionEnvelope`, `CoordinatorRuntime.coordinator_root -> publish transaction root`, `commit-stable approval pre-images`, `authentic normal/prebuilt crash resume`
- **Interfaces consumed**: `FABPUB immutable test digest`, `FABPUB stage selectors`, `PreAdmissionEnvelope`, `PublishIntent`, `PublishCheckpoint`, `PublishTransactionState`, `publish_from_worktree transaction recovery route`, `publish_attempt_id(repo, branch, head_sha)`, `FencedAdmissionFactory` (pre-existing), `ApprovalBinding` (pre-existing)
- **Parallel-safe**: no; `train_runner.py` is phase-single-writer and this lane consumes the complete SL-2 transaction contract.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-3-T1` | `test` | (none) | live normal/prebuilt train routes | immutable train handoff/resume selectors | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_train_handoff or fabpub_train_resume"` is RED immediately before the edit |
| `FABPUB-3-T2` | `impl` | `FABPUB-3-T1` | SL-3 owned production files | none | Return the envelope and pass the coordinator-owned transaction root on both routes |
| `FABPUB-3-T3` | `verify` | `FABPUB-3-T2` | live normal/prebuilt train routes | immutable SL-0 files only | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_fencing or test_train_prebuilt or test_convergence_train_integration) and not fabpub_changelog"` is GREEN |

- **Tasks**:
  - test: Run the authentic selectors that start above `BrokerService`. In `fabpub_train_resume_post_commit_pre_checkpoint`, normal work must persist `PREPARED`, commit successfully, and die at the faithful `after_git_commit_success_before_committed_checkpoint` injection; assert the retry begins from a clean worktree with the committed `HEAD` and no post-commit checkpoint. Restart must recover that exact commit from parent/tree/trailer identity without staging or committing, persist `COMMITTED_HEAD_RESOLVED`, and reach the real `BrokerService.execute` with `PreAdmissionEnvelope`, never a finalized caller epoch. Preserve independent `after_committed_checkpoint_before_broker_execute` coverage. In the compound arm, kill again at `after_broker_intent_before_adapter_started`; the final restart must reconcile/dedup one broker-allocated admission, execute the adapter exactly once total, write terminal broker evidence, seal `TERMINAL_SEALED`, and reach `pr_open`. Repeat the applicable sequence for a prebuilt head, then exercise faithful retry, mismatched authority, stale/rewritten/descendant intent, safe abandonment/cleanup, different-head allocation, `ADAPTER_STARTED` ambiguity refusal, and replay after terminal evidence. Assert every crash anchor, transition, clean-worktree condition, envelope broker entry, allocated admission/effect count, and evidence record.
  - impl: Refactor `_default_build_admission` to return `PreAdmissionEnvelope`; preserve surrogate-escaped owned-path digest behavior and all roadmap/dependency/verification/authority inputs, but remove the literal epoch-1 lease and every pre-commit `HEAD`, attempt, fence, approval, or fencing-key binding.
  - impl: Pass a deterministic node-scoped transaction root beneath `CoordinatorRuntime.coordinator_root` to `publish_from_worktree` for both normal and prebuilt routes. Capture the immutable envelope and presentation pre-images before `PREPARED`; resume-time authority comes from the exact transaction, not a fresh clean-worktree diff. Do not create a second train-ledger status or treat a blocked ledger append as transaction state. The durable pre-commit intent plus exact Git reconciliation is the only route that repairs the commit-to-checkpoint gap.
  - impl: Keep the fencing digest formulas unchanged: when SL-2's `make_request` invokes the factory, pass its deterministic `attempt_id` to `lease`, its allocated epoch as `lease_epoch`, and its post-commit approval binding to `create`. Do not change the FAB review-round `epoch` namespace.
  - verify: `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_fencing or test_train_prebuilt or test_convergence_train_integration) and not fabpub_changelog"`

### SL-4 — Documentation, capability activation, chronology, and verification reducer

- **Scope**: Retract publish byte-neutrality, prove the complete implementation under explicit activation, install the production capability marker in a marker-only commit, and reduce JUnit plus chronology evidence without widening into FABREADMIT or RESIDUAL.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py`, `CHANGELOG.md`
- **Interfaces provided**: `phase_loop_runtime.fabpub_capability.FABPUB_CAPABILITY_VERSION == 1`, `IF-0-FABPUB-1`, `FABPUB verification evidence`, `publish byte-neutrality retraction`
- **Interfaces consumed**: `FABPUB immutable test digest`, `PHASE_LOOP_TDD_EXPECT_FABPUB=1`, frozen selectors/nodeids/counts/anchors, `fabpub_tdd_chronology executable`, `LinearizableAdmissionStore.admit_next`, `PreAdmissionEnvelope`, `PublishIntent`, `PublishCheckpoint`, `PublishTransactionState`, `publish_from_worktree transaction recovery route`, `_default_build_admission -> PreAdmissionEnvelope`
- **Parallel-safe**: no; this terminal documentation/activation/verification reducer runs after every producer and is excluded from writer waves.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-4-T1` | `test` | (none) | `CHANGELOG.md` observable | immutable CHANGELOG selector | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_changelog"` is RED before the doc edit |
| `FABPUB-4-T2` | `impl` | `FABPUB-4-T1` | `CHANGELOG.md` | none | Add the explicit byte-neutrality retraction; the capability marker remains absent |
| `FABPUB-4-T3` | `verify` | `FABPUB-4-T2` | complete pre-activation implementation | all immutable FABPUB assertions | Run the exact focused suite under `PHASE_LOOP_TDD_EXPECT_FABPUB=1`; every expected nodeid passes and the reducer accepts the pre-activation JUnit before marker installation |
| `FABPUB-4-T4` | `impl` | `FABPUB-4-T3` | `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py` only | none | In a marker-only commit, install exact `FABPUB_CAPABILITY_VERSION = 1`; do not edit any other path |
| `FABPUB-4-T5` | `verify` | `FABPUB-4-T4` | default focused suite | immutable SL-0 suite | Run the frontmatter `automation.suite_command` with activation absent, then audit structured JUnit for the exact expected nodeids and zero phase skips/errors/xfails |
| `FABPUB-4-T6` | `verify` | `FABPUB-4-T5` | broad compatible surface | all non-wrapper verification | Before implementation panel and merge, run every compatible command under `## Verification`, excluding only the explicitly labeled final-landing chronology wrappers; all must pass |
| `FABPUB-4-T7` | `verify` | `FABPUB-4-T6` | landed Git topology/evidence | final-landing wrappers | After merge, run the runner-owned immutable-blob preflight plus chronology reducer and rerun final JUnit reduction before emitting `IF-0-FABPUB-1` |

- **Tasks**:
  - test: Run the activated `fabpub_changelog` assertion before the doc edit and retain its intended RED anchor; the marker remains absent, so no unactivated new falsifier is exposed to default CI.
  - impl: Add one explicit CHANGELOG entry retracting publish byte-neutrality because publish admission epochs and derived fence records are intentionally renumbered. Do not claim FABREADMIT, its dormant flag flip, or RESIDUAL's unresolved `base` identity carry as delivered.
  - verify: With the marker still absent, run the exact focused selector under activation and emit `.phase-loop/runs/fabpub-preactivation.junit.xml`. The frozen reducer requires every `FABPUB_EXPECTED_NODEIDS` entry once, all new/migrated assertions active—including the fresh-finalized-publish rejection node—zero skips/errors/xfails, and a passing result. Only after this passes may the marker commit be created.
  - impl: Add `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py` with exact `FABPUB_CAPABILITY_VERSION = 1` in a separate marker-only commit whose first parent is the recorded pre-activation head. No test, guard, selector, expected nodeid/count, anchor, reducer, production implementation, or CHANGELOG edit may share that commit.
  - verify: Unset `PHASE_LOOP_TDD_EXPECT_FABPUB`, run the frontmatter suite command, and emit `.phase-loop/runs/fabpub-final.junit.xml`. `fabpub_tdd_chronology --mode final-junit` compares the JUnit testcases and integer totals to `FABPUB_EXPECTED_NODEIDS` and its independently frozen count, proves the fresh-finalized-publish rejection node ran by default, and requires zero phase skips/errors/xfails.
  - verify: Before the implementation panel and merge, run the focused JUnit/audit, broad `broker or admission or convergence or fab` suite, whole compatible `-m "not dotfiles_integration"` suite, ruff, validators, and `git diff --check`. The only exclusions are the final-landing ancestry/blob wrappers, which require the actual landed SHA; no ordinary compatibility suite or pre-existing test may be excluded.
  - verify: After landing, the runner-owned wrapper first uses Git outside the candidate verifier process to compare every SL-0 blob at the landed SHA byte-for-byte with `FABPUB_TESTS_ONLY_COMMIT_SHA` and to verify the recorded ancestry. Only after that independent preflight may it launch the stdlib-only chronology blob whose SHA-256 matches the test-panel record. This removes the same-process self-verification cycle: a current-head verifier never certifies its own immutability. Rerun default focused JUnit/audit and the full verification set on the landed SHA.

## Execution Policy

- work-unit defaults: work-unit=`lane_execute`, effort=`high`, unsupported=`inherit_default`, inherit-default=`true`
- SL-4: work-unit=`phase_reducer`, effort=`high`, unsupported=`inherit_default`, inherit-default=`true`, reason=`cross-lane documentation chronology and acceptance synthesis`

## Execution Notes

- The prior plan-panel record at `.phase-loop/reviews/v10-fabpub-plan-panel.json` binds digest `93aae35552d2e17220e69aae211b35d4ded598c31622579ea9cf26c47b37d8b3`, Grok `AGREE`, Gemini `AGREE`, Sol `DISAGREE`, and Fable intentionally deferred. This repaired plan records that exact digest as `supersedes_plan_sha256`, computes its own SHA-256 externally (never self-embeds it), and must clear the roadmap-required Fable 5, GPT-5.6 Sol, Gemini 3.6 Flash, and Grok 4.5 board before SL-0. Fable and Sol remain mandatory for the revised digest; any dissent or unusable mandatory leg blocks dispatch. The narrowed repair removes only the fresh-finalized-publish allocator bypass; prior TDD lifecycle, crash/idempotency, publication, and provenance contracts remain frozen.
- The coordinator selects one implementation author vendor for the whole phase and records the explicit executor/model/effort. Do not combine cross-vendor work-unit rotation with lane scheduling.
- SL-0 is a hard default-green Git boundary. Let `B=FABPUB_PREIMPLEMENTATION_BASE_SHA` and `T=FABPUB_TESTS_ONLY_COMMIT_SHA`; chronology requires `T^ == B`, the `B..T` changed paths equal a non-empty subset of the exact SL-0 set, production/marker/docs blobs are unchanged, and the marker is absent. The activated T1 run executes against T's tests plus B-identical production, including the exact fresh-finalized-publish rejection falsifier and every migrated fresh-publish node. The unactivated T2 run proves ordinary CI compatibility: only newly added nodes skip, migrated nodes execute their transition branch, and no bypass assertion is carried beyond activation. Tests-only merge occurs only after both evidence audits and the exact test digest panel pass.
- `_fabpub_tdd_guard.py` is the single source of truth for activation, the marker probe, selectors, exact nodeid sets/counts, skip reason, RED anchors, and migrated-node transition modes. The exact collected sets and counts are recomputed from T after the new rejection node and all migrations; `default_skip_count == len(FABPUB_NEW_PRODUCTION_NODEIDS)`, every activated RED node fails once at its intended assertion, and preactivation/final each run `len(FABPUB_EXPECTED_NODEIDS)` cases with zero skips/errors/xfails. SL-1 through SL-4 cannot edit any SL-0 path or retained evidence. A needed test repair returns to SL-0, creates a new exact test digest, recomputes every affected set/count, reruns activated RED and default-green evidence against a production-identical base, and re-panels before implementation resumes.
- Runner chronology metadata freezes `roadmap_sha256`, revised `plan_sha256`, `supersedes_plan_sha256=93aae35552d2e17220e69aae211b35d4ded598c31622579ea9cf26c47b37d8b3`, `B`, `T`, `FABPUB_IMPLEMENTATION_BASE_SHA=I`, first production commit `P`, `FABPUB_PREACTIVATION_HEAD_SHA=C`, marker-only commit `M`, implementation candidate `H`, and landed SHA `L`, plus the test-tree/guard/selector/nodeid-set/nodeid-count/migration-mode/anchor/raw-RED/RED-JUnit/default-JUnit/preactivation-JUnit/final-JUnit digests. It requires `T` to be an ancestor of `I`, `I` and `T` to be ancestors of `P^`, `P` to be the first first-parent commit after `I` changing a FABPUB production path, `C` to contain every non-marker source/doc change with no SL-0 change, `M^ == C`, `C..M` to contain only the exact marker file, `H` to descend from `M`, and every phase-owned blob at `L` to equal the reviewed `H` blob. The marker is absent at `B/T/I/C` and exact at `M/H/L`.
- `phase_loop_runtime.fabpub_tdd_chronology` is evidence logic frozen in T, stdlib-only, and imports no FABPUB production module. Final landing never trusts the current process to certify that verifier: a runner-owned wrapper outside the implementation write set checks Git ancestry and every SL-0 blob against T, verifies the chronology blob SHA-256 from the test-panel record, then executes that exact blob in a fresh process against L. Runner timestamps, commit messages, a plain `git log`, moving refs, or self-reported current-head hashes are not substitutes.
- The three faithful anchors remain distinct and frozen before production changes: `after_git_commit_success_before_committed_checkpoint`, `after_committed_checkpoint_before_broker_execute`, and `after_broker_intent_before_adapter_started`. The first closes the commit/checkpoint hole; the latter two preserve the approved broker crash surfaces.
- The finalized `AdmissionRequest` compatibility contract is narrow: non-publish BrokerService callers use `.admit()`, provider adapters receive the broker-finalized request, and already-completed publish evidence can replay before authorization. Every other publish entry—including direct services/fakes, live builders, routing clients, normal/prebuilt train paths, absent evidence, and in-flight continuation—uses `PreAdmissionEnvelope` plus `admit_next`. The broad compatible suite must prove migrated revocation locking, credential-separation fakes, live/routing clients, non-publish finalized behavior, finalized completed-terminal replay, and rejection of a fresh finalized publish with zero admission/evidence-intent/adapter side effects.
- The transaction state machine has one authority per boundary: the transaction file for prepared input/projection, Git commit/ref for committed-head resolution, `admissions.jsonl` for allocation/dedup, and broker `evidence.jsonl` for effect intent/terminal outcome. Projection lag is recoverable; identity disagreement is not.
- `train_runner.py` remains SL-3's sole file owner. The complete phase-owned write set is exactly the disjoint union of SL-0 through SL-4, now including `_fabpub_tdd_guard.py` and the marker module. There are no dependency, lockfile, migration, generated package artifact, snapshot, environment-example, package-export, external-dispatch, or plan-manifest metadata fields for owned paths/digests. Any other dirty path is `phase_owned_dirty` and blocks closeout.
- `refresh_downstream_after_merge` remains outside FABPUB. FABREADMIT owns the readmit consumer and `_FAB_DELTA_BROKER_READMIT_READY` flip; RESIDUAL owns any later `base` change to completed-effect identity. Documentation impact remains limited to the CHANGELOG retraction.
- Policy precedence is CLI/operator override, this phase plan, roadmap policy, `Dispatch Hints`, then registry defaults. Silent model, effort, executor, or capability downgrade is forbidden unless an explicit fallback is selected or `inherit_default` applies.
- FABPUB cuts no tag or external release workflow and is not a visible render deliverable. Post-dispatch back-fill is inapplicable; closeout records `visual_render_declared=false` and a typed non-render opt-out.

## Spec Closeout Plan

- schema: `spec_delta_closeout.v1`
- decision: `no_spec_delta`
- target surfaces: `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/admission.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/contracts.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/verbs.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/fencing.py`, `phase-loop-runtime/src/phase_loop_runtime/publishing.py`, `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`, `phase-loop-runtime/src/phase_loop_runtime/fabpub_tdd_chronology.py`, `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py`, `phase-loop-runtime/tests/_fabpub_tdd_guard.py`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py`, `phase-loop-runtime/tests/test_convergence_broker_admission.py`, `phase-loop-runtime/tests/test_convergence_broker_verbs.py`, `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py`, `phase-loop-runtime/tests/test_convergence_broker_credsep.py`, `phase-loop-runtime/tests/test_convergence_live_enable.py`, `phase-loop-runtime/tests/test_convergence_fencing.py`, `phase-loop-runtime/tests/test_publishing.py`, `phase-loop-runtime/tests/test_train_prebuilt.py`, `phase-loop-runtime/tests/test_convergence_train_integration.py`, `CHANGELOG.md`
- evidence paths: `plans/phase-plan-v10-FABPUB.md`, `.phase-loop/reviews/v10-fabpub-plan-panel.json`, `phase-loop-runtime/src/phase_loop_runtime/fabpub_tdd_chronology.py`, `phase-loop-runtime/tests/_fabpub_tdd_guard.py`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py`, `.phase-loop/runs/*/fabpub-*.junit.xml`, `.phase-loop/runs/*/fabpub-*.raw.log`, `.phase-loop/runs/*/fabpub-chronology.json`, `.phase-loop/runs/*/verification.json`, `.phase-loop/runs/*/verification.log`, `.phase-loop/events.jsonl`
- redaction posture: `metadata_only`
- downstream handling: `none`

## Verification

- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli validate-roadmap specs/phase-plans-v10.md`
- `PYTHONPATH=phase-loop-runtime/src python3 skills-src/claude/claude-plan-phase/scripts/validate_plan_doc.py plans/phase-plan-v10-FABPUB.md`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_ or test_convergence_broker_admission or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_convergence_fencing or test_publishing or test_train_prebuilt or test_convergence_train_integration"`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode final-junit`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "broker or admission or convergence or fab"`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -m "not dotfiles_integration"`
- `ruff check phase-loop-runtime/src/phase_loop_runtime/`
- `git diff --check`
- Final-landing wrapper (post-merge only): runner-owned immutable-blob/ancestry preflight followed by `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode final`; it emits `.phase-loop/runs/*/fabpub-chronology.json` and then the default focused JUnit/audit is rerun.

## Acceptance Criteria

- [ ] EC-FABPUB-0 — proven by the runner-owned immutable-blob/ancestry preflight plus `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode final`, retained raw/JUnit activated RED and default-green evidence, exact superseded/revised plan and test-panel digests, and final default JUnit. The reducer must prove the full `B -> T -> I -> P -> C -> M -> H -> L` lifecycle, immutable SL-0 blobs, marker-only activation, the exact fresh-finalized-publish rejection node and all migrated fresh-publish nodeids present in the recomputed frozen sets/counts, every RED nodeid failing its intended assertion before implementation, inactive default skips equal exactly `FABPUB_NEW_PRODUCTION_NODEIDS`, every migrated node executing rather than skipping, every expected FABPUB nodeid running after implementation, and zero preactivation/final skips/errors/xfails.
- [ ] EC-FABPUB-1 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator_stale_epoch or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; the first keeps the roadmap's direct `admit()` stale-epoch falsifier, while the second proves no fresh publish can present any caller epoch to that compatibility surface.
- [ ] EC-FABPUB-2 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator_epoch_equality or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; the rejection node's envelope positive control reaches `admit_next`, while its fresh finalized arm proves the enforced equality cannot be bypassed.
- [ ] EC-FABPUB-3 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator_attempt_equality or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; the rejection node proves every fresh publish attempt identity is broker-derived from the exact transaction rather than accepted from a finalized caller request.
- [ ] EC-FABPUB-4 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_train_resume_post_commit_pre_checkpoint or fabpub_train_resume_normal or fabpub_train_resume_prebuilt or fabpub_terminal_replay or fabpub_different_head or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; the blocking normal arm must kill immediately after real commit success and before `COMMITTED_HEAD_RESOLVED`, restart clean from `PREPARED`, resolve the exact parent/tree/trailer-bound head without re-commit, enter the real broker with an envelope, dedup to one broker-allocated admission across the preserved broker-intent crash, execute the adapter exactly once, and seal terminal evidence. The rejection node's completed-terminal positive control proves replay still precedes the fresh-envelope gate and performs no second admission or adapter call.
- [ ] EC-FABPUB-5 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_faithful_retry or fabpub_checkpoint_conflict or fabpub_intent_conflict or fabpub_intent_cleanup"`; every retry that proceeds beyond terminal replay rebuilds from the original envelope and exact transaction identity.
- [ ] EC-FABPUB-6 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_conflicting_resume or fabpub_genuine_resume"`; both arms enter through `PreAdmissionEnvelope`, so finalized caller authority cannot bypass the comparison.
- [ ] EC-FABPUB-7 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_changelog"`.
