---
phase_loop_plan_version: 1
phase: FABPUB
roadmap: specs/phase-plans-v10.md
roadmap_sha256: 4d652aaff71b484806ea6d1770c9475e0c1e8de90c39e5447c6fadb8d0fa2c6f
automation:
  suite_command:
    - env
    - PYTHONPATH=phase-loop-runtime/src
    - python3
    - -m
    - pytest
    - phase-loop-runtime/tests
    - -q
    - -k
    - fabpub_ or test_convergence_broker_admission or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_convergence_fencing or test_publishing or test_train_prebuilt or test_convergence_train_integration or test_train_runner
    - --junitxml=.phase-loop/runs/fabpub-final.junit.xml
---

# FABPUB: Shared Epoch Allocator — Publish Identity

## Context

FABPUB migrates every fresh `publish_committed_branch` admission from caller-stamped `lease_epoch=1` to the broker's single repository-wide monotonic allocator. Repository authority is one canonical Git-common-dir identity, not an absolute linked-worktree path or train-roadmap path; every train using the same broker root and every linked worktree of that repository therefore reaches one allocator/store, while distinct repositories retain distinct stores and ambiguity blast radii. The phase is an independent v10 root and produces `IF-0-FABPUB-1`; FABREADMIT and RESIDUAL remain downstream and must not execute beside this phase because they share publish-identity surfaces.

The roadmap fixes four boundaries. First, `LinearizableAdmissionStore.admit_next` allocates under the existing advisory lock and enforces both values supplied to `make_request(epoch, attempt_id)`. Second, completed-effect identity remains the roadmap-frozen, base-free `publish_committed_branch_idempotency_key(repo, branch, head_sha)`; the `base` question carried by `EC-RESIDUAL-1` is not reopened. Third, approval identity is derived from the transaction's resolved post-commit head and the merge base against its frozen full `base_tip_sha`, never from moving live `HEAD` or a raw base ref after preparation. Fourth, `BrokerService.execute` performs terminal replay first, then requires every fresh or continuing `PUBLISH_COMMITTED_BRANCH` to carry the new epoch-free `PreAdmissionEnvelope` and use `admit_next`; a finalized `AdmissionRequest` remains compatible only for non-publish verbs and already-completed terminal replay. A fresh finalized publish cannot provide transaction identity or commit-stable approval and is rejected before either admission method, broker-intent persistence, or adapter execution.

The live crash hole is also part of this phase. A normal `publish_from_worktree` call currently commits and then enters the broker, but a crash in that gap leaves a clean worktree; the next normal call exits at `nothing_staged` and never reaches `BrokerService.execute`. A checkpoint written only after ref movement leaves the same unrecoverable kill before that write. Object-first construction creates a second production-reachability constraint: at `COMMIT_OBJECT_DURABLE` before ref CAS the transaction-owned tree remains staged against the parent, so current `run_train` returns `preflight_failed` at its unconditional clean gate; even if that gate were broadly bypassed, the execute path reruns `run_loop` before it calls `publish_from_worktree`. FABPUB therefore adds a versioned, metadata-only `PublishTransaction` under the train-local coordinator root, persists `PREPARED`, constructs the actual Git commit object without moving the branch, durably persists its exact expected OID/raw-object digest at `COMMIT_OBJECT_DURABLE`, and only then CAS-updates the exact branch ref. The default production `run_train` route must discover and validate that durable transaction before ordinary clean-worktree evaluation, admit only its exact state-specific index/worktree projection, and resume publication before any upstream injection or author work.

Transaction construction is acyclic and object-first. Stage 1 captures the exact original caller message bytes before any FABPUB trailer, rejects any preexisting `FABPUB-Intent-ID` line, hashes those bytes, and canonicalizes only the immutable pre-trailer intent fields. `transaction_id` is the domain-separated SHA-256 of those canonical pre-trailer bytes; the allowlist contains neither `transaction_id` nor any trailer, final-message, expected/committed-head, or final-commit digest. Stage 2 appends exactly one deterministic `FABPUB-Intent-ID: <transaction_id>` trailer, freezes the exact final message bytes and every commit-construction input—parent OID, tree OID, Git object format, author and committer names/emails/dates/timezones, encoding and ordered extra-header policy, signing mode/key/format and signer input policy, constructor argv/config/environment, and every pre-construction byte that can affect the commit object—and persists `PREPARED`. The normal path then constructs the actual commit object once without changing any ref; when signing is enabled, the resulting exact signature header bytes are captured from that object rather than regenerated on retry. It reads the raw object bytes, obtains `expected_commit_oid` plus `final_commit_object_sha256`, and durably records both, the signature/extra-header bytes, and the complete construction inputs at `COMMIT_OBJECT_DURABLE` before an exact-ref CAS from `parent_head_sha` to `expected_commit_oid`. Recovery never infers or adopts a commit from matching semantic fields: exact ref equality with `expected_commit_oid` may finalize after raw-object digest verification; exact ref equality with `parent_head_sha` may retry the same CAS to the already-recorded object; any other OID blocks. A commit with the same parent/tree/message/trailer but a different author, committer date, signature, encoding, extra header, raw-object digest, or OID is therefore rejected. No identity derivation ever hashes a value that contains the identity being derived. Prebuilt publication never rewrites an existing commit: it applies the same pre-trailer identity construction to its exact caller/presentation inputs, rejects a misleading preexisting FABPUB trailer, and freezes its supplied head as the exact expected/committed OID plus raw-object digest before broker entry.

The allowed durable state progression is `PREPARED -> COMMIT_OBJECT_DURABLE -> COMMITTED_HEAD_RESOLVED -> ADMISSION_DURABLE -> BROKER_INTENT_DURABLE -> ADAPTER_STARTED -> TERMINAL_SEALED`. Every local transition is serialized by the transaction lock and persisted by temp-file write, file fsync, atomic replace, and parent-directory fsync; Git object creation/ref CAS, the admission append, and broker evidence appends remain their authoritative atomic records. Recovery may advance a lagging transaction projection only after exact-OID/raw-object reconciliation proves the corresponding Git object and exact ref, admission record, or broker evidence already exist. The adapter-start marker is durable before the adapter effect: a crash at the preserved broker-intent arm occurs before that marker and may retry, while an `ADAPTER_STARTED` record without terminal evidence is fail-closed as outcome-ambiguous and never re-invokes the adapter. `ABANDONED` and `CONFLICTED` are terminal non-authorizing tombstones. An active mismatch is never overwritten; cleanup can archive a terminal/tombstoned transaction and clear its active pointer only under the same lock and only after the tombstone is durable. A new different-head publish then creates a fresh fully validated intent. Exact terminal records remain replayable so a whole-path retry reaches broker terminal replay instead of `nothing_staged`.

The production allocator route is equally explicit. `CanonicalRepositoryIdentity.v1` is the full domain-separated SHA-256 of the normalized, symlink-resolved absolute Git common-directory identity plus Git object format; linked worktrees share it, distinct local repositories do not, and neither train path nor worktree top-level path is hashed into it. The absolute worktree path remains only a validated adapter execution location and must resolve back to the carried canonical identity. `publish_from_worktree`, `PreAdmissionEnvelope`, broker authority, `publish_attempt_id`, and the unchanged base-free `publish_committed_branch_idempotency_key(repo, branch, head_sha)` use the canonical repository identity as `repo`. `live.py` routes every service/store for that identity to one full-digest directory beneath the shared broker root even when adapters run in different linked worktrees. `cli.py` keeps train-local transaction/coordinator roots but removes the train-path hash from broker allocation scope and passes the same `<ledger-dir>/broker` root to every train invocation.

That identity switch is not activation-safe by itself. Current production state lives under `<ledger-dir>/broker/<sha256(resolved-train-path)[:16]>/<sha256(serialized-repo-preimage)[:16]>`; the repository preimage is the exact historical string serialized by that caller, which may be relative and is not normalized before hashing, while its resolution context determines the Git repository it denotes. Completed-effect keys hash that same exact historical repository string. FABPUB therefore owns one pre-activation, fail-closed, global repository-partitioned cutover transaction for the whole shared broker root. It inventories every legacy leaf before activation; authenticates each exact serialized repository preimage together with its immutable resolution context and resolved train-path preimage to exactly one canonical Git identity; partitions sources, admission high-water marks, terminal effects, and permanent ambiguity by that identity; then retires all sources and arms all canonical partitions atomically as one crash-idempotent transaction. No per-repository receipt may claim that every legacy leaf resolves to itself. A missing, unmapped, multiply mapped, malformed, colliding, conflicting, changed, or partially retired source blocks global activation; there is no empty-store fallback, epoch reset, partial repository activation, or adapter retry around legacy evidence.

Canonical runner state is `.phase-loop/`. Legacy `.codex/phase-loop/` compatibility files have no planning authority. The tracked roadmap bytes match the frontmatter digest. An earlier exact-digest panel record at `.phase-loop/reviews/v10-fabpub-plan-panel.json` bound an earlier plan and found the now-repaired legacy-state invisibility blocker. The preserved current local-three advisory artifact at that same path has SHA-256 `3b3cda3d14442b909ec3078e0f749f7e11dcfa813227bf048a603da2197f3e6a`, binds predecessor plan digest `c0b895a9f758edfd1944ed4a154f5aee429dab4b96d1fc828037e6ac9ab96857`, roadmap digest `1e8ea70ceae55d326cd84b092e1b9e879180d7b0e774140c3dd00e6ed63b7071`, reviewed bundle digest `d0d2e8210cd52a3a50da22c18a22031a0650905577cc720ae90c2411fc7bd67a`, and instructions digest `d90bdadb68b9c9d2b37ffbc0e9c7e82cca5bcdc1745f3e5b3200b6cbe71c4b85`; it records usable Grok/Gemini `AGREE`, usable GPT-5.6 Sol `DISAGREE`, and no Fable result, so it authorizes nothing. Sol found that the manifest assumed an absolute worktree preimage even though live callers hash the exact serialized repository string, including relative forms, and that a per-repository receipt could not safely inventory a shared train root containing leaves for several repositories. This revision repairs that blocker with six literal legacy-cutover RED nodeids in the existing test inventory while retaining the exact-OID/default-`run_train` recovery, five serial lanes, literal tests-before-implementation chronology, finalized-request rejection, acyclic transaction/trailer rules, fencing, crash/idempotency/conflict controls, roadmap scope, and every roadmap acceptance criterion. Its new exact plan/roadmap digest pair and revised immutable test digest must clear fresh by-reference reviews from Fable 5, GPT-5.6 Sol, Gemini 3.6 Flash, and Grok 4.5 before SL-0; any dissent or unusable mandatory leg blocks dispatch.

## Interface Freeze Gates

- [ ] IF-0-FABPUB-1 — `LinearizableAdmissionStore.admit_next(make_request, *, attempt_id, precondition) -> AdmissionRecord` holds `admissions.lock` while it checks `epoch_blocked`, policy, prior records, and the in-lock precondition. On an `attempt_id` hit it rebuilds with `make_request(prior.epoch, attempt_id)`, requires `rebuilt.lease_epoch == prior.epoch` and `rebuilt.attempt_id == attempt_id`, rejects any other authority-field mismatch, and returns the original record. On a miss it allocates `(max(record.epoch) if records else 0) + 1`, calls `make_request(epoch, attempt_id)`, requires `request.lease_epoch == epoch` and `request.attempt_id == attempt_id`, and appends plus fsyncs one record. All production construction routes use one `CanonicalRepositoryIdentity.v1`: normalize and symlink-resolve `git -C <worktree> rev-parse --path-format=absolute --git-common-dir`, pair it with `git rev-parse --show-object-format`, serialize the versioned explicit fields, and derive a full 64-hex domain-separated SHA-256. The worktree path and train path are excluded; distinct Git common directories remain distinct. `publish_from_worktree` carries that identity as repository authority and carries the absolute worktree only as a non-authoritative adapter location that must resolve back to the same identity. `live.py` chooses exactly `<shared_broker_root>/repositories/<canonical_repository_identity>` for both admission and evidence stores regardless of linked worktree or train, never a worktree-path hash or truncated slug. `cli.py` passes train-independent `<ledger-dir>/broker` as `shared_broker_root` while retaining a separately namespaced train-local coordinator/transaction root. Before normal publication, `publish_from_worktree` rejects any caller message containing a `FABPUB-Intent-ID` line and constructs `PublishPreTrailerIntent.v1` from the exact immutable field allowlist: canonical repository identity, branch, base, frozen full `base_tip_sha`, mode, pre-commit parent OID, staged tree OID, ordered owned paths, staged-diff SHA-256, exact original-message byte length and SHA-256, draft/body identity, and envelope-authority pre-image digest. The non-authoritative worktree path is not hashed into repository/transaction/effect identity. The nested authority pre-image is canonicalized from an explicit pre-transaction allowlist and cannot contain `transaction_id` or any final-message/expected-head/committed-head/commit-object field; opaque caller-supplied digests are not accepted. The pre-trailer object is serialized exactly with `json.dumps(payload, sort_keys=True, separators=(",", ":"), ensure_ascii=True).encode("ascii")`, then `pre_trailer_intent_sha256` is recorded and `transaction_id = sha256(b"FABPUB-PUBLISH-TRANSACTION-v1\\0" + pre_trailer_intent_bytes)` is derived. The allowlist excludes `transaction_id`, the trailer, `final_commit_message_sha256`, `expected_commit_oid`, `committed_head_sha`, `final_commit_object_sha256`, and every other field whose bytes could contain or derive from `transaction_id`. Only afterward does it construct exact final message bytes as `original_message_bytes + b"\\n\\nFABPUB-Intent-ID: " + transaction_id.encode("ascii") + b"\\n"` and record `final_commit_message_sha256` separately. Before `PREPARED`, it freezes exact `parent_head_sha`, tree OID, final message bytes, object format, author and committer names/emails/exact dates/timezones, encoding header policy, ordered extra-header allowlist/bytes, signing mode/key/format and signer input policy, constructor argv/config/environment, and every pre-construction byte affecting the object; unsupported or implicit header/signing inputs fail closed. After durable `PREPARED`, it constructs the actual object once without ref mutation, captures exact signature/extra-header bytes from the result, verifies and durably flushes the raw object, obtains exact `expected_commit_oid` and `final_commit_object_sha256`, and persists those values plus the captured bytes atomically at `COMMIT_OBJECT_DURABLE` before any ref change. It then requires `HEAD` to be the exact symbolic `refs/heads/<branch>` and uses `git update-ref refs/heads/<branch> <expected_commit_oid> <parent_head_sha>` (or an equivalently exact transaction) as the sole CAS. Recovery from `COMMIT_OBJECT_DURABLE` reads that exact ref: `ref == expected_commit_oid` may advance after raw-object digest verification; `ref == parent_head_sha` may retry the same CAS to the already-recorded object; every other OID, missing/corrupt expected object, raw-digest mismatch, detached/wrong ref, or alternate same-semantic commit is `CONFLICTED` before admission. It never searches by parent/tree/message/trailer and never adopts, re-signs, or reconstructs another OID. `COMMITTED_HEAD_RESOLVED` records `committed_head_sha == expected_commit_oid`; a same parent/tree/message/single trailer with any different author, committer date, signature, encoding/extra header, raw digest, or OID is rejected. Prebuilt mode does not append, construct, CAS, or rewrite a commit and rejects a preexisting FABPUB trailer; its input head is frozen as exact expected/committed OID plus raw-object digest before broker entry. `BrokerService.execute` computes the base-free completed-effect key with canonical repository identity as the unchanged `repo` input and returns any already-completed terminal-evidence replay before fresh authorization. If replay does not return—including absent evidence or a non-terminal in-flight record—`PUBLISH_COMMITTED_BRANCH` requires an epoch-free `PreAdmissionEnvelope`; a finalized `AdmissionRequest` raises exact `TypeError("fresh publish_committed_branch requires PreAdmissionEnvelope")` before policy classification, either admission method, broker-intent/evidence mutation, or adapter execution. Finalized `AdmissionRequest` retains legacy `admit()` semantics only for non-publish verbs; non-publish envelopes fail before admission. The publish envelope is legal only for `PUBLISH_COMMITTED_BRANCH`, names the exact `COMMITTED_HEAD_RESOLVED` transaction, carries canonical repository identity plus its original-message digest, pre-trailer digest, transaction ID, final-message digest, expected/committed OID, and final-commit-object digest as separately compared bindings, and carries the validated worktree path separately for adapter execution; derives `attempt_id = sha256("publish\\0" + canonical_repository_identity + "\\0" + branch + "\\0" + expected_commit_oid)`; derives approval `base_sha = merge-base(expected_commit_oid, transaction.base_tip_sha)` from full frozen SHAs; rebuilds lease/fence/idempotency fields at the allocated epoch through `admit_next`; and passes only the stored finalized request to the adapter. Thus no fresh publish can bypass the single repository store, broker allocation, acyclic transaction identity, exact expected commit, or commit-stable approval. The transaction reconciles and durably projects `ADMISSION_DURABLE`, `BROKER_INTENT_DURABLE`, `ADAPTER_STARTED`, and `TERMINAL_SEALED` from authoritative admission/evidence records that preserve repository identity, pre-trailer identity, and exact final-commit bindings; only the pre-`ADAPTER_STARTED` broker-intent state may retry adapter execution. Active mismatch, stale/ref-moved state, a trailer-shape conflict, or an abandoned/conflicted tombstone fails closed before admission, and cleanup cannot turn it into publish authority. Terminal replay retains the separate base-free `publish_committed_branch_idempotency_key(repo, branch, head_sha)` with canonical repository identity as `repo`.

- **Pre-activation global authenticated legacy-store cutover freeze**: No canonical repository store is usable until one `ARMED` `LegacyBrokerCutoverTransaction.v2` covers the complete shared broker root and its `LegacyRepositoryPartitionReceipt.v2` set. Absence is allowed only when a complete global scan proves no legacy-shaped entry exists and seals the zero-source inventory. Input is a metadata-only `LegacyBrokerCutoverManifest.v2`; each row records the exact historical serialized repository string and UTF-8 bytes used by the legacy call, its canonical absolute invocation working directory, the workspace source/argument/default resolution mode, the exact resolved train-roadmap path preimage, expected truncated keys, expected leaf, and expected canonical Git identity. Its canonical-byte SHA-256 arrives only through immutable runner-stamped activation metadata. For every row the runtime recomputes `train_key = sha256(str(train_path.resolve()).encode("utf-8"))[:16]`, `repo_key = sha256(serialized_repo_bytes)[:16]`, resolves the repository string relative to the recorded working directory under the recorded resolution mode, and validates the live Git common directory and object format into `CanonicalRepositoryIdentity.v1`. Every legacy-shaped child under the root must be claimed exactly once and partitioned to exactly one canonical identity; multiple leaves and multiple repositories are valid, but an unclaimed or multiply claimed leaf, two contexts mapping one leaf incompatibly, one serialized preimage mapping to conflicting identities, a truncated-hash collision, symlink/path escape, unexpected file, or unattested canonical store blocks globally with typed `legacy_cutover_conflict`.

- **Global snapshot, repository partition, retirement, and authority freeze**: The transaction acquires one global cutover lock plus every legacy `admissions.lock` and canonical admissions lock in sorted absolute-path order before reading or moving anything. Under those locks it strictly parses complete newline-terminated JSONL: admission sequences must be contiguous, epochs positive, requests schema-valid, identical idempotency/attempt identities byte-equivalent, and divergent duplicate authority fails closed; evidence is validated as full per-key histories, not last-line-only, with only legal transitions and permanent `OUTCOME_AMBIGUOUS_BLOCKED`. It computes each raw-file and canonical-record digest, count, exact serialized-preimage digest, resolution-context digest, effective evidence map, and repository partition; each partition freezes its admission high-water, exact legacy completed-effect key preimages, terminal outcomes, and ambiguity state. The fsynced global journal progresses only through `INVENTORY_SEALED -> SNAPSHOTS_VERIFIED -> SOURCES_RETIRED -> PARTITIONS_WRITTEN -> ARMED`. It atomically renames every source leaf on the same filesystem into `<shared_broker_root>/legacy-archive/<cutover_id>/<source_id>`, fsyncs both parents, and replaces each original leaf path with an fsynced regular-file retirement tombstone. It writes all immutable `LegacyRepositoryPartitionReceipt.v2` records before the single global `ARMED` transition; no partition is routable earlier. Every crash boundary revalidates the exact global inventory, archive, tombstone, partition, and journal digests before resuming; no source is deleted.

- **Legacy replay, collision, and crash-idempotent multi-repository resume freeze**: For repository partition `R`, `LinearizableAdmissionStore.admit_next` allocates `max(canonical_epoch_high_water(R), receipt.legacy_epoch_high_water(R)) + 1`; another repository's leaf never raises or clears that floor. Before fresh authorization or any canonical append, `BrokerService.execute` probes the canonical completed-effect key and every receipt-authenticated exact legacy key derived from the original serialized repository bytes—never from the resolved path or a normalized alias—against immutable archives for `R`. Identical terminal records collapse; completed effects are promoted with global transaction, partition, source, preimage, context, and digest provenance and replay with zero admission/adapter calls. Divergent terminals or authority, canonical-vs-legacy disagreement, invalid history, preimage/context drift, or digest drift blocks the affected transaction before any partition is armed. Archived `OUTCOME_AMBIGUOUS_BLOCKED` or orphaned `PROVIDER_CALL_IN_FLIGHT` permanently blocks only its authenticated repository partition, but global activation still waits until every partition is sealed. Restart accepts only the same `cutover_id`, complete inventory, partition mapping, and exact step/digests: each crash before or after any source retirement or partition write resumes idempotently to the same global `ARMED` result; source recreation, source-plus-archive coexistence, changed bytes, a different partition map/receipt, or partial unattested canonical state blocks. The FABPUB capability marker and canonical routing are forbidden until all six legacy RED controls, preactivation suite, exact global/partition receipt-vector audit, and the global `ARMED` barrier pass.

- **Production `run_train` recovery freeze**: After schema validation/topological ordering and before `_default_preflight` evaluates ordinary cleanliness, default production `run_train` probes each deterministic node-scoped transaction root beneath `CoordinatorRuntime.coordinator_root`. No checkpoint means the existing clean gate is unchanged. A present active or `TERMINAL_SEALED` transaction must validate its version/state transition, canonical repository/train/node/checkpoint-root binding, transaction and authority digests, exact symbolic ref, parent/expected OID arm, expected-object raw digest, frozen dependency/fencing inputs, and exact state-specific workspace projection. At `PREPARED` or `COMMIT_OBJECT_DURABLE` with the ref at the parent, the index tree, cached binary diff digest, and staged path tuple must equal the frozen tree/digest/ordered owned paths byte-for-byte; there may be no conflict entry, unstaged byte, untracked path, extra staged path, submodule drift, or ignored broad-dirt allowance. At the expected-ref and later arms the index/worktree must already equal the expected commit tree and be ordinarily clean. A corrupt/mismatched checkpoint, wrong root/repository/node, alternate ref/OID, extra or missing staged byte/path, unstaged or untracked dirt, or non-authorizing `ABANDONED`/`CONFLICTED` tombstone fails the all-repo preflight with zero PRs and zero author work. Every other auth/remote/base check still runs; there is no CLI/env `allow_dirty`, no injected empty-preflight substitute, and no path-only exception. After all repos pass preflight, the execute node revalidates the candidate to close the probe/use race and calls the real `publish_from_worktree` recovery route before upstream injection or `run_loop`; that route reuses the exact OID/CAS/checkpoint/envelope, preserves broker fencing/idempotence, and either returns the prior/finished published result or blocks. The coordinator may rejoin ordinary ledger/downstream processing only after `TERMINAL_SEALED` and a second unqualified `_check_repo_clean` proves the ref, index, and worktree reconciled with no allowance. The recovered node never reruns author work; nodes with no candidate retain the current preflight → injection → `run_loop` → publish order.

- **Tests-first activation freeze**: SL-0 owns `_fabpub_tdd_guard.py`, the exact selectors, expected nodeid sets/counts, per-node RED anchors, and `PHASE_LOOP_TDD_EXPECT_FABPUB=1`. Before the production marker exists, exact activation runs every new or migrated production-dependent assertion and ordinary unactivated CI skips only the frozen newly added production-dependent nodeids. Migrated existing tests are never skipped: inactive tests-only execution runs their legacy-compatible branch against production-identical `main`, while explicit activation and the final marker run the replacement envelope/transaction branch; an obsolete assertion that a fresh finalized publish succeeds is transition-only and forbidden after activation. The final marker is `phase_loop_runtime.fabpub_capability.FABPUB_CAPABILITY_VERSION == 1`; after its marker-only commit, the same immutable tests run without activation. `xfail`, import/collection failure, and implementation edits to any frozen test/evidence surface are forbidden.
- **Merge-safe default**: Ordinary default CI remains GREEN before the marker. No collection/import failure is permitted. Final verification reruns the reviewed implementation in a fresh process at the exact candidate HEAD and reruns the landed result in a fresh process at the exact landed SHA.

## Lane Index & Dependencies

SL-0 — Governed default-green tests-only contract and chronology reducer
  Depends on: (none)
  Blocks: SL-1, SL-2, SL-3
  Parallel-safe: no

SL-1 — Shared allocator, authenticated legacy cutover, and enforced equalities
  Depends on: SL-0
  Blocks: SL-2, SL-4
  Parallel-safe: no

SL-2 — Broker fresh-publish gate and recoverable publish transaction
  Depends on: SL-0, SL-1
  Blocks: SL-3, SL-4
  Parallel-safe: no

SL-3 — Live train authority handoff and authentic resume
  Depends on: SL-0, SL-1, SL-2
  Blocks: SL-4
  Parallel-safe: no

SL-4 — Documentation, capability activation, chronology, and verification reducer
  Depends on: SL-0, SL-1, SL-2, SL-3
  Blocks: (none)
  Parallel-safe: no

## Lanes

### SL-0 — Governed default-green tests-only contract and chronology reducer

- **Scope**: Land the complete FABPUB falsifier set, its deterministic test-owned activation guard, and its executable evidence/chronology reducer as one tests-and-verification-only commit that is RED only under explicit activation and remains green in ordinary default-main CI.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/fabpub_tdd_chronology.py`, `phase-loop-runtime/tests/_fabpub_tdd_guard.py`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py`, `phase-loop-runtime/tests/test_convergence_broker_admission.py`, `phase-loop-runtime/tests/test_convergence_broker_verbs.py`, `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py`, `phase-loop-runtime/tests/test_convergence_broker_credsep.py`, `phase-loop-runtime/tests/test_convergence_live_enable.py`, `phase-loop-runtime/tests/test_convergence_fencing.py`, `phase-loop-runtime/tests/test_publishing.py`, `phase-loop-runtime/tests/test_train_prebuilt.py`, `phase-loop-runtime/tests/test_convergence_train_integration.py`, `phase-loop-runtime/tests/test_train_runner.py`
- **Interfaces provided**: `FABPUB immutable test digest`, `PHASE_LOOP_TDD_EXPECT_FABPUB=1`, `FABPUB_NEW_PRODUCTION_NODEIDS`, `FABPUB_MIGRATED_NODEIDS`, `FABPUB_RED_NODEIDS`, `FABPUB_EXPECTED_NODEIDS`, `FABPUB_IDENTITY_NODEIDS`, `FABPUB_SHARED_ALLOCATOR_NODEIDS`, `FABPUB_LEGACY_CUTOVER_NODEIDS`, `FABPUB_RUN_TRAIN_RECOVERY_NODEIDS`, `FABPUB_IDENTITY_VECTOR_SHA256`, `FABPUB_COMMIT_OBJECT_VECTOR_SHA256`, `FABPUB_REPOSITORY_IDENTITY_VECTOR_SHA256`, `FABPUB_LEGACY_CUTOVER_VECTOR_SHA256`, frozen nodeid counts/selectors/RED anchors, `fabpub_tdd_chronology executable`, `fresh-publish envelope gate`, `acyclic pre-trailer transaction identity`, `pre-ref expected-commit state machine`, `single repository-wide allocator route`, `authenticated legacy-store cutover`, `default-run_train preflight-before-run-loop recovery`, `finalized-admission compatibility matrix`
- **Interfaces consumed**: `plan-frozen FABPUB contract` (pre-existing), `AdmissionRequest` (pre-existing), `BrokerRequest` (pre-existing), `LinearizableAdmissionStore` (pre-existing), `BrokerService` (pre-existing), `publish_from_worktree` (pre-existing), `_default_build_admission` (pre-existing), `build_routing_broker_client` (pre-existing), `run-train CLI broker construction` (pre-existing)
- **Parallel-safe**: no; the exact test/guard/reducer digest must clear the governed four-seat test panel and land as one default-green tests-only commit before any production writer starts.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-0-T1` | `test` | (none) | SL-0 owned set | exact `FABPUB_RED_NODEIDS` | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_ or test_convergence_broker_admission or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_convergence_fencing or test_publishing or test_train_prebuilt or test_convergence_train_integration or test_train_runner" --junitxml=.phase-loop/runs/fabpub-red.junit.xml` exits exactly 1; every frozen RED nodeid fails its intended assertion anchor |
| `FABPUB-0-T2` | `test` | `FABPUB-0-T1` | SL-0 owned set plus all pre-existing tests | guard/default compatibility | with activation absent and marker absent, the same focused command plus the ordinary `-m "not dotfiles_integration"` suite are GREEN; only `FABPUB_NEW_PRODUCTION_NODEIDS` skip and no pre-existing test is disabled |
| `FABPUB-0-T3` | `impl` | `FABPUB-0-T1`, `FABPUB-0-T2` | SL-0 owned set only | none beyond SL-0 | Commit only tests, `_fabpub_tdd_guard.py`, and the verification-only `phase_loop_runtime.fabpub_tdd_chronology` module; no production capability, marker, doc, or other path is legal |
| `FABPUB-0-T4` | `verify` | `FABPUB-0-T3` | Git topology and evidence | chronology/default/RED audit | `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode tests-only` |
| `FABPUB-0-T5` | `verify` | `FABPUB-0-T4` | exact tests-only digest | four-seat governed test panel | Panel and persist the test-tree, guard, identity-vector/test, selector, nodeid/count, anchor-map, raw RED, RED JUnit, and default-green JUnit digests before merge |

- **Tasks**:
  - test: Add test-owned `_fabpub_tdd_guard.py`. Exact activation is only `PHASE_LOOP_TDD_EXPECT_FABPUB=1`; otherwise capability is active only when importing `phase_loop_runtime.fabpub_capability` yields exact `FABPUB_CAPABILITY_VERSION == 1`. A missing marker module is handled without collection failure. Freeze literal `FABPUB_RED_SELECTOR` and final selector plus exact `FABPUB_NEW_PRODUCTION_NODEIDS`, `FABPUB_MIGRATED_NODEIDS`, `FABPUB_RED_NODEIDS`, `FABPUB_EXPECTED_NODEIDS`, `FABPUB_IDENTITY_NODEIDS`, `FABPUB_SHARED_ALLOCATOR_NODEIDS`, `FABPUB_LEGACY_CUTOVER_NODEIDS`, `FABPUB_RUN_TRAIN_RECOVERY_NODEIDS`, their independently collected integer counts, `FABPUB_RED_ANCHORS`, `FABPUB_IDENTITY_VECTOR_SHA256`, `FABPUB_COMMIT_OBJECT_VECTOR_SHA256`, `FABPUB_REPOSITORY_IDENTITY_VECTOR_SHA256`, `FABPUB_LEGACY_CUTOVER_VECTOR_SHA256`, and the one skip reason. Guard-control tests never skip. Only each exact newly added production-dependent nodeid may use per-test `skipif` while inactive; there is no module-level skip and no `xfail`. The exact new node `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter` contributes one new/RED/expected/inactive-skip entry. The two exact identity nodes `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_intent_identity_pre_trailer_vector_rejects_self_or_final_message_derivation` and `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_intent_identity_retry_is_deterministic_and_commit_bound_separately` are the complete `FABPUB_IDENTITY_NODEIDS`; their existing nodeids gain pre-ref expected-OID/header-rewrite subcases without adding a nodeid. The one exact shared-allocator node `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_allocator_shared_repository_epoch_across_linked_worktrees_and_train_roots` is the complete `FABPUB_SHARED_ALLOCATOR_NODEIDS` and remains the exact `+1` delta from superseded digest `d9cda3e05c1719111c50ed556625b7068fa28d623debad94276d170fd9d47100`. The six exact legacy-cutover nodes named below are the complete `FABPUB_LEGACY_CUTOVER_NODEIDS`; the four predecessor nodes plus two new current-repair nodes contribute exactly `+6`, so the cumulative delta from `d9cda3e05c1719111c50ed556625b7068fa28d623debad94276d170fd9d47100` is exactly `+7`, and the delta from panel-dissented predecessor `c0b895a9f758edfd1944ed4a154f5aee429dab4b96d1fc828037e6ac9ab96857` is exactly `+2`. The exact existing planned node `phase-loop-runtime/tests/test_train_runner.py::test_fabpub_train_resume_post_commit_pre_checkpoint` remains the sole `FABPUB_RUN_TRAIN_RECOVERY_NODEIDS` entry. Other existing CLI nodeids are migrated in place and never skip. No total count or test/vector digest from a superseded plan may be copied: collect the revised T tree, recompute every integer total with the exact `+1` shared-allocator plus `+6` legacy-cutover delta, and freeze every transaction-identity, commit-object, repository-identity, legacy-cutover, test-tree, guard, JUnit, and raw-evidence SHA-256 before the test panel.
  - test: New production-dependent tests delay new-symbol imports/probes until after the guard and fail under activation with `AssertionError` carrying their unique frozen RED anchor, never an import/collection error. Existing nodeids whose fresh publish currently supplies finalized `AdmissionRequest` are enumerated exactly in `FABPUB_MIGRATED_NODEIDS` and are never skipped. With marker and explicit activation absent they run only their unchanged legacy-compatible branch and stay GREEN against production-identical `main`; with explicit activation or the final marker they run the replacement `PreAdmissionEnvelope` plus transaction branch. Any assertion that a fresh finalized publish is accepted exists only in the inactive transition branch and must not execute after activation; valid lower adapter, non-publish, and terminal-replay assertions remain positive controls.
  - test: Add independently runnable `test_fabpub_allocator_*`, `test_fabpub_legacy_cutover_*`, `test_fabpub_broker_*`, `test_fabpub_intent_*`, `test_fabpub_expected_commit_*`, `test_fabpub_checkpoint_*`, `test_fabpub_transaction_*`, `test_fabpub_dual_path_*`, `test_fabpub_train_handoff_*`, `test_fabpub_train_resume_*`, `test_fabpub_faithful_retry_*`, `test_fabpub_terminal_replay_*`, `test_fabpub_different_head_*`, `test_fabpub_conflicting_resume_*`, `test_fabpub_genuine_resume_*`, and `test_fabpub_changelog_*` cases. Assert every mutation's live source anchor before applying it and retain GREEN reachability controls.
  - test: Freeze inline, independently serialized `PublishPreTrailerIntent.v1`, `CommitConstructionInputs.v1`, and `CanonicalRepositoryIdentity.v1` vectors and exact `FABPUB_IDENTITY_VECTOR_SHA256`, `FABPUB_COMMIT_OBJECT_VECTOR_SHA256`, and `FABPUB_REPOSITORY_IDENTITY_VECTOR_SHA256`. `test_fabpub_intent_identity_pre_trailer_vector_rejects_self_or_final_message_derivation` independently computes original-message digest, canonical pre-trailer bytes/digest, domain-separated `transaction_id`, exact single-trailer final message, and final-message digest from literal inputs; it asserts production outputs byte-for-byte. Its mutation audit first asserts the identity-derivation, top-level allowlist, nested authority allowlist, and nested-digest recomputation anchors, then separately injects `transaction_id`, `final_commit_message_sha256`, `expected_commit_oid`, `committed_head_sha`, or `final_commit_object_sha256` into the top-level or nested hashed preimage and replaces pre-trailer derivation with final-message derivation. Every mutation is killed by the fixed vector or a typed `circular_transaction_identity`/`forbidden_transaction_identity_field` failure before `PREPARED`; no fixed-point search, opaque caller digest, placeholder ID, iterative rehash, or post-object ID rewrite is accepted. Positive controls show that changing only a separately bound commit-object field leaves `transaction_id` unchanged but fails final-binding validation, while changing one original-message or pre-trailer authority byte changes both pre-trailer digest and transaction ID.
  - test: Extend the existing exact node `test_fabpub_intent_identity_retry_is_deterministic_and_commit_bound_separately` with table-driven subcases rather than new nodeids. First construction and faithful pre-object, post-object/pre-ref, and post-ref retries from the same literal inputs must retain byte-identical original-message digest, canonical pre-trailer bytes/digest, transaction ID, final message/digest, full `CommitConstructionInputs.v1`, exact `expected_commit_oid`, and raw-object digest; exactly one matching trailer and one ref update exist. Independently rewrite the current branch to commits with the same parent/tree/message/trailer but a different author identity, author date/timezone, committer identity, committer date/timezone, signature, encoding header, or other ordered extra header; each produces a different OID and must be rejected before admission, never adopted. Freeze exact ref-state arms: expected OID finalizes, parent OID retries the same object/CAS without reconstructing another expected OID, and every other OID blocks. Mutations that restore semantic-field reconciliation, move the ref before the expected-OID checkpoint, drop `expected_commit_oid`/raw digest from `COMMIT_OBJECT_DURABLE`, or replace CAS with an unguarded ref update must fail this node. Preserve one-byte caller/authority drift, all trailer-shape refusals, and the prebuilt no-rewrite positive control.
  - test: Freeze `test_fabpub_allocator_shared_repository_epoch_across_linked_worktrees_and_train_roots` as one node with internal sequential, concurrent, replay, and distinct-repository subcases. Create one real repository with two linked worktrees and two distinct train-roadmap roots sharing one explicit broker root. Publish different exact heads through independently constructed routing clients: sequential epochs are `1, 2`; concurrent distinct attempts atomically produce unique consecutive epochs with no duplicate; replaying the first exact canonical-repository/branch/head transaction from the other worktree/train returns the original admission/effect and allocates nothing; and a distinct Git common directory maps to a different store. Inspect the broker root and require exactly one non-empty store for the linked worktrees containing the full sequence, no train-hash/worktree-hash stores, and no isolated empty stores. Preserve per-repository ambiguity isolation by proving an ambiguous record for a truly distinct canonical repository does not poison this one. Before each mutation, assert the live source anchor; restore `_repo_store_slug`/absolute-worktree hashing in `live.py` and train-path broker-root hashing in `cli.py` independently and together, and require this same node or the preserved CLI nodeids to fail for duplicate epoch `1`, divergent roots, replay miss, or an isolated store.
  - test: Freeze these six literal activated RED controls in `test_fabpub_shared_epoch.py` and include all six exact nodeids in `FABPUB_LEGACY_CUTOVER_NODEIDS`: `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_legacy_cutover_preserves_high_water_before_activation`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_legacy_cutover_replays_terminal_completed_effect_before_activation`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_legacy_cutover_preserves_outcome_ambiguous_block_before_activation`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_legacy_cutover_conflict_collision_and_idempotent_resume_before_activation`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_legacy_cutover_preserves_exact_serialized_repo_preimages_and_resolution_context_before_activation`, and `phase-loop-runtime/tests/test_fabpub_shared_epoch.py::test_fabpub_global_legacy_cutover_partitions_multiple_repositories_crash_idempotently_before_activation`. Seed production-shaped legacy roots with relative and absolute serialized repository strings, distinct invocation working directories/resolution modes, exact resolved train-path preimages, `admissions.jsonl`, `evidence.jsonl`, and completed-effect keys derived from the exact serialized bytes before constructing any canonical client. The high-water, completed-effect, and ambiguity nodes retain their predecessor assertions. The conflict/resume node covers malformed histories, collisions, path escapes, unattested canonical rows, source recreation, and every global journal transition. The serialized-preimage node proves two textually distinct but contextually equivalent strings map to one canonical repository while keeping distinct old repo/effect keys, and that changing the recorded cwd, resolution mode, serialized bytes, or recomputing an old key from the resolved path blocks. The multi-repository node places at least two canonical repositories under one train root, proves complete global inventory and disjoint partition receipts, preserves each partition's terminal/high-water/ambiguity state, kills before and after every source retirement and partition write, and resumes to one byte-identical global `ARMED` transaction without duplicate moves, rows, effects, or partial routing. A per-repository “every leaf resolves to me” implementation must fail this node. Inline canonical seed bytes plus expected global manifest, resolution-context, source, archive, tombstone, partition-receipt, and transaction digests form `FABPUB_LEGACY_CUTOVER_VECTOR_SHA256`. Under current production the six unique RED anchors are `legacy_high_water_would_restart`, `legacy_terminal_would_reenter_adapter`, `legacy_ambiguity_would_become_invisible`, `legacy_cutover_not_atomic_or_authenticated`, `legacy_serialized_repo_preimage_or_context_lost`, and `legacy_global_partition_transaction_missing`; failures are assertions, never collection/import errors.
  - test: Freeze `test_fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter` as the allocator-bypass falsifier. Against an empty admission/evidence store and a test-local supported publish contract, submit a finalized `AdmissionRequest` and assert the exact `TypeError`, both `admit` and `admit_next` call counts zero, `admission_store.replay() == ()`, no broker-intent/evidence append, and adapter call count zero. Its mutation audit first asserts the exact production guard/error literal anchor, disables only that guard, and reruns the node; the observable must become one caller-epoch admission plus one adapter call, proving the path was entered. In the same node, run the positive controls before the rejecting arm: (a) a finalized request for a test-local supported non-publish verb still reaches legacy `admit()` and its adapter once, (b) a manually seeded completed terminal publish record replays through a finalized request with neither admission method nor adapter called, and (c) a fresh envelope publish reaches `admit_next` and the adapter with the broker-allocated epoch. These controls prove the guard is publish/freshness-specific rather than a blanket request rejection or dead seam.
  - test: Add normal and prebuilt lower-level `publish_from_worktree` crash controls, then strengthen the exact `FABPUB_RUN_TRAIN_RECOVERY_NODEIDS` node with a mandatory real `run_train` sequence. Seed a real execute-mode workspace by entering the real publisher through `run_train`, durably create `PREPARED` and the actual object, persist `expected_commit_oid` plus raw-object digest at `COMMIT_OBJECT_DURABLE`, and kill at `after_commit_object_checkpoint_before_ref_cas` with the exact transaction-owned tree still staged and the ref at its parent. Restart by calling `run_train` with its production `_preflight_fn`, `_run_loop`, and `_publish` defaults selected—no empty preflight, direct publisher call, prebuilt-mode substitution, or manual clean-up. The default transaction probe must run before the ordinary clean check, the all-repo auth/remote/base checks must still run, the exact state must be admitted, and recovery must reuse the recorded object/CAS and reach the real broker before any upstream injection or default `run_loop` author-launch canary fires. Then kill at preserved `after_git_commit_success_before_committed_checkpoint`, now immediately after exact ref CAS, and prove a default `run_train` restart accepts only the expected OID/raw digest, advances `COMMITTED_HEAD_RESOLVED`, skips staging/object reconstruction/ref mutation/author work, and enters the broker. Continue through `after_committed_checkpoint_before_broker_execute` and `after_broker_intent_before_adapter_started`; the final default-path restart dedups one shared-store admission, executes the adapter exactly once total, reaches `TERMINAL_SEALED`, passes an unqualified post-recovery clean check, records one `pr_open`, and resumes normal downstream ordering. In the same single node, table-loop negative controls independently add an extra staged path, alter one staged byte/index tree, remove one frozen staged path, add unstaged dirt, add an untracked path, change checkpoint root/node/repository binding, corrupt the expected object/raw digest, detach or move the exact ref, present a tombstone, and omit the checkpoint while retaining dirt; each must return typed `preflight_failed` or conflict at its frozen boundary with zero PR, admission, adapter, upstream injection, and author-work calls. Positive controls prove an ordinary clean no-transaction execute node still reaches default `run_loop`, and ordinary unrelated dirt with no transaction retains the existing `preflight_failed` behavior.
  - test: Migrate every BrokerService-level fresh publish in `test_convergence_broker_verbs.py`, `test_convergence_broker_revocation_race.py`, `test_convergence_broker_credsep.py`, and `test_convergence_live_enable.py` to an envelope plus exact transaction fixture in the activated/final branch; revocation race injection moves from legacy `admit` to `admit_next`, and `_AdmitAll`-style service fakes grow the allocator surface instead of preserving fresh publish through `admit()`. Migrate `test_publishing.py`, `test_train_prebuilt.py`, and `test_convergence_train_integration.py` expectations from the finalized train handoff to the envelope/transaction handoff. In `test_train_runner.py`, preserve the existing CLI nodeids: their inactive branches retain legacy per-train expectations, while activated/final branches prove distinct train-local coordinator/transaction roots pass the same shared broker root and do not select train-hashed allocator roots. Direct `GitHubBrokerAdapter.execute` tests continue to receive finalized requests because the broker has already allocated by that boundary. Finalized BrokerService compatibility remains only for non-publish verbs and already-completed terminal replay.
  - test: The activated RED run uses the tests-only tree on the production-identical pre-implementation base, captures exact argv/exit code plus unabridged stdout/stderr as raw evidence, and emits JUnit. The reducer requires every `FABPUB_RED_NODEIDS` testcase—including the exact `FABPUB_RUN_TRAIN_RECOVERY_NODEIDS` entry—to be present once, failed (not passed/skipped/errored), and matched to its unique intended assertion anchor in both structured failure data and raw output. It rejects pytest exit codes other than 1, missing/duplicate/unexpected RED nodeids, collection errors, `xfail`, or a vacuous selector.
  - test: The unactivated tests-only run emits separate JUnit. The reducer requires the skip count to equal exactly `len(FABPUB_NEW_PRODUCTION_NODEIDS)` and the skipped nodeids to equal that set byte-for-byte—including both exact `FABPUB_IDENTITY_NODEIDS`, the one exact `FABPUB_SHARED_ALLOCATOR_NODEIDS` entry, all six exact `FABPUB_LEGACY_CUTOVER_NODEIDS` entries, and the exact `FABPUB_RUN_TRAIN_RECOVERY_NODEIDS` entry—while no migrated or other phase nodeid may skip. Every migrated/control nodeid, including the existing `test_train_runner.py` CLI nodes, runs its inactive transition branch and passes. Preactivation and final JUnit each require every expected entry once, both identity nodes once, the shared-allocator node once, all six legacy-cutover nodes once, the default-`run_train` recovery node once, honest integer totals with the exact cumulative `+7` delta from `d9cda3e05c1719111c50ed556625b7068fa28d623debad94276d170fd9d47100`, and zero skips/errors/xfails.
  - test: Keep `phase_loop_runtime.fabpub_tdd_chronology` stdlib-only and independent of production modules changed by FABPUB. It audits guard/JUnit/raw evidence, exact nodeid counts and `+7` cumulative delta, all four frozen vector digests, identity/shared-allocator/legacy-cutover test digests, the exact 24-path ownership digest, the exact 12-path test inventory digest, and the lifecycle SHAs described in Execution Notes, and prints deterministic JSON. It accepts runner-stamped metadata or exact frozen environment names, never a moving/raw base ref.
  - impl: Land only SL-0 after the revised plan digest and the exact test/evidence digest each clear Fable 5, GPT-5.6 Sol, Gemini 3.6 Flash, and Grok 4.5. The implementation base must contain that landed commit; SL-1 through SL-4 cannot edit tests, guard, reducer, selectors, expected nodeids/counts, RED anchors, or retained evidence.
  - verify: `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode tests-only`

### SL-1 — Shared repository allocator route, legacy cutover, and enforced equalities

- **Scope**: Add the one in-lock allocator; authenticate, partition, and retire every legacy train/repository-string-hashed store in one global cutover before activation; and route every linked worktree/train for one canonical repository to its single store while keeping distinct repositories isolated.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/admission.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/evidence.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/live.py`, `phase-loop-runtime/src/phase_loop_runtime/cli.py`
- **Interfaces provided**: `LinearizableAdmissionStore.admit_next`, `make_request(epoch, attempt_id) contract`, `attempt_id authority comparison`, `CanonicalRepositoryIdentity.v1`, `LegacyBrokerCutoverManifest.v2`, `LegacyBrokerCutoverTransaction.v2`, `LegacyRepositoryPartitionReceipt.v2`, `LegacyBrokerCutoverState.v2`, exact serialized-repository preimage plus resolution-context binding, per-repository `legacy_epoch_high_water`, authenticated legacy-key/archive evidence view, `shared_broker_root`, `repository store root`, `train-local coordinator root`
- **Interfaces consumed**: `FABPUB immutable test digest`, `FABPUB stage selectors`, `FABPUB_LEGACY_CUTOVER_NODEIDS`, `FABPUB_LEGACY_CUTOVER_VECTOR_SHA256`, `AdmissionRequest` (pre-existing), `AdmissionRecord` (pre-existing), `EvidenceRecord` (pre-existing), `BrokerAdmissionPolicy` (pre-existing), `build_routing_broker_client` (pre-existing), `run-train CLI broker construction` (pre-existing)
- **Parallel-safe**: no; allocator implementation and every production store-root selector move together on an implementation base that already contains SL-0.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-1-T1` | `test` | (none) | SL-1 behavior only | immutable allocator/cutover-barrier/repository-route/CLI selectors | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator or fabpub_legacy_cutover_preserves_high_water or fabpub_legacy_cutover_preserves_outcome_ambiguous or fabpub_legacy_cutover_conflict_collision or cli_main_run_train_wires_a_routing_broker or cli_same_stem_trains_get_distinct_broker_roots"` is RED immediately before the edit |
| `FABPUB-1-T2` | `impl` | `FABPUB-1-T1` | SL-1 owned production files | none | Implement `admit_next`, global authenticated inventory/partition/snapshot/retirement/receipt handling, canonical repository store routing, and train-independent broker-root construction |
| `FABPUB-1-T3` | `verify` | `FABPUB-1-T2` | SL-1 behavior only | immutable SL-0 allocator/cutover-barrier/CLI selectors | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator or fabpub_legacy_cutover_preserves_high_water or fabpub_legacy_cutover_preserves_outcome_ambiguous or fabpub_legacy_cutover_conflict_collision or cli_main_run_train_wires_a_routing_broker or cli_same_stem_trains_get_distinct_broker_roots"` is GREEN |

- **Tasks**:
  - test: Run the activated allocator/cutover-barrier/repository-route selector plus the two preserved CLI construction nodeids before and after the edit; the marker is still absent, every cutover test creates production-shaped legacy files before canonical client construction, and the SL-2 completed-effect replay, later broker transaction/resume, and CHANGELOG tests are deliberately not selected.
  - impl: Implement `admit_next(make_request, *, attempt_id, precondition)` under the existing flock; preserve fail-closed `policy is None`, policy denial, and `epoch_blocked` checks on fresh and dedup paths; rebuild and compare a dedup hit; allocate only after the in-lock precondition; enforce both equalities; retain append-only fsync persistence and the legacy `admit()` surface. Compute fresh epoch from the maximum of canonical records and the authenticated repository partition receipt's `legacy_epoch_high_water`; an absent global `ARMED` transaction, missing partition, invalidity, or digest drift is denial, never epoch 1.
  - impl: In `evidence.py`, add a read-only global-transaction/partition-receipt-bound archive view and a cutover-only idempotent terminal-promotion path. Validate complete source histories, exact serialized preimages/resolution contexts, and raw/parsed digests before exposing effective records; surface any archived ambiguous or orphaned in-flight record through the matching partition's `epoch_blocked`; never mutate archives or let ordinary callers invoke the import path. Promotion writes canonical intent/terminal records under the shared lock and records global transaction, partition, source, preimage, context, and digest provenance so restart can finish only the same transition.
  - impl: In `live.py`, replace `_repo_store_slug(repo_absolute_path)` with strict `CanonicalRepositoryIdentity.v1`; use the full identity under `<shared_broker_root>/repositories/`, key stores by that identity, validate every adapter worktree resolves to it, and allow distinct worktree-bound services/adapters to share that one admission/evidence root. Implement `LegacyBrokerCutoverManifest.v2` and the single global `LegacyBrokerCutoverTransaction.v2`: inventory every legacy-shaped leaf before mutation; recompute exact serialized-repository and resolved-train preimages with their recorded resolution contexts; partition every leaf to one canonical Git identity; acquire all locks deterministically; strictly validate JSONL histories; retire sources by same-filesystem rename plus tombstone; write every `LegacyRepositoryPartitionReceipt.v2`; and arm globally only after every partition and source re-verifies. Crash at every transition must resume to the same bytes, while ambiguity, conflicts, or incomplete inventory fail closed. A per-repository client never scans and claims the global root independently.
  - impl: In `cli.py`, split train-local `CoordinatorRuntime.coordinator_root`/transaction storage from allocation storage. Keep a train-path-derived coordinator root if needed for transaction collision avoidance, but always pass exact `<ledger-dir>/broker` to `build_routing_broker_client`; no train path, stem, or roadmap digest may namespace the broker root. Before any client construction, load the explicit metadata-only global cutover manifest, freeze the invocation/resolution context used by each exact serialized repository argument, inventory the whole legacy root, and invoke the authenticated global cutover barrier once. If legacy roots exist and the manifest is absent/incomplete, if any serialized preimage cannot be resolved under its recorded context, or if any leaf cannot be partitioned, stop before `run_train` with a typed non-human cutover blocker. Two train roadmaps sharing a ledger directory therefore share repository allocator state and cutover evidence but not transaction checkpoints.
  - verify: `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator or fabpub_legacy_cutover_preserves_high_water or fabpub_legacy_cutover_preserves_outcome_ambiguous or fabpub_legacy_cutover_conflict_collision or cli_main_run_train_wires_a_routing_broker or cli_same_stem_trains_get_distinct_broker_roots"`

### SL-2 — Broker fresh-publish gate and recoverable publish transaction

- **Scope**: Freeze canonical-plus-authenticated-legacy terminal replay before a mandatory fresh-publish envelope gate, allocate every fresh publish through `admit_next`, and close the ref/checkpoint crash hole by constructing and durably checkpointing the exact expected commit object before CAS-moving the branch.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/convergence/contracts.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/verbs.py`, `phase-loop-runtime/src/phase_loop_runtime/publishing.py`
- **Interfaces provided**: `PreAdmissionEnvelope`, `PublishPreTrailerIntent.v1`, `CommitConstructionInputs.v1`, `PublishIntent`, `PublishCheckpoint`, `PublishTransactionState`, `PublishTransactionStore`, `PublishResumeCandidate`, `inspect_publish_resume_candidate`, `validate_transaction_owned_workspace`, `derive_publish_transaction_id(pre_trailer_intent_bytes)`, `original_commit_message_sha256`, `pre_trailer_intent_sha256`, `final_commit_message_sha256`, `expected_commit_oid`, `committed_head_sha`, `final_commit_object_sha256`, `COMMIT_OBJECT_DURABLE`, `exact-ref CAS recovery`, `publish_attempt_id(repo, branch, head_sha)`, `fresh publish requires PreAdmissionEnvelope`, `non-publish finalized AdmissionRequest compatibility path`, `canonical-plus-legacy completed terminal replay compatibility path`, `receipt-bound terminal promotion`, `publish_from_worktree transaction recovery route`, `base-free publish_committed_branch_idempotency_key(repo, branch, head_sha)`
- **Interfaces consumed**: `FABPUB immutable test digest`, `FABPUB stage selectors`, `FABPUB_LEGACY_CUTOVER_NODEIDS`, `LegacyBrokerCutoverTransaction.v2`, `LegacyRepositoryPartitionReceipt.v2`, authenticated exact-preimage/archive evidence view, `CanonicalRepositoryIdentity.v1`, `repository store root`, `LinearizableAdmissionStore.admit_next`, `make_request(epoch, attempt_id) contract`, `FencedAdmissionFactory` (pre-existing), `BrokerEvidenceStore` (pre-existing), `BrokerProviderAdapter` (pre-existing)
- **Parallel-safe**: no; the request union, service dispatch, transaction validation, and publish resume route move together after SL-1.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-2-T1` | `test` | (none) | SL-2 behavior only | immutable broker/legacy-terminal/intent/checkpoint/transaction/dual-path selectors | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_broker or fabpub_legacy_cutover_replays_terminal_completed_effect or fabpub_intent or fabpub_checkpoint or fabpub_transaction or fabpub_dual_path"` is RED immediately before the edit |
| `FABPUB-2-T2` | `impl` | `FABPUB-2-T1` | SL-2 owned production files | none | Implement the request union, service branch, and recoverable transaction state machine |
| `FABPUB-2-T3` | `verify` | `FABPUB-2-T2` | SL-2 plus finalized compatibility callers | immutable SL-0 files only | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_publishing) and not fabpub_train_handoff and not fabpub_train_resume and not fabpub_changelog"` is GREEN |

- **Tasks**:
  - test: Run the activated SL-2 RED selector first, including the exact fresh-finalized-publish rejection node, the literal legacy completed-effect replay node, and their mutation/positive controls, then the activated GREEN command that includes all migrated direct `BrokerService.execute` callers/fakes in `test_convergence_broker_verbs.py`, `test_convergence_broker_revocation_race.py`, `test_convergence_broker_credsep.py`, and `test_convergence_live_enable.py`. The marker is still absent. Do not run the SL-3 authentic train-resume or SL-4 CHANGELOG selectors here.
  - impl: Add an epoch-free `PreAdmissionEnvelope` carrying canonical repository identity plus separately validated adapter worktree path, train/node/action identity, roadmap/effective-code/dependency/verification pre-images, expected-version predicate, authority scope, deterministic operation identity, transaction ID, original-message digest, pre-trailer intent digest, and separately compared final message/expected OID/raw-commit digests, but no epoch, caller attempt ID, fence token, caller approval digest, or caller fencing idempotency key. Permit it only on `PUBLISH_COMMITTED_BRANCH`.
  - impl: Widen `BrokerRequest.admission` only to `AdmissionRequest | PreAdmissionEnvelope`. In `BrokerService.execute`, compute the completed-effect key from canonical repository identity, branch, and exact head and return an already-completed terminal replay before inspecting that union. Immediately after replay, require `PreAdmissionEnvelope` for every remaining publish; exact `TypeError("fresh publish_committed_branch requires PreAdmissionEnvelope")` leaves admission, evidence, and adapter untouched. Envelope routes to `admit_next`, finalized request routes to `admit()`, and the preceding guard makes that branch non-publish-only. A fresh envelope must name the exact `COMMITTED_HEAD_RESOLVED` transaction and match canonical repository identity, validated adapter worktree, original-message/pre-trailer/transaction identity, final message, `expected_commit_oid == committed_head_sha`, and raw-object digest; resolve approval from `merge-base(expected_commit_oid, base_tip_sha)`; bind complete authority without feeding post-object values into `transaction_id`; call `admit_next`; replace the envelope with the stored finalized request; and only then record broker intent/call the adapter. Moving `HEAD`/base refs, absolute worktree identity, caller epoch/attempt/approval, and any alternate OID are never substitutes.
  - impl: Before that fresh-publish gate or any admission/evidence mutation, derive the canonical completed-effect key and every exact legacy key from the repository partition receipt's preserved serialized repository bytes, not from the resolved worktree or a normalized alias. Validate the global transaction, partition receipt, resolution-context, and archive digests under the shared lock; collapse only byte-identical effective terminals and fail closed on ambiguity, in-flight legacy intent, invalid transition, canonical/legacy disagreement, or divergent source terminals/references. Promote one authenticated completed terminal to the canonical key with global/partition/source/preimage/context digest provenance and replay it without invoking either admission method or the adapter. A crash after canonical intent but before terminal append resumes only that same promotion; a second identical request is a no-op. Never use a base-blind scan, PR URL search, semantic commit match, guessed/resolved alias, or unhashed directory name to translate evidence.
  - impl: Keep `publish_committed_branch_idempotency_key(repo, branch, head_sha)` byte-for-byte base-free and epoch-free as required by `EC-FABPUB-4`, but define `repo` as `CanonicalRepositoryIdentity.v1`, never an absolute worktree/train path. Do not fold `base`, adapter path, train path, `attempt_id`, `lease_epoch`, or randomness into the completed-effect key.
  - impl: Add versioned metadata-only `PublishPreTrailerIntent.v1`, `CommitConstructionInputs.v1`, `PublishIntent`, and `PublishCheckpoint` plus `PublishTransactionStore` under an explicit train-local `checkpoint_root`, never under the published repo or shared allocator store. Stage/audit first; resolve exact symbolic branch ref, full parent/base-tip OIDs, object format, tree OID, staged-diff/presentation/authority digests, canonical repository identity, and exact original caller/default message bytes with no Unicode/newline normalization; reject any `^FABPUB-Intent-ID:` line. Exclude `transaction_id` and every final-message/expected-head/committed-head/commit-object field from all hashed transaction preimages and recompute nested digests. Canonicalize the explicit allowlist, derive only the domain-separated transaction ID, append the exact one-trailer suffix, and freeze the final bytes separately. Freeze author and committer names/emails plus exact `@<unix-seconds> <timezone>` dates, encoding header presence/value, ordered extra-header names/bytes, Git object format, and explicit signing mode/format/key. Implicit config, hooks, cleanup, unsupported headers, or ambient dates are forbidden. If signing is enabled, construct once and capture the exact produced signature bytes as part of the raw object; retries after `COMMIT_OBJECT_DURABLE` never re-sign.
  - impl: Persist `PREPARED` with every commit-construction input before object construction. Construct the actual commit object without changing a ref using exact commit-tree/object plumbing, then read and validate its raw bytes, obtain its repository-format OID as `expected_commit_oid`, compute SHA-256 `final_commit_object_sha256`, and ensure the object is readable. Under the transaction lock, atomically persist both exact values and all construction inputs at `COMMIT_OBJECT_DURABLE` with file fsync, replace, and parent-directory fsync before ref mutation. Only then require `HEAD` to be the recorded symbolic branch and CAS-update that exact ref from `parent_head_sha` to `expected_commit_oid`; never use porcelain `git commit`, an unguarded update, or a moving `HEAD` target.
  - impl: Recovery resolves the transaction before new staging. Add a read-only `inspect_publish_resume_candidate` plus authoritative `validate_transaction_owned_workspace`; they derive the deterministic checkpoint from explicit coordinator/node context, refuse ambiguous multiple active pointers, validate every checkpoint/identity/fencing/dependency binding, and return a typed candidate only for an active resumable or `TERMINAL_SEALED` transaction. For a parent-ref `PREPARED`/`COMMIT_OBJECT_DURABLE` arm, require exact equality of index tree, cached binary-diff digest, and ordered staged path tuple with the frozen transaction plus zero conflicts, unstaged bytes, untracked paths, extra/missing paths, or submodule drift. For expected-ref/later arms require index/worktree equality with the expected commit tree and ordinary cleanliness. `PREPARED` with no object checkpoint and ref still at parent may reconstruct from the frozen inputs, then must durably establish a single expected OID before CAS. `COMMIT_OBJECT_DURABLE` must find the exact object/raw digest. If the exact ref equals parent, retry only `update-ref <exact-ref> <expected> <parent>`; if it equals expected, advance; any other OID, wrong/detached symbolic ref, missing/corrupt object, raw-digest mismatch, construction-input/workspace mismatch, or probe/use drift becomes a typed `CONFLICTED`/stale intent before admission. `COMMITTED_HEAD_RESOLVED` records only `committed_head_sha == expected_commit_oid`; recovery never selects by parent/tree/message/trailer. Exact final message parsing still requires one matching terminal trailer, but that validates the expected raw object and never authorizes another OID. Transaction-owned staged state after `PREPARED`/`COMMIT_OBJECT_DURABLE` is a recoverable exact CAS arm, not `nothing_staged` or a general dirty-worktree exception.
  - impl: Enforce only `PREPARED -> COMMIT_OBJECT_DURABLE -> COMMITTED_HEAD_RESOLVED -> ADMISSION_DURABLE -> BROKER_INTENT_DURABLE -> ADAPTER_STARTED -> TERMINAL_SEALED`, plus terminal `ABANDONED`/`CONFLICTED`. `PREPARED` carries transaction identity, exact final message, and complete object inputs; `COMMIT_OBJECT_DURABLE` adds exact expected OID/raw digest before ref movement; `COMMITTED_HEAD_RESOLVED` proves exact-ref equality. After each authoritative object/ref/admission/evidence fsync, atomically project the matching state and all identity classes. Projection lag advances only on exact matches. Persist `ADAPTER_STARTED` immediately before the adapter call; broker intent without it may resume, while adapter-start without terminal evidence fails outcome-ambiguous and never re-invokes. Seal canonical repository identity, original/pre-trailer/transaction identity, final message, expected/committed OID, and raw digest into `TERMINAL_SEALED`.
  - impl: Preserve the finalized-admission API only for non-publish BrokerService callers and the finalized request passed from broker to provider adapters. A completed terminal publish replay remains shape-agnostic because it returns before authorization and performs no admission or adapter call. An envelope requires `checkpoint_root`; absence fails closed. Exact active input resumes only after reconstructing and comparing the same original-message/pre-trailer identity and separate final bindings; active mismatches never overwrite the record. Cleanup first writes and fsyncs an `ABANDONED` or `CONFLICTED` tombstone, then archives/clears the active pointer under the same lock; those states never authorize admission or effect. Only a terminal/tombstoned prior transaction may be superseded by a freshly validated different-head intent. Retain all sealed pre-trailer/final transaction and evidence bindings for exact terminal replay.
  - verify: `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_publishing) and not fabpub_train_handoff and not fabpub_train_resume and not fabpub_changelog"`

### SL-3 — Live train authority handoff and authentic resume

- **Scope**: Make normal and prebuilt train publication supply canonical repository/epoch-free authority and a train-local transaction root, then route default production `run_train` through an exact transaction-aware preflight and pre-author resume that publishes only the pre-ref-recorded commit through the shared idempotent broker path.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/convergence/fencing.py`, `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`
- **Interfaces provided**: `_default_build_admission -> PreAdmissionEnvelope`, `CoordinatorRuntime.coordinator_root -> train-local publish transaction root`, `run_train pre-clean transaction probe`, `_default_preflight exact transaction-owned workspace allowance`, `run_train pre-author publication resume`, `post-terminal unqualified clean reconciliation`, `canonical repository authority pre-images`, `original caller message identity`, `commit-stable approval pre-images`, `authentic normal/prebuilt exact-OID crash resume`
- **Interfaces consumed**: `FABPUB immutable test digest`, `FABPUB stage selectors`, `CanonicalRepositoryIdentity.v1`, `shared_broker_root`, `PreAdmissionEnvelope`, `PublishPreTrailerIntent.v1`, `CommitConstructionInputs.v1`, `PublishIntent`, `PublishCheckpoint`, `PublishTransactionState`, `PublishResumeCandidate`, `inspect_publish_resume_candidate`, `validate_transaction_owned_workspace`, `derive_publish_transaction_id(pre_trailer_intent_bytes)`, `expected_commit_oid`, `publish_from_worktree transaction recovery route`, `publish_attempt_id(repo, branch, head_sha)`, `FencedAdmissionFactory` (pre-existing), `ApprovalBinding` (pre-existing)
- **Parallel-safe**: no; `train_runner.py` is phase-single-writer and this lane consumes the complete SL-2 transaction contract.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-3-T1` | `test` | (none) | live normal/prebuilt train routes | immutable train handoff/resume selectors including exact default-`run_train` recovery node | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_train_handoff or fabpub_train_resume"` is RED immediately before the edit |
| `FABPUB-3-T2` | `impl` | `FABPUB-3-T1` | SL-3 owned production files | none | Return the envelope, pass the node transaction root, and add the narrow pre-clean/pre-author `run_train` resume route |
| `FABPUB-3-T3` | `verify` | `FABPUB-3-T2` | live normal/prebuilt train routes | immutable SL-0 files only | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_fencing or test_train_prebuilt or test_convergence_train_integration) and not fabpub_changelog"` is GREEN |

- **Tasks**:
  - test: Run authentic selectors above `BrokerService`. In exact node `test_fabpub_train_resume_post_commit_pre_checkpoint`, retain lower-level publisher controls but make the mandatory arm invoke real execute-mode `run_train` with default preflight, default `run_loop` selection, and default publisher selection. At `after_commit_object_checkpoint_before_ref_cas`, restart must recognize the exact staged transaction before the ordinary clean gate, preserve every other preflight check, resume exact CAS/broker publication before injection or the default author-work canary, and never return `preflight_failed`. It then dies at preserved `after_git_commit_success_before_committed_checkpoint`; default-path restart sees the exact expected OID, advances without semantic adoption or `run_loop`, and reaches the real broker with canonical repository authority. Preserve `after_committed_checkpoint_before_broker_execute`; in the compound arm kill at `after_broker_intent_before_adapter_started`, then prove one shared-store admission, one adapter effect, exact terminal sealing, an unqualified clean index/worktree, and one `pr_open`. Run the frozen unrelated/mismatched dirty-state negative table and ordinary clean/dirty controls in the same node without parametrized nodeid expansion. Repeat lower-level controls for prebuilt no-rewrite, faithful parent/expected recovery, all trailer/authority/final-binding drifts, alternate OID/descendant/detached ref, cleanup/different-head, adapter ambiguity, and terminal replay. Assert every crash/ordering anchor, state, ref/OID/raw digest, state-specific index/worktree condition, canonical repository/root/store path, allocation/effect/author/injection count, and evidence/ledger record.
  - impl: Refactor `_default_build_admission` to return `PreAdmissionEnvelope`; preserve surrogate-escaped owned-path digest behavior and roadmap/dependency/verification inputs, derive/carry canonical repository identity separately from adapter worktree path, and remove the literal epoch-1 lease plus every pre-object `HEAD`, attempt, fence, approval, or fencing-key binding.
  - impl: Pass a deterministic node-scoped transaction root beneath train-local `CoordinatorRuntime.coordinator_root` to `publish_from_worktree` for normal and prebuilt routes; never reuse the shared broker store for transaction checkpoints. In `run_train`, after schema/topology resolution but before ordinary clean-worktree evaluation, probe those exact roots and pass only typed, fully validated candidates into `_default_preflight`; absent candidates use `_check_repo_clean` unchanged, and candidates still run auth/remote/base checks. A present invalid/corrupt/tombstoned transaction is a fail-closed preflight error, not permission to ignore dirt. After the all-repo gate and before upstream injection or `run_loop`, revalidate the candidate and resume through the ordinary `publish_from_worktree`/broker call with the checkpoint-frozen owned paths, message/body/envelope, dependency/fencing authority, exact object, and ref CAS. Do not call `run_loop`, restage, reconstruct an alternate object, or derive authority from current dirt. Require `TERMINAL_SEALED` plus a second ordinary no-allowance clean check before using the normal success-ledger/downstream path. Capture immutable envelope/presentation pre-images, exact caller/default message bytes, and commit-construction inputs before `PREPARED`; resume reconstructs the pre-trailer identity and compares the recorded exact OID/raw object/ref, never a fresh clean-worktree diff or semantic commit search. Do not add a global flag/env bypass, suppress other nodes' checks, create a second train-ledger status, or treat a blocked ledger append as transaction state.
  - impl: Keep the fencing digest formulas unchanged: when SL-2's `make_request` invokes the factory, pass its deterministic `attempt_id` to `lease`, its allocated epoch as `lease_epoch`, and its post-commit approval binding to `create`. Do not change the FAB review-round `epoch` namespace.
  - verify: `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "(fabpub_ or test_convergence_fencing or test_train_prebuilt or test_convergence_train_integration) and not fabpub_changelog"`

### SL-4 — Documentation, capability activation, chronology, and verification reducer

- **Scope**: Retract publish byte-neutrality, prove the complete implementation under explicit activation, install the production capability marker in a marker-only commit, and reduce JUnit plus chronology evidence without widening into FABREADMIT or RESIDUAL.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py`, `CHANGELOG.md`
- **Interfaces provided**: `phase_loop_runtime.fabpub_capability.FABPUB_CAPABILITY_VERSION == 1`, `IF-0-FABPUB-1`, `FABPUB verification evidence`, `publish byte-neutrality retraction`
- **Interfaces consumed**: `FABPUB immutable test digest`, `PHASE_LOOP_TDD_EXPECT_FABPUB=1`, frozen selectors/nodeids/counts/anchors, `FABPUB_IDENTITY_NODEIDS`, `FABPUB_SHARED_ALLOCATOR_NODEIDS`, `FABPUB_LEGACY_CUTOVER_NODEIDS`, `FABPUB_IDENTITY_VECTOR_SHA256`, `FABPUB_COMMIT_OBJECT_VECTOR_SHA256`, `FABPUB_REPOSITORY_IDENTITY_VECTOR_SHA256`, `FABPUB_LEGACY_CUTOVER_VECTOR_SHA256`, `fabpub_tdd_chronology executable`, `CanonicalRepositoryIdentity.v1`, `LegacyBrokerCutoverTransaction.v2`, `LegacyRepositoryPartitionReceipt.v2`, `LegacyBrokerCutoverState.v2`, `shared_broker_root`, `LinearizableAdmissionStore.admit_next`, `PreAdmissionEnvelope`, `PublishPreTrailerIntent.v1`, `CommitConstructionInputs.v1`, `PublishIntent`, `PublishCheckpoint`, `PublishTransactionState`, `expected_commit_oid`, `publish_from_worktree transaction recovery route`, `_default_build_admission -> PreAdmissionEnvelope`
- **Parallel-safe**: no; this terminal documentation/activation/verification reducer runs after every producer and is excluded from writer waves.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `FABPUB-4-T1` | `test` | (none) | `CHANGELOG.md` observable | immutable CHANGELOG selector | `env PHASE_LOOP_TDD_EXPECT_FABPUB=1 PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_changelog"` is RED before the doc edit |
| `FABPUB-4-T2` | `impl` | `FABPUB-4-T1` | `CHANGELOG.md` | none | Add the explicit byte-neutrality retraction; the capability marker remains absent |
| `FABPUB-4-T3` | `verify` | `FABPUB-4-T2` | complete pre-activation implementation | all immutable FABPUB assertions | Run the exact focused suite under `PHASE_LOOP_TDD_EXPECT_FABPUB=1`; every expected nodeid passes and the reducer accepts the pre-activation JUnit before marker installation |
| `FABPUB-4-T4` | `impl` | `FABPUB-4-T3` | `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py` only | none | In a marker-only commit, install exact `FABPUB_CAPABILITY_VERSION = 1`; do not edit any other path |
| `FABPUB-4-T5` | `verify` | `FABPUB-4-T4` | default focused suite | immutable SL-0 suite | Run the frontmatter `automation.suite_command` with activation absent, then audit structured JUnit for the exact expected nodeids and zero phase skips/errors/xfails |
| `FABPUB-4-T6` | `verify` | `FABPUB-4-T5` | broad compatible surface | all non-wrapper verification | Before implementation panel and merge, run every compatible command under `## Verification`, excluding only the explicitly labeled final-landing chronology wrappers; all must pass |
| `FABPUB-4-T7` | `verify` | `FABPUB-4-T6` | landed Git topology/evidence | final-landing wrappers | After merge, run the runner-owned immutable-blob preflight plus chronology reducer and rerun final JUnit reduction before emitting `IF-0-FABPUB-1` |

- **Tasks**:
  - test: Run the activated `fabpub_changelog` assertion before the doc edit and retain its intended RED anchor; the marker remains absent, so no unactivated new falsifier is exposed to default CI.
  - impl: Add one explicit CHANGELOG entry retracting publish byte-neutrality because publish admission epochs and derived fence records are intentionally renumbered. Do not claim FABREADMIT, its dormant flag flip, or RESIDUAL's unresolved `base` identity carry as delivered.
  - verify: With the marker still absent, run the exact focused selector under activation and emit `.phase-loop/runs/fabpub-preactivation.junit.xml`. The frozen reducer requires every `FABPUB_EXPECTED_NODEIDS` entry once, all new/migrated assertions active—including fresh-finalized-publish rejection, both identity/exact-OID nodes, the shared-allocator node, all six production-shaped legacy-cutover nodes, and preserved CLI nodeids—the independently parsed totals and all four vector digests to match their panelled values, zero skips/errors/xfails, and a passing result. It also requires `.phase-loop/runs/*/fabpub-legacy-cutover.json` to bind the exact global manifest, serialized-preimage, resolution-context, source, archive, tombstone, partition-receipt, transaction, counts, per-repository high-water/ambiguity, and global `ARMED` digests without raw credential values. Only after this passes may the marker commit be created.
  - impl: Add `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py` with exact `FABPUB_CAPABILITY_VERSION = 1` in a separate marker-only commit whose first parent is the recorded pre-activation head. No test, guard, selector, expected nodeid/count, anchor, reducer, production implementation, or CHANGELOG edit may share that commit. The marker enables the cutover-capable code but does not bypass the global `ARMED` transaction barrier: every canonical client refuses absent, partial, or conflicting cutover evidence.
  - verify: Unset `PHASE_LOOP_TDD_EXPECT_FABPUB`, run the frontmatter suite command, and emit `.phase-loop/runs/fabpub-final.junit.xml`. `fabpub_tdd_chronology --mode final-junit` compares testcases/totals to the frozen inventory, proves fresh-finalized-publish rejection, both identity/exact-OID nodes, the one shared-allocator node, all six legacy-cutover nodes, and migrated CLI controls ran by default, checks all vector/test/JUnit/path-inventory/cutover-evidence digests, and requires zero phase skips/errors/xfails.
  - verify: Before the implementation panel and merge, run the focused JUnit/audit, broad `broker or admission or convergence or fab` suite, whole compatible `-m "not dotfiles_integration"` suite, ruff, validators, and `git diff --check`. The only exclusions are the final-landing ancestry/blob wrappers, which require the actual landed SHA; no ordinary compatibility suite or pre-existing test may be excluded.
  - verify: After landing, the runner-owned wrapper first uses Git outside the candidate verifier process to compare every SL-0 blob at the landed SHA byte-for-byte with `FABPUB_TESTS_ONLY_COMMIT_SHA` and to verify the recorded ancestry. Only after that independent preflight may it launch the stdlib-only chronology blob whose SHA-256 matches the test-panel record. This removes the same-process self-verification cycle: a current-head verifier never certifies its own immutability. Rerun default focused JUnit/audit and the full verification set on the landed SHA.

## Execution Policy

- work-unit defaults: work-unit=`lane_execute`, effort=`high`, unsupported=`inherit_default`, inherit-default=`true`
- execute: executor=`claude`, model=`claude-opus-5`, effort=`high`, work-unit=`lane_execute`, unsupported=`inherit_default`, inherit-default=`true`, reason=`FABPUB is assigned to one whole-phase Claude Opus 5 implementation author`
- SL-4: work-unit=`phase_reducer`, effort=`high`, unsupported=`inherit_default`, inherit-default=`true`, reason=`cross-lane documentation chronology and acceptance synthesis`

## Execution Notes

- The preserved local-three panel record binds predecessor plan SHA-256 `c0b895a9f758edfd1944ed4a154f5aee429dab4b96d1fc828037e6ac9ab96857` to predecessor roadmap SHA-256 `1e8ea70ceae55d326cd84b092e1b9e879180d7b0e774140c3dd00e6ed63b7071`, with Grok/Gemini `AGREE`, GPT-5.6 Sol `DISAGREE` on exact serialized-repository preimages and global multi-repository cutover, and no Fable result. Earlier digests remain superseded and unapproved. This plan computes its own digest externally, never self-embeds it, and must clear a fresh exact current-roadmap panel from Fable 5, GPT-5.6 Sol, Gemini 3.6 Flash, and Grok 4.5 before SL-0. Every panel is by-reference: each seat receives an immutable staged bundle path or `context_refs` naming the exact plan, roadmap, manifest FABPUB record, panel predecessor, cited production/test paths, and all digests, and must open those bytes. Pasted prose, a different checkout, unusable mandatory legs, or any dissent does not authorize dispatch.
- The coordinator assigns every implementation/repair lane to one whole-phase `executor=claude`, `model=claude-opus-5`, `effort=high` author and records that exact assignment. Both runtime schedulers remain off with `--phase-scheduler off --lane-scheduler off`; the lane DAG does not authorize scheduler fanout, a second author vendor, cross-vendor work-unit rotation, or silent model fallback.
- SL-0 is a hard default-green Git boundary. Let `B=FABPUB_PREIMPLEMENTATION_BASE_SHA` and `T=FABPUB_TESTS_ONLY_COMMIT_SHA`; chronology requires `T^ == B`, the `B..T` changed paths equal a non-empty subset of the exact SL-0 set, production/marker/docs blobs are unchanged, and the marker is absent. Activated T1 runs against T tests plus B-identical production and includes finalized-request rejection, both identity/exact-OID falsifiers, the shared-allocator falsifier, all six production-shaped legacy-cutover falsifiers, the exact default-`run_train` recovery falsifier, preserved CLI migration nodes, and every migrated fresh-publish node. Unactivated T2 permits only new nodes to skip; migrated nodes execute their legacy-compatible branches. Tests-only merge occurs only after both evidence audits and a fresh by-reference four-seat exact test/vector digest review pass.
- `_fabpub_tdd_guard.py` is the single source of truth for activation, marker probe, selectors, exact nodeid sets/counts, skip reason, RED anchors, all four vectors, and migration modes. Counts are recomputed from T after the preserved rejection node, two exact `FABPUB_IDENTITY_NODEIDS`, one exact `FABPUB_SHARED_ALLOCATOR_NODEIDS`, six exact `FABPUB_LEGACY_CUTOVER_NODEIDS`, one exact `FABPUB_RUN_TRAIN_RECOVERY_NODEIDS`, and all migrations. The identity and run-train nodes retain their planned counts while gaining exact-OID/header-rewrite and default-preflight/pre-author recovery subcases; the shared-allocator node contributes `+1` and the legacy-cutover repair contributes `+6`, for exactly `+7` new/RED/expected/inactive-skip entries relative to `d9cda3e05c1719111c50ed556625b7068fa28d623debad94276d170fd9d47100`. `default_skip_count == len(FABPUB_NEW_PRODUCTION_NODEIDS)`, every activated RED node fails once at its intended anchor, and preactivation/final each run the exact expected count with zero skips/errors/xfails. Panel evidence freezes test-tree, guard, transaction-identity/commit-object/repository-identity/legacy-cutover vectors, selector, nodeid-set/count, anchor-map, raw RED, global cutover/partition receipt evidence, and every JUnit SHA-256. SL-1 through SL-4 cannot edit an SL-0 path/evidence; any test repair returns to SL-0, recomputes every affected count/digest, reruns RED/default evidence on B-identical production, and re-panels.
- Runner chronology metadata freezes `roadmap_sha256`, revised `plan_sha256`, all superseded plan digests, `B`, `T`, `FABPUB_IMPLEMENTATION_BASE_SHA=I`, first production commit `P`, `FABPUB_PREACTIVATION_HEAD_SHA=C`, marker-only commit `M`, implementation candidate `H`, and landed SHA `L`, plus ownership/test-inventory, test-tree/guard, all vector/test, selector/nodeid/count/migration/anchor, raw-RED, cutover-receipt, and every JUnit digest. It requires `T` ancestor of `I`, `I` and `T` ancestors of `P^`, `P` the first first-parent commit after `I` changing any of the eleven production/doc paths, `C` containing every non-marker source/doc change with no SL-0 change, `M^ == C`, `C..M` containing only the exact marker, `H` descending from `M`, and every phase-owned blob at `L` equal to reviewed `H`. The marker is absent at `B/T/I/C` and exact at `M/H/L`.
- `phase_loop_runtime.fabpub_tdd_chronology` is evidence logic frozen in T, stdlib-only, and imports no FABPUB production module. Final landing never trusts the current process to certify that verifier: a runner-owned wrapper outside the implementation write set checks Git ancestry and every SL-0 blob against T, verifies the chronology blob SHA-256 from the test-panel record, then executes that exact blob in a fresh process against L. Runner timestamps, commit messages, a plain `git log`, moving refs, or self-reported current-head hashes are not substitutes.
- Four faithful crash anchors remain distinct and frozen before production changes: new `after_commit_object_checkpoint_before_ref_cas`, preserved historical `after_git_commit_success_before_committed_checkpoint` immediately after successful exact ref CAS, `after_committed_checkpoint_before_broker_execute`, and `after_broker_intent_before_adapter_started`. The first proves expected OID/raw digest are durable before ref movement; the second proves exact-OID recovery after ref movement; the latter two preserve approved broker crash surfaces. The same immutable run-train node also asserts the production ordering anchors `before_default_clean_preflight_transaction_probe`, `before_run_loop_publish_transaction_resume`, and `after_terminal_publish_before_unqualified_clean_recheck`; deleting/reordering the probe, resume, or clean reconciliation must kill it.
- The finalized `AdmissionRequest` compatibility contract is narrow: non-publish BrokerService callers use `.admit()`, provider adapters receive the broker-finalized request, and already-completed publish evidence can replay before authorization. Every other publish entry—including direct services/fakes, live builders, routing clients, normal/prebuilt train paths, absent evidence, and in-flight continuation—uses `PreAdmissionEnvelope` plus `admit_next`. The broad compatible suite must prove migrated revocation locking, credential-separation fakes, live/routing clients, non-publish finalized behavior, finalized completed-terminal replay, and rejection of a fresh finalized publish with zero admission/evidence-intent/adapter side effects.
- The publish transaction state machine has one authority per boundary: the transaction file for canonical repository/pre-trailer identity and projection; its exact state-specific index/worktree attestation; Git object storage; exact branch-ref CAS; canonical `admissions.jsonl` plus the authenticated partition floor; canonical `evidence.jsonl` plus immutable partition-bound archives; `LegacyBrokerCutoverTransaction.v2` for whole-root completeness/activation; and `LegacyRepositoryPartitionReceipt.v2` for repository keyspace provenance. `PREPARED` freezes every object input; `COMMIT_OBJECT_DURABLE` freezes expected OID/raw digest before ref movement; `COMMITTED_HEAD_RESOLVED` requires exact-ref equality. Ref-at-parent may retry the same CAS/object, ref-at-expected may finalize, every other OID blocks. Parent/tree/message/trailer equality, a path-only dirt match, a guessed/resolved legacy alias, or a custom preflight result is never adoption or resume authority. All later projections/evidence carry canonical repository identity and exact final bindings without folding them into `transaction_id`.
- Allocation state is shared only within a canonical repository: all linked worktrees and train roots under one explicit broker root use one full-digest store and one epoch sequence, seeded above every authenticated legacy high-water; distinct Git common directories use distinct stores and ambiguity domains. The train-local coordinator root holds transaction checkpoints only. Tests require sequential `legacy_max + 1, legacy_max + 2`, concurrent unique consecutive allocation, alternate-worktree/train replay without reallocation, authenticated old-key completed-effect replay without adapter entry, permanent legacy ambiguity/in-flight blocking, distinct-repository isolation, no live writable worktree/train-hash stores after retirement, and independent mutations restoring either production hash or bypassing the receipt to fail.
- The complete execution-owned write set is the sorted 24-path disjoint union of SL-0 through SL-4. Its digest is SHA-256 over the LF-joined sorted repo-relative paths plus terminal LF: `93f3a95a277ee2e5d2543aed6baea4c839c55e8be32ee1264a0283e1fd829ead`. The sorted 12-path test inventory uses the same framing and digest `c7c498a73c76364dee1d02b0ee73eb6b6fae5ed4311d135d82ed17c4fec18e51`. The complete sorted six-node `FABPUB_LEGACY_CUTOVER_NODEIDS` inventory uses the same framing and SHA-256 `7c89b013678556c360f0257301d414c23bb6744500fa37c2b370497af815cb8f`; the current repair adds two of those nodeids inside already-owned `phase-loop-runtime/tests/test_fabpub_shared_epoch.py`. Lane count remains five, test-path count remains twelve, and ownership remains disjoint. `plans/manifest.json` mirrors all exact inventories/counts/digests but is a planning registry, not an execution-owned lane path. Runtime archive/receipt/tombstone data lives only beneath the operator-selected external ledger broker root; there are no dependency, lockfile, repository migration-file, generated artifact, snapshot, environment-example, package-export, or external-dispatch paths. Any other execution dirty path is `phase_owned_dirty` and blocks closeout.
- `refresh_downstream_after_merge` remains outside FABPUB. FABREADMIT owns the readmit consumer and `_FAB_DELTA_BROKER_READMIT_READY` flip; RESIDUAL owns any later `base` change to completed-effect identity. Documentation impact remains limited to the CHANGELOG retraction.
- Policy precedence is CLI/operator override, this phase plan, roadmap policy, `Dispatch Hints`, then registry defaults. Silent model, effort, executor, or capability downgrade is forbidden unless an explicit fallback is selected or `inherit_default` applies.
- FABPUB cuts no tag or external release workflow and is not a visible render deliverable. Post-dispatch back-fill is inapplicable; closeout records `visual_render_declared=false` and a typed non-render opt-out.

## Spec Closeout Plan

- schema: `spec_delta_closeout.v1`
- decision: `no_spec_delta`
- target surfaces: `phase-loop-runtime/src/phase_loop_runtime/cli.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/admission.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/evidence.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/live.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/verbs.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/contracts.py`, `phase-loop-runtime/src/phase_loop_runtime/convergence/fencing.py`, `phase-loop-runtime/src/phase_loop_runtime/fabpub_capability.py`, `phase-loop-runtime/src/phase_loop_runtime/fabpub_tdd_chronology.py`, `phase-loop-runtime/src/phase_loop_runtime/publishing.py`, `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`, `phase-loop-runtime/tests/_fabpub_tdd_guard.py`, `phase-loop-runtime/tests/test_convergence_broker_admission.py`, `phase-loop-runtime/tests/test_convergence_broker_credsep.py`, `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py`, `phase-loop-runtime/tests/test_convergence_broker_verbs.py`, `phase-loop-runtime/tests/test_convergence_fencing.py`, `phase-loop-runtime/tests/test_convergence_live_enable.py`, `phase-loop-runtime/tests/test_convergence_train_integration.py`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py`, `phase-loop-runtime/tests/test_publishing.py`, `phase-loop-runtime/tests/test_train_prebuilt.py`, `phase-loop-runtime/tests/test_train_runner.py`, `CHANGELOG.md`
- evidence paths: `plans/phase-plan-v10-FABPUB.md`, `.phase-loop/reviews/v10-fabpub-plan-panel.json`, `phase-loop-runtime/src/phase_loop_runtime/fabpub_tdd_chronology.py`, `phase-loop-runtime/tests/_fabpub_tdd_guard.py`, `phase-loop-runtime/tests/test_fabpub_shared_epoch.py`, `.phase-loop/runs/*/fabpub-legacy-cutover.json`, `.phase-loop/runs/*/fabpub-*.junit.xml`, `.phase-loop/runs/*/fabpub-*.raw.log`, `.phase-loop/runs/*/fabpub-chronology.json`, `.phase-loop/runs/*/verification.json`, `.phase-loop/runs/*/verification.log`, `.phase-loop/events.jsonl`
- redaction posture: `metadata_only`
- downstream handling: `none`

## Verification

- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli validate-roadmap specs/phase-plans-v10.md`
- `PYTHONPATH=phase-loop-runtime/src python3 skills-src/claude/claude-plan-phase/scripts/validate_plan_doc.py plans/phase-plan-v10-FABPUB.md`
- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli goal-coverage-audit --repo . --plan plans/phase-plan-v10-FABPUB.md`
- `PYTHONPATH=phase-loop-runtime/src python3 -c 'import hashlib; from pathlib import Path; from phase_loop_runtime.plan_manifest import read_manifest, validate_manifest; p=Path("plans").joinpath("manifest.json"); plan=Path("plans").joinpath("phase-plan-v10-FABPUB.md"); v=validate_manifest(p); assert v.valid, "; ".join(v.errors); rows=[e for e in read_manifest(Path(".")) .plans if e.file==plan.as_posix()]; assert len(rows)==1 and rows[0].phase_alias=="FABPUB" and rows[0].lanes==("SL-0","SL-1","SL-2","SL-3","SL-4"); raw=__import__("json").loads(p.read_text()); row=next(e for e in raw["plans"] if e.get("file")==plan.as_posix()); c=row["lifecycle"][0]["metadata"]["fabpub_plan_contract"]; assert c["plan_sha256"]==hashlib.sha256(plan.read_bytes()).hexdigest(); assert (c["owned_paths_count"],c["owned_paths_sha256"],c["test_paths_count"],c["test_paths_sha256"])==(24,"93f3a95a277ee2e5d2543aed6baea4c839c55e8be32ee1264a0283e1fd829ead",12,"c7c498a73c76364dee1d02b0ee73eb6b6fae5ed4311d135d82ed17c4fec18e51")'`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_ or test_convergence_broker_admission or test_convergence_broker_verbs or test_convergence_broker_revocation_race or test_convergence_broker_credsep or test_convergence_live_enable or test_convergence_fencing or test_publishing or test_train_prebuilt or test_convergence_train_integration or test_train_runner"`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode final-junit`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "broker or admission or convergence or fab"`
- `env PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -m "not dotfiles_integration"`
- `ruff check phase-loop-runtime/src/phase_loop_runtime/`
- `git diff --check`
- Final-landing wrapper (post-merge only): runner-owned immutable-blob/ancestry preflight followed by `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode final`; it emits `.phase-loop/runs/*/fabpub-chronology.json` and then the default focused JUnit/audit is rerun.

## Acceptance Criteria

- [ ] EC-FABPUB-0 — proven by the runner-owned immutable-blob/ancestry preflight plus `env PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.fabpub_tdd_chronology --repo . --head HEAD --mode final`, retained RED/default evidence, exact superseded/revised plan and four-seat test-panel digests, and final JUnit. The reducer proves `B -> T -> I -> P -> C -> M -> H -> L`, immutable SL-0, marker-only activation, fresh-finalized rejection, both identity/exact-OID nodes, the shared-allocator node, all six literal legacy-cutover nodes seeded before activation, the exact default-`run_train` recovery node, every migrated CLI/fresh-publish node, honest counts with exactly seven new nodes relative to `d9cda3e05c1719111c50ed556625b7068fa28d623debad94276d170fd9d47100`, exact 24-path/12-test inventories and all vector/test/cutover/evidence digests, intended RED/ordering anchors, exact inactive skip set, and zero preactivation/final skips/errors/xfails.
- [ ] EC-FABPUB-1 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator_stale_epoch or fabpub_allocator_shared_repository_epoch_across_linked_worktrees_and_train_roots or fabpub_legacy_cutover_preserves_high_water_before_activation or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; stale presentation is refused against the maximum of authenticated legacy and canonical high-water even from another linked worktree/train, the first post-cutover epoch is `legacy_max + 1`, and no fresh caller epoch or absent-receipt bypass exists.
- [ ] EC-FABPUB-2 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator_epoch_equality or fabpub_allocator_shared_repository_epoch_across_linked_worktrees_and_train_roots or fabpub_legacy_cutover_preserves_high_water_before_activation or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; sequential/concurrent linked-worktree/train publications continue from the authenticated legacy floor in one atomic epoch sequence and every stored lease equals the allocated epoch.
- [ ] EC-FABPUB-3 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_allocator_attempt_equality or fabpub_allocator_shared_repository_epoch_across_linked_worktrees_and_train_roots or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; canonical-repository exact transaction identity derives every attempt, replay from another worktree/train preserves it, and no finalized caller authority bypass exists.
- [ ] EC-FABPUB-4 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_intent_identity or fabpub_train_resume_post_commit_pre_checkpoint or fabpub_train_resume_normal or fabpub_train_resume_prebuilt or fabpub_terminal_replay or fabpub_legacy_cutover_replays_terminal_completed_effect_before_activation or fabpub_legacy_cutover_preserves_outcome_ambiguous_block_before_activation or fabpub_different_head or fabpub_allocator_shared_repository_epoch_across_linked_worktrees_and_train_roots or fabpub_fresh_finalized_publish_rejected_before_admission_and_adapter"`; lower-level and default production `run_train` recovery prove transaction identity remains acyclic, complete commit inputs are frozen, the actual object/OID/raw digest are durable before ref CAS, exact transaction-owned staged state is recognized before the ordinary clean gate, recovery runs before author work, ref-at-parent retries the same CAS/object, ref-at-expected finalizes, every unrelated dirt/mismatch/alternate OID—including same parent/tree/message/trailer with changed author/committer/signature/header—blocks with zero side effects, terminal recovery reconciles to an ordinarily clean tree, authenticated legacy completed effects replay under canonical identity with zero adapter entry, legacy ambiguous/in-flight outcomes remain permanent, and alternate-worktree/train replay dedups one shared-store admission/effect while a genuine different head obtains the next epoch.
- [ ] EC-FABPUB-5 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_intent_identity_retry or fabpub_faithful_retry or fabpub_train_resume_post_commit_pre_checkpoint or fabpub_checkpoint_conflict or fabpub_intent_conflict or fabpub_intent_cleanup or fabpub_legacy_cutover_conflict_collision_and_idempotent_resume_before_activation"`; every legitimate lower-level/default-`run_train` retry and cutover restart reconstructs the same authenticated identity and inputs, then compares exact checkpoint or cutover ID, source/archive/receipt digest, workspace, dependency, fencing, OID, raw-digest, and ref state; exact transaction parent/expected and identical cutover-journal arms proceed, while one-byte source/index/worktree/input drift, extra/missing path, source recreation, alternate OID/header rewrite, wrong root, tombstone conflict, hash collision, or trailer conflict is refused without a broad bypass.
- [ ] EC-FABPUB-6 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_conflicting_resume or fabpub_genuine_resume or fabpub_intent_identity_retry or fabpub_legacy_cutover_conflict_collision_and_idempotent_resume_before_activation or fabpub_legacy_cutover_preserves_exact_serialized_repo_preimages_and_resolution_context_before_activation or fabpub_global_legacy_cutover_partitions_multiple_repositories_crash_idempotently_before_activation"`; all publish arms use `PreAdmissionEnvelope`, canonical repository identity, exact expected/committed OID bindings, a global `ARMED` transaction, and a matching partition receipt whose serialized preimages, contexts, and digests agree, so finalized caller authority, normalized/resolved legacy aliases, per-repository partial activation, conflicting terminal authority, semantic commit adoption, or final-message-derived transaction identity cannot bypass comparison.
- [ ] EC-FABPUB-7 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -k "fabpub_changelog"`.
