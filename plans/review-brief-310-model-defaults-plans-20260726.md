# Advisor review brief: Consiliency/agent-harness#310 detailed-plan bundle

Review all six detailed plans as one ordered implementation series. Evaluate:

1. whether the sequencing prevents review from accidentally moving off Fable;
2. whether normal implementation remains regular/Sonnet while repeated repair
   failure reaches heavy/Opus with honest persistence and operator precedence;
3. whether the model-first four-vendor default and Gemini 3.6 Flash renderer
   preserve the legacy explicit `invoke_panel(PANEL_LEGS)` contract cleanly;
4. whether the PMCP prerequisite and version-pinned integration are independently
   bounded, fail closed, and produce audit-derived rather than self-reported evidence;
5. whether the Claude constraint is enforced: subscription Claude Code TUI only,
   never Anthropic API/API-key calls and never transcript-string refusal matching;
6. whether each plan is bounded and immediately implementable, with sufficient
   tests, verification commands, and acceptance criteria.

Identify load-bearing omissions, contradictions, unsafe assumptions, or scope that
should be split. End with exactly one terminal verdict: AGREE, PARTIALLY AGREE, or
DISAGREE.
