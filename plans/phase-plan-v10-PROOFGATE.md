---
phase_loop_plan_version: 1
phase: PROOFGATE
roadmap: specs/phase-plans-v10.md
roadmap_sha256: 7f2590bdebf5a892cf0987b67916d2c3b95970b547117b8cbf4adc7c7220838e
proofgate_lifecycle_contract: proofgate_tdd_candidate_main.v1
proofgate_tdd_activation_env: PHASE_LOOP_TDD_EXPECT_PROOFGATE
proofgate_capability_marker: phase_loop_runtime.proofgate_capability:PROOFGATE_CAPABILITY_VERSION=proofgate.v1
proofgate_expected_nodeids: 39
proofgate_default_skip_nodeids: 36
proofgate_activated_red_failures: 37
proofgate_attended_live_env: PHASE_LOOP_PROOFGATE_ATTENDED_LIVE
proofgate_ordinary_phase_nodeids: 39
proofgate_attended_phase_nodeids: 39
proofgate_attended_real_provider_rows: 4
automation:
  suite_command: ["bash", "-lc", "test -n \"${PHASE_LOOP_RUN_DIR:-}\" && PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli validate-roadmap specs/phase-plans-v10.md && env -u PHASE_LOOP_TDD_EXPECT_PROOFGATE -u PHASE_LOOP_PROOFGATE_ATTENDED_LIVE PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -m 'not dotfiles_integration' --junitxml=\"$PHASE_LOOP_RUN_DIR/proofgate-compatible-suite.junit.xml\" && ruff check phase-loop-runtime/src/phase_loop_runtime/ && PYTHONPATH=phase-loop-runtime/src python3 phase-loop-skills/plan-phase/scripts/validate_plan_doc.py plans/phase-plan-v10-PROOFGATE.md && git diff --check"]
  mutation_manifest: phase-loop-runtime/tests/fixtures/proofgate/v10-proofgate-mutations.json
---

# PROOFGATE: Falsifier Gate

## Context

PROOFGATE extends the existing acceptance and goal-coverage path. Current GitHub `main` is `648be2f68d6804ecdc4046bb7d4f5ee81a90c356`; at that source `launcher.py` sends ordinary Codex authors through `--sandbox danger-full-access`, `child_executor_env()` copies the ambient environment except Claude self-markers, `runner.py` owns only the repo-local dispatch lock, and `tdd_chronology.py`, a proof receipt writer, an external receipt-head anchor, and an author-inaccessible production boundary do not exist. None of those missing capabilities is treated as pre-existing.

The immediately preceding exact-digest panel record `.phase-loop/reviews/v10-proofgate-plan-panel.json` has SHA-256 `212450de27eeee121a7adb8598f91716202a1b4a17620b1aaa38a1cb86468a9d`, binds reviewed plan SHA-256 `02da87c7bdebd1b83e2edeb08ec3b2ee7e763155233882168d8e97d637f0220c`, and records usable Grok `AGREE`, usable GPT-5.6 Sol `DISAGREE`, usable Gemini `AGREE`, and no Fable result. The durable local bundle `.phase-loop/reviews/v10-proofgate-plan-review-input.md` and the panel-recorded staged copy `/tmp/pl-panel-ky7d_5il/review/review-bundle.md` bind bundle SHA-256 `bce687439d93765f8b34813cadb4a3395b4968765ef30e0eab80b3394c8b3021` and `127190` bytes; `.phase-loop/reviews/v10-proofgate-plan-review-instructions.md` binds instructions SHA-256 `bbc1b530a6b701dda0094947138c88fbc19265e7c5f610ba1d3a69cd479e2939`. Sol identifies two blockers: ordinary marker-present pull-request CI has no live subscription clients, sessions, broker evidence, or attended isolation environment, so it cannot both run the four genuine provider positives and remain hermetic; and the exclusive credential owner is also specified to semantically parse hostile envelopes/provider content while a credential-aware taint process scans general logs, source, and Git objects. This repair freezes separate ordinary-hermetic and attended-live executions over the same immutable 39 phase nodeids, with the four real provider rows mandatory only as additional attended assertions at candidate and canonical main, never skipped, deselected, cached, synthesized, or counted by ordinary CI. It also makes the minimum session owner a tiny fixed-function transport and final-hop credential injector: every semantic request validator, provider adapter, response parser, diagnostic processor, artifact scanner, model, and tool remains credentialless; the owner performs only fixed framing, byte equality/digest checks against preregistered validated request material, fixed-route transport, and bounded byte-level live-credential filtering before releasing taint-clean bounded raw bytes to a separate credentialless response parser. It preserves the exact EC-5/EC-7 nodeids, all previously closed credential-projection, parent-model-boundary, reachability, and taint findings, full-real-history audit, source-head-parented supplemental candidate, per-mutation pristine green baseline, raw-byte criterion grandfathering with a server-receipted first-parent cutoff, direct child canaries, decisive pre-merge four-seat `PR-T`/`PR-B` unanimity, immutable verifier, history and receipt contracts, and the exact 39/36/37 TDD lifecycle. The changed plan digest requires a fresh full panel.

The phase remains three serial lanes but gains a staged bootstrap inside SL-0. A complete immutable tests-and-fixtures-only PR lands first. A second, separately reviewed bootstrap implementation PR then lands only the minimum writer, attestation workflow, and OS-isolation call sites, with no test edit. Those two landings use a manually coordinator-controlled Bubblewrap launch and GitHub PR/merge metadata; they do not claim receipts from code that was not yet merged. Trust begins only after the merged bootstrap implementation is independently verified and its activation head is signed by the landed GitHub Actions attestation workflow. Every later author/reviewer, panel/RED observation, production dispatch, mutation run, and closeout uses that bootstrapped path.

Literal TDD feasibility is part of the frozen contract, not an execution convention. The tests-only landing owns one deterministic functional guard: `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1` forces the new contract against the pre-implementation base, while the exact production capability marker `phase_loop_runtime.proofgate_capability.PROOFGATE_CAPABILITY_VERSION == "proofgate.v1"` activates the same immutable tests by default only in the complete implementation candidate. With both absent, ordinary/default tests-only CI is GREEN: one migrated legacy nodeid and two unchanged positive controls pass, only the exact 36 frozen new nodeids skip, and every other pre-existing test remains collected and active. Forced pre-implementation RED is assertion RED, never collection RED and never an attended run: all 36 new production-dependent nodeids plus the one migrated active branch execute and fail at their intended `PROOFGATE_RED::<case-id>` assertion after their frozen source injection anchor is asserted, without any provider client, session, credential, DNS, network, broker, or live-proof prerequisite. With the marker present and `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE` absent, ordinary `python -m pytest -m "not dotfiles_integration"` remains hermetic and GREEN; all exact 39 phase nodeids execute once with zero phase skips or deselections, while their four real-provider case rows explicitly record `not_executed_in_ordinary_mode` and cannot satisfy live acceptance. Only a runner-attested candidate or canonical-main proof may set `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE=1`; that attended run collects the same exact 39 nodeids once, executes all four genuine subscription-backed provider rows plus the multi-turn controls, and passes with zero skips/deselections. Synthetic credentials remain negative taint canaries only and never become a positive substitute. No `xfail`/`xpass`, import/collection failure, setup/teardown error, module-level capability import, selector drift, broad skip marker, provider-row omission, or ordinary-JUnit substitution is admissible.
No collection/import failure is accepted as RED, ordinary compatibility evidence, or attended live evidence.

## Interface Freeze Gates

- [ ] IF-0-PROOFGATE-1 — Acceptance, bootstrap, chronology, mutation, and closeout contract:
  - The authoritative item-leading grammar remains `- [ ] EC-<ALIAS>-<N> — proven by <command>, falsified by <executable mutation>`. A guard/gate/fence/interlock item also names `production construction site`; an absence/non-occurrence item also names a `path-entered control`. `goal_coverage.py` preserves existing EC-ID coverage behavior while adding typed `valid`, `invalid`, and `grandfathered` criterion dispositions.
  - `automation.mutation_manifest` resolves a repo-relative `mutation_manifest.v1` with exactly one declaration per non-grandfathered criterion. Each declaration has `criterion_id`, `mutation_id`, `target_kind`, and nonempty `parameters`; each unique parameter has `parameter_id`, repo-relative `target_path`, exact disambiguated `injection_anchor`, replacement bytes, and one argv-valued `proof_command`. The runner derives sorted IDs/count/digests and command digests from this declaration; evidence fields are never authoritative.
  - `proofgate_tdd_contract.v1` and `proofgate_test_selection.v1` are test-owned at `phase-loop-runtime/tests/proofgate_tdd_guard.py`. They freeze literal tuples/maps for `EXPECTED_PHASE_NODEIDS` (39), `DEFAULT_SKIP_NODEIDS` (36), `MIGRATED_NODEIDS` (1), `POSITIVE_CONTROL_NODEIDS` (2), `ORDINARY_BROAD_PHASE_NODEIDS` (the same exact 39), `ATTENDED_PHASE_NODEIDS` (the same exact 39), `ATTENDED_REAL_PROVIDER_CASES` (exactly `fable_subscription_transport_reachable`, `sol_terra_subscription_transport_reachable`, `gemini_subscription_transport_reachable`, and `grok_subscription_transport_reachable`), and `RED_CASES_BY_NODEID` (37 failure nodeids plus every parameter/case and asserted source anchor). They assert set disjointness, exact union/count equality, and byte-identical nodeid ownership across default, forced-RED, ordinary-hermetic, candidate-attended, and canonical-main-attended modes. Only the exact 36 new nodeids may use the test-owned default skip mark. The migrated nodeid is never skipped: before capability it runs its byte-for-byte legacy assertions; with forced activation or the marker it runs the new hermetic isolation assertions. No provider case is a separately marked, parameter-deselected, dynamically generated, or optionally collected nodeid. New modules keep capability-specific imports inside active test bodies and convert an expected missing pre-implementation symbol into the named assertion failure, never an import or collection error.
  - Functional activation remains exactly `os.environ.get("PHASE_LOOP_TDD_EXPECT_PROOFGATE") == "1"` or a successful, value-equal read of `phase_loop_runtime.proofgate_capability.PROOFGATE_CAPABILITY_VERSION == "proofgate.v1"`. `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE=1` never activates missing production and never changes collection; it enables only the four frozen attended case assertions after the test-owned verifier confirms runner-attested `candidate` or `canonical-main` stage, exact head/module equality, live broker/profile digests, and a fresh run nonce. Forced RED rejects that live mode before provider setup and proves every intended failure without auth or network. Marker-present ordinary mode with the live env absent executes all 39 phase nodeids hermetically, records zero executed real-provider rows, and cannot emit an attended proof. No other environment value, branch name, Git state, import exception, runner/model claim, partial bootstrap module, local flag, pytest marker, skip, deselection, cached receipt, or ordinary JUnit activates or satisfies live evidence. The marker file is absent from `PR-T`, `PR-B`, and every partial implementation commit; SL-2 adds it only after every ordinary production, validator, skill, generated, and contract-doc byte is complete. `.github/workflows/test.yml` remains read-only and its exact ordinary `python -m pytest -m "not dotfiles_integration"` invocation must stay hermetic and GREEN before and after the marker. Implementation may not edit tests, the guard, selectors, nodeids, counts, anchors, mode predicates, attended case IDs, RED case IDs, or test-owned bootstrap verifier.
  - The test-owned `proofgate_bootstrap_verifier.py` consumes only frozen guard constants, separately labeled default/RED/ordinary-hermetic/attended-live canonical pytest JUnit, taint-clean raw logs, sealed coordinator-reference inference/provider-adapter/transport-owner/credentialless-parser/scanner policy and code/profile/protocol digests, pre-network hostile-request, hostile-model, OS-isolation, and positive-reachability canary evidence, Git object/parent/diff data, exact per-seat review records, and coordinator-observed GitHub PR/merge metadata. It requires exact 39-nodeid collection and mode accounting: default `3/36`, forced RED `2/37`, marker-present ordinary `39 passed` with four provider rows explicitly not executed, and each attended candidate/main run `39 passed` with the exact four real rows executed once and no skip/deselect. It neither imports nor calls the acceptance grammar, `goal_coverage.py`, the new validator check, `tdd_chronology.py`, `verification.json` v3, `ProofgateReceiptSupervisor`, the candidate `IntendedInferenceBroker`/`IntendedInferenceAgentAdapter`/provider adapters/`SubscriptionTransportBroker`/`CredentialTaintGate`, or any candidate-owned parser. This immutable verifier applies `bootstrap_merge_review_gate.v1` before the coordinator may merge either `PR-T` or `PR-B`; a review count, a single `AGREE`, merge metadata, candidate-authored clean verdict, ordinary-hermetic provider case, or later attestation cannot satisfy or cure that pre-merge gate. The verifier and the manual coordinator/GitHub boundary authorize only those two exact landings. After `PR-B` is a verified two-parent target landing and `bootstrap_activated` is externally attested, a fresh process at that exact fetched canonical-main head proves its loaded bootstrap modules match the landed blobs and cross-runs the immutable request/result/turn/adapter/transport-owner/response-parser/misuse/taint vector corpus against both the pre-bootstrap coordinator reference and landed implementation; only then does authority transition once to the self-hosted `ProofgateReceiptSupervisor`. The self-hosted verifier re-imports and cross-checks the two bootstrap records but does not retroactively create, approve, taint-scan, or attest them.
  - The manifest declares all `EC-PROOFGATE-0..7`. EC-PROOFGATE-4 uses `github_builder` and `routing_builder`, each with context sufficient to match exactly one `epoch_blocked=lambda: evidence_store.epoch_blocked` construction-site argument in `convergence/broker/live.py`, and the two named production-builder tests in `test_convergence_broker_revocation_race.py`. Helper-only mutation is invalid.
  - `proofgate_bootstrap_boundary.v1` is manual, coordinator-controlled, and narrowly scoped. Until the one-time post-`PR-B` authority transition, a sealed coordinator-owned credentialless inference policy broker, credentialless provider request compiler/response parser, tiny fixed-function subscription session owner, and credentialless artifact scanner—not any `PR-T`/`PR-B` candidate byte—serve every child; their exact code/policy/profile/protocol/endpoint/session-inventory digests and immutable request/response/canary vectors are panel/verifier inputs, and they are outside every child mount. The reference path never launches a credential-bearing provider CLI, semantic parser, diagnostic scanner, or tool-capable process. The separate `SubscriptionTransportBroker` is only the minimum session owner and final-hop transport: it never parses prompts, envelopes, provider response semantics, logs, source, or Git objects. Live preflight must prove `/usr/bin/bwrap` plus unprivileged user/mount/PID/network namespaces work. Before launching any test author, bootstrap author, or `PR-T`/`PR-B` reviewer, the coordinator runs the complete direct non-model, adapter/parser isolation, fixed-function transport, pre-network misuse, credentialless artifact-scan, and positive-reachability matrices below with byte-identical production profiles; any failed negative or positive probe prevents that child launch, and the taint-clean raw probe/log/profile digests become immutable pre-merge verifier inputs. The coordinator repeats this prelaunch proof for every changed child, request compiler, response parser, artifact scanner, provider-adapter profile, transport adapter, session-owner protocol/endpoint/session inventory, peer policy, or candidate digest. It then creates a self-contained, remote-less assigned clone and launches the child through Bubblewrap with `--unshare-net`, user/PID/IPC/UTS/cgroup namespaces, `--new-session`, `--die-with-parent`, all capabilities dropped, host `/home`, `/root`, `/mnt`, `/run`, `/tmp`, sibling worktrees, coordinator state, provider credentials, provider subscription state, and credential sockets masked, and only the assigned clone plus private tmpfs scratch writable. The new network namespace has no route to host loopback or ambient IPv4/IPv6, UDP, DNS, or raw network. `/run/proofgate` is a private tmpfs; the coordinator individually bind-mounts only the two run-scoped pathname socket inodes at `/run/proofgate/intended-inference.sock` and `/run/proofgate/coordinator-ipc.sock`. The peer-bound adapter-to-session-owner sockets and clean-raw-response pipe are never mounted into the child. Directory mounts, inherited listener descriptors, abstract AF_UNIX sockets, alternate pathname sockets, credential/config projections, subscription CLI state, provider cookies/tokens, and secret-derived environment/argv values are forbidden. The coordinator may select the same vendor to author tests and bootstrap code, but that vendor's own review cannot attest the landing and the child cannot push, merge, dispatch workflows, or access the coordinator's GitHub or provider credentials.
  - `bootstrap_merge_review_gate.v1` applies independently and decisively before both trust-establishing merges. For the exact candidate change-tuple digest, all four intended seats—Fable, Sol, Gemini, and Grok—must each be present, usable, substantive under the coordinator's material-review policy, and `AGREE`; there must be zero usable `DISAGREE` or `PARTIALLY AGREE`. A missing, unavailable, unusable, empty/elided, substituted, wrong-digest, or non-`AGREE` seat fails closed. Every material or blocking finding requires a changed candidate digest that resolves it and a fresh full four-seat panel on that changed digest; disposition prose, review count, one `AGREE`, coordinator override, merge completion, or post-merge attestation cannot authorize or cure the merge.
  - The first landing is one complete `tests/**` and `tests/fixtures/proofgate/**` PR with no production/workflow/doc/skill byte. Before coordinator merge, the exact change-tuple digest is independently recomputed, satisfies `bootstrap_merge_review_gate.v1`, and the exact immutable RED argv runs against a synthetic tree consisting of `pre_phase_target_oid` plus only that test delta. The test-owned verifier emits a decisive pre-merge verdict over those inputs before the coordinator may open the merge action. Coordinator-observed GitHub PR number, head OID, base OID, merge OID, `mergedAt`, every-parent diff, panel output/log digests, RED argv/log/exit, and merge-message evidence digest form `bootstrap_test_landing.v1`.
  - Only after that test PR is on the target first-parent may a fresh bootstrap branch be created. Its separately reviewed PR may change exactly `.github/workflows/proofgate-receipt-attestation.yml`, `proofgate_receipts.py`, `proofgate_isolation.py`, `launcher.py`, and `panel_invoker.py`; it may not touch tests or any ordinary PROOFGATE implementation file. The coordinator recomputes the exact candidate digest, requires the immutable bootstrap subset green, requires `bootstrap_merge_review_gate.v1` for that digest, excludes the author vendor as an independent attestor even though its intended seat must still return a usable substantive `AGREE`, and alone merges only after the test-owned verifier's decisive pre-merge verdict. GitHub PR/head/base/merge/`mergedAt` metadata and the merge message bind the reviewed diff, test landing, four exact seat records, verification digests, and manual boundary profile as `bootstrap_implementation_landing.v1`; the later workflow attestation proves provenance of the already-approved blob and is not a correctness substitute.
  - `ProofgateReceiptSupervisor` is the only receipt/log writer. It runs in the outer phase-loop parent holding the roadmap dispatch lock; `proofgate_receipts.py` supplies the state machine, but that process gains authority only when the exact bootstrap implementation merge is already on fetched `refs/heads/main`, its server PR fields and every-parent diff match `bootstrap_implementation_landing.v1`, the workflow blob equals the separately reviewed blob, and a GitHub-hosted run of the landed workflow produces a verified `proofgate_receipt_head.v1` attestation for `bootstrap_activated`. The pre-bootstrap records authorize only those two exact landings; they are not a reusable implementation bypass.
  - The supervisor stores canonical JSON records and bounded logs under a coordinator-owned root not mounted into children or the session owner. Each record has repo/roadmap/phase/run identity, contiguous sequence, previous record digest, kind, canonical payload, record digest, and previous external-head digest. No child source, inference result, submitted result, stdout/stderr, response-parser output, or adapter diagnostic may enter that root until the credentialless `CredentialTaintGate` accepts the exact framed bytes. That gate holds only public structural rules and synthetic canary bytes; it never receives a live credential, secret-derived digest/length/corpus, provider session, or session-owner fd. The fixed-function owner separately rejects any raw provider bytes containing its finite live-credential byte needles before those bytes leave its namespace. On either rejection the supervisor records only a fixed redacted `credential_taint_rejected` class plus non-secret policy/artifact digests, revokes the grant, and forbids every dependent attestation, source import, commit, and push. An accepted record is not committed and no dependent transition may run until `.github/workflows/proofgate-receipt-attestation.yml` attests the new `proofgate_receipt_head.v1` subject and `gh attestation verify` succeeds for `Consiliency/agent-harness`, the exact signer workflow, GitHub-hosted runner, source ref, workflow blob, subject digest, and transparency timestamp. The next record binds that verified bundle digest. A same-UID child can rewrite local bytes only inside its mount; it cannot see the journal/logs or mint the external signature, so replacement/recomputation, truncation, fork, rollback, or taint-verdict substitution fails.
  - `proofgate_isolation.v1` is mandatory for every post-bootstrap write-capable author or reviewer, including `panel_invoker.py` legs, every credentialless provider request compiler/response parser, and every credentialless artifact-scanner process. The coordinator creates a self-contained clone at the authorized base with no remote and mounts it at the expected repo path inside Bubblewrap; only that assigned clone is a persistent writable bind. Reviewers receive only an assigned staged review workspace and private tmpfs. The host worktree, sibling worktrees, receipt root, descriptors, logs, coordinator process namespace, SSH agent, keyring/D-Bus sockets, Docker socket, `.netrc`, global/system Git credential helpers, `gh` config, 1Password, every provider credential/config/cache/cookie/token/subscription-state path, and every secret-bearing environment/argv value are absent. `close_fds=True` is required; no receipt/log/credential/provider descriptor is passed; `/proc` and the mount table must expose no session-owner process, secret inode, or provider state. The session owner has the inverse minimum view: its broker-private root and pre-opened fixed transport/session descriptors, but no repo, worktree, staged review, child scratch, receipt/log root, Git object database/index, source exporter, shell, provider SDK/CLI, model runtime, semantic parser, or general scanner.
  - `IntendedInferenceBroker` runs only in the trusted outer credentialless policy namespace and is the child's sole provider-facing capability. It holds no provider credential or subscription state and never loads a hostile prompt into a provider CLI, model runtime, plugin, MCP server, or tool loop. It validates policy, canonicalizes the exact child turn, seals one single-use `intended_inference_grant.v1`, launches the credentialless adapter named by that grant, validates the adapter's provider-specific `validated_provider_request.v1`, and preregisters that exact bounded request material plus its SHA-256, byte count, grant digest, provider profile digest, and single-use peer identity with the session owner before the adapter may submit it. The grant binds exact `run_id`, monotonic `sequence`, `request_id`, assignment/candidate digest, author-or-review `seat_key`, `provider_id`, `model_id`, effort, operation=`infer`, turn number, canonical prompt/history/observation/schema SHA-256 values and byte counts, output/turn/tool/time bounds, provider-adapter/request-compiler/response-parser profile and code digests, transport-owner code/protocol/endpoint/session-inventory/peer-policy digests, and expiry. `/run/proofgate/intended-inference.sock` accepts only canonical length-prefixed `intended_inference_request.v1`; every field and size is semantically equality-checked in this credentialless namespace before adapter launch. Unknown, duplicate, reordered, mutable, replayed, digest-mismatched, or size-mismatched fields fail closed. There is no child-selected method, URL, scheme, host, port, DNS name, header, cookie, path, file reference, proxy opcode, tool/plugin/MCP operation, raw transport, or provider/model/seat override.
  - `provider_adapter_profile.v1` makes every provider turn a fresh disposable credentialless request-compiler/response-parser worker, never a thread or subprocess in the policy parent, session owner, or author/reviewer child. Its receipt binds the immutable OCI image and unpacked read-only rootfs SHA-256, first-party executable/library/protocol version and blob SHA-256, complete argv and environment-name digest, request compiler/response parser code/schema digest, seccomp/capability/namespace profile digest, peer-socket policy digest, run-local input/output digests, and destruction outcome. The adapter semantically parses and validates hostile prompt/history/request and provider response content only while credentialless. It has fresh user/mount/PID/IPC/UTS/cgroup/network namespaces, no default route, DNS, raw socket, inherited descriptor, general proxy, or filesystem outside a read-only rootfs plus bounded tmpfs prompt/schema/output area; all capabilities are dropped and local command execution, tools, plugins, MCP, hooks, browser, subagents, Git, cloud/package/workflow clients, and arbitrary network are structurally unavailable. Provider credentials, session files, caches, cookies, tokens, keyrings, auth sockets, secret-bearing env/argv, and every session-owner file/fd/memory namespace are absent. Its sole egress is one run-scoped peer-credential-bound one-shot request socket to `SubscriptionTransportBroker`; it may submit exactly the preregistered opaque `validated_provider_request.v1` bytes and receive only one `taint_clean_bounded_raw_response.v1` or fixed refusal. It then applies the frozen provider response schema, rejects unknown/diagnostic/redirect/tool-widening shapes, strips raw upstream headers/errors/challenges/traces, and converts tool use only to canonical `local_tool_request`. If a first-party CLI can only self-execute tools, requires real subscription state in this namespace, or cannot externalize every model tool request, that CLI is not launched; the adapter instead uses the frozen first-party client/app-server request and response schemas while the owner performs only fixed transport/auth injection. The adapter is destroyed after one turn whether it succeeds, refuses, times out, or taint-rejects.
  - `subscription_transport_broker.v1` is the minimum session owner: a tiny fixed-function, non-model, non-prompt-executing process under a distinct uid, mount/PID/network namespace, cgroup, seccomp policy, non-dumpable process policy, private keyring, and broker-private root. It alone holds the minimum seat-specific subscription session. It contains no JSON/YAML/provider-response semantic parser, provider SDK/CLI, shell, model/tool runtime, plugin, MCP server, diagnostics mode, source/log/Git scanner, or repo path; hostile prompt/request/response bytes are opaque bounded byte strings to it. A credentialless adapter can access only its one sealed request-socket inode and cannot inspect the owner listener fd, other adapter sockets, process namespace, or session capability. After `SO_PEERCRED`, pidfd/cgroup, executable-blob, profile, socket-inode, and single-use admission checks, the owner reads only a fixed binary frame header and the already validated bounded `validated_provider_request.v1` material. Before DNS or any other network syscall it recomputes SHA-256 and byte count and requires constant-time equality of the complete material, grant digest, request digest, profile digest, and peer identity to the preregistered values; it does not parse an envelope or any prompt/provider field. Mismatch, replay, extra byte, alternate length, or unregistered material produces the frozen refusal with zero network effect. The immutable seat transport profile supplies the sole TLS host/certificate/address policy, TCP 443 destination, method, path/query, static header names, request body position/limit, streaming/framing limit, and response byte cap; the owner injects subscription auth only as a separate final-hop fixed header/cookie iovec. There is no caller-controlled endpoint/method/path/header slot, redirect follower, retry to another route, secondary endpoint, CONNECT/SOCKS/general proxy, raw-TLS/arbitrary-byte tunnel, browser/device/auth flow, or local/private/link-local destination. A provider redirect or secondary-endpoint instruction can cause no second request and is classified only later by the credentialless response parser. The owner performs only a finite, startup-bounded byte scan of the raw bounded response and fixed transport error bytes for exact live credential bytes and the fixed auth header/cookie serializations it itself injects, including chunk-boundary matches; it performs no decoding, semantic parsing, or general diagnostics. A hit returns only fixed `credential_taint_rejected`. Otherwise it closes the network side and releases exactly one `taint_clean_bounded_raw_response.v1` over a fresh one-shot pipe to the credentialless provider response parser; no raw byte is logged, receipted, or exposed elsewhere.
  - `subscription_auth_transport_adapter.v1` freezes one metadata-only transport fixture per installed first-party subscription surface under `phase-loop-runtime/tests/fixtures/proofgate/transports/`; each fixture binds executable/library blob, credentialless request/response schema source, session inventory names/types/modes, immutable owner transport profile, literal TLS host, method, path/query, static header names and value sources, request body layout, response schema, model mapping, no-follow redirect/secondary-endpoint posture, byte limits, and a successful real-turn receipt without secret-derived data. **Fable / Claude Code 2.1.220** is grounded in its first-party stream-json/JSON-schema client contract, but the CLI is not launched because its subscription OAuth/keychain and built-in model/tool loop share one process; the credentialless adapter compiles/parses that schema and the owner only injects the first-party subscription session into the fixed final-hop transport. **Sol/Terra / Codex CLI 0.145.0** is grounded in the installed `codex app-server` generated JSON-RPC schema and first-party subscription transport; the credentialless adapter consumes/produces that protocol with all tool requests returned as turn frames, while the owner only transports preregistered bytes—`codex exec` is never launched in a credential-bearing namespace. **Gemini Flash / agy 1.1.8** and **Grok / grok 0.2.114** are grounded in their installed stream-JSON/JSON-schema client protocols; because their CLIs combine subscription state with agentic execution, neither CLI is launched and each credentialless adapter owns semantic request/response handling while the owner performs fixed transport only. No adapter or owner may use an API key, service-account fallback, alternate model/provider, browser/device reauthentication, ambient config, undocumented secondary route, synthetic success, or cached answer. Before `PR-T`, an attended metadata-only protocol extraction and a real benign turn must establish each frozen fixture; all four mandatory Fable/Sol/Gemini/Grok transports must later repeat a real positive through the production owner and credentialless response parser, and Terra must do so before authoring. A refusal or unavailable result never satisfies a provider positive.
  - `IntendedInferenceAgentAdapter` runs inside the separately credentialless author/reviewer child and makes the work path reachable. It owns a bounded `proofgate_agent_loop.v1`: each call sends conversation state plus the last local observation through `IntendedInferenceBroker`; each taint-clean transport result must validate against `proofgate_agent_turn.v1` as exactly one of `local_tool_request`, `final_result`, or `refusal`; the child validates and executes a requested local tool, appends a bounded canonical observation, and requests the next fresh provider turn until a final result or the sealed turn/time/byte/tool-call cap. Model tool requests are never executed by the transport broker, policy parent, or provider adapter. Author grants allow only plan-owned read/list/search/write/delete and local command operations inside the remote-less assigned clone; reviewer grants allow only staged-workspace read/list/search and final verdict, never write/delete/command/git/network. Absolute/traversing paths, symlink escape, remote Git, credential/cloud/publish/workflow/package-registry tools, sockets, and network syscalls fail locally without reaching the parent. Immutable positive tests require every mandatory Fable, Sol, Gemini, and Grok transport to complete real intended inference and at least one child-side multi-turn sequence that reads a staged nonce-bearing file and returns its value; author profiles must additionally perform a harmless run-local write/read correction inside the assigned clone, while reviewer profiles prove the write is refused and still produce a substantive exact-digest verdict. An always-deny broker, a one-shot text-only stub, a transport that cannot return the turn schema, or a loop that never reaches the local tool seam fails.
  - `intended_inference_result.v1` contains only normalized schema-valid turn content from the credentialless response parser, terminal status, request/result SHA-256 values, exact provider/model/seat/grant/turn binding, provider-adapter image/rootfs/request-compiler/response-parser/protocol/profile digests, transport-owner code/protocol/endpoint/session-inventory/peer/grant/request/raw-response-frame digests, refusal-order digest, loaded-blob digests, and non-secret provider receipt/provenance. Raw CLI or upstream stdout/stderr, headers, auth challenges, debug text, stack traces, config paths, and unparsed response bytes remain quarantined and cannot enter a receipt. The owner's bounded live-credential byte filter must accept the complete raw response before it leaves the owner; the credentialless `CredentialTaintGate` must then accept the complete canonical result before any byte or success status is released to the child. Fixed `provider_adapter_boundary_refused`, `provider_transport_refused`, `provider_protocol_refused`, `provider_unavailable`, and `credential_taint_rejected` frames contain no provider-returned bytes. The chronology records grant sealing before adapter creation, credentialless semantic request validation and exact preregistration before owner receipt, complete byte equality/refusal checks before any network effect, fixed request framing/auth injection immediately before the final hop, bounded raw-response live-credential filtering before owner release, credentialless response parse/strip and structural/synthetic-canary scan before child release, each child tool observation before the next turn, and adapter destruction before grant completion; a missing or reordered transition refuses.
  - `credential_taint_gate.v1` is enforced downstream by a trusted non-model but strictly credentialless `CredentialTaintGate` whose code/policy digest is frozen in the bootstrap review. It never receives live broker-held credentials, subscription material, their derived hashes or lengths, or a corpus built from them. It scans complete canonical parser outputs, adapter diagnostics proposed for retention, every child-visible result/error, child `submit_untrusted_result.v1` frame, child stdout/stderr, and every regular-file byte exported from the assigned clone for the immutable public synthetic-canary corpus, forbidden credential/auth field shapes, forbidden diagnostic/config-path structures, framing violations, and unknown encodings. It rejects symlinks, hardlinks outside the clone, devices, FIFOs, sockets, sparse/path-confused entries, undecodable result framing, and any canary/structural hit. A trusted credentialless exporter ignores the child's Git index and objects, walks only plan-owned regular paths against the pristine authorized base, scans tracked and untracked bytes plus deletion metadata, imports accepted bytes into a new coordinator-owned pristine staging clone, and rescans its complete diff/index/tree and pending commit object; this process owns general log/source/commit scanning and has no path, fd, IPC, ptrace, pidfd, keyring, or namespace route to the session owner. Only then may the coordinator attest, commit, or push. The untrusted clone is never committed or pushed directly. Each owner raw-response filter, credentialless parser, downstream-source/result/log admission, pre-ingest, pre-attestation, pre-commit, and pre-push gate consumes the prior clean-verdict digest and rechecks artifact equality; a byte change or missing scan fails closed. Clean receipts contain only public policy/code/artifact/provenance digests and typed outcomes, never secret-derived material.
  - Coordinator IPC remains separately mounted only at `/run/proofgate/coordinator-ipc.sock`, with a distinct random capability, `SO_PEERCRED`, strict size/schema/run/sequence/assignment binding, and exactly `heartbeat` plus canonical length-prefixed `submit_untrusted_result.v1`; receipt append, attestation, authorization, merge, provider inference, filesystem-path input, and remote-mutation opcodes are structurally unavailable. Result frames are immutable after acceptance, remain explicitly untrusted, and do not become evidence until the credentialless `CredentialTaintGate` accepts their complete bytes. The exact two child pathname mounts, inference-grant digest, session-owner/request-compiler/response-parser/scanner code-policy digests, and taint-clean verdict chain are recorded in every boundary/panel/receipt subject.
  - Before any ordinary implementation worktree is created, the landed bootstrap runs four immutable canary matrices. The **direct child matrix** preserves the exact production Bubblewrap proof for every distinct test-author, bootstrap-author, `PR-T` reviewer, and `PR-B` reviewer profile: host IPv4/IPv6 loopback, host abstract AF_UNIX, alternate pathname AF_UNIX, DNS, raw sockets, direct TCP/UDP, non-provider destinations/ports, arbitrary proxy CONNECT, and every other egress are unreachable; alternate inference/IPC paths fail; the exact two child socket paths succeed; the credentialless child can read/write only its assigned profile; `gh auth status`, Git/SSH/askpass/netrc/keyring mutation, authenticated dry-run push, and privileged IPC fail. The **credentialless semantic-process matrix** runs separately for every request compiler, response parser, artifact scanner, mandatory Fable/Sol/Gemini Flash/Grok adapter, and Terra-before-author profile. Hostile request/provider content attempts parent/live/sibling/coordinator/session-owner/session reads; credential enumeration; file/Git/cloud/package/publish/workflow mutation; arbitrary network; socket/fd/pidfd/proc-memory access; and tool/plugin/MCP/browser/subagent execution. From each model/tool/adapter/parser/scanner namespace, exact negative probes require `openat`/`readlink` of the owner root and `/proc/<owner>/{mem,environ,fd,root,mountinfo}`, `ptrace`, `process_vm_readv`, `pidfd_open`/`pidfd_getfd`, `SCM_RIGHTS`, abstract or alternate pathname socket connect, `keyctl`, coredump/core-pattern access, and owner listener/session inode access to fail; namespace inventory proves no credential/session mount, env, argv, file, fd, socket capability, keyring, process visibility, local tool, or arbitrary-network surface. Reciprocal probes from the owner require repo/worktree/review/log/receipt/Git-object paths, parser/scanner executables, shell/exec, and non-fixed network routes to be absent or seccomp-denied. The only cross-boundary success is exact preregistration plus one-shot request equality followed by a taint-clean bounded raw-response pipe; no secret crosses it. The **pre-network transport misuse matrix** mutates the hostile semantic request before credentialless validation and separately mutates each already validated byte frame after preregistration: wrong provider/model/seat/turn/prompt/history/schema/size/digest, method, path/query, header name/value/source, body field/value/schema, raw body/TLS bytes, redirect, retry, secondary endpoint, proxy/CONNECT, or recovered-credential exfiltration must be rejected by the credentialless validator or owner byte-equality gate before DNS/connect/TLS/HTTP/provider-trap count changes. No irreversible request may be sent. The **response-and-artifact matrix** proves a provider redirect causes no follow-up request, owner raw bytes containing exact synthetic session credentials are stopped by the owner's bounded byte filter, credentialless parsing alone classifies provider semantics, and public synthetic canaries in parser output/diagnostics/result/log/source/index/tree/commit are stopped by the credentialless scanner. Positive controls send the exact preregistered request and observe one exact request, then deliver a taint-clean provider tool request through the credentialless parser as only a canonical child turn frame, proving the denials are field-specific rather than always-refuse or tool-swallowing.
  - Each mandatory transport also has a non-degenerate real positive matrix. Only in runner-attested `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE=1` candidate and canonical-main runs, Fable, Sol, Gemini Flash, and Grok must each complete one distinct real subscription-authenticated provider turn through exact `/run/proofgate/intended-inference.sock`, its frozen credentialless request compiler/response parser, and the production fixed-function `SubscriptionTransportBroker`, returning schema-valid nonce-bearing intended inference with matching adapter/owner/parser/protocol/endpoint/session-inventory/peer/grant/request/raw-response provenance; Terra must pass before authoring. No ordinary-hermetic pass, refusal, API-key fallback, synthetic provider, cached answer, alternate model, or one-provider proxy may satisfy another provider's row. The attended JUnit must report all four exact `ATTENDED_REAL_PROVIDER_CASES` executed once; missing/extra/duplicate/not-executed rows fail even when the enclosing nodeid passes. The credentialless `IntendedInferenceAgentAdapter` must then complete a bounded multi-turn local-tool sequence against the assigned staged workspace, including a read followed by a corrected final answer. Author profiles must perform a harmless write/read correction inside the remote-less assigned clone; reviewer profiles must refuse that write yet still read the staged bytes and emit a substantive exact-digest verdict. Separately the child submits one safe framed result only through exact `/run/proofgate/coordinator-ipc.sock`. Synthetic canary sessions are isolated test-only owners: the owner-side filter receives their known canary bytes, while the credentialless parser/scanner receives only already filtered raw material plus public artifact canaries. The immutable tests delete each owner filter, parser boundary, scanner gate, and OS denial in turn and observe the named `PROOFGATE_RED` case while all four attended real provider positives, multi-turn tool use, and taint-clean import still succeed. Immutable evidence binds owner/request-compiler/response-parser/scanner code/protocol/endpoint/session-inventory/profile, sealed grant, preregistered request material, equality check, final-hop auth transition, raw-response filter, credentialless parsed response, refusal order, OS probes, request count, provider receipt, and clean-verdict-chain digests without any secret, secret-derived digest, length, excerpt, raw diagnostic, or auth value. The supervisor independently observes and externally attests `isolation_preflight_passed`; any unavailable owner/parser/scanner/adapter, denied namespace primitive, reachable child canary, readable owner session/memory/file/fd/socket capability from a semantic namespace, owner access to repo/log/Git objects, successful rejected-frame network effect, same-endpoint misuse, missing attended real provider row or multi-turn reachability, component/provenance drift, visible credential/subscription material, retained tainted source/result/log bytes, remote authentication, IPC widening, or missing owner-response/parser/downstream/pre-ingest/pre-attestation/pre-commit/pre-push clean verdict stops before dispatch with non-human `contract_bug`.
  - After activation and denial preflight, the bootstrapped supervisor recomputes the landed test subject, appends/attests `subject_frozen`, launches the exact four seats, admits only taint-clean immutable `panel_seat` result frames with exact inference provenance, reconstructs the tests-only preimplementation tree and appends/attests nonzero `red_run`, then imports/attests the two server landings as the single-use bootstrap exception. Only then may it append/attest `implementation_authorized` and `implementation_dispatch`. Supplied identities/outcomes/digests are evidence only. Missing, dissenting, unusable, substituted, unanchored, tainted, or out-of-order evidence cannot be backfilled.
  - `test_lane_chronology.v1` binds full 40-hex `pre_phase_target_oid`, `test_source_head_oid`, `test_landing_oid`, `bootstrap_source_head_oid`, `bootstrap_landing_oid`, `implementation_base_oid`, exact pushed `candidate_head_oid`, two-parent `implementation_landing_oid`, and exact fetched `canonical_main_oid`. The target is independently derived by agreement among launch target, `origin/HEAD`, and fresh remote HEAD. Tests-only, bootstrap, and final implementation landings must each be server-recorded two-parent PR merges on the target first-parent with ordered parents `[target-before-merge, exact-reviewed-source-head]`; bootstrap descends from the tests landing, the ordinary implementation base equals a fetched target containing both, candidate descends from that base, and canonical main contains the exact candidate through the recorded implementation landing. Same-branch linear tests→implementation, direct/squash/rebase landings, abbreviated/missing OIDs, target movement not represented by an observed PR merge, shallow history, grafts, or replace refs fail closed.
  - The chronology audit walks the complete real DAG and every-parent diff from `pre_phase_target_oid` through `canonical_main_oid` using change-kind/path/mode/blob/file-SHA tuples. It permits only the exact frozen tests delta before the exact bootstrap five-file delta, requires every ordinary production byte to descend from both landings and an attested implementation dispatch, requires an artifact-equal `CredentialTaintGate` clean-verdict chain before every child result/log ingest, attestation, trusted source import, commit, and push, and requires the complete implementation PR range to contain no `phase-loop-runtime/tests/**`, test guard, selector, fixture, count, anchor, workflow-bootstrap, roadmap, or panel-record edit. Copied/preexisting/side-merged blobs, direct untrusted-clone Git objects, test/implementation laundering, tainted or unscanned artifacts, hidden parent deltas, an unobserved landing, or any digest substituted across lifecycle stages fails.
  - Only after the real-history audit, `candidate_snapshot.v1` materializes exact live permitted bytes with a temporary index and synthetic commit whose sole parent is actual `source_head_oid`. It seals complete tree, real committed implementation delta, supplemental dirty delta, trusted-export and taint-clean verdict chain, and tuple/path digests; rejects unmerged/unowned/test/tainted/unscanned deltas; and is rematerialized byte-identically and re-scanned at closeout. It supplements and never replaces the real-history audit.
  - `verification.json` advances additively to v3. Each parameter first runs the exact proof argv to exit zero in a pristine checkout of the sealed candidate commit; a fresh checkout then asserts exactly one anchor, applies only that replacement, and runs byte-identical argv. Only zero-to-nonzero is `killed`; nonzero/unrun baseline is `baseline_failed`; unmatched/unapplied/unrun mutation is `mutation_not_applied`; zero after mutation is `survived`. Exact criterion/parameter/command/candidate coverage is required.
  - v1/v2 artifacts remain parseable only for individually grandfathered criterion bytes proven by `grammar_cutover.v1`. The raw complete Markdown item bytes must exactly equal the same-ID item at cutoff OID `5328694ae31b4f13f091903d96ed89395d74f3b2`, whose GitHub receipt and immediate first-parent successor receipt bracket the cutoff. A one-byte change is non-grandfathered; pathname age and author/committer/manifest timestamps never authorize.
  - Because PROOFGATE changes `runner.py`, `verification_evidence.py`, `goal_coverage.py`, `tdd_chronology.py`, the plan validator parser, train re-verification, and closeout enforcement used for its own evidence, the implementation child is an untrusted builder only. It writes only the remote-less assigned clone, submits a framed result, and exits; it never supplies provider credentials, attests, commits the authoritative branch, or pushes. The trusted credentialless parent requires clean result/log/source frames, exports only allowed regular-file bytes into a new pristine coordinator-owned clone, scans the complete diff/index/tree/pending commit without any live secret corpus, creates one clean candidate commit, re-scans before push, pushes it, proves remote branch OID equals local `HEAD`, persists a runner-owned restart handoff, and exits `awaiting_phase_closeout`. A new repo-local process in a clean worktree at that exact pushed candidate must prove startup/expected/remote head equality and loaded module path/blob/SHA equality before running the ordinary broad suite/JUnit, a separately runner-attested attended 39-nodeid/four-provider JUnit, candidate audit, and exact-head four-seat implementation panel. That process exits before merge. After the exact candidate lands, another distinct fresh repo-local process at exact fetched canonical main repeats both ordinary and freshly attended modes before the canonical-main audit. Loaded-parent, builder-process, stale-head, repaired-in-process, executor-authored, missing owner-filter/parser/scanner gate, direct untrusted-clone commit/push, omitted mode, substituted attended receipt, or same-process candidate→main evidence is invalid.
  - `EmitPhaseCloseout` and `PhaseLoopCloseoutV1` gain no mutation field. Executor self-report cannot authorize proof. Direct, delegated, train, and terminal closeout independently validate the attested journal/head, bootstrap exception, isolation denial, panel/RED authorization, full history, candidate equality, mutations, and grandfather cutoff before `complete`.

## Corpus Provenance

- Consiliency/agent-harness#358 is pinned to commit `0196f19c7e9fd90e9a707de076271057b521e1d1`, path `plans/detailed-board-silent-degradation-358-20260728.md`, blob `98765f511edebce39ea59f1b54ca77393f737904`, and file SHA-256 `c56c6d940b45a79524cf764bd4d7d2fce8aca4c84b96471d6a3362cd496d4fe0`.
- Consiliency/agent-harness#288 is pinned to commit `4e7dbf419b85ffc5e57b43d424a680b1e92e9461`, path `plans/detailed-288-fab-broker-readmission-20260726.md`, blob `d1be22ceadd78ba87ac800d1ed0872a8e63698fb`, file SHA-256 `77dc3eb21bd0390a13fca2a3ac54258145092a7f98ec246c9ae160a4889351ec`, AC-1 slice SHA-256 `5bb80a50e6f7942d62bc58839bc6b66efa5b66439b899b9b90ddbbc4fe662329`, and AC-4 slice SHA-256 `9239214f44821e38eae26820b8d5304d04bf5753a3cb3d6ff7d61eb1e50574d0`.
- These immutable fixtures prove validator behavior only, not historical implementation or satisfaction.

## Test Activation And Evidence Freeze

`EXPECTED_PHASE_NODEIDS` is the exact 39-nodeid union of the 36-item
`DEFAULT_SKIP_NODEIDS` below, the one migrated nodeid, and the two unchanged
positive controls. The one migrated nodeid is never skipped:

- `phase-loop-runtime/tests/test_review_leg_sandbox.py::test_codex_execute_command_is_danger_full_access_and_live_repo`

Before activation it retains its literal legacy assertions (`--sandbox
danger-full-access`, live `--cd`, user config present). Under
`PHASE_LOOP_TDD_EXPECT_PROOFGATE=1`, and automatically after the capability
marker, the same nodeid asserts the isolated remote-less assigned clone,
scrubbed config/credentials, and no live-repo path. The implementation may not
rename it merely because the legacy name becomes historical.

The two positive-control nodeids are pre-existing, never skipped, and must pass
in default, activated RED, candidate, and canonical-main runs:

- `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_github_broker_admission_store_is_wired_to_evidence_revocation`
- `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_routing_broker_admission_store_is_wired_to_evidence_revocation`

The exact inactive default skip set is these 36 new nodeids—no glob, module
skip, generated selector, future nodeid, or marker-wide skip is admitted:

- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_bootstrap_records_are_single_use_and_server_bound`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_receipt_chain_rejects_rewrite_truncation_fork_or_backfill`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_receipt_chain_rejects_wrong_workflow_signer_source_blob_subject_or_timestamp`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_implementation_authorization_requires_activation_preflight_panel_and_red_order`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_runner_routes_reject_child_claims_and_missing_latest_external_head`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_isolation_preflight_masks_host_sibling_receipt_logs_fds_and_credentials`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_provider_projection_allows_only_selected_vendor_subscription_material`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_capability_socket_rejects_privileged_unknown_replayed_or_wrong_peer_requests`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_execute_and_panel_routes_use_remote_less_assigned_clone_or_refuse`
- `phase-loop-runtime/tests/test_proofgate_attestation_workflow.py::test_attestation_workflow_is_github_hosted_exact_subject_and_blob_bound`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_missing_falsifier_is_invalid`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_negative_assertion_requires_path_entered_control`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_guard_requires_production_construction_site`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_mutation_manifest_requires_exact_criterion_parameter_and_command_coverage`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_requires_two_parent_tests_bootstrap_and_implementation_landings`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_tests_only_range_with_non_test_bytes`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_bootstrap_range_outside_frozen_set_or_test_edit`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_implementation_range_test_guard_selector_nodeid_count_or_anchor_edit`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_same_branch_squash_rebase_direct_push_copy_or_hidden_parent`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_candidate_snapshot_is_source_head_parented_and_rematerializes_byte_identically`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_fresh_process_lifecycle_rejects_builder_stale_head_loaded_parent_or_same_process`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_junit_lifecycle_requires_exact_nodeids_default_skip_red_failures_and_final_zero_skips`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_unmatched_anchor_is_mutation_not_applied`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_matched_anchor_kill_requires_green_identical_command_baseline`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_missing_duplicate_extra_substituted_or_surviving_parameter`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_candidate_or_command_digest_drift`
- `phase-loop-runtime/tests/test_closeout_verification_gate.py::CloseoutVerificationGateTest::test_proofgate_closeout_rejects_missing_or_invalid_attested_proof`
- `phase-loop-runtime/tests/test_preflight_verification.py::PreflightVerificationTest::test_proofgate_preflight_requires_attested_authorization_and_exact_candidate`
- `phase-loop-runtime/tests/test_train_invariants.py::TestInvariant6LiveReverifyRunsVerification::test_proofgate_train_reverify_requires_exact_attested_proof`
- `phase-loop-runtime/tests/test_goal_coverage.py::GoalCoverageTest::test_acceptance_contracts_classify_valid_invalid_and_grandfathered`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_agent_harness_358_original_is_rejected`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_agent_harness_288_ac1_and_ac4_are_rejected`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_grandfathering_uses_exact_cutoff_criterion_bytes_and_warns`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_changed_or_new_criterion_requires_v3_evidence`
- `phase-loop-runtime/tests/test_skills_canon_parity.py::SkillsCanonParityTest::test_plan_phase_skills_publish_falsifier_grammar`
- `phase-loop-runtime/tests/test_skills_bundle_drift.py::SkillsBundleDriftTest::test_proofgate_validator_and_guidance_are_generated_without_drift`

The historical frozen nodeid
`test_provider_projection_allows_only_selected_vendor_subscription_material`
is retained byte-for-byte so the 39/36/37 inventory does not drift, but its
active contract permits no projection into any child, model, tool, provider
adapter, parser, CLI, policy-parent, or receipt namespace: it proves the selected
provider/model/seat remains usable only through `IntendedInferenceBroker` and a
credentialless adapter while the minimum seat-specific session exists only
inside the separately isolated fixed-function `SubscriptionTransportBroker`,
which never semantically parses hostile material or scans general artifacts. Its
`RED_CASES_BY_NODEID` cases include literal
`credential_env_absent`, `credential_argv_absent`,
`credential_mount_absent`, `credential_fd_absent`,
`subscription_state_absent`, `inference_exact_provider_model_seat`,
`inference_rejects_method_url_header_path`,
`inference_request_result_frame_immutable`,
`inference_output_provenance_bound`, `broker_result_taint_rejected`,
`trusted_parent_outside_model_tool_boundary`,
`provider_adapter_image_rootfs_protocol_policy_bound`,
`provider_adapter_has_no_subscription_projection`,
`subscription_transport_broker_exclusive_session_owner`,
`credentialless_request_semantics_validated_before_owner`,
`session_owner_preregistered_bytes_pre_network_exact_equality`,
`session_owner_has_no_semantic_parser_or_general_scanner`,
`broker_rejects_method_path_header_body_model_misuse`,
`broker_rejects_redirect_secondary_endpoint_proxy_and_raw_tls`,
`broker_rejects_credential_exfiltration_before_network`,
`semantic_namespaces_cannot_read_owner_memory_files_fds_or_socket`,
`owner_cannot_read_repo_logs_source_or_git_objects`,
`credentialless_response_parser_has_no_session_access`,
`credentialless_artifact_scanner_has_no_live_secret_corpus`,
`fable_subscription_transport_reachable`,
`sol_terra_subscription_transport_reachable`,
`gemini_subscription_transport_reachable`,
`grok_subscription_transport_reachable`,
`hostile_prompt_parent_read_refused`,
`hostile_prompt_parent_write_delete_git_publish_cloud_refused`,
`hostile_prompt_secret_retrieval_and_arbitrary_egress_refused`,
`owner_raw_response_live_credential_bytes_rejected`,
`credentialless_parser_and_adapter_diagnostics_taint_rejected`,
`child_multi_turn_local_tool_loop_reachable`,
`mandatory_provider_real_inference_reachable`,
`submitted_result_taint_rejected`, `child_log_taint_rejected`,
`tracked_source_taint_rejected`, `untracked_source_taint_rejected`,
`encoded_and_split_taint_rejected`, `taint_required_before_attestation`,
`taint_required_before_commit`, `taint_required_before_push`, and
`safe_intended_inference_and_import_succeeds`. The other three frozen isolation
nodeids consume the same cases for namespace inventory, capability/framing
rejection, and execute/panel/export routing; no new nodeid or skip is added.

The guard freezes these pre-implementation source anchors verbatim and maps
every `RED_CASES_BY_NODEID` entry to at least one; the two broker anchors remain
separate because each must match exactly once:

| Anchor ID | Frozen path and literal |
|---|---|
| `PG-A-LAUNCH` | `phase-loop-runtime/src/phase_loop_runtime/launcher.py`: `command.extend(["--sandbox", "danger-full-access"])` |
| `PG-A-PANEL` | `phase-loop-runtime/src/phase_loop_runtime/panel_invoker.py`: `env = _subscription_env() if env is None else dict(env)` |
| `PG-A-RUNNER` | `phase-loop-runtime/src/phase_loop_runtime/runner.py`: `from .dispatch_lock import DispatchLock, DispatchLockContention` |
| `PG-A-GOAL` | `phase-loop-runtime/src/phase_loop_runtime/goal_coverage.py`: `_ACCEPTANCE_SECTION_RE = re.compile(` |
| `PG-A-VERIFY` | `phase-loop-runtime/src/phase_loop_runtime/verification_evidence.py`: `SCHEMA_VERSION = 2` and `_SUPPORTED_SCHEMA_VERSIONS = frozenset({1, 2})` |
| `PG-A-CLOSEOUT` | `phase-loop-runtime/src/phase_loop_runtime/closeout.py`: `def _apply_verification_evidence_gate(` |
| `PG-A-TRAIN` | `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`: `def _live_reverify(workspace: Path, roadmap_path: Path, run_mode: str) -> bool:` |
| `PG-A-VALIDATOR` | `skills-src/claude/claude-plan-phase/scripts/validate_plan_doc.py`: `def _check_p_goal_id_coverage(` |
| `PG-A-WORKFLOW` | `.github/workflows/test.yml`: `permissions:` followed by `contents: read`, and `run: python -m pytest -m "not dotfiles_integration"` |
| `PG-A-BROKER-GITHUB` | `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/live.py`: the `Path(broker_root), admission_policy or _default_admission_policy, epoch_blocked=lambda: evidence_store.epoch_blocked,` constructor context |
| `PG-A-BROKER-ROUTING` | `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/live.py`: the `root, self._admission_policy, epoch_blocked=lambda: evidence_store.epoch_blocked` constructor context |

The exact activated failure set is the 36 default-skip nodeids plus the one
migrated nodeid. Each first asserts its mapped anchor(s), then fails with
`PROOFGATE_RED::<nodeid-function-name-without-test_>` on the pre-implementation
base. `RED_CASES_BY_NODEID` additionally enumerates every signer/workflow/source/
blob/subject/timestamp forgery, isolation path/descriptor/credential/socket
escape, credential/subscription-state exposure to any model/tool/adapter/parser/
scanner/child,
provider/model/seat or immutable-frame drift, parent-side model/tool execution,
adapter image/rootfs/client-protocol/profile/peer drift, credentialless request-
compiler/response-parser/artifact-scanner or fixed-function session-owner code/
protocol/endpoint/session-inventory/grant/request/raw-response/refusal-order
drift, owner semantic parsing/general scanning, same-endpoint method/path/header/
body/model misuse, redirect/secondary endpoint/proxy/raw-TLS or credential-
exfiltration attempt, any rejected-frame network effect, any semantic namespace
access to owner memory/files/fds/socket/session capability, any owner access to
repo/log/source/Git-object namespaces,
hostile-prompt read/write/delete/Git/publish/cloud/secret/network effects
against parent/live/canary state, missing real provider inference for any of
the four mandatory providers or child multi-turn/tool-loop reachability,
arbitrary method/URL/header/path, live-credential bytes leaving the owner,
synthetic-canary or structurally credential-bearing parser/adapter/result/log/
source egress, missing owner-response/parser/downstream-source/result/log admission or pre-ingest/
pre-attestation/pre-commit/pre-push taint verdict, privileged opcode, receipt
reorder/fork/backfill, `EC-PROOFGATE-0..7`
manifest row, `github_builder`/`routing_builder` construction-site mutation,
history/parent/diff laundering form, mutation parameter/survivor form, corpus
slice, cutoff byte/receipt form, runner route, and generated-skill target.
Running only an aggregate nodeid is insufficient: the coordinator runs every
case separately, asserts the exact anchor match count before mutation, and
retains its raw stdout/stderr, exit, structured JUnit testcase, and mode-specific
case accounting. `pytest --collect-only` output is parsed before every mode:
the focused phase intersection must equal the frozen 39 exactly once, the
ordinary broad intersection must equal those same 39, and each attended run
must execute the four frozen real-provider case rows exactly once. Any missing,
extra, duplicate, skipped, deselected, or mode-mislabeled row fails closed.

The JUnit lifecycle is exact:

- Tests-only default: `tests=39`, `passed=3`, `skipped=36`, `failures=0`,
  `errors=0`; skipped nodeid set equals `DEFAULT_SKIP_NODEIDS`; all broader
  pre-existing tests remain active and green.
- Forced pre-implementation RED: same 39 collected exactly once, `passed=2`,
  `failures=37`, `skipped=0`, `errors=0`; failure nodeid/tag/anchor set equals
  `RED_CASES_BY_NODEID`, with no `xfail`/`xpass`, deselection, collection,
  import, setup, teardown, provider auth, broker, or network prerequisite.
- Marker-present ordinary-hermetic candidate and ordinary implementation PR CI,
  with `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE` absent: the focused intersection is
  the same 39 nodeids exactly once with `passed=39`, `failures=0`, `errors=0`,
  zero phase skips/deselections, and all four real-provider rows recorded
  `not_executed_in_ordinary_mode`. The unchanged `.github/workflows/test.yml`
  broad `-m "not dotfiles_integration"` command and `automation.suite_command`
  run this mode without provider clients, sessions, credentials, live broker
  evidence, or network, and remain GREEN.
- Attended candidate and attended canonical main, each in its own fresh exact-
  head process with `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE=1` plus runner-attested
  stage/nonce/profile evidence: the same 39 nodeids run exactly once and report
  `passed=39`, `failures=0`, `errors=0`, zero skips/deselections, and the exact
  four `ATTENDED_REAL_PROVIDER_CASES` each execute one genuine subscription-
  backed schema-valid nonce turn. The candidate and canonical-main attended
  JUnits are distinct from the ordinary broad JUnit and are both mandatory.
All focused, ordinary-broad, and attended JUnits are parsed by nodeid and case
row, never trusted by aggregate counts or substituted across modes.

## Lane Index & Dependencies

SL-0 — Tests-only landing, trusted bootstrap, acceptance grammar, and chronology
  Depends on: (none)
  Blocks: SL-1, SL-2
  Parallel-safe: no
SL-1 — Mutation execution, sealed evidence, and live closeout enforcement
  Depends on: SL-0
  Blocks: SL-2
  Parallel-safe: no
SL-2 — Documentation sweep, Check-P integration, grandfathering, corpus, skill regeneration, and reducer
  Depends on: SL-0, SL-1
  Blocks: (none)
  Parallel-safe: no

## Lanes

### SL-0 — Tests-Only Landing, Trusted Bootstrap, Acceptance Grammar, And Chronology

- **Scope**: Freeze and two-parent-land the exact default/RED test contract, use its independent bootstrap verifier to two-parent-land and activate the minimum writer/isolation bootstrap, transition once to the self-hosted supervisor, and only then implement grammar and chronology on the distinct ordinary implementation branch without test edits.
- **Owned files**: `.github/workflows/proofgate-receipt-attestation.yml`, `phase-loop-runtime/src/phase_loop_runtime/proofgate_receipts.py`, `phase-loop-runtime/src/phase_loop_runtime/proofgate_isolation.py`, `phase-loop-runtime/src/phase_loop_runtime/launcher.py`, `phase-loop-runtime/src/phase_loop_runtime/panel_invoker.py`, `phase-loop-runtime/src/phase_loop_runtime/goal_coverage.py`, `phase-loop-runtime/src/phase_loop_runtime/tdd_chronology.py`, `phase-loop-runtime/tests/proofgate_tdd_guard.py`, `phase-loop-runtime/tests/proofgate_bootstrap_verifier.py`, `phase-loop-runtime/tests/test_proofgate_receipts.py`, `phase-loop-runtime/tests/test_proofgate_isolation.py`, `phase-loop-runtime/tests/test_proofgate_attestation_workflow.py`, `phase-loop-runtime/tests/test_review_leg_sandbox.py`, `phase-loop-runtime/tests/test_goal_coverage.py`, `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py`, `phase-loop-runtime/tests/test_tdd_chronology.py`, `phase-loop-runtime/tests/test_verification_evidence.py`, `phase-loop-runtime/tests/test_closeout_verification_gate.py`, `phase-loop-runtime/tests/test_preflight_verification.py`, `phase-loop-runtime/tests/test_train_invariants.py`, `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py`, `phase-loop-runtime/tests/test_skills_canon_parity.py`, `phase-loop-runtime/tests/test_skills_bundle_drift.py`, `phase-loop-runtime/tests/fixtures/proofgate/**`
- **Interfaces provided**: `proofgate_tdd_contract.v1`, `proofgate_test_selection.v1`, `proofgate_bootstrap_verifier`, `bootstrap_merge_review_gate.v1`, `proofgate_bootstrap_boundary.v1`, `ProofgateReceiptSupervisor`, `proofgate_receipt_head.v1`, `proofgate_isolation.v1`, `IntendedInferenceBroker`, `IntendedInferenceAgentAdapter`, `proofgate_agent_loop.v1`, `proofgate_agent_turn.v1`, `provider_adapter_profile.v1`, `subscription_auth_transport_adapter.v1`, `subscription_transport_broker.v1`, `validated_provider_request.v1`, `taint_clean_bounded_raw_response.v1`, `intended_inference_grant.v1`, `intended_inference_request.v1`, `intended_inference_result.v1`, credentialless provider request compiler/response parser, credentialless `CredentialTaintGate`, `credential_taint_gate.v1`, `bootstrap_activated`, `isolation_preflight_passed`, `implementation_authorized.v1`, `PaneledTestLane`, `test_lane_chronology.v1`, `mutation_manifest.v1`, `AcceptanceCriterionContract`, `AcceptanceGrammarResult`, `extract_acceptance_contracts()`, `check_acceptance_falsifiers()`, `verify_test_lane_chronology()`
- **Interfaces consumed**: (pre-existing) `EC_ID_LEADING_RE` (verified live source), check E task ordering (verified live source), check P goal coverage (verified live source), `launcher.launch_with_spec()` and `panel_invoker._exec_leg()` (verified live but unisolated source), dispatch lock (verified live concurrency primitive only), Git complete-history semantics, GitHub PR/workflow/attestation server APIs, `/usr/bin/bwrap` and Linux namespaces (verified live host primitives, not yet a trusted boundary), `verification.json` v1/v2 loader (verified live source)
- **Parallel-safe**: no
- **Tasks**:
  - test: `SL0-T1` creates the complete immutable guard/verifier/tests/fixtures-only delta, panels its exact blobs and inventories, proves marker-absent default compatibility plus forced assertion RED, and lands it through its own two-parent PR.
  - impl: `SL0-B1` starts from fetched canonical main containing `PR-T`; it changes only the five bootstrap files, while `SL0-I1` starts only after the independently verified bootstrap landing, fresh-head activation, denial preflight, trusted panel/RED receipts, and `implementation_authorized`.
  - verify: `SL0-V1` uses only the test-owned verifier before bootstrap trust, transitions through a fresh exact-bootstrap-main process, then forces the frozen grammar/chronology subset until the final marker exists.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `SL0-T1` | `test` | (none) | Every SL-0 test helper/test/fixture path only | exact 39-nodeid/36-skip/37-RED contract, ordinary/attended selection contract, and all mutation cases | Marker absent: focused JUnit must be `3 passed / 36 skipped`; forced `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1`: the same 39 nodeids must be `2 passed / 37 intended failures / 0 skipped`, with per-case raw logs/JUnit and asserted anchors and no live prerequisite. Freeze the marker-present ordinary-hermetic and attended-live modes over the same 39 nodeids, with exact four-row attended accounting. The frozen isolation nodeids enumerate credentialless request/response semantics, exclusive fixed-function session ownership, preregistered byte equality before network, method/path/header/body/model/redirect/secondary-endpoint/raw-TLS/credential-exfil misuse, reciprocal OS denial between every semantic namespace and the owner, owner inability to read logs/source/Git, owner raw-response byte filtering, credentialless diagnostic/artifact scanning, four mandatory attended real-provider positives, and child multi-turn/local-tool controls; no nodeid or skip is added. |
| `SL0-T2` | `verify` | `SL0-T1` | Tests-only branch, panel bundle, raw default/RED/ordinary/reference-live evidence | immutable test blobs, nodeid/mode tuple, guard, bootstrap verifier | Run ordinary broad compatible CI with marker/env absent, require only the exact 36 new skips, and prove forced RED needs no provider setup. Separately run the attended coordinator-reference boundary: require direct-child denial, credentialless adapter/parser/scanner OS isolation, fixed-function owner exclusive session ownership, reciprocal namespace probes, zero request count for every semantically invalid or post-preregistration-tampered request, all four real schema-valid Fable/Sol/Gemini/Grok subscription positives plus child multi-turn controls, and owner-filter/parser/scanner provenance without copying live secrets into a parser/scanner. The reference live run is bootstrap evidence, never ordinary CI evidence. Then require the test-owned verifier's decisive `bootstrap_merge_review_gate.v1` verdict for all exact-digest Fable/Sol/Gemini/Grok seats before opening and coordinator-merging `PR-T` as a server-recorded two-parent PR whose every-parent diff is only the SL-0 test helper/test/fixture paths. |
| `SL0-B1` | `impl` | fetched target containing `PR-T` | Exact five-file bootstrap set only; no tests, fixtures, guard, grammar, verifier-v3, or marker | immutable `SL0-T1` tests | Implement the credentialless child `--unshare-net` launch and exact two child pathname-socket mounts; keep `IntendedInferenceBroker` credentialless and policy-only; implement fresh credentialless provider request compilers/response parsers with no local tools or arbitrary network; implement the peer-bound `SubscriptionTransportBroker` only as exclusive minimum-session owner, preregistered byte-equality gate, fixed route/framing, final-hop auth injector, bounded raw-response live-credential byte filter, and one-shot clean-raw-response releaser; implement frozen Fable/Sol/Terra/Gemini/Grok adapters with no API-key fallback, bounded child-side `IntendedInferenceAgentAdapter`, immutable grant/request/raw-response/turn/result frames, credentialless `CredentialTaintGate`, trusted credentialless source exporter, and all four canary matrices. The owner contains no semantic parser or general scanner and has no repo/log/Git view. With activation forced require the exact bootstrap selector subset to pass without live setup and run all pre-existing launcher/panel regressions; with activation absent run the broad suite and require the unchanged `3 passed / 36 skipped` focused state. |
| `SL0-B2` | `verify` | `SL0-B1` | Read-only bootstrap candidate review, JUnit, Git/GitHub metadata | test-owned `proofgate_bootstrap_verifier.py` only | From a fresh process bound to the exact landed `PR-T` head, run the immutable test-owned verifier over the pushed bootstrap candidate and exact five-path diff. For every bootstrap-author and `PR-B` reviewer profile, require direct child denials; credentialless Fable/Sol/Gemini/Grok adapter/parser/scanner isolation; reciprocal denial of owner memory/session and repo/log/Git access using the exact syscall probes; zero-network rejection of every malformed request and every post-preregistration byte mismatch; fixed request framing/final-hop auth, raw-response live-credential filtering, credentialless response parsing, all four real subscription positives and child multi-turn positives; complete component/refusal-order provenance; and credentialless downstream artifact rejection before release/ingest/attestation. Require activated bootstrap-subset JUnit, ordinary default broad JUnit, workflow blob, server metadata, and the decisive `bootstrap_merge_review_gate.v1` verdict for all exact-digest seats before the coordinator may merge only that exact candidate as two-parent `PR-B`. |
| `SL0-B3` | `verify` | server reports `PR-B` merged | Exact fetched canonical-main bootstrap head, loaded module blobs, external activation and denial evidence | immutable bootstrap/guard tests | Start a different fresh repo-local process at exact fetched canonical main, prove loaded `proofgate_receipts.py`, `proofgate_isolation.py`, `launcher.py`, and `panel_invoker.py` bytes equal that head, verify `bootstrap_activated`, rerun direct-child, credentialless semantic-process OS isolation, reciprocal owner-denial, pre-network transport-misuse, owner raw-response filter, credentialless response-parser/scanner, and provider-specific real-positive canaries with exact component provenance, prove the child multi-turn/tool-loop remains reachable, repeat downstream/ingest/attestation/source-import/commit/push credentialless scans, transition once to the self-hosted supervisor, and re-observe panel/RED without accepting self-created bootstrap history or treating attestation as review correctness. |
| `SL0-I1` | `impl` | `SL0-B3` plus attested `implementation_authorized` | `goal_coverage.py` and `tdd_chronology.py` only on distinct `PR-I` branch | frozen tests; no test/helper/fixture edit | `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests/test_goal_coverage.py phase-loop-runtime/tests/test_acceptance_falsifier_contract.py phase-loop-runtime/tests/test_tdd_chronology.py -q` |
| `SL0-V1` | `verify` | `SL0-I1` | All SL-0 ordinary implementation paths read-only; `PR-I` remains unmerged | immutable forced subset plus bootstrap regressions | Force the grammar/chronology subset and require its owned nodeids pass; rerun bootstrap, launcher, panel, and all pre-existing goal-coverage tests with no unexpected skip or regression. |

`SL0-T1` must falsify manual-boundary escape; trusted-parent provider CLI/model/tool execution; any credential-bearing semantic parser or general scanner; missing/ineffective child, adapter, response-parser, artifact-scanner, or owner namespace; component image/rootfs/client-protocol/profile/peer drift; any credential/subscription material entering a child/model/tool/adapter/parser/CLI/policy/receipt/scanner namespace; semantic-process access to owner memory/files/fds/socket/session capability; owner access to repo/log/source/Git-object namespaces; owner semantic parsing of hostile request/response content; owner scanning of general artifacts; component protocol/endpoint/session-inventory/peer/grant/request/raw-response/refusal-order drift; parent/live/sibling/coordinator/canary visibility; host IPv4/IPv6 loopback, host abstract AF_UNIX, alternate pathname AF_UNIX, DNS/raw/direct/other egress; general proxy, raw TLS, non-provider/secondary endpoint, or redirect follow; inference or adapter socket/path widening; provider/model/seat/turn/prompt/history/schema/size/grant substitution; mutable/replayed framing; wrong method/path/header/body/model or credential-exfil request; any rejected semantic request or byte mismatch causing DNS/connect/TLS/HTTP bytes; missing output provenance; hostile Fable/Sol/Gemini/Grok content reading, writing, deleting, renaming, retrieving secrets, mutating Git, pushing/publishing, invoking cloud/package/workflow actions, executing any non-child tool, or reaching arbitrary egress; an always-deny, one-shot, or unreachable provider/child adapter; an ordinary-hermetic row, refusal, or synthetic/cached/alternate-provider result counted as a real positive; any missing/extra/duplicate/skipped/deselected nodeid or attended row; live credential bytes leaving the owner; public canary/forbidden credential structures surviving credentialless response parsing/adapter diagnostics, child release, submitted result/stdout/stderr, downstream source/result/log admission, or tracked/untracked export; any missing owner-response/parser/downstream/pre-ingest/pre-attestation/pre-commit/pre-push gate; direct commit/push of the untrusted clone; inherited receipt descriptors; authenticated remote mutation; privileged IPC; local-journal replacement/recomputation; any missing/unusable/non-substantive/non-`AGREE` Fable/Sol/Gemini/Grok exact-digest seat; any usable `DISAGREE`/`PARTIALLY AGREE`; post-merge review substitution; false GitHub signer/workflow/source/blob/subject/timestamp claims; bootstrap self-attestation/self-merge; test edits in bootstrap; ordinary production hidden in bootstrap; early/forged marker or attended activation; and any ordinary dispatch before activation/preflight/panel/RED/authorization. Genuine schema-valid subscription inference through each mandatory provider in both attended candidate and canonical-main runs, a bounded child-side multi-turn local read, an author-only harmless local write/read correction, a reviewer write refusal plus substantive verdict, and taint-clean source import must still pass, so isolation cannot be satisfied by disabling work. It also covers every grammar, corpus, full-history, candidate, mutation, cutoff, fresh-process, JUnit, BAML, direct/delegated/train, and closeout case named by IF-0-PROOFGATE-1. `PR-T` is complete before landing; no later test PR or selector amendment may repair an implementation.

### SL-1 — Mutation Execution, Sealed Evidence, And Live Closeout Enforcement

- **Scope**: Integrate the already-bootstrapped writer/authorization into every live runner route, audit real history, materialize the source-head-parented candidate, execute pristine-baseline then mutation proof commands, and enforce schema-v3 evidence through closeout.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/verification_evidence.py`, `phase-loop-runtime/src/phase_loop_runtime/runner.py`, `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`, `phase-loop-runtime/src/phase_loop_runtime/closeout.py`, `phase-loop-runtime/src/phase_loop_runtime/_contract_docs/runtime/verification-evidence-contract.md`
- **Interfaces provided**: `CandidateSnapshot`, `MutationDeclaration`, `MutationEvidence`, `MutationParameterEvidence`, `MutationEvidenceStatus`, `VerificationResult.candidate_snapshot`, `VerificationResult.mutations`, `VerificationResult.chronology`, `validate_proofgate_receipt_chain()`, `audit_real_phase_history()`, `validate_verification_artifact_for_plan()`
- **Interfaces consumed**: `ProofgateReceiptSupervisor`, verified external receipt heads, `proofgate_isolation.v1`, `proofgate_test_selection.v1`, `IntendedInferenceBroker`, `IntendedInferenceAgentAdapter`, `provider_adapter_profile.v1`, `subscription_auth_transport_adapter.v1`, `subscription_transport_broker.v1`, `validated_provider_request.v1`, `taint_clean_bounded_raw_response.v1`, credentialless provider response parser, credentialless `CredentialTaintGate`, trusted credentialless source exporter and clean-verdict chain, `implementation_authorized.v1`, `PaneledTestLane`, `test_lane_chronology.v1`, `mutation_manifest.v1`, `AcceptanceCriterionContract`, existing verification command/suite intake and artifact seal
- **Parallel-safe**: no
- **Tasks**:
  - test: `SL1-T1` forces the already-landed mutation/runner/closeout subset read-only through the bootstrapped boundary.
  - impl: `SL1-T2` wires receipt validation/writes, dispatch authorization, full DAG audit, candidate materialization, mutation execution, and direct/delegated/train/closeout enforcement on the same distinct `PR-I` branch without test edits.
  - verify: `SL1-T3` reruns the immutable forced subset and all pre-existing verifier/closeout/train suites, rejecting any unanchored, unisolated, stale-process, or substituted live path.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `SL1-T1` | `test` | `SL0-V1` | Read-only immutable tests | SL-0-owned verifier/runner/closeout/train nodeids | `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests/test_verification_evidence.py phase-loop-runtime/tests/test_closeout_verification_gate.py phase-loop-runtime/tests/test_preflight_verification.py phase-loop-runtime/tests/test_train_invariants.py -q` |
| `SL1-T2` | `impl` | `SL1-T1` | SL-1 production/doc files only | Immutable `SL0-T1` tests | Implement against the forced nodeids; no marker and no test/guard/fixture change. |
| `SL1-T3` | `verify` | `SL1-T2` | All SL-1 owned files; `PR-I` remains unmerged | Immutable forced subset plus complete pre-existing files | Rerun the `SL1-T1` command with activation forced, then run each complete named test file without `-k`; every pre-existing nodeid stays active/green and only frozen new nodeids are guard-controlled when activation is absent. |

`runner.py` and `train_runner.py` must refuse every production launch unless the fetched target contains the exact activated bootstrap, the latest external head verifies, the direct-child, credentialless semantic-process/fixed-owner reciprocal OS-isolation, pre-network semantic-and-byte transport-misuse, owner raw-response filter, credentialless parser/scanner, and mandatory real-provider preflights and implementation authorization are attested, and the launch uses the isolated assigned clone with an exact provider/model/seat/turn grant. The runner writes security transitions itself, never from child claims; never launches a credential-bearing provider CLI, semantic parser, general scanner, or tool-capable process; and cannot release or attest owner raw-response material, parser output, adapter diagnostic, child result/log, or source export without the ordered owner filter then credentialless parser/scanner gates. Closeout re-fetches server target/PR/attestation metadata, re-audits all parents and blobs, re-materializes exact live bytes, repeats credentialless downstream/complete-tree scans, requires distinct ordinary-hermetic and attended-live candidate/main accounting, and blocks on any changed head, candidate, journal, test/mode tuple, workflow blob, child-isolation profile, adapter/request-compiler/response-parser/scanner image/rootfs/client-protocol/profile/peer, owner code/protocol/endpoint/session-inventory/grant/request/raw-response/refusal order, request-count evidence, inference turn/provenance, source export, or clean-verdict chain.

### SL-2 — Documentation Sweep, Check-P Integration, Grandfathering, Corpus, Skill Regeneration, And Reducer

- **Scope**: Extend check P without weakening goal coverage, enforce raw-byte cutoff grandfathering, wire the pinned corpus, update canonical planner guidance, regenerate derived bundles, install the final capability marker, and perform candidate/two-parent/canonical-main reduction from fresh exact-head processes.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/proofgate_capability.py`, `skills-src/claude/claude-plan-phase/SKILL.md`, `skills-src/codex/codex-plan-phase/SKILL.md`, `skills-src/gemini/gemini-plan-phase/SKILL.md`, `skills-src/opencode/opencode-plan-phase/SKILL.md`, `skills-src/claude/claude-plan-phase/scripts/validate_plan_doc.py`, `phase-loop-skills/plan-phase/**`, `phase-loop-runtime/src/phase_loop_runtime/skills_bundle/*-plan-phase/**`
- **Interfaces provided**: check P falsifier disposition, `grammar_cutover.v1`, criterion-byte grandfather record, regenerated plan-phase skill bundle, `PROOFGATE_CAPABILITY_VERSION`, fresh exact-head candidate/canonical-main attestations, PROOFGATE closeout reduction
- **Interfaces consumed**: `AcceptanceGrammarResult`, `check_acceptance_falsifiers()`, `validate_verification_artifact_for_plan()`, `proofgate_test_selection.v1`, bootstrapped chronology/attestation/isolation evidence, ordinary-hermetic and attended-live JUnit contracts, trusted target first-parent provenance, existing skill regeneration/sync commands
- **Parallel-safe**: no
- **Tasks**:
  - test: `SL2-T1` forces the already-landed validator/corpus/parity/drift subset read-only before any SL-2 production byte changes.
  - impl: `SL2-T2` changes only canonical skills/validator and generated plan-phase outputs, then adds the exact marker as the final implementation byte after all three lanes are complete; no tests.
  - verify: `SL2-T3` proves marker-default ordinary-hermetic activation, freezes/pushes the exact candidate, exits the builder, uses a fresh exact-candidate process for separate ordinary-broad and attended-live proof before panel/candidate audit, two-parent-lands that exact head, and uses another fresh exact-canonical-main process for the same two-mode final proof.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `SL2-T1` | `test` | `SL1-T3` | Read-only immutable tests | SL-0-owned validator/corpus/parity/drift nodeids | `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py phase-loop-runtime/tests/test_goal_coverage.py phase-loop-runtime/tests/test_skills_canon_parity.py phase-loop-runtime/tests/test_skills_bundle_drift.py -q` |
| `SL2-T2` | `impl` | `SL2-T1` | Canonical sources, generated outputs, then final marker only | Immutable `SL0-T1` tests | Update canonical sources/validator; run `python3 phase-loop-runtime/scripts/regenerate_skills_bundle.py` then `python3 phase-loop-runtime/scripts/sync_skills_bundle.py`; after every lane-owned non-test byte is complete add only `proofgate_capability.py` with `PROOFGATE_CAPABILITY_VERSION = "proofgate.v1"`. |
| `SL2-T3` | `verify` | `SL2-T2` | All phase-owned implementation paths and reducer inputs | exact immutable 39-nodeid/mode contract plus ordinary broad compatible suite | With both activation envs absent and marker present, require ordinary-hermetic focused JUnit `39 passed / 0 skipped / 0 deselected`, four real-provider rows `not_executed_in_ordinary_mode`, and GREEN unchanged broad `-m 'not dotfiles_integration'`; run every hermetic Verification command and `automation.suite_command`. Terminate the untrusted builder after its framed handoff; have the trusted credentialless parent scan/export into a pristine clone, rescan the diff/index/tree/pending commit, freeze one clean `PR-I` candidate commit, rescan and push it, record exact remote equality plus the pre-ingest/pre-attestation/pre-commit/pre-push clean-verdict chain, and write the restart handoff without accepting child attestation. |
| `SL2-T4` | `verify` | `SL2-T3` | Exact pushed candidate in new clean worktree | separate candidate ordinary-broad and attended-live structured JUnits, panel, mutation and chronology evidence | Start a distinct repo-local process whose startup/loaded bytes equal exact candidate head; repeat credentialless complete-tree scanning; first run the unchanged ordinary broad suite hermetically, then runner-attest a fresh nonce and run the same exact 39 phase nodeids with `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE=1`, requiring each genuine Fable/Sol/Gemini/Grok row once plus multi-turn controls and zero skips/deselections. Bind both non-substitutable JUnits and every owner/parser/scanner provenance/OS/request-count receipt before the exact four-seat implementation panel, admit only credentialless-scanned provenance-bound panel frames, and run the candidate audit. Any repair creates a new candidate, nonce, ordinary JUnit, attended JUnit, and process. Exclude only the exact post-landing wrappers `tdd_chronology audit --stage canonical-main` and canonical-main panel, which are impossible before landing; exclude no pytest nodeid or other suite command. |
| `SL2-T5` | `verify` | `SL2-T4` | Server PR and exact canonical-main worktree | ordered two-parent implementation landing and separate final ordinary/attended evidence | Coordinator-merge only the exact reviewed, taint-clean candidate as two-parent `PR-I`, terminate the candidate process, fetch canonical main, and start another distinct repo-local process at that exact clean head. Repeat plan/manifest/roadmap validation and credentialless complete-tree scan; run the hermetic broad suite and focused 39-pass ordinary JUnit; then use a new runner-attested nonce for a distinct 39-pass attended JUnit with all four genuine provider rows exactly once and zero skips/deselections; run the exact-main panel, full mutation/chronology/receipt/provenance/clean-verdict audit, and terminal reduction. Candidate attended evidence cannot satisfy canonical-main attended evidence. Only this process may emit `complete`. |

`grammar_cutover.v1` freezes cutoff OID `5328694ae31b4f13f091903d96ed89395d74f3b2`, GitHub receipt `Consiliency/agent-harness#397 mergedAt=2026-07-29T22:09:58Z`, and immediate first-parent successor `a3fbb196b3b57d75e403bcea3bad972e9491f675` from Consiliency/agent-harness#382 at `2026-07-30T00:30:32Z`. Validator and runner extract identical raw item bytes; server receipt/target/adjacency/parser/digest disagreement fails closed. Check P stays WARN by default; schema-v3 proof remains mandatory at closeout.

## Execution Notes

- The predecessor panel record is dissent evidence only; it does not approve this changed digest. Before `PR-T`, the exact final plan SHA-256 recorded in the PROOFGATE manifest entry must receive a new panel in which the intended Fable, Sol, Gemini, and Grok seats are each usable, substantive, exact-digest, and `AGREE`, with zero usable `DISAGREE`/`PARTIALLY AGREE` and no unresolved material or blocking finding. Any plan edit invalidates that panel and restarts the plan gate.
- PR chronology is literal: `PR-T` contains only the complete test helpers/tests/fixtures and lands first as a two-parent merge; `PR-B` branches from fetched target containing `PR-T`, contains only the exact five bootstrap files, and lands second as a separately reviewed two-parent merge; after activation/preflight/self-host transition/authorization, one distinct `PR-I` branch is created from fetched target containing both landings. The three serial lane implementations accumulate on `PR-I`; its final pushed marker-bearing candidate receives the broad suite and implementation panel from a fresh exact-candidate process, then that exact head lands once as a two-parent merge. There are no separate mergeable `PR-I0`/`PR-I1`/`PR-I2` ranges that could hide test or parent drift.
- The manual bootstrap is not called trusted. It assumes the human/coordinator process and GitHub account are the temporary root, relies on current `/usr/bin/bwrap` 0.6.1 user/network namespaces, the exact mounted child AF_UNIX boundary, disposable credentialless request-compiler/response-parser/scanner rootfs images, a separately isolated fixed-function `SubscriptionTransportBroker` as exclusive minimum-session owner, frozen first-party subscription protocols, and public-repo GitHub Actions/attestation primitives, and authorizes only the exact test and bootstrap landings. The policy parent never holds subscription state or hosts a model/tool runtime; the owner never executes model content/local tools, semantically parses hostile bytes, scans diagnostics/logs/source/Git, or sees repo state. If any primitive, intended-inference broker, provider adapter/parser/scanner, owner protocol/endpoint/session inventory/peer profile, child multi-turn adapter, reciprocal semantic-process/owner OS denial, pre-network semantic-or-byte misuse rejection, owner raw-response filter, credentialless downstream scan, direct-child denial, mandatory real provider positive, or pre-merge four-seat gate fails, PROOFGATE stops independently before the affected merge or after already-landed `PR-T`; the exact non-human prerequisite after an approved/merged `PR-B` is a verified `bootstrap_activated` `proofgate_receipt_head.v1` attestation. No ad-hoc local writer, credential-bearing CLI/parser/scanner fallback, API-key fallback, ambient/general proxy, raw-TLS forwarder, credentialless-child CLI fallback, or post-merge correctness substitute is accepted.
- Runtime lane scheduling is serialized in work-unit mode after bootstrap so every author has a plan-owned assignment. The same selected author vendor may implement all lanes, but may never count as its own attestor or invoke merge/attestation credentials.
- The chronology verifier owns `proofgate_tdd_candidate_main.v1`. It reads the phase manifest contract and requires exact plan/roadmap/manifest-entry digests; owned/test path tuples; activation/attended-env/marker/count literals; test guard/bootstrap-verifier/test/fixture blob OIDs and SHA-256 values at `PR-T`, `PR-B`, implementation base, candidate, implementation landing, and canonical main; workflow/bootstrap blob digests; candidate tree and every phase-owned production blob; nodeid tuple, mode/accounting tuple, and anchor/case map digests; separate default/RED/ordinary-candidate/attended-candidate/ordinary-main/attended-main JUnit and clean raw-log digests; exact provider/model/seat/turn and inference grant/validated-request/raw-response/result/provenance digests; provider-adapter/request-compiler/response-parser/scanner image/rootfs/first-party-client/protocol/profile/peer digests; session-owner code/protocol/endpoint/session-inventory/peer/grant/preregistered-request/equality/final-hop-auth/raw-response-filter/refusal-order and rejected-frame zero-request-count digests; hostile-model, pre-network semantic/byte misuse, reciprocal OS-isolation, and parent/live/canary pre/post evidence; four mandatory real provider positives in each attended final stage and child multi-turn/tool-loop evidence; trusted credentialless exporter and owner-response/parser/downstream-source/result/log/pre-ingest/pre-attestation/pre-commit/pre-push clean-verdict-chain digests; typed refusal semantics; panel subject/verdict digests; receipt/external-head digests; process startup nonces/PIDs/heads/loaded-module paths/blobs/SHA values; remote equality; server PR identities, ordered parents, `mergedAt`, and every-parent path/mode/blob tuples. No secret value, secret-derived digest, length, excerpt, auth field, or raw diagnostic is recorded. Every lifecycle transition consumes the previous immutable digest; no later stage may replace an earlier digest with a recomputed local value.
- Full Git history, server PR metadata, and attested dispatch receipts all participate. `source_head_oid` in `candidate_snapshot.v1` is the exact committed/pushed `candidate_head_oid`; the source-head-parented synthetic snapshot supplements that audit and never substitutes for it. Test/bootstrap/implementation blob reuse through copies, merge parents, rebases, squashes, direct pushes, or preexisting objects is rejected, even if the final tree looks correct.
- The implementation branch/base is recreated from the fresh trusted target after both bootstrap landings and authorization. Any production dirt/commit before authorization permanently denies the run; later receipts cannot cure it.
- Every mutation gets a pristine green baseline and fresh mutation checkout from the same candidate commit. Review legs inspect sealed metadata only; mutations never run in the authoritative worktree.
- Broad compatibility and attended live proof are separate candidate and merge preconditions. Before the implementation panel and again on canonical main, the respective fresh exact-head process first runs every hermetic `## Verification` command plus `automation.suite_command`, including the unchanged full `phase-loop-runtime/tests` suite under `-m 'not dotfiles_integration'` and an ordinary structured JUnit with no live provider work. It then starts a distinct runner-attested attended invocation over the same exact 39 phase nodeids and requires all four real provider rows once, multi-turn controls, and zero skip/deselect. Neither mode can substitute for the other. The only deferred operations at candidate are the exact post-landing evidence wrappers `python3 -m phase_loop_runtime.tdd_chronology audit --stage canonical-main ...` and the canonical-main panel; no test file, nodeid, existing regression, lint, validator, candidate-stage evidence command, or attended provider row is excluded. A material panel finding changes the candidate and requires a new push, process, ordinary broad gate, attended proof, and panel.
- The untrusted candidate builder cannot attest loaded code, commit/push the authoritative branch, or supply its own taint verdict. It records only a bounded framed handoff and exits. The trusted credentialless parent scans result/log/source bytes, exports allowed regular files into a pristine clone, scans the diff/index/tree/pending commit, commits, scans again, pushes, and records a handoff binding startup identity, candidate head/tree/remote ref, plan/manifest/roadmap/test-selection digests, loaded proof-module digests, inference provenance, and clean-verdict chain; it never receives live provider credentials or a secret-derived corpus. The candidate verifier must be a new process at a clean worktree whose `HEAD`, expected head, fetched remote candidate OID, and loaded repo-local `runner.py`, `verification_evidence.py`, `goal_coverage.py`, `tdd_chronology.py`, `proofgate_receipts.py`, `closeout.py`, `train_runner.py`, validator script, and capability-marker blobs all equal the exact candidate. It re-scans the complete candidate, runs both ordinary and attended modes, and exits before merge. The final verifier must be another new process at clean exact fetched canonical main and repeat equality, credentialless scan, ordinary mode, and fresh attended proof. Checkout, repair, direct child commit/push, missing scan, missing mode, or process reuse invalidates evidence.
- `EmitPhaseCloseout`, generated BAML schema, and launchspec goldens are outside the write set. If implementation requires executor-authored proof or a BAML mutation field, stop with non-human `contract_bug` and amend the plan.
- Record `no_doc_delta` for `README.md`, `CHANGELOG.md`, and release notes unless a new public surface is discovered. PROOFGATE publishes no package and dispatches no release.

## Spec Closeout Plan

- schema: `spec_delta_closeout.v1`
- decision: `dotfiles_skill_source_update`
- target surfaces: `skills-src/*/*-plan-phase/**`, `phase-loop-skills/plan-phase/**`, `phase-loop-runtime/src/phase_loop_runtime/skills_bundle/*-plan-phase/**`, `phase-loop-runtime/src/phase_loop_runtime/_contract_docs/runtime/verification-evidence-contract.md`
- evidence paths: `plans/phase-plan-v10-PROOFGATE.md`, `plans/manifest.json`, `phase-loop-runtime/tests/proofgate_tdd_guard.py`, `phase-loop-runtime/tests/proofgate_bootstrap_verifier.py`, `phase-loop-runtime/tests/test_proofgate_receipts.py`, `phase-loop-runtime/tests/test_proofgate_isolation.py`, `phase-loop-runtime/tests/test_proofgate_attestation_workflow.py`, `phase-loop-runtime/tests/test_tdd_chronology.py`, `phase-loop-runtime/tests/test_verification_evidence.py`, `phase-loop-runtime/tests/test_closeout_verification_gate.py`, `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py`, `phase-loop-runtime/tests/fixtures/proofgate/**`, `phase-loop-runtime/tests/test_skills_canon_parity.py`, `phase-loop-runtime/tests/test_skills_bundle_drift.py`, `.phase-loop/runs/**/proofgate-tests-only-default.junit.xml`, `.phase-loop/runs/**/proofgate-tests-only-red.junit.xml`, `.phase-loop/runs/**/proofgate-compatible-suite.junit.xml`, `.phase-loop/runs/**/proofgate-candidate-ordinary.junit.xml`, `.phase-loop/runs/**/proofgate-candidate-attended.junit.xml`, `.phase-loop/runs/**/proofgate-canonical-main-ordinary.junit.xml`, `.phase-loop/runs/**/proofgate-canonical-main-attended.junit.xml`, `.phase-loop/runs/**/proofgate-candidate-evidence.json`, `.phase-loop/runs/**/proofgate-canonical-main-evidence.json`
- redaction posture: `metadata_only`
- downstream handling: `mirror cutover`

## Verification

- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli validate-roadmap specs/phase-plans-v10.md`
- `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_goal_coverage.py phase-loop-runtime/tests/test_verification_evidence.py phase-loop-runtime/tests/test_closeout_verification_gate.py phase-loop-runtime/tests/test_preflight_verification.py phase-loop-runtime/tests/test_train_invariants.py phase-loop-runtime/tests/test_review_leg_sandbox.py phase-loop-runtime/tests/test_convergence_broker_revocation_race.py phase-loop-runtime/tests/test_skills_canon_parity.py phase-loop-runtime/tests/test_skills_bundle_drift.py -q`
- `env -u PHASE_LOOP_TDD_EXPECT_PROOFGATE -u PHASE_LOOP_PROOFGATE_ATTENDED_LIVE PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -m 'not dotfiles_integration'`
- `PHASE_LOOP_PROOFGATE_ATTENDED_LIVE=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -m 'not dotfiles_integration'`

The runner executes the attended command only in fresh exact-head candidate and canonical-main stages and stamps the distinct attended JUnit plus run nonce, head, loaded-module, broker/profile, and four-row real-provider accounting digests.
- `ruff check phase-loop-runtime/src/phase_loop_runtime/`
- `PYTHONPATH=phase-loop-runtime/src python3 phase-loop-skills/plan-phase/scripts/validate_plan_doc.py plans/phase-plan-v10-PROOFGATE.md`
- `git diff --check`

## Acceptance Criteria

- [ ] EC-PROOFGATE-0 — proven by the test-owned bootstrap verifier plus final `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.tdd_chronology audit --stage canonical-main --repo . --plan plans/phase-plan-v10-PROOFGATE.md --json`; the audit must prove the immutable 39-nodeid/36-default-skip/37-activated-failure lifecycle with asserted anchors/raw assertion RED and no live prerequisite, plus the exact mode contract: marker-present ordinary tests and unchanged broad `-m "not dotfiles_integration"` CI run all 39 phase nodeids hermetically with zero phase skips/deselections and zero executed real-provider rows, while fresh exact-head candidate and canonical-main attended runs each execute those same 39 nodeids with zero skips/deselections and the exact four genuine Fable/Sol/Gemini/Grok subscription rows once. Ordinary, candidate-attended, and main-attended JUnits are distinct and non-substitutable; no API-key, synthetic, cached, refused, alternate-provider/model, or one-provider proxy result counts positive. The audit must also prove immutable guard/tests/selectors/mode predicates/counts/cases; the single-use bootstrap transition; decisive pre-merge exact-digest four-seat `AGREE` gates for `PR-T` and `PR-B`; credentialless-child `--unshare-net` isolation with exact intended-inference/coordinator socket paths; and a credential boundary in which every model, tool, provider request compiler/adapter, provider response parser, diagnostics processor, receipt writer, and general artifact scanner is credentialless. The separately isolated `SubscriptionTransportBroker` alone owns the minimum seat session and is only a fixed-function transport/credential injector: it accepts already semantically validated grant-bound bounded request material, checks complete preregistered byte equality/digests/peer identity before DNS or network without parsing the prompt/envelope, uses only a frozen literal TLS host/certificate/address/method/path/header/body layout with no redirect follow/secondary endpoint/retry/general proxy/raw-TLS/arbitrary-byte surface, injects auth only as a separate final-hop iovec, performs only finite bounded byte-level live-credential checks over raw response/error bytes, and releases only one taint-clean bounded raw-response frame to a separate credentialless provider parser. For Fable, Sol, Terra, Gemini, and Grok evidence binds the installed first-party executable/library/protocol, credentialless compiler/parser image/rootfs/profile/peer, owner code/protocol/literal endpoint/session inventory, sealed provider/model/seat/turn/prompt/history/observation/schema/size grant, preregistered request bytes/digest, equality result, final-hop transition, raw-response filter, credentialless parsed response, refusal order, and non-secret provenance. Pre-network canaries mutate both hostile semantic input and post-validation bytes and require every wrong method/path/header/body/model/endpoint/proxy/raw-TLS/credential-exfiltration attempt to produce zero DNS/connect/TLS/HTTP/provider-trap effect. Exact reciprocal OS probes require every semantic namespace to fail `openat`/`readlink` of owner state and `/proc/<owner>/{mem,environ,fd,root,mountinfo}`, `ptrace`, `process_vm_readv`, `pidfd_open`/`pidfd_getfd`, `SCM_RIGHTS`, alternate socket, `keyctl`, and coredump routes, while the owner cannot see repo/worktree/review/log/receipt/Git-object paths or execute a shell/parser/scanner; the sole successful crossings are preregistration, one-shot exact request, and one-shot clean raw response. The credentialless `CredentialTaintGate` owns general parser-output/diagnostic/result/log/source/index/tree/commit scans using only public structural rules and synthetic canaries, never live credentials or secret-derived hashes/lengths/corpora. Hostile model tool requests surface only as canonical child turn frames; attended real positives and child multi-turn author/reviewer controls remain reachable. The audit retains ordered owner-filter/parser/scanner and pre-ingest/pre-attestation/pre-commit/pre-push clean verdicts, attested panel/RED/authorization order, ordered two-parent `PR-T`/`PR-B`/`PR-I` ancestry and every-parent diffs, implementation range without any test byte, exact pushed candidate and canonical main, distinct fresh processes with head/loaded-module equality, hermetic broad plus attended live proof before panel and at final closeout, and only then the source-head-parented supplemental candidate; falsified by accepting collection/import/setup RED, any skipped/deselected/extra/missing/duplicate phase nodeid or provider row, ordinary CI attempting live auth/network or counting as live proof, forced RED requiring a client/session/broker/network, early/forged marker or attended mode, a changed test/guard/count/anchor/mode predicate, self-authored/self-merged/unattested bootstrap evidence, acceptance-grammar bootstrap circularity, any credential-bearing provider CLI/model/tool/semantic-parser/general-scanner process, semantic parsing or repo/log/Git scanning inside the credential owner, any live credential copied to a parser/scanner, subscription state outside the owner, missing/ineffective namespace or reciprocal OS denial, parent/live/canary visibility or mutation, component/provenance drift, a general proxy/raw-TLS forwarder or successful rejected-frame network effect, missing/widened socket path, provider/model/seat/turn/prompt/history/schema/size/grant substitution, mutable/replayed/arbitrary framing, missing output provenance, an always-deny/one-shot/nonfunctional transport or child adapter, missing distinct attended Fable/Sol/Gemini/Grok positive at either candidate or main, missing multi-turn control, any hostile local tool or authenticated side effect outside the child, live credential bytes leaving the owner, a public synthetic canary/forbidden credential structure surviving credentialless downstream scans, a missing/stale ordered clean verdict, direct commit/push of the untrusted clone, any other boundary/credential/IPC escape, a missing/unusable/non-substantive/non-`AGREE` intended seat, any usable `DISAGREE`/`PARTIALLY AGREE`, unresolved material/blocking finding, one-`AGREE`/count/post-merge-attestation authorization, author-recomputed journal, missing/late authorization, wrong nodeid/seat/subject/command/anchor/log/exit, target/server or process/head/module disagreement, same-branch/squash/rebase/direct landing, pre-test/copied/merged/preexisting production bytes, hidden test edits, loaded-parent self-attestation, synthetic-history substitution, or dirty-path omission.
- [ ] EC-PROOFGATE-1 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_missing_falsifier_is_invalid -q`, falsified by removing the clause requirement and observing a falsifier-less fixture validate.
- [ ] EC-PROOFGATE-2 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_verification_evidence.py -k 'baseline or mutation_not_applied or matched_anchor_kill' -q`, falsified by a nonzero pristine baseline, substituted command, or zero-match anchor recording PASS/`killed`; the matched-anchor path-entered control requires baseline zero, one applied replacement, and identical-command nonzero mutation exit.
- [ ] EC-PROOFGATE-3 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py -k 'agent_harness_358_original or agent_harness_288_ac1 or agent_harness_288_ac4' -q`, falsified by wrong provenance or any unchanged original criterion validating without a typed missing/vacuous-falsifier rejection.
- [ ] EC-PROOFGATE-4 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_github_broker_admission_store_is_wired_to_evidence_revocation phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_routing_broker_admission_store_is_wired_to_evidence_revocation -q` plus both exact candidate-bound mutation rows, falsified by helper-only mutation, non-unique anchor, command/candidate substitution, failed baseline, or either constructor deletion surviving.
- [ ] EC-PROOFGATE-5 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_matched_anchor_kill_requires_green_identical_command_baseline phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_missing_duplicate_extra_substituted_or_surviving_parameter phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_candidate_or_command_digest_drift -q`, falsified by any missing/duplicate/extra/substituted/baseline-failed/surviving parameter leaving aggregate `killed` or an empty survivor set.
- [ ] EC-PROOFGATE-6 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_acceptance_falsifier_contract.py -k 'path_entered_control' -q`, falsified by deleting the control from a negative assertion and observing a valid disposition; the positive control executes the guarded path before absence is asserted.
- [ ] EC-PROOFGATE-7 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_grandfathering_uses_exact_cutoff_criterion_bytes_and_warns phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_changed_or_new_criterion_requires_v3_evidence -q`, falsified by failing to WARN for exact same-ID/raw bytes at the trusted cutoff, grandfathering a one-byte-changed/new item, trusting local timestamps/path age, tolerating server/adjacency/parser/digest disagreement, or accepting v1/v2 evidence for a non-grandfathered criterion.
