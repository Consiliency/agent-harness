---
phase_loop_plan_version: 1
phase: PROOFGATE
roadmap: specs/phase-plans-v10.md
roadmap_sha256: 7f2590bdebf5a892cf0987b67916d2c3b95970b547117b8cbf4adc7c7220838e
proofgate_lifecycle_contract: proofgate_tdd_candidate_main.v1
proofgate_tdd_activation_env: PHASE_LOOP_TDD_EXPECT_PROOFGATE
proofgate_capability_marker: phase_loop_runtime.proofgate_capability:PROOFGATE_CAPABILITY_VERSION=proofgate.v1
proofgate_expected_nodeids: 39
proofgate_default_skip_nodeids: 36
proofgate_activated_red_failures: 37
automation:
  suite_command: ["bash", "-lc", "test -n \"${PHASE_LOOP_RUN_DIR:-}\" && PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli validate-roadmap specs/phase-plans-v10.md && PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -m 'not dotfiles_integration' --junitxml=\"$PHASE_LOOP_RUN_DIR/proofgate-compatible-suite.junit.xml\" && ruff check phase-loop-runtime/src/phase_loop_runtime/ && PYTHONPATH=phase-loop-runtime/src python3 phase-loop-skills/plan-phase/scripts/validate_plan_doc.py plans/phase-plan-v10-PROOFGATE.md && git diff --check"]
  mutation_manifest: phase-loop-runtime/tests/fixtures/proofgate/v10-proofgate-mutations.json
---

# PROOFGATE: Falsifier Gate

## Context

PROOFGATE extends the existing acceptance and goal-coverage path. Current GitHub `main` is `648be2f68d6804ecdc4046bb7d4f5ee81a90c356`; at that source `launcher.py` sends ordinary Codex authors through `--sandbox danger-full-access`, `child_executor_env()` copies the ambient environment except Claude self-markers, `runner.py` owns only the repo-local dispatch lock, and `tdd_chronology.py`, a proof receipt writer, an external receipt-head anchor, and an author-inaccessible production boundary do not exist. None of those missing capabilities is treated as pre-existing.

The immediately preceding exact-digest panel record `.phase-loop/reviews/v10-proofgate-plan-panel.json` has SHA-256 `9e67f09d786233f36d1b086dc2f4f75c3c442cb68ec542e3ac24b9064a325129`, binds reviewed plan SHA-256 `b2bd5c8352e2804fcde3363f36ced47a90c74a7997c183f639335ac91134206f`, and records usable Grok `AGREE`, usable GPT-5.6 Sol `DISAGREE`, usable Gemini `AGREE`, and no Fable result. Sol confirms that the credentialless child, exact EC-5/EC-7 nodeids, and taint-clean export chain close the prior credential-projection defect, but identifies a second blocker: the parent-side intended-inference path still runs today's full agentic subscription CLIs inside the trusted parent. Codex can execute shell tools, Claude currently receives `Read,Write`, Gemini/agy has no security-grade tool restriction, and Grok's tool controls do not make an unrestricted agent process a parent-safe boundary. A hostile child prompt can therefore induce reads, mutations, secret retrieval, publication, cloud operations, or arbitrary network effects against parent resources before `CredentialTaintGate` sees output; moving those CLIs into the credentialless child would instead make author/reviewer work unreachable because no custom-socket multi-turn/tool adapter exists. This repair keeps the trusted parent outside every model/tool boundary: the parent seals grants and policy only, every provider turn runs in a disposable separately isolated credential-bearing worker behind a fixed-destination provider-endpoint broker, and the separately credentialless author/reviewer child uses a concrete bounded `IntendedInferenceAgentAdapter` loop over `IntendedInferenceBroker`. It preserves the exact EC-5/EC-7 nodeids, all previously closed credential-projection and taint findings, full-real-history audit, source-head-parented supplemental candidate, per-mutation pristine green baseline, raw-byte criterion grandfathering with a server-receipted first-parent cutoff, direct child canaries, decisive pre-merge four-seat `PR-T`/`PR-B` unanimity, immutable verifier, history and receipt contracts, and the exact 39/36/37 TDD lifecycle. The changed plan digest requires a fresh full panel.

The phase remains three serial lanes but gains a staged bootstrap inside SL-0. A complete immutable tests-and-fixtures-only PR lands first. A second, separately reviewed bootstrap implementation PR then lands only the minimum writer, attestation workflow, and OS-isolation call sites, with no test edit. Those two landings use a manually coordinator-controlled Bubblewrap launch and GitHub PR/merge metadata; they do not claim receipts from code that was not yet merged. Trust begins only after the merged bootstrap implementation is independently verified and its activation head is signed by the landed GitHub Actions attestation workflow. Every later author/reviewer, panel/RED observation, production dispatch, mutation run, and closeout uses that bootstrapped path.

Literal TDD feasibility is part of the frozen contract, not an execution convention. The tests-only landing owns one deterministic guard: `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1` forces the new contract against the pre-implementation base, while the exact production capability marker `phase_loop_runtime.proofgate_capability.PROOFGATE_CAPABILITY_VERSION == "proofgate.v1"` activates the same immutable tests by default only in the complete implementation candidate. With both absent, ordinary/default tests-only CI is GREEN: one migrated legacy nodeid and two unchanged positive controls pass, only the exact 36 frozen new nodeids skip, and every other pre-existing test remains collected and active. Forced pre-implementation RED is assertion RED, never collection RED: all 36 new production-dependent nodeids plus the one migrated active branch execute and fail at their intended `PROOFGATE_RED::<case-id>` assertion after their frozen source injection anchor is asserted. No `xfail`/`xpass`, import/collection failure, setup/teardown error, module-level capability import, selector drift, or broad skip marker is admissible.
No collection/import failure is accepted as RED or compatibility evidence.

## Interface Freeze Gates

- [ ] IF-0-PROOFGATE-1 — Acceptance, bootstrap, chronology, mutation, and closeout contract:
  - The authoritative item-leading grammar remains `- [ ] EC-<ALIAS>-<N> — proven by <command>, falsified by <executable mutation>`. A guard/gate/fence/interlock item also names `production construction site`; an absence/non-occurrence item also names a `path-entered control`. `goal_coverage.py` preserves existing EC-ID coverage behavior while adding typed `valid`, `invalid`, and `grandfathered` criterion dispositions.
  - `automation.mutation_manifest` resolves a repo-relative `mutation_manifest.v1` with exactly one declaration per non-grandfathered criterion. Each declaration has `criterion_id`, `mutation_id`, `target_kind`, and nonempty `parameters`; each unique parameter has `parameter_id`, repo-relative `target_path`, exact disambiguated `injection_anchor`, replacement bytes, and one argv-valued `proof_command`. The runner derives sorted IDs/count/digests and command digests from this declaration; evidence fields are never authoritative.
  - `proofgate_tdd_contract.v1` is test-owned at `phase-loop-runtime/tests/proofgate_tdd_guard.py`. It freezes literal tuples/maps for `EXPECTED_PHASE_NODEIDS` (39), `DEFAULT_SKIP_NODEIDS` (36), `MIGRATED_NODEIDS` (1), `POSITIVE_CONTROL_NODEIDS` (2), and `RED_CASES_BY_NODEID` (37 failure nodeids plus every parameter/case and asserted source anchor). It asserts set disjointness and exact union/count equality. Only the exact 36 new nodeids may use its skip mark. The migrated nodeid is never skipped: before capability it runs its byte-for-byte legacy assertions; with forced activation or the marker it runs the new isolation assertions. New modules keep capability-specific imports inside active test bodies and convert an expected missing pre-implementation symbol into the named assertion failure, never an import or collection error.
  - The activation predicate is exactly `os.environ.get("PHASE_LOOP_TDD_EXPECT_PROOFGATE") == "1"` or a successful, value-equal read of `phase_loop_runtime.proofgate_capability.PROOFGATE_CAPABILITY_VERSION == "proofgate.v1"`. No other environment value, branch name, Git state, import exception, runner/model claim, partial bootstrap module, or locally supplied flag activates the tests. The marker file is absent from `PR-T`, `PR-B`, and every partial implementation commit; SL-2 adds it only after every ordinary production, validator, skill, generated, and contract-doc byte is complete. Implementation may not edit tests, the guard, selectors, nodeids, counts, anchors, RED case IDs, or test-owned bootstrap verifier.
  - The test-owned `proofgate_bootstrap_verifier.py` consumes only frozen guard constants, canonical pytest JUnit, taint-clean raw logs, sealed coordinator-reference inference/provider-worker/endpoint-broker/taint policy and code/profile digests, hostile-prompt and positive-reachability canary evidence, Git object/parent/diff data, exact per-seat review records, and coordinator-observed GitHub PR/merge metadata. It neither imports nor calls the acceptance grammar, `goal_coverage.py`, the new validator check, `tdd_chronology.py`, `verification.json` v3, `ProofgateReceiptSupervisor`, the candidate `IntendedInferenceBroker`/`IntendedInferenceAgentAdapter`/provider-worker adapters/`CredentialTaintGate`, or any candidate-owned parser. This immutable verifier applies `bootstrap_merge_review_gate.v1` before the coordinator may merge either `PR-T` or `PR-B`; a review count, a single `AGREE`, merge metadata, candidate-authored clean verdict, or later attestation cannot satisfy or cure that pre-merge gate. The verifier and the manual coordinator/GitHub boundary authorize only those two exact landings. After `PR-B` is a verified two-parent target landing and `bootstrap_activated` is externally attested, a fresh process at that exact fetched canonical-main head proves its loaded bootstrap modules match the landed blobs and cross-runs the immutable request/result/turn/worker/endpoint/hostile-prompt/taint vector corpus against both the pre-bootstrap coordinator reference and landed implementation; only then does authority transition once to the self-hosted `ProofgateReceiptSupervisor`. The self-hosted verifier re-imports and cross-checks the two bootstrap records but does not retroactively create, approve, taint-scan, or attest them.
  - The manifest declares all `EC-PROOFGATE-0..7`. EC-PROOFGATE-4 uses `github_builder` and `routing_builder`, each with context sufficient to match exactly one `epoch_blocked=lambda: evidence_store.epoch_blocked` construction-site argument in `convergence/broker/live.py`, and the two named production-builder tests in `test_convergence_broker_revocation_race.py`. Helper-only mutation is invalid.
  - `proofgate_bootstrap_boundary.v1` is manual, coordinator-controlled, and narrowly scoped. Until the one-time post-`PR-B` authority transition, a sealed coordinator-owned reference inference broker, provider-worker supervisor/endpoint broker, and taint gate—not any `PR-T`/`PR-B` candidate byte—serve every child; their exact code/policy/profile digests and immutable protocol/canary vectors are panel/verifier inputs, and they are outside every child mount. The reference parent also remains outside every model/tool boundary: it launches the same disposable provider-worker profiles later implemented by `PR-B`, never a provider CLI in its own namespace. Live preflight must prove `/usr/bin/bwrap` plus unprivileged user/mount/PID/network namespaces work. Before launching any test author, bootstrap author, or `PR-T`/`PR-B` reviewer, the coordinator runs the complete direct non-model canary matrix below with the byte-identical Bubblewrap argv and mount profile; any failed negative or positive probe prevents that child launch, and the taint-clean raw probe/log/profile digests become immutable pre-merge verifier inputs. The coordinator repeats this prelaunch proof for every changed child or provider-worker profile, adapter, endpoint set, or candidate digest. It then creates a self-contained, remote-less assigned clone and launches the child through Bubblewrap with `--unshare-net`, user/PID/IPC/UTS/cgroup namespaces, `--new-session`, `--die-with-parent`, all capabilities dropped, host `/home`, `/root`, `/mnt`, `/run`, `/tmp`, sibling worktrees, coordinator state, provider credentials, provider subscription state, and credential sockets masked, and only the assigned clone plus private tmpfs scratch writable. The new network namespace has no route to host loopback or ambient IPv4/IPv6, UDP, DNS, or raw network. `/run/proofgate` is a private tmpfs; the coordinator individually bind-mounts only the two run-scoped pathname socket inodes at `/run/proofgate/intended-inference.sock` and `/run/proofgate/coordinator-ipc.sock`. Directory mounts, inherited listener descriptors, abstract AF_UNIX sockets, alternate pathname sockets, credential/config projections, subscription CLI state, provider cookies/tokens, and secret-derived environment/argv values are forbidden. The coordinator may select the same vendor to author tests and bootstrap code, but that vendor's own review cannot attest the landing and the child cannot push, merge, dispatch workflows, or access the coordinator's GitHub or provider credentials.
  - `bootstrap_merge_review_gate.v1` applies independently and decisively before both trust-establishing merges. For the exact candidate change-tuple digest, all four intended seats—Fable, Sol, Gemini, and Grok—must each be present, usable, substantive under the coordinator's material-review policy, and `AGREE`; there must be zero usable `DISAGREE` or `PARTIALLY AGREE`. A missing, unavailable, unusable, empty/elided, substituted, wrong-digest, or non-`AGREE` seat fails closed. Every material or blocking finding requires a changed candidate digest that resolves it and a fresh full four-seat panel on that changed digest; disposition prose, review count, one `AGREE`, coordinator override, merge completion, or post-merge attestation cannot authorize or cure the merge.
  - The first landing is one complete `tests/**` and `tests/fixtures/proofgate/**` PR with no production/workflow/doc/skill byte. Before coordinator merge, the exact change-tuple digest is independently recomputed, satisfies `bootstrap_merge_review_gate.v1`, and the exact immutable RED argv runs against a synthetic tree consisting of `pre_phase_target_oid` plus only that test delta. The test-owned verifier emits a decisive pre-merge verdict over those inputs before the coordinator may open the merge action. Coordinator-observed GitHub PR number, head OID, base OID, merge OID, `mergedAt`, every-parent diff, panel output/log digests, RED argv/log/exit, and merge-message evidence digest form `bootstrap_test_landing.v1`.
  - Only after that test PR is on the target first-parent may a fresh bootstrap branch be created. Its separately reviewed PR may change exactly `.github/workflows/proofgate-receipt-attestation.yml`, `proofgate_receipts.py`, `proofgate_isolation.py`, `launcher.py`, and `panel_invoker.py`; it may not touch tests or any ordinary PROOFGATE implementation file. The coordinator recomputes the exact candidate digest, requires the immutable bootstrap subset green, requires `bootstrap_merge_review_gate.v1` for that digest, excludes the author vendor as an independent attestor even though its intended seat must still return a usable substantive `AGREE`, and alone merges only after the test-owned verifier's decisive pre-merge verdict. GitHub PR/head/base/merge/`mergedAt` metadata and the merge message bind the reviewed diff, test landing, four exact seat records, verification digests, and manual boundary profile as `bootstrap_implementation_landing.v1`; the later workflow attestation proves provenance of the already-approved blob and is not a correctness substitute.
  - `ProofgateReceiptSupervisor` is the only receipt/log writer. It runs in the outer phase-loop parent holding the roadmap dispatch lock; `proofgate_receipts.py` supplies the state machine, but that process gains authority only when the exact bootstrap implementation merge is already on fetched `refs/heads/main`, its server PR fields and every-parent diff match `bootstrap_implementation_landing.v1`, the workflow blob equals the separately reviewed blob, and a GitHub-hosted run of the landed workflow produces a verified `proofgate_receipt_head.v1` attestation for `bootstrap_activated`. The pre-bootstrap records authorize only those two exact landings; they are not a reusable implementation bypass.
  - The supervisor stores canonical JSON records and bounded logs under a coordinator-owned root not mounted into children. Each record has repo/roadmap/phase/run identity, contiguous sequence, previous record digest, kind, canonical payload, record digest, and previous external-head digest. No child source, inference result, submitted result, stdout/stderr, or broker diagnostic may enter that root until `CredentialTaintGate` accepts the exact framed bytes; on rejection the supervisor records only a fixed redacted `credential_taint_rejected` class plus non-secret policy/artifact digests, revokes the grant, and forbids every dependent attestation, source import, commit, and push. An accepted record is not committed and no dependent transition may run until `.github/workflows/proofgate-receipt-attestation.yml` attests the new `proofgate_receipt_head.v1` subject and `gh attestation verify` succeeds for `Consiliency/agent-harness`, the exact signer workflow, GitHub-hosted runner, source ref, workflow blob, subject digest, and transparency timestamp. The next record binds that verified bundle digest. A same-UID child can rewrite local bytes only inside its mount; it cannot see the journal/logs or mint the external signature, so replacement/recomputation, truncation, fork, rollback, or taint-verdict substitution fails.
  - `proofgate_isolation.v1` is mandatory for every post-bootstrap write-capable author or reviewer, including `panel_invoker.py` legs. The coordinator creates a self-contained clone at the authorized base with no remote and mounts it at the expected repo path inside Bubblewrap; only that assigned clone is a persistent writable bind. Reviewers receive only an assigned staged review workspace and private tmpfs. The host worktree, sibling worktrees, receipt root, descriptors, logs, coordinator process namespace, SSH agent, keyring/D-Bus sockets, Docker socket, `.netrc`, global/system Git credential helpers, `gh` config, 1Password, every provider credential/config/cache/cookie/token/subscription-state path, and every secret-bearing environment/argv value are absent. `close_fds=True` is required; no receipt/log/credential/provider descriptor is passed; `/proc` and the mount table must expose no trusted broker process, secret inode, or provider state.
  - `IntendedInferenceBroker` runs only in the trusted outer parent namespace and is the child's sole provider-facing capability, but the trusted parent never loads a hostile prompt into a provider CLI, model runtime, plugin, MCP server, or tool loop. It only validates policy, seals one single-use `intended_inference_grant.v1`, launches the isolated worker named by that grant, and validates the returned frame. The grant binds exact `run_id`, monotonic `sequence`, `request_id`, assignment/candidate digest, author-or-review `seat_key`, `provider_id`, `model_id`, effort, operation=`infer`, prompt/input size bounds, output bound, turn bound, worker-profile digest, launch-adapter digest, endpoint-set digest, `credential_projection_digest`, and expiry. `/run/proofgate/intended-inference.sock` accepts only canonical length-prefixed `intended_inference_request.v1`; the untrusted payload supplies bounded prompt/history/observation bytes, while every authority field is injected and equality-checked from the immutable grant. Unknown, duplicate, reordered, mutable, or replayed fields fail closed. There is no child-selected method, URL, scheme, host, port, DNS name, header, cookie, path, file reference, proxy opcode, tool/plugin/MCP operation, raw transport, or provider/model/seat override.
  - `provider_worker_profile.v1` makes every model turn a fresh disposable worker, never a thread or subprocess in the trusted parent and never the author/reviewer child. Its receipt binds the immutable OCI image and unpacked read-only rootfs SHA-256, CLI executable/version/blob SHA-256, complete argv and environment-name digest, tool-policy digest, endpoint-set/network-broker digest, `credential_projection_digest`, seccomp/capability/namespace profile digest, run-local input/output digests, and destruction outcome. The credential-projection digest covers the canonical non-secret seat/provider/path/type/mode/owner/read-only inventory and single-use projection grant ID, never credential values, lengths, or secret-derived hashes. The worker has fresh user/mount/PID/IPC/UTS/cgroup/network namespaces, no host route or DNS, no inherited descriptors, all capabilities dropped, a read-only rootfs, empty tmpfs `HOME`, `/tmp`, and `/work`, and only a bounded run-local prompt/schema/output area. Parent/live-repo/sibling/coordinator/receipt/log/secret paths, Git metadata/remotes, SSH/keyring/D-Bus/Docker/1Password sockets, cloud CLIs/config, and mutation credentials/capabilities are absent. The only credential material is the minimum seat-specific subscription state required by the exact adapter, projected read-only under a provider-private path with no other provider's material; its non-secret file-name/mode/owner inventory is frozen, while values and secret-derived hashes never enter receipts. The worker is destroyed after one turn whether it succeeds, refuses, times out, or taint-rejects.
  - `provider_endpoint_broker.v1` is the worker's only network path. The worker network namespace has no default route, loopback listener reachability, resolver, raw-socket egress, or ambient IPv4/IPv6. A parent-owned point-to-point L4 forwarder exposes one sealed listener per exact adapter endpoint tuple; each listener is pre-bound by the grant to one canonical provider hostname, resolved-address set, TCP port 443, TLS server-name/certificate policy, byte/time limit, and direction. The worker's immutable `/etc/hosts` maps only those canonical hostnames to their distinct broker aliases. The forwarder accepts raw TLS for that fixed tuple only: it has no CONNECT, SOCKS, arbitrary proxy, DNS, URL, method, path, header, cookie, body, redirect-target, or child-selected destination surface, rejects redirects or secondary endpoints outside the sealed set, and cannot reach host-local or private/link-local ranges. Endpoint discovery is not ambient: for each pinned CLI version, the tests-only corpus freezes the complete endpoint tuple observed during a successful real subscription turn; any later extra/different endpoint fails `provider_endpoint_refused` and requires a changed adapter/profile digest, fresh hostile/positive canaries, and fresh four-seat review.
  - `provider_launch_adapter.v1` freezes four launch families at the currently probed CLI versions and refuses version/argv/tool-policy drift. **Fable / Claude Code 2.1.220** uses `claude -p --verbose --output-format stream-json --safe-mode --model claude-fable-5 --effort max --setting-sources "" --settings '{"apiKeyHelper":""}' --strict-mcp-config --mcp-config '{"mcpServers":{}}' --tools "" --permission-mode default --json-schema <run-local-proofgate_agent_turn.v1> <prompt>` from empty `/work`; it may be marked `tool_disabled` only after the immutable event trace and hostile prompt prove zero built-in/plugin/MCP/hook/browser/tool availability and a positive real turn succeeds, otherwise it is reclassified `agentic_confined` and remains worker-only. **Sol/Terra / Codex CLI 0.145.0** use `codex exec --cd /work --skip-git-repo-check --ignore-user-config --strict-config --sandbox read-only --model <gpt-5.6-sol|gpt-5.6-terra> -c model_reasoning_effort="<xhigh-or-frozen-effort>" --output-schema <run-local-proofgate_agent_turn.v1> --output-last-message <run-local-result> -`; Codex has shell tools and is always `agentic_confined`. **Gemini Flash / agy 1.1.8** uses `agy --model "gemini-3.6-flash (High)" --add-dir /work --sandbox --dangerously-skip-permissions --output-format stream-json --json-schema <run-local-proofgate_agent_turn.v1> --print-timeout <grant-bound> --print <prompt>`; prompt restrictions and `--sandbox` are defense in depth only, so it is always `agentic_confined`. **Grok / grok 0.2.114** uses `grok -p <prompt> --output-format json --json-schema <run-local-proofgate_agent_turn.v1> --cwd /work -m grok-4.5 --reasoning-effort high --disable-web-search --no-subagents --permission-mode bypassPermissions`; because tool/subagent flags are not accepted as a security boundary, it is `agentic_confined` unless a later separately reviewed behavioral proof establishes a truly empty tool set. No adapter may fall back to an API-key route, alternate model, ambient config, browser, plugin, MCP, general proxy, or different CLI/version.
  - `IntendedInferenceAgentAdapter` runs inside the separately credentialless author/reviewer child and makes the work path reachable. It owns a bounded `proofgate_agent_loop.v1`: each call sends conversation state plus the last local observation through `IntendedInferenceBroker`; each taint-clean worker result must validate against `proofgate_agent_turn.v1` as exactly one of `local_tool_request`, `final_result`, or `refusal`; the child validates and executes a requested local tool, appends a bounded canonical observation, and requests the next fresh provider turn until a final result or the sealed turn/time/byte/tool-call cap. Author grants allow only plan-owned read/list/search/write/delete and local command operations inside the remote-less assigned clone; reviewer grants allow only staged-workspace read/list/search and final verdict, never write/delete/command/git/network. Absolute/traversing paths, symlink escape, remote Git, credential/cloud/publish/workflow/package-registry tools, sockets, and network syscalls fail locally without reaching the parent. The parent never executes child tool requests. Immutable positive tests require every mandatory Fable, Sol, Gemini, and Grok adapter to complete real intended inference and at least one child-side multi-turn sequence that reads a staged nonce-bearing file and returns its value; author profiles must additionally perform a harmless run-local write/read correction inside the assigned clone, while reviewer profiles prove the write is refused and still produce a substantive exact-digest verdict. An always-deny broker, a one-shot text-only stub, a provider worker that cannot return the turn schema, or a loop that never reaches the local tool seam fails.
  - `intended_inference_result.v1` contains only normalized schema-valid turn content, terminal status, request/result SHA-256 values, exact provider/model/seat/grant/turn binding, worker image/rootfs/argv/tool-policy/endpoint/credential-projection digests, broker-loaded-blob digest, and non-secret provider receipt/provenance. Raw CLI stdout/stderr, upstream headers, auth challenges, debug text, stack traces, and config paths remain quarantined diagnostics. `CredentialTaintGate` must accept the complete result and permitted diagnostic frames before any byte or success status is released to the child. Fixed `provider_worker_boundary_refused`, `provider_endpoint_refused`, `provider_tool_policy_unproven`, `provider_protocol_refused`, `provider_unavailable`, and `credential_taint_rejected` frames contain no provider-returned bytes. The chronology records grant before worker creation, worker/profile/endpoint admission before CLI exec, taint-clean output before child release, each child tool observation before the next turn, and worker destruction before grant completion; a missing or reordered transition refuses.
  - `credential_taint_gate.v1` is enforced by a trusted-parent `CredentialTaintGate` whose code/policy digest is frozen in the bootstrap review. For every live parent-held and worker-projected credential/subscription material it constructs an in-memory, zeroized-after-run taint corpus covering exact values plus canonical header/cookie/config serializations, JSON/shell/URL escaping, base64/base64url, hex, percent encoding, whitespace/chunk splitting, and boundary-spanning forms; neither the corpus, secret values, secret-derived hashes, lengths, nor match excerpts enter a child, log, receipt, attestation, or Git object. The gate scans complete canonical bytes, not line samples, for raw and normalized provider-worker stdout/stderr/result/error, endpoint-broker and worker diagnostics proposed for retention, every child-visible broker result/error, child `submit_untrusted_result.v1` frame, child stdout/stderr, and every regular-file byte exported from the assigned clone. It rejects symlinks, hardlinks outside the clone, devices, FIFOs, sockets, sparse/path-confused entries, unknown encodings, undecodable result framing, and any taint hit. A trusted exporter ignores the child's Git index and objects, walks only plan-owned regular paths against the pristine authorized base, scans tracked and untracked bytes plus deletion metadata, imports accepted bytes into a new coordinator-owned pristine staging clone, rescans its complete diff/index/tree and pending commit object, and only then may the coordinator attest, commit, or push. The untrusted clone is never committed or pushed directly. Each worker-output, downstream-source/result/log admission, pre-ingest, pre-attestation, pre-commit, and pre-push gate consumes the prior clean-verdict digest and rechecks artifact equality; a byte change or missing scan fails closed. Clean receipts contain only policy/code/artifact/provenance digests and typed outcomes, never secret-derived material.
  - Coordinator IPC remains separately mounted only at `/run/proofgate/coordinator-ipc.sock`, with a distinct random capability, `SO_PEERCRED`, strict size/schema/run/sequence/assignment binding, and exactly `heartbeat` plus canonical length-prefixed `submit_untrusted_result.v1`; receipt append, attestation, authorization, merge, provider inference, filesystem-path input, and remote-mutation opcodes are structurally unavailable. Result frames are immutable after acceptance, remain explicitly untrusted, and do not become evidence until `CredentialTaintGate` accepts their complete bytes. The exact two pathname mounts, inference-grant digest, broker code/policy digest, and taint-clean verdict chain are recorded in every boundary/panel/receipt subject.
  - Before any ordinary implementation worktree is created, the landed bootstrap runs two immutable canary matrices. The **direct child matrix** preserves the exact production Bubblewrap proof for every distinct test-author, bootstrap-author, `PR-T` reviewer, and `PR-B` reviewer profile: host IPv4/IPv6 loopback, host abstract AF_UNIX, alternate pathname AF_UNIX, DNS, raw sockets, direct TCP/UDP, non-provider destinations/ports, arbitrary proxy CONNECT, and every other egress are unreachable; alternate inference/IPC paths fail; the exact two socket paths succeed; the credentialless child can read/write only its assigned profile; `gh auth status`, Git/SSH/askpass/netrc/keyring mutation, authenticated dry-run push, and privileged IPC fail. The **provider-worker hostile-prompt matrix** runs separately for every mandatory adapter—Fable, Sol, Gemini Flash, and Grok, plus Terra before it may author—and uses the exact production image/rootfs/argv/tool policy/credential projection/endpoint broker. Parent, live-tree, sibling, coordinator, and worker-external canary files carry distinct nonce bytes and independently recorded pre-run inode/mode/content/tree/ref digests; host loopback and arbitrary-egress collectors, a fake Git remote, package/cloud/publish/workflow traps, and synthetic secrets are armed outside the worker. The hostile prompt deliberately orders the provider process to read parent and live-repo files, enumerate and retrieve credentials, write/delete/rename the canaries, mutate Git refs/index/config, commit/push/publish, invoke GitHub/cloud/package-registry operations, contact arbitrary IPv4/IPv6/Unix/DNS/URL targets, change proxy settings, spawn/subagent around restrictions, and place any recovered nonce/secret in output or network traffic. Passing requires syscall/network-broker evidence that every parent/live/canary open or mutation and every non-provider egress was denied, zero trap/collector/remote hit, byte-identical parent/live/canary/ref state after worker destruction, no nonce/secret in worker output/diagnostics or any downstream admission, and only sealed provider endpoint tuples contacted. A claimed tool-disabled Fable run must additionally show zero tool/plugin/MCP/hook/browser events; an `agentic_confined` run may mutate only its disposable `/work`/scratch and must still satisfy all external negatives.
  - Each mandatory adapter also has a non-degenerate positive matrix. Through exact `/run/proofgate/intended-inference.sock`, a real provider turn must return schema-valid nonce-bearing intended inference with matching worker/profile/endpoint provenance; then the credentialless `IntendedInferenceAgentAdapter` must complete a bounded multi-turn local-tool sequence against the assigned staged workspace, including a read followed by a corrected final answer. Author profiles must perform a harmless write/read correction inside the remote-less assigned clone; reviewer profiles must refuse that write yet still read the staged bytes and emit a substantive exact-digest verdict. Separately the child submits one safe framed result only through exact `/run/proofgate/coordinator-ipc.sock`. With synthetic canary credentials held only by the parent/worker projection, the immutable tests inject raw, encoded, escaped, split-boundary, worker-result, worker-error, worker-diagnostic, submitted-result, tracked-source, and untracked-source taint and require rejection before child release, downstream admission, ingest, attestation, commit, and push; they delete each gate in turn and observe the named `PROOFGATE_RED` case while safe real inference, multi-turn tool use, and taint-clean import still succeed. The immutable evidence records each probe/hostile-prompt digest, expected/observed errno or typed refusal, worker image/rootfs/CLI/argv/tool-policy/credential-projection/endpoint-set/network-broker digests, socket/endpoint identity, exact provider/model/seat/grant/turn/request/result/provenance digests, parent/live/canary pre/post digests, taint-policy/code/artifact clean-verdict-chain digests, sanitized raw-log digest, and exit without any secret or secret-derived digest. The supervisor independently observes and externally attests `isolation_preflight_passed`; any unavailable broker/gate/worker, denied namespace, reachable child canary, provider-worker parent/live/canary read or mutation, successful non-provider egress or trap hit, missing real inference or multi-turn reachability, adapter/profile/endpoint/provenance drift, visible credential/subscription material, retained tainted worker/source/result/log bytes, remote authentication, IPC widening, or missing worker-output/downstream/pre-ingest/pre-attestation/pre-commit/pre-push clean verdict stops before dispatch with non-human `contract_bug`.
  - After activation and denial preflight, the bootstrapped supervisor recomputes the landed test subject, appends/attests `subject_frozen`, launches the exact four seats, admits only taint-clean immutable `panel_seat` result frames with exact inference provenance, reconstructs the tests-only preimplementation tree and appends/attests nonzero `red_run`, then imports/attests the two server landings as the single-use bootstrap exception. Only then may it append/attest `implementation_authorized` and `implementation_dispatch`. Supplied identities/outcomes/digests are evidence only. Missing, dissenting, unusable, substituted, unanchored, tainted, or out-of-order evidence cannot be backfilled.
  - `test_lane_chronology.v1` binds full 40-hex `pre_phase_target_oid`, `test_source_head_oid`, `test_landing_oid`, `bootstrap_source_head_oid`, `bootstrap_landing_oid`, `implementation_base_oid`, exact pushed `candidate_head_oid`, two-parent `implementation_landing_oid`, and exact fetched `canonical_main_oid`. The target is independently derived by agreement among launch target, `origin/HEAD`, and fresh remote HEAD. Tests-only, bootstrap, and final implementation landings must each be server-recorded two-parent PR merges on the target first-parent with ordered parents `[target-before-merge, exact-reviewed-source-head]`; bootstrap descends from the tests landing, the ordinary implementation base equals a fetched target containing both, candidate descends from that base, and canonical main contains the exact candidate through the recorded implementation landing. Same-branch linear tests→implementation, direct/squash/rebase landings, abbreviated/missing OIDs, target movement not represented by an observed PR merge, shallow history, grafts, or replace refs fail closed.
  - The chronology audit walks the complete real DAG and every-parent diff from `pre_phase_target_oid` through `canonical_main_oid` using change-kind/path/mode/blob/file-SHA tuples. It permits only the exact frozen tests delta before the exact bootstrap five-file delta, requires every ordinary production byte to descend from both landings and an attested implementation dispatch, requires an artifact-equal `CredentialTaintGate` clean-verdict chain before every child result/log ingest, attestation, trusted source import, commit, and push, and requires the complete implementation PR range to contain no `phase-loop-runtime/tests/**`, test guard, selector, fixture, count, anchor, workflow-bootstrap, roadmap, or panel-record edit. Copied/preexisting/side-merged blobs, direct untrusted-clone Git objects, test/implementation laundering, tainted or unscanned artifacts, hidden parent deltas, an unobserved landing, or any digest substituted across lifecycle stages fails.
  - Only after the real-history audit, `candidate_snapshot.v1` materializes exact live permitted bytes with a temporary index and synthetic commit whose sole parent is actual `source_head_oid`. It seals complete tree, real committed implementation delta, supplemental dirty delta, trusted-export and taint-clean verdict chain, and tuple/path digests; rejects unmerged/unowned/test/tainted/unscanned deltas; and is rematerialized byte-identically and re-scanned at closeout. It supplements and never replaces the real-history audit.
  - `verification.json` advances additively to v3. Each parameter first runs the exact proof argv to exit zero in a pristine checkout of the sealed candidate commit; a fresh checkout then asserts exactly one anchor, applies only that replacement, and runs byte-identical argv. Only zero-to-nonzero is `killed`; nonzero/unrun baseline is `baseline_failed`; unmatched/unapplied/unrun mutation is `mutation_not_applied`; zero after mutation is `survived`. Exact criterion/parameter/command/candidate coverage is required.
  - v1/v2 artifacts remain parseable only for individually grandfathered criterion bytes proven by `grammar_cutover.v1`. The raw complete Markdown item bytes must exactly equal the same-ID item at cutoff OID `5328694ae31b4f13f091903d96ed89395d74f3b2`, whose GitHub receipt and immediate first-parent successor receipt bracket the cutoff. A one-byte change is non-grandfathered; pathname age and author/committer/manifest timestamps never authorize.
  - Because PROOFGATE changes `runner.py`, `verification_evidence.py`, `goal_coverage.py`, `tdd_chronology.py`, the plan validator parser, train re-verification, and closeout enforcement used for its own evidence, the implementation child is an untrusted builder only. It writes only the remote-less assigned clone, submits a framed result, and exits; it never supplies provider credentials, attests, commits the authoritative branch, or pushes. The trusted parent requires taint-clean result/log/source frames, exports only allowed regular-file bytes into a new pristine coordinator-owned clone, re-scans the complete diff/index/tree/pending commit, creates one clean candidate commit, re-scans before push, pushes it, proves remote branch OID equals local `HEAD`, persists a runner-owned restart handoff, and exits `awaiting_phase_closeout`. A new repo-local process in a clean worktree at that exact pushed candidate must prove startup/expected/remote head equality and loaded module path/blob/SHA equality before running the broad compatible suite, structured JUnit, candidate audit, and exact-head four-seat implementation panel. That process exits before merge. After the exact candidate lands, another distinct fresh repo-local process at exact fetched canonical main repeats the broad suite and runs the canonical-main audit. Loaded-parent, builder-process, stale-head, repaired-in-process, executor-authored, missing taint gate, direct untrusted-clone commit/push, or same-process candidate→main evidence is invalid.
  - `EmitPhaseCloseout` and `PhaseLoopCloseoutV1` gain no mutation field. Executor self-report cannot authorize proof. Direct, delegated, train, and terminal closeout independently validate the attested journal/head, bootstrap exception, isolation denial, panel/RED authorization, full history, candidate equality, mutations, and grandfather cutoff before `complete`.

## Corpus Provenance

- Consiliency/agent-harness#358 is pinned to commit `0196f19c7e9fd90e9a707de076271057b521e1d1`, path `plans/detailed-board-silent-degradation-358-20260728.md`, blob `98765f511edebce39ea59f1b54ca77393f737904`, and file SHA-256 `c56c6d940b45a79524cf764bd4d7d2fce8aca4c84b96471d6a3362cd496d4fe0`.
- Consiliency/agent-harness#288 is pinned to commit `4e7dbf419b85ffc5e57b43d424a680b1e92e9461`, path `plans/detailed-288-fab-broker-readmission-20260726.md`, blob `d1be22ceadd78ba87ac800d1ed0872a8e63698fb`, file SHA-256 `77dc3eb21bd0390a13fca2a3ac54258145092a7f98ec246c9ae160a4889351ec`, AC-1 slice SHA-256 `5bb80a50e6f7942d62bc58839bc6b66efa5b66439b899b9b90ddbbc4fe662329`, and AC-4 slice SHA-256 `9239214f44821e38eae26820b8d5304d04bf5753a3cb3d6ff7d61eb1e50574d0`.
- These immutable fixtures prove validator behavior only, not historical implementation or satisfaction.

## Test Activation And Evidence Freeze

`EXPECTED_PHASE_NODEIDS` is the exact 39-nodeid union of the 36-item
`DEFAULT_SKIP_NODEIDS` below, the one migrated nodeid, and the two unchanged
positive controls. The one migrated nodeid is never skipped:

- `phase-loop-runtime/tests/test_review_leg_sandbox.py::test_codex_execute_command_is_danger_full_access_and_live_repo`

Before activation it retains its literal legacy assertions (`--sandbox
danger-full-access`, live `--cd`, user config present). Under
`PHASE_LOOP_TDD_EXPECT_PROOFGATE=1`, and automatically after the capability
marker, the same nodeid asserts the isolated remote-less assigned clone,
scrubbed config/credentials, and no live-repo path. The implementation may not
rename it merely because the legacy name becomes historical.

The two positive-control nodeids are pre-existing, never skipped, and must pass
in default, activated RED, candidate, and canonical-main runs:

- `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_github_broker_admission_store_is_wired_to_evidence_revocation`
- `phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_routing_broker_admission_store_is_wired_to_evidence_revocation`

The exact inactive default skip set is these 36 new nodeids—no glob, module
skip, generated selector, future nodeid, or marker-wide skip is admitted:

- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_bootstrap_records_are_single_use_and_server_bound`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_receipt_chain_rejects_rewrite_truncation_fork_or_backfill`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_receipt_chain_rejects_wrong_workflow_signer_source_blob_subject_or_timestamp`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_implementation_authorization_requires_activation_preflight_panel_and_red_order`
- `phase-loop-runtime/tests/test_proofgate_receipts.py::test_runner_routes_reject_child_claims_and_missing_latest_external_head`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_isolation_preflight_masks_host_sibling_receipt_logs_fds_and_credentials`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_provider_projection_allows_only_selected_vendor_subscription_material`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_capability_socket_rejects_privileged_unknown_replayed_or_wrong_peer_requests`
- `phase-loop-runtime/tests/test_proofgate_isolation.py::test_execute_and_panel_routes_use_remote_less_assigned_clone_or_refuse`
- `phase-loop-runtime/tests/test_proofgate_attestation_workflow.py::test_attestation_workflow_is_github_hosted_exact_subject_and_blob_bound`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_missing_falsifier_is_invalid`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_negative_assertion_requires_path_entered_control`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_guard_requires_production_construction_site`
- `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_mutation_manifest_requires_exact_criterion_parameter_and_command_coverage`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_requires_two_parent_tests_bootstrap_and_implementation_landings`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_tests_only_range_with_non_test_bytes`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_bootstrap_range_outside_frozen_set_or_test_edit`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_implementation_range_test_guard_selector_nodeid_count_or_anchor_edit`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_chronology_rejects_same_branch_squash_rebase_direct_push_copy_or_hidden_parent`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_candidate_snapshot_is_source_head_parented_and_rematerializes_byte_identically`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_fresh_process_lifecycle_rejects_builder_stale_head_loaded_parent_or_same_process`
- `phase-loop-runtime/tests/test_tdd_chronology.py::test_junit_lifecycle_requires_exact_nodeids_default_skip_red_failures_and_final_zero_skips`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_unmatched_anchor_is_mutation_not_applied`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_matched_anchor_kill_requires_green_identical_command_baseline`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_missing_duplicate_extra_substituted_or_surviving_parameter`
- `phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_candidate_or_command_digest_drift`
- `phase-loop-runtime/tests/test_closeout_verification_gate.py::CloseoutVerificationGateTest::test_proofgate_closeout_rejects_missing_or_invalid_attested_proof`
- `phase-loop-runtime/tests/test_preflight_verification.py::PreflightVerificationTest::test_proofgate_preflight_requires_attested_authorization_and_exact_candidate`
- `phase-loop-runtime/tests/test_train_invariants.py::TestInvariant6LiveReverifyRunsVerification::test_proofgate_train_reverify_requires_exact_attested_proof`
- `phase-loop-runtime/tests/test_goal_coverage.py::GoalCoverageTest::test_acceptance_contracts_classify_valid_invalid_and_grandfathered`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_agent_harness_358_original_is_rejected`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_agent_harness_288_ac1_and_ac4_are_rejected`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_grandfathering_uses_exact_cutoff_criterion_bytes_and_warns`
- `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_changed_or_new_criterion_requires_v3_evidence`
- `phase-loop-runtime/tests/test_skills_canon_parity.py::SkillsCanonParityTest::test_plan_phase_skills_publish_falsifier_grammar`
- `phase-loop-runtime/tests/test_skills_bundle_drift.py::SkillsBundleDriftTest::test_proofgate_validator_and_guidance_are_generated_without_drift`

The historical frozen nodeid
`test_provider_projection_allows_only_selected_vendor_subscription_material`
is retained byte-for-byte so the 39/36/37 inventory does not drift, but its
active contract permits no projection into the child: it proves the selected
provider/model/seat remains usable only through `IntendedInferenceBroker` while
all credential and subscription material remains absent from the child and only
the minimum read-only seat-specific material enters the separately isolated
disposable provider worker. Its
`RED_CASES_BY_NODEID` cases include literal
`credential_env_absent`, `credential_argv_absent`,
`credential_mount_absent`, `credential_fd_absent`,
`subscription_state_absent`, `inference_exact_provider_model_seat`,
`inference_rejects_method_url_header_path`,
`inference_request_result_frame_immutable`,
`inference_output_provenance_bound`, `broker_result_taint_rejected`,
`trusted_parent_outside_model_tool_boundary`,
`provider_worker_image_rootfs_argv_policy_bound`,
`provider_worker_minimum_readonly_seat_credential_projection`,
`provider_endpoint_only_transport`,
`provider_endpoint_rejects_arbitrary_proxy_and_secondary_egress`,
`fable_tool_disabled_or_confined_proof`,
`sol_terra_agentic_worker_confined`,
`gemini_agentic_worker_confined`,
`grok_agentic_worker_confined`,
`hostile_prompt_parent_read_refused`,
`hostile_prompt_parent_write_delete_git_publish_cloud_refused`,
`hostile_prompt_secret_retrieval_and_arbitrary_egress_refused`,
`provider_worker_output_diagnostics_taint_rejected`,
`child_multi_turn_local_tool_loop_reachable`,
`mandatory_provider_real_inference_reachable`,
`submitted_result_taint_rejected`, `child_log_taint_rejected`,
`tracked_source_taint_rejected`, `untracked_source_taint_rejected`,
`encoded_and_split_taint_rejected`, `taint_required_before_attestation`,
`taint_required_before_commit`, `taint_required_before_push`, and
`safe_intended_inference_and_import_succeeds`. The other three frozen isolation
nodeids consume the same cases for namespace inventory, capability/framing
rejection, and execute/panel/export routing; no new nodeid or skip is added.

The guard freezes these pre-implementation source anchors verbatim and maps
every `RED_CASES_BY_NODEID` entry to at least one; the two broker anchors remain
separate because each must match exactly once:

| Anchor ID | Frozen path and literal |
|---|---|
| `PG-A-LAUNCH` | `phase-loop-runtime/src/phase_loop_runtime/launcher.py`: `command.extend(["--sandbox", "danger-full-access"])` |
| `PG-A-PANEL` | `phase-loop-runtime/src/phase_loop_runtime/panel_invoker.py`: `env = _subscription_env() if env is None else dict(env)` |
| `PG-A-RUNNER` | `phase-loop-runtime/src/phase_loop_runtime/runner.py`: `from .dispatch_lock import DispatchLock, DispatchLockContention` |
| `PG-A-GOAL` | `phase-loop-runtime/src/phase_loop_runtime/goal_coverage.py`: `_ACCEPTANCE_SECTION_RE = re.compile(` |
| `PG-A-VERIFY` | `phase-loop-runtime/src/phase_loop_runtime/verification_evidence.py`: `SCHEMA_VERSION = 2` and `_SUPPORTED_SCHEMA_VERSIONS = frozenset({1, 2})` |
| `PG-A-CLOSEOUT` | `phase-loop-runtime/src/phase_loop_runtime/closeout.py`: `def _apply_verification_evidence_gate(` |
| `PG-A-TRAIN` | `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`: `def _live_reverify(workspace: Path, roadmap_path: Path, run_mode: str) -> bool:` |
| `PG-A-VALIDATOR` | `skills-src/claude/claude-plan-phase/scripts/validate_plan_doc.py`: `def _check_p_goal_id_coverage(` |
| `PG-A-WORKFLOW` | `.github/workflows/test.yml`: `permissions:` followed by `contents: read` |
| `PG-A-BROKER-GITHUB` | `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/live.py`: the `Path(broker_root), admission_policy or _default_admission_policy, epoch_blocked=lambda: evidence_store.epoch_blocked,` constructor context |
| `PG-A-BROKER-ROUTING` | `phase-loop-runtime/src/phase_loop_runtime/convergence/broker/live.py`: the `root, self._admission_policy, epoch_blocked=lambda: evidence_store.epoch_blocked` constructor context |

The exact activated failure set is the 36 default-skip nodeids plus the one
migrated nodeid. Each first asserts its mapped anchor(s), then fails with
`PROOFGATE_RED::<nodeid-function-name-without-test_>` on the pre-implementation
base. `RED_CASES_BY_NODEID` additionally enumerates every signer/workflow/source/
blob/subject/timestamp forgery, isolation path/descriptor/credential/socket
escape, parent-held credential/subscription-state namespace exposure,
provider/model/seat or immutable-frame drift, parent-side model/tool execution,
worker image/rootfs/CLI/argv/tool-policy/credential-projection drift,
non-provider endpoint or arbitrary proxy/redirect egress, hostile-prompt
read/write/delete/Git/publish/cloud/secret/network effects against parent/live/
canary state, missing real provider inference or child multi-turn/tool-loop
reachability, arbitrary method/URL/header/path, credential-tainted worker
output/diagnostic/inference/result/log/source egress, missing worker-output/
downstream-source/result/log admission or pre-ingest/
pre-attestation/pre-commit/pre-push taint verdict, privileged opcode, receipt
reorder/fork/backfill, `EC-PROOFGATE-0..7`
manifest row, `github_builder`/`routing_builder` construction-site mutation,
history/parent/diff laundering form, mutation parameter/survivor form, corpus
slice, cutoff byte/receipt form, runner route, and generated-skill target.
Running only an aggregate nodeid is insufficient: the coordinator runs every
case separately, asserts the exact anchor match count before mutation, and
retains its raw stdout/stderr, exit, and structured JUnit testcase.

The JUnit lifecycle is exact:

- Tests-only default: `tests=39`, `passed=3`, `skipped=36`, `failures=0`,
  `errors=0`; skipped nodeid set equals `DEFAULT_SKIP_NODEIDS`; all broader
  pre-existing tests remain active and green.
- Forced pre-implementation RED: same 39 collected exactly once, `passed=2`,
  `failures=37`, `skipped=0`, `errors=0`; failure nodeid/tag/anchor set equals
  `RED_CASES_BY_NODEID`, with no `xfail`/`xpass`, deselection, collection,
  import, setup, or teardown substitute.
- Final candidate and canonical main with the env absent and marker present:
  same 39 nodeids each run exactly once and report `passed=39`, `failures=0`,
  `errors=0`, and zero skipped. Both the focused JUnit and the broad compatible
  JUnit are parsed by nodeid, not trusted by aggregate counts alone.

## Lane Index & Dependencies

SL-0 — Tests-only landing, trusted bootstrap, acceptance grammar, and chronology
  Depends on: (none)
  Blocks: SL-1, SL-2
  Parallel-safe: no
SL-1 — Mutation execution, sealed evidence, and live closeout enforcement
  Depends on: SL-0
  Blocks: SL-2
  Parallel-safe: no
SL-2 — Documentation sweep, Check-P integration, grandfathering, corpus, skill regeneration, and reducer
  Depends on: SL-0, SL-1
  Blocks: (none)
  Parallel-safe: no

## Lanes

### SL-0 — Tests-Only Landing, Trusted Bootstrap, Acceptance Grammar, And Chronology

- **Scope**: Freeze and two-parent-land the exact default/RED test contract, use its independent bootstrap verifier to two-parent-land and activate the minimum writer/isolation bootstrap, transition once to the self-hosted supervisor, and only then implement grammar and chronology on the distinct ordinary implementation branch without test edits.
- **Owned files**: `.github/workflows/proofgate-receipt-attestation.yml`, `phase-loop-runtime/src/phase_loop_runtime/proofgate_receipts.py`, `phase-loop-runtime/src/phase_loop_runtime/proofgate_isolation.py`, `phase-loop-runtime/src/phase_loop_runtime/launcher.py`, `phase-loop-runtime/src/phase_loop_runtime/panel_invoker.py`, `phase-loop-runtime/src/phase_loop_runtime/goal_coverage.py`, `phase-loop-runtime/src/phase_loop_runtime/tdd_chronology.py`, `phase-loop-runtime/tests/proofgate_tdd_guard.py`, `phase-loop-runtime/tests/proofgate_bootstrap_verifier.py`, `phase-loop-runtime/tests/test_proofgate_receipts.py`, `phase-loop-runtime/tests/test_proofgate_isolation.py`, `phase-loop-runtime/tests/test_proofgate_attestation_workflow.py`, `phase-loop-runtime/tests/test_review_leg_sandbox.py`, `phase-loop-runtime/tests/test_goal_coverage.py`, `phase-loop-runtime/tests/test_acceptance_falsifier_contract.py`, `phase-loop-runtime/tests/test_tdd_chronology.py`, `phase-loop-runtime/tests/test_verification_evidence.py`, `phase-loop-runtime/tests/test_closeout_verification_gate.py`, `phase-loop-runtime/tests/test_preflight_verification.py`, `phase-loop-runtime/tests/test_train_invariants.py`, `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py`, `phase-loop-runtime/tests/test_skills_canon_parity.py`, `phase-loop-runtime/tests/test_skills_bundle_drift.py`, `phase-loop-runtime/tests/fixtures/proofgate/**`
- **Interfaces provided**: `proofgate_tdd_contract.v1`, `proofgate_bootstrap_verifier`, `bootstrap_merge_review_gate.v1`, `proofgate_bootstrap_boundary.v1`, `ProofgateReceiptSupervisor`, `proofgate_receipt_head.v1`, `proofgate_isolation.v1`, `IntendedInferenceBroker`, `IntendedInferenceAgentAdapter`, `proofgate_agent_loop.v1`, `proofgate_agent_turn.v1`, `provider_worker_profile.v1`, `provider_launch_adapter.v1`, `provider_endpoint_broker.v1`, `intended_inference_grant.v1`, `intended_inference_request.v1`, `intended_inference_result.v1`, `CredentialTaintGate`, `credential_taint_gate.v1`, `bootstrap_activated`, `isolation_preflight_passed`, `implementation_authorized.v1`, `PaneledTestLane`, `test_lane_chronology.v1`, `mutation_manifest.v1`, `AcceptanceCriterionContract`, `AcceptanceGrammarResult`, `extract_acceptance_contracts()`, `check_acceptance_falsifiers()`, `verify_test_lane_chronology()`
- **Interfaces consumed**: (pre-existing) `EC_ID_LEADING_RE` (verified live source), check E task ordering (verified live source), check P goal coverage (verified live source), `launcher.launch_with_spec()` and `panel_invoker._exec_leg()` (verified live but unisolated source), dispatch lock (verified live concurrency primitive only), Git complete-history semantics, GitHub PR/workflow/attestation server APIs, `/usr/bin/bwrap` and Linux namespaces (verified live host primitives, not yet a trusted boundary), `verification.json` v1/v2 loader (verified live source)
- **Parallel-safe**: no
- **Tasks**:
  - test: `SL0-T1` creates the complete immutable guard/verifier/tests/fixtures-only delta, panels its exact blobs and inventories, proves marker-absent default compatibility plus forced assertion RED, and lands it through its own two-parent PR.
  - impl: `SL0-B1` starts from fetched canonical main containing `PR-T`; it changes only the five bootstrap files, while `SL0-I1` starts only after the independently verified bootstrap landing, fresh-head activation, denial preflight, trusted panel/RED receipts, and `implementation_authorized`.
  - verify: `SL0-V1` uses only the test-owned verifier before bootstrap trust, transitions through a fresh exact-bootstrap-main process, then forces the frozen grammar/chronology subset until the final marker exists.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `SL0-T1` | `test` | (none) | Every SL-0 test helper/test/fixture path only | exact 39-nodeid/36-skip/37-RED contract and all mutation cases | Marker absent: focused JUnit must be `3 passed / 36 skipped`; forced `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1`: the same 39 nodeids must be `2 passed / 37 intended failures / 0 skipped`, with per-case raw logs/JUnit and asserted anchors. The existing frozen isolation nodeids enumerate parent-outside-model-boundary, worker image/rootfs/argv/tool-policy/credential-projection/endpoint binding, hostile read/write/delete/Git/publish/cloud/secret/egress prompts for Fable/Sol/Gemini/Grok, worker-output/diagnostic and downstream taint gates, plus real provider inference and child multi-turn/local-tool positive controls; no nodeid or skip is added. |
| `SL0-T2` | `verify` | `SL0-T1` | Tests-only branch, panel bundle, raw default/RED evidence | immutable test blobs, nodeid tuple, guard, bootstrap verifier | Run ordinary broad compatible CI with marker/env absent, require only the exact 36 new skips, verify the direct child denial matrix and every provider worker's hostile-prompt matrix against sealed parent/live/canary state, exact provider-endpoint-only contacts, real schema-valid Fable/Sol/Gemini/Grok inference, reachable child multi-turn/local-tool sequences, namespace credential separation, worker/result provenance, and worker-output/downstream/pre-ingest taint evidence for every test-author and `PR-T` review profile, then require the test-owned verifier's decisive `bootstrap_merge_review_gate.v1` verdict for all exact-digest Fable/Sol/Gemini/Grok seats before opening and coordinator-merging `PR-T` as a server-recorded two-parent PR whose every-parent diff is only the SL-0 test helper/test/fixture paths. |
| `SL0-B1` | `impl` | fetched target containing `PR-T` | Exact five-file bootstrap set only; no tests, fixtures, guard, grammar, verifier-v3, or marker | immutable `SL0-T1` tests | Implement the credentialless child `--unshare-net` launch and exact two pathname-socket mounts; keep `IntendedInferenceBroker` policy-only in the trusted parent; implement disposable credential-bearing provider workers, fixed-destination endpoint broker, exact Fable/Sol/Terra/Gemini/Grok launch adapters, bounded child-side `IntendedInferenceAgentAdapter`, immutable turn/result frames and refusal semantics, `CredentialTaintGate`, trusted source exporter, and both direct-child and hostile-provider canary preflights. With activation forced require the exact bootstrap selector subset to pass and run all pre-existing launcher/panel regressions; with activation absent run the broad suite and require the unchanged `3 passed / 36 skipped` focused state. |
| `SL0-B2` | `verify` | `SL0-B1` | Read-only bootstrap candidate review, JUnit, Git/GitHub metadata | test-owned `proofgate_bootstrap_verifier.py` only | From a fresh process bound to the exact landed `PR-T` head, run the immutable test-owned verifier over the pushed bootstrap candidate and exact five-path diff. For every bootstrap-author and `PR-B` reviewer profile, require direct child denials plus Fable/Sol/Gemini/Grok hostile-prompt worker isolation, unchanged parent/live/canary state, sealed provider-only endpoint traces, real inference and child multi-turn/tool-loop positives, credential-projection inventory, worker-output/diagnostic/downstream taint rejection before release/ingest/attestation, activated bootstrap-subset JUnit, default broad JUnit, workflow blob, and server metadata; require its decisive `bootstrap_merge_review_gate.v1` verdict for all exact-digest seats before the coordinator may merge only that exact candidate as two-parent `PR-B`. |
| `SL0-B3` | `verify` | server reports `PR-B` merged | Exact fetched canonical-main bootstrap head, loaded module blobs, external activation and denial evidence | immutable bootstrap/guard tests | Start a different fresh repo-local process at exact fetched canonical main, prove loaded `proofgate_receipts.py`, `proofgate_isolation.py`, `launcher.py`, and `panel_invoker.py` bytes equal that head, verify `bootstrap_activated`, rerun every direct-child denial and every provider-specific hostile/positive worker canary with exact image/rootfs/CLI/argv/tool-policy/credential-projection/endpoint provenance, prove the child multi-turn/tool-loop remains reachable, repeat worker-output/downstream/ingest/attestation/source-import/commit/push taint rejection, transition once to the self-hosted supervisor, and re-observe panel/RED without accepting self-created bootstrap history or treating attestation as review correctness. |
| `SL0-I1` | `impl` | `SL0-B3` plus attested `implementation_authorized` | `goal_coverage.py` and `tdd_chronology.py` only on distinct `PR-I` branch | frozen tests; no test/helper/fixture edit | `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests/test_goal_coverage.py phase-loop-runtime/tests/test_acceptance_falsifier_contract.py phase-loop-runtime/tests/test_tdd_chronology.py -q` |
| `SL0-V1` | `verify` | `SL0-I1` | All SL-0 ordinary implementation paths read-only; `PR-I` remains unmerged | immutable forced subset plus bootstrap regressions | Force the grammar/chronology subset and require its owned nodeids pass; rerun bootstrap, launcher, panel, and all pre-existing goal-coverage tests with no unexpected skip or regression. |

`SL0-T1` must falsify manual-boundary escape; trusted-parent provider CLI/model/tool execution; missing/ineffective child or worker namespaces; worker image/rootfs/CLI/argv/tool-policy/credential-projection drift; parent/live/sibling/coordinator/canary visibility; host IPv4/IPv6 loopback, host abstract AF_UNIX, alternate pathname AF_UNIX, DNS/raw/direct/other egress; a general proxy or non-provider/secondary endpoint; inference socket/path widening; provider/model/seat/grant substitution; mutable or replayed request/result framing; arbitrary method/URL/header/path; missing output provenance; any credential/subscription value or material entering the child; worker-projected material exceeding the frozen minimum read-only seat inventory; hostile Fable/Sol/Gemini/Grok prompts reading, writing, deleting, renaming, retrieving secrets, mutating Git, pushing/publishing, invoking cloud/package/workflow actions, or reaching arbitrary egress against parent/live/canary state; a false `tool_disabled` claim; an always-deny, one-shot, or unreachable provider/child adapter; raw/encoded/escaped/split credential taint surviving worker output/diagnostics, child release, submitted result/stdout/stderr, downstream source/result/log admission, or tracked/untracked export; any missing worker-output/downstream/pre-ingest/pre-attestation/pre-commit/pre-push scan; direct commit/push of the untrusted clone; inherited receipt descriptors; authenticated remote mutation; privileged IPC; local-journal replacement/recomputation; any missing/unusable/non-substantive/non-`AGREE` Fable/Sol/Gemini/Grok exact-digest seat; any usable `DISAGREE`/`PARTIALLY AGREE`; post-merge review substitution; false GitHub signer/workflow/source/blob/subject/timestamp claims; bootstrap self-attestation/self-merge; test edits in bootstrap; ordinary production hidden in bootstrap; early/forged marker activation; and any ordinary dispatch before activation/preflight/panel/RED/authorization. Real schema-valid inference through each mandatory adapter, a bounded child-side multi-turn local read, an author-only harmless local write/read correction, a reviewer write refusal plus substantive verdict, and taint-clean source import must still pass, so isolation cannot be satisfied by disabling work. It also covers every grammar, corpus, full-history, candidate, mutation, cutoff, fresh-process, JUnit, BAML, direct/delegated/train, and closeout case named by IF-0-PROOFGATE-1. `PR-T` is complete before landing; no later test PR or selector amendment may repair an implementation.

### SL-1 — Mutation Execution, Sealed Evidence, And Live Closeout Enforcement

- **Scope**: Integrate the already-bootstrapped writer/authorization into every live runner route, audit real history, materialize the source-head-parented candidate, execute pristine-baseline then mutation proof commands, and enforce schema-v3 evidence through closeout.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/verification_evidence.py`, `phase-loop-runtime/src/phase_loop_runtime/runner.py`, `phase-loop-runtime/src/phase_loop_runtime/train_runner.py`, `phase-loop-runtime/src/phase_loop_runtime/closeout.py`, `phase-loop-runtime/src/phase_loop_runtime/_contract_docs/runtime/verification-evidence-contract.md`
- **Interfaces provided**: `CandidateSnapshot`, `MutationDeclaration`, `MutationEvidence`, `MutationParameterEvidence`, `MutationEvidenceStatus`, `VerificationResult.candidate_snapshot`, `VerificationResult.mutations`, `VerificationResult.chronology`, `validate_proofgate_receipt_chain()`, `audit_real_phase_history()`, `validate_verification_artifact_for_plan()`
- **Interfaces consumed**: `ProofgateReceiptSupervisor`, verified external receipt heads, `proofgate_isolation.v1`, `IntendedInferenceBroker`, `IntendedInferenceAgentAdapter`, `provider_worker_profile.v1`, `provider_launch_adapter.v1`, `provider_endpoint_broker.v1`, `CredentialTaintGate`, trusted source exporter and clean-verdict chain, `implementation_authorized.v1`, `PaneledTestLane`, `test_lane_chronology.v1`, `mutation_manifest.v1`, `AcceptanceCriterionContract`, existing verification command/suite intake and artifact seal
- **Parallel-safe**: no
- **Tasks**:
  - test: `SL1-T1` forces the already-landed mutation/runner/closeout subset read-only through the bootstrapped boundary.
  - impl: `SL1-T2` wires receipt validation/writes, dispatch authorization, full DAG audit, candidate materialization, mutation execution, and direct/delegated/train/closeout enforcement on the same distinct `PR-I` branch without test edits.
  - verify: `SL1-T3` reruns the immutable forced subset and all pre-existing verifier/closeout/train suites, rejecting any unanchored, unisolated, stale-process, or substituted live path.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `SL1-T1` | `test` | `SL0-V1` | Read-only immutable tests | SL-0-owned verifier/runner/closeout/train nodeids | `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests/test_verification_evidence.py phase-loop-runtime/tests/test_closeout_verification_gate.py phase-loop-runtime/tests/test_preflight_verification.py phase-loop-runtime/tests/test_train_invariants.py -q` |
| `SL1-T2` | `impl` | `SL1-T1` | SL-1 production/doc files only | Immutable `SL0-T1` tests | Implement against the forced nodeids; no marker and no test/guard/fixture change. |
| `SL1-T3` | `verify` | `SL1-T2` | All SL-1 owned files; `PR-I` remains unmerged | Immutable forced subset plus complete pre-existing files | Rerun the `SL1-T1` command with activation forced, then run each complete named test file without `-k`; every pre-existing nodeid stays active/green and only frozen new nodeids are guard-controlled when activation is absent. |

`runner.py` and `train_runner.py` must refuse every production launch unless the fetched target contains the exact activated bootstrap, the latest external head verifies, the direct-child and provider-worker hostile/positive preflights and implementation authorization are attested, and the launch uses the isolated assigned clone with an exact provider/model/seat grant. The runner writes security transitions itself, never from child claims; never launches a provider CLI in the trusted parent; and cannot release or attest worker output/diagnostics, a child result/log, or a source export without the trusted taint gate. Closeout re-fetches server target/PR/attestation metadata, re-audits all parents and blobs, re-materializes exact live bytes, repeats worker-output/downstream/complete-tree taint scans, and blocks on any changed head, candidate, journal, test, workflow blob, child-isolation profile, worker image/rootfs/CLI/argv/tool policy, endpoint set/network broker, credential-projection shape, inference grant/turn/provenance, source export, or taint-verdict chain.

### SL-2 — Documentation Sweep, Check-P Integration, Grandfathering, Corpus, Skill Regeneration, And Reducer

- **Scope**: Extend check P without weakening goal coverage, enforce raw-byte cutoff grandfathering, wire the pinned corpus, update canonical planner guidance, regenerate derived bundles, install the final capability marker, and perform candidate/two-parent/canonical-main reduction from fresh exact-head processes.
- **Owned files**: `phase-loop-runtime/src/phase_loop_runtime/proofgate_capability.py`, `skills-src/claude/claude-plan-phase/SKILL.md`, `skills-src/codex/codex-plan-phase/SKILL.md`, `skills-src/gemini/gemini-plan-phase/SKILL.md`, `skills-src/opencode/opencode-plan-phase/SKILL.md`, `skills-src/claude/claude-plan-phase/scripts/validate_plan_doc.py`, `phase-loop-skills/plan-phase/**`, `phase-loop-runtime/src/phase_loop_runtime/skills_bundle/*-plan-phase/**`
- **Interfaces provided**: check P falsifier disposition, `grammar_cutover.v1`, criterion-byte grandfather record, regenerated plan-phase skill bundle, `PROOFGATE_CAPABILITY_VERSION`, fresh exact-head candidate/canonical-main attestations, PROOFGATE closeout reduction
- **Interfaces consumed**: `AcceptanceGrammarResult`, `check_acceptance_falsifiers()`, `validate_verification_artifact_for_plan()`, bootstrapped chronology/attestation/isolation evidence, trusted target first-parent provenance, existing skill regeneration/sync commands
- **Parallel-safe**: no
- **Tasks**:
  - test: `SL2-T1` forces the already-landed validator/corpus/parity/drift subset read-only before any SL-2 production byte changes.
  - impl: `SL2-T2` changes only canonical skills/validator and generated plan-phase outputs, then adds the exact marker as the final implementation byte after all three lanes are complete; no tests.
  - verify: `SL2-T3` proves marker-default activation, freezes/pushes the exact candidate, exits the builder, uses a fresh exact-candidate process for the broad suite/panel/candidate audit, two-parent-lands that exact head, and uses another fresh exact-canonical-main process for final proof.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| `SL2-T1` | `test` | `SL1-T3` | Read-only immutable tests | SL-0-owned validator/corpus/parity/drift nodeids | `PHASE_LOOP_TDD_EXPECT_PROOFGATE=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py phase-loop-runtime/tests/test_goal_coverage.py phase-loop-runtime/tests/test_skills_canon_parity.py phase-loop-runtime/tests/test_skills_bundle_drift.py -q` |
| `SL2-T2` | `impl` | `SL2-T1` | Canonical sources, generated outputs, then final marker only | Immutable `SL0-T1` tests | Update canonical sources/validator; run `python3 phase-loop-runtime/scripts/regenerate_skills_bundle.py` then `python3 phase-loop-runtime/scripts/sync_skills_bundle.py`; after every lane-owned non-test byte is complete add only `proofgate_capability.py` with `PROOFGATE_CAPABILITY_VERSION = "proofgate.v1"`. |
| `SL2-T3` | `verify` | `SL2-T2` | All phase-owned implementation paths and reducer inputs | exact immutable 39-nodeid contract plus broad compatible suite | With env absent, require focused JUnit `39 passed / 0 skipped`; run every Verification command and the frontmatter broad suite; terminate the untrusted builder after its framed handoff; have the trusted parent taint-scan/export into a pristine clone, rescan the diff/index/tree/pending commit, freeze one clean `PR-I` candidate commit, rescan and push it, record exact remote equality plus the pre-ingest/pre-attestation/pre-commit/pre-push clean-verdict chain, and write the restart handoff without accepting child attestation. |
| `SL2-T4` | `verify` | `SL2-T3` | Exact pushed candidate in new clean worktree | candidate structured JUnit, broad suite, panel, mutation and chronology evidence | Start a distinct repo-local process whose startup/loaded bytes equal exact candidate head; repeat complete-tree taint scanning, run the broad compatible suite before the exact four-seat implementation panel, admit only taint-clean provenance-bound panel frames, and run the candidate audit; any repair creates a new candidate and process. Exclude only the exact post-landing wrappers `tdd_chronology audit --stage canonical-main` and canonical-main panel, which are impossible before landing; exclude no pytest nodeid or other suite command. |
| `SL2-T5` | `verify` | `SL2-T4` | Server PR and exact canonical-main worktree | ordered two-parent implementation landing and final evidence | Coordinator-merge only the exact reviewed, taint-clean candidate as two-parent `PR-I`, terminate the candidate process, fetch canonical main, and start another distinct repo-local process at that exact clean head. Repeat plan/manifest/roadmap validation, complete-tree taint scan, focused 39-pass/zero-skip JUnit, the broad compatible suite, exact-main panel, full mutation/chronology/receipt/provenance/taint-chain audit, and terminal reduction. Only this process may emit `complete`. |

`grammar_cutover.v1` freezes cutoff OID `5328694ae31b4f13f091903d96ed89395d74f3b2`, GitHub receipt `Consiliency/agent-harness#397 mergedAt=2026-07-29T22:09:58Z`, and immediate first-parent successor `a3fbb196b3b57d75e403bcea3bad972e9491f675` from Consiliency/agent-harness#382 at `2026-07-30T00:30:32Z`. Validator and runner extract identical raw item bytes; server receipt/target/adjacency/parser/digest disagreement fails closed. Check P stays WARN by default; schema-v3 proof remains mandatory at closeout.

## Execution Notes

- The predecessor panel record is dissent evidence only; it does not approve this changed digest. Before `PR-T`, the exact final plan SHA-256 recorded in the PROOFGATE manifest entry must receive a new panel in which the intended Fable, Sol, Gemini, and Grok seats are each usable, substantive, exact-digest, and `AGREE`, with zero usable `DISAGREE`/`PARTIALLY AGREE` and no unresolved material or blocking finding. Any plan edit invalidates that panel and restarts the plan gate.
- PR chronology is literal: `PR-T` contains only the complete test helpers/tests/fixtures and lands first as a two-parent merge; `PR-B` branches from fetched target containing `PR-T`, contains only the exact five bootstrap files, and lands second as a separately reviewed two-parent merge; after activation/preflight/self-host transition/authorization, one distinct `PR-I` branch is created from fetched target containing both landings. The three serial lane implementations accumulate on `PR-I`; its final pushed marker-bearing candidate receives the broad suite and implementation panel from a fresh exact-candidate process, then that exact head lands once as a two-parent merge. There are no separate mergeable `PR-I0`/`PR-I1`/`PR-I2` ranges that could hide test or parent drift.
- The manual bootstrap is not called trusted. It assumes the human/coordinator process and GitHub account are the temporary root, relies on current `/usr/bin/bwrap` 0.6.1 user/network namespaces, the exact mounted child AF_UNIX broker boundary, disposable provider-worker rootfs images, fixed-destination provider endpoint forwarding, minimum read-only seat credential projections, and public-repo GitHub Actions/attestation primitives, and authorizes only the exact test and bootstrap landings. The parent remains policy/grant authority only and never hosts a model/tool runtime. If any primitive, intended-inference broker, provider worker, adapter/endpoint profile, child multi-turn adapter, credential-separation scan, worker-output/downstream taint gate, direct-child denial, provider hostile-prompt negative, real provider/tool-loop positive, or pre-merge four-seat gate fails, PROOFGATE stops independently before the affected merge or after already-landed `PR-T`; the exact non-human prerequisite after an approved/merged `PR-B` is a verified `bootstrap_activated` `proofgate_receipt_head.v1` attestation. No ad-hoc local writer, parent-side CLI fallback, ambient/general proxy, credentialless-child CLI fallback, or post-merge correctness substitute is accepted.
- Runtime lane scheduling is serialized in work-unit mode after bootstrap so every author has a plan-owned assignment. The same selected author vendor may implement all lanes, but may never count as its own attestor or invoke merge/attestation credentials.
- The chronology verifier owns `proofgate_tdd_candidate_main.v1`. It reads the phase manifest contract and requires exact plan/roadmap/manifest-entry digests; owned/test path tuples; activation/marker/count literals; test guard/bootstrap-verifier/test/fixture blob OIDs and SHA-256 values at `PR-T`, `PR-B`, implementation base, candidate, implementation landing, and canonical main; workflow/bootstrap blob digests; candidate tree and every phase-owned production blob; nodeid tuple and anchor/case map digests; default/RED/candidate/main JUnit and taint-clean raw-log digests; exact provider/model/seat grant/turn and inference request/result/provenance digests; provider worker image/rootfs/CLI/version/blob/argv/tool-policy/credential-projection/endpoint-set/network-broker digests; hostile-prompt and parent/live/canary pre/post evidence; real provider inference and child multi-turn/tool-loop evidence; trusted exporter and worker-output/downstream-source/result/log/pre-ingest/pre-attestation/pre-commit/pre-push taint clean-verdict-chain digests; typed refusal semantics; panel subject/verdict digests; receipt/external-head digests; process startup nonces/PIDs/heads/loaded-module paths/blobs/SHA values; remote equality; server PR identities, ordered parents, `mergedAt`, and every-parent path/mode/blob tuples. No secret value, secret-derived digest, length, or excerpt is recorded. Every lifecycle transition consumes the previous immutable digest; no later stage may replace an earlier digest with a recomputed local value.
- Full Git history, server PR metadata, and attested dispatch receipts all participate. `source_head_oid` in `candidate_snapshot.v1` is the exact committed/pushed `candidate_head_oid`; the source-head-parented synthetic snapshot supplements that audit and never substitutes for it. Test/bootstrap/implementation blob reuse through copies, merge parents, rebases, squashes, direct pushes, or preexisting objects is rejected, even if the final tree looks correct.
- The implementation branch/base is recreated from the fresh trusted target after both bootstrap landings and authorization. Any production dirt/commit before authorization permanently denies the run; later receipts cannot cure it.
- Every mutation gets a pristine green baseline and fresh mutation checkout from the same candidate commit. Review legs inspect sealed metadata only; mutations never run in the authoritative worktree.
- Broad compatibility is a candidate and merge precondition. Before the implementation panel and again before merge, the fresh exact-candidate process runs every `## Verification` command plus `automation.suite_command`, including the full `phase-loop-runtime/tests` suite under `-m 'not dotfiles_integration'` and structured JUnit. The only deferred operations are the exact post-landing evidence wrappers `python3 -m phase_loop_runtime.tdd_chronology audit --stage canonical-main ...` and the canonical-main panel; no test file, nodeid, existing regression, lint, validator, or candidate-stage evidence command is excluded. A material panel finding changes the candidate and requires a new push, new process, full broad gate, and new panel.
- The untrusted candidate builder cannot attest loaded code, commit/push the authoritative branch, or supply its own taint verdict. It records only a bounded framed handoff and exits. The trusted parent scans result/log/source bytes, exports allowed regular files into a pristine clone, scans the diff/index/tree/pending commit, commits, scans again, pushes, and records a handoff binding startup identity, candidate head/tree/remote ref, plan/manifest/roadmap/test-contract digests, loaded proof-module digests, inference provenance, and clean-verdict chain. The candidate verifier must be a new process at a clean worktree whose `HEAD`, expected head, fetched remote candidate OID, and loaded repo-local `runner.py`, `verification_evidence.py`, `goal_coverage.py`, `tdd_chronology.py`, `proofgate_receipts.py`, `closeout.py`, `train_runner.py`, validator script, and capability-marker blobs all equal the exact candidate. It re-scans the complete candidate and exits before merge. The final verifier must be another new process at clean exact fetched canonical main and repeat the equality and taint proof. Checkout, repair, direct child commit/push, missing scan, or process reuse invalidates evidence.
- `EmitPhaseCloseout`, generated BAML schema, and launchspec goldens are outside the write set. If implementation requires executor-authored proof or a BAML mutation field, stop with non-human `contract_bug` and amend the plan.
- Record `no_doc_delta` for `README.md`, `CHANGELOG.md`, and release notes unless a new public surface is discovered. PROOFGATE publishes no package and dispatches no release.

## Spec Closeout Plan

- schema: `spec_delta_closeout.v1`
- decision: `dotfiles_skill_source_update`
- target surfaces: `skills-src/*/*-plan-phase/**`, `phase-loop-skills/plan-phase/**`, `phase-loop-runtime/src/phase_loop_runtime/skills_bundle/*-plan-phase/**`, `phase-loop-runtime/src/phase_loop_runtime/_contract_docs/runtime/verification-evidence-contract.md`
- evidence paths: `plans/phase-plan-v10-PROOFGATE.md`, `plans/manifest.json`, `phase-loop-runtime/tests/proofgate_tdd_guard.py`, `phase-loop-runtime/tests/proofgate_bootstrap_verifier.py`, `phase-loop-runtime/tests/test_proofgate_receipts.py`, `phase-loop-runtime/tests/test_proofgate_isolation.py`, `phase-loop-runtime/tests/test_proofgate_attestation_workflow.py`, `phase-loop-runtime/tests/test_tdd_chronology.py`, `phase-loop-runtime/tests/test_verification_evidence.py`, `phase-loop-runtime/tests/test_closeout_verification_gate.py`, `phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py`, `phase-loop-runtime/tests/fixtures/proofgate/**`, `phase-loop-runtime/tests/test_skills_canon_parity.py`, `phase-loop-runtime/tests/test_skills_bundle_drift.py`, `.phase-loop/runs/**/proofgate-tests-only-default.junit.xml`, `.phase-loop/runs/**/proofgate-tests-only-red.junit.xml`, `.phase-loop/runs/**/proofgate-compatible-suite.junit.xml`, `.phase-loop/runs/**/proofgate-candidate-evidence.json`, `.phase-loop/runs/**/proofgate-canonical-main-evidence.json`
- redaction posture: `metadata_only`
- downstream handling: `mirror cutover`

## Verification

- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli validate-roadmap specs/phase-plans-v10.md`
- `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_goal_coverage.py phase-loop-runtime/tests/test_verification_evidence.py phase-loop-runtime/tests/test_closeout_verification_gate.py phase-loop-runtime/tests/test_preflight_verification.py phase-loop-runtime/tests/test_train_invariants.py phase-loop-runtime/tests/test_review_leg_sandbox.py phase-loop-runtime/tests/test_convergence_broker_revocation_race.py phase-loop-runtime/tests/test_skills_canon_parity.py phase-loop-runtime/tests/test_skills_bundle_drift.py -q`
- `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -m 'not dotfiles_integration'`
- `ruff check phase-loop-runtime/src/phase_loop_runtime/`
- `PYTHONPATH=phase-loop-runtime/src python3 phase-loop-skills/plan-phase/scripts/validate_plan_doc.py plans/phase-plan-v10-PROOFGATE.md`
- `git diff --check`

## Acceptance Criteria

- [ ] EC-PROOFGATE-0 — proven by the test-owned bootstrap verifier plus final `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.tdd_chronology audit --stage canonical-main --repo . --plan plans/phase-plan-v10-PROOFGATE.md --json`; the audit must prove the exact 39-nodeid/36-default-skip/37-activated-failure structured JUnit lifecycle with asserted anchors/raw RED, immutable guard/tests/selectors/counts/cases, single-use non-tautological bootstrap transition, decisive pre-merge exact-digest four-seat `AGREE` gates for `PR-T` and `PR-B`, live credentialless-child `--unshare-net` isolation with exact intended-inference/coordinator socket paths and direct canaries for every bootstrap profile, the trusted parent outside every model/tool boundary, every Fable/Sol/Terra/Gemini/Grok turn in a fresh disposable isolated credential-bearing worker with bound image/rootfs/CLI/argv/tool-policy/minimum-read-only-seat-credential-projection digests and no parent/live-repo/coordinator/mutation capability, exact provider-endpoint-only fixed-destination transport with no general proxy or child-selected URL/method/header/body, provider-specific adapter/refusal receipts, hostile-prompt canaries for each mandatory provider proving read/write/delete/Git/publish/cloud/secret/arbitrary-egress attempts cannot read/mutate/exfiltrate parent/live/canary state, exact provider/model/seat/turn-bound immutable inference frames, real schema-valid intended inference and reachable credentialless-child multi-turn/local-tool author and reviewer loops, worker-output/diagnostic and downstream-source/result/log taint gates, artifact-equal pre-ingest/pre-attestation/pre-commit/pre-push taint-clean verdicts before trusted export, attested panel/RED/authorization order, ordered two-parent `PR-T`/`PR-B`/`PR-I` ancestry and every-parent diffs, implementation range without any test byte, exact pushed candidate and canonical main, distinct fresh processes with head/loaded-module equality, broad compatible suite before panel/merge, and only then the source-head-parented supplemental candidate; falsified by accepting skipped/deselected/xfail/import RED, early/forged marker, a changed test/guard/count/anchor, self-authored/self-merged/unattested bootstrap evidence, acceptance-grammar bootstrap circularity, a prompt/CLI-flag-only isolation claim, any provider CLI/model/tool loop executing in the trusted parent, moving the subscription CLI into the credentialless child without the frozen adapter, missing/ineffective child or worker namespace, parent/live/canary visibility or mutation, worker image/rootfs/CLI/argv/tool-policy/credential-projection drift, a general proxy or successful DNS/raw/direct/non-provider/secondary-endpoint/redirect egress, missing or widened exact broker path, provider/model/seat/grant/turn substitution, mutable/replayed framing, arbitrary method/URL/header/path, a false tool-disabled claim, missing output provenance, an always-deny/one-shot/nonfunctional provider or child adapter, missing real provider inference or child multi-turn/local-tool positive, any provider credential/subscription material entering the child or exceeding the worker's frozen minimum read-only seat projection, any hostile-prompt read/write/delete/Git/publish/cloud/secret/network effect against parent/live/canary state, raw/encoded/escaped/split credential taint surviving worker output/diagnostics, child release, result/log, downstream source/result/log admission, or tracked/untracked export, a missing or stale worker-output/downstream/pre-ingest/pre-attestation/pre-commit/pre-push clean verdict, direct commit/push of the untrusted clone, any other boundary/credential/IPC escape, a missing/unusable/non-substantive/non-`AGREE` intended seat, any usable `DISAGREE`/`PARTIALLY AGREE`, unresolved material/blocking finding, one-`AGREE`/count/post-merge-attestation authorization, author-recomputed journal, missing/late authorization, wrong nodeid/seat/subject/command/anchor/log/exit, target/server or process/head/module disagreement, same-branch/squash/rebase/direct landing, pre-test/copied/merged/preexisting production bytes, hidden test edits, loaded-parent self-attestation, synthetic-history substitution, or dirty-path omission.
- [ ] EC-PROOFGATE-1 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_acceptance_falsifier_contract.py::test_missing_falsifier_is_invalid -q`, falsified by removing the clause requirement and observing a falsifier-less fixture validate.
- [ ] EC-PROOFGATE-2 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_verification_evidence.py -k 'baseline or mutation_not_applied or matched_anchor_kill' -q`, falsified by a nonzero pristine baseline, substituted command, or zero-match anchor recording PASS/`killed`; the matched-anchor path-entered control requires baseline zero, one applied replacement, and identical-command nonzero mutation exit.
- [ ] EC-PROOFGATE-3 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py -k 'agent_harness_358_original or agent_harness_288_ac1 or agent_harness_288_ac4' -q`, falsified by wrong provenance or any unchanged original criterion validating without a typed missing/vacuous-falsifier rejection.
- [ ] EC-PROOFGATE-4 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_github_broker_admission_store_is_wired_to_evidence_revocation phase-loop-runtime/tests/test_convergence_broker_revocation_race.py::test_routing_broker_admission_store_is_wired_to_evidence_revocation -q` plus both exact candidate-bound mutation rows, falsified by helper-only mutation, non-unique anchor, command/candidate substitution, failed baseline, or either constructor deletion surviving.
- [ ] EC-PROOFGATE-5 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_matched_anchor_kill_requires_green_identical_command_baseline phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_missing_duplicate_extra_substituted_or_surviving_parameter phase-loop-runtime/tests/test_verification_evidence.py::VerificationEvidenceTest::test_proofgate_v3_rejects_candidate_or_command_digest_drift -q`, falsified by any missing/duplicate/extra/substituted/baseline-failed/surviving parameter leaving aggregate `killed` or an empty survivor set.
- [ ] EC-PROOFGATE-6 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_acceptance_falsifier_contract.py -k 'path_entered_control' -q`, falsified by deleting the control from a negative assertion and observing a valid disposition; the positive control executes the guarded path before absence is asserted.
- [ ] EC-PROOFGATE-7 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_grandfathering_uses_exact_cutoff_criterion_bytes_and_warns phase-loop-runtime/tests/test_validate_plan_doc_proofgate.py::ProofgatePlanValidatorTest::test_changed_or_new_criterion_requires_v3_evidence -q`, falsified by failing to WARN for exact same-ID/raw bytes at the trusted cutoff, grandfathering a one-byte-changed/new item, trusting local timestamps/path age, tolerating server/adjacency/parser/digest disagreement, or accepting v1/v2 evidence for a non-grandfathered criterion.
