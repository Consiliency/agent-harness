# Two input-binding corrections to ordering R3

This immutable addendum addresses the two remaining Opus R3 findings only. R3 proposal SHA256 `fa4b7f3832b32bc499eecb1c86eff4fc17e1297710aa285268e73cebbb661786` and the advisory-provenance addendum remain unchanged. Production source is still frozen at `6eb8f507a2da2744db9fc60b79e2571a0ebffcb5`; no implementation approval or supplier review authorization is implied.

1. **Make the existing helper-entry revocation check authoritative.** Pass the actual freshly resolved `BrokerEvidenceStore(repository_broker_namespace(workspace))` from the runner's real-store gate as `evidence_store=` to `_fab_delta_readmit`. Retain the whole-train revocation barrier and fresh check immediately before each recovery. The helper's existing entry and post-grant `_check_readmission_revocation(evidence_store)` calls then both replay actual durable state rather than treating None as clear. If revocation appears between recovery and helper entry, entry refuses before scope-back, delta review, provenance capture or broker grant. An incompatible/unreadable store exception is caught as the existing typed `merge_halted` / `fab_readmit_failed` hold, preserving the latest durable admission as required by R3. No duplicate checker or authority writer is added.

   Test with the actual repository evidence store and its `OUTCOME_AMBIGUOUS_BLOCKED` record: revoke after the recovery callback but before helper entry; assert zero delta reviewer calls, zero grant/commit, zero train review/approval/merge, and no subsequent FAB mutation. Retain the earlier node-2 revoke-before-recovery variant and preserve earlier durable admissions. Revocation is repository-scoped; fixtures using separate nodes must distinguish separate repositories from siblings sharing one revoked namespace.

2. **Preserve complete admission input on real P3 live-read failure.** In the existing `live_pr_head_read_failed` blocked append in owned `train_runner.py`, carry `fab_run_id=rec.fab_run_id` and `merge_order=rec.merge_order` alongside the already preserved node, branch, head and PR URL. This closes the input loss before R3's complete-blocked-binding path: last-wins ledger folding must not turn a previously FAB-scoped admitted node into an apparently non-FAB node after a transient read error. Do not synthesize missing FAB scope for historical incomplete rows; branch-only blocked rows remain unadmitted.

   Test the actual `run_train` path with a FAB-bound node: first live-head read raises, inspect the blocked record for preserved FAB ID/order/head/PR, then resume. Assert the binding survives into packet preparation and actual real-store revocation/recovery/promotion gates remain applicable; a durable revocation on resume prevents merge. Include a positive unchanged-head resume control so a fixture-only terminal cannot mask the preserved routing. Preserve the existing typed live-read-failure terminal and zero new admission on the failed attempt.

All other R3 requirements, test scope, exact frozen negative-arm changes, no-fetch/no-extra-round rules, native/cache behavior and advisory-only provenance remain as written. Implement tests first and retain actual RED receipts after the focused plan reconciliation completes. The parent owns the bounded review-round exception, compatible vote reconciliation and subsequent implementation CR; this addendum grants no publication or merge authority to the worker.
