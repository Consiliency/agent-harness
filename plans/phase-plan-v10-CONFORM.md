---
phase_loop_plan_version: 1
phase: CONFORM
roadmap: specs/phase-plans-v10.md
roadmap_sha256: 040fe81fd36fd48486bb4d6d9550296a830789b5d7a94a9300d3d19ff31cfd2e
automation:
  suite_command: 'PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q -m "not dotfiles_integration"'
---

# CONFORM: Canonical Contract Conformance

## Context

CONFORM makes the outside-agent validator consume the canonical contract dialect it claims to pin. The canonical `.phase-loop/` state, handoff, and event ledger agree that CONFORM is `planned`; that state binds the earlier superseded plan digest `8c90f63a2a122b34808d182ac23244957aa61b346ee5bdaf1f0c8b361e0232bb`, and the exact-digest four-seat panels for `ddf6083004da1de66f58046adbc9945c3618f6b0795fc89a4676eb9b496862d1` and `dadf71fd3d1e757f76b0123c38c2e503d2aa5920b46ab6cc309dc0a5d1798fca` respectively found the heterogeneous-corpus and pre-implementation-anchor defects that this plan preserves as repaired. The exact four-seat panel for `f01994b3beb5bfe9b45af0cf6ee271b3bfca53b4e48bf73e190036f10e741901` then found one blocking chronology contradiction: SL-1/A2 demanded full `compatibility` and EC-CONFORM-0…8 before SL-2 could write the v7 disposition, handoff inventory, conformance docs, and CHANGELOG those checks consume. It also required a closed redaction/projection classification before tests freeze, an explicit EC-CONFORM-1 legacy-allow-list falsifier, exact-oracle Ruff proof, preservation of the read-only public exports, a fail-closed immutable-anchor re-plan route, frontmatter import proof, and an explicit docs-policy reversal. The exact four-seat panel for `460ab46a60f96f5adca220dae26dfd870ed4c5842418a2b6247d005720f1734d` found the residual form of that same chronology defect: A2 still required the broad `-k outside_agent` suite green on the pre-doc candidate even though B0 requires the frozen SL-2 documentation nodes in that suite red on the identical tree. This repair freezes a literal broad-minus-stale A2 selector, preserves the exact stale-documentation partition for B0, and reserves the broad green replay for final B2 after B1. The exact four-seat panel for `2718f8e157abbc89c11756107a4c7baff30040a03e003cb9008ea36fbfb1ca61` then found the remaining lifecycle contradiction: all four stale-documentation nodes were classified as `NEW_PRODUCTION` even though two frozen release-surface node IDs already exist and pass at the reviewed source head. This repair makes the stale set a frozen two-new/two-migrated partition while leaving the four IDs and A2/B0/B2 chronology unchanged. This repaired digest must clear a fresh exact-digest panel with Fable, GPT-5.6 Sol, Gemini, and Grok all usable and agreeing before any test work. The exact reviewed planning source head is `e13902708073ae9df3e44d8e7be57d6eb398ab44`; the outside-agent behavior baseline remains `1627e3fe51d34a9b8be46fa1d9718d300a606d3c`. Current `main`/`origin/main` is `648be2f68d6804ecdc4046bb7d4f5ee81a90c356`, whose intervening tracked changes from that behavior baseline are confined to the lane-IR parser and its test. Only this plan and its existing manifest entry may remain staged. Legacy `.codex/phase-loop/` state is compatibility-only.

The latest authoritative exact-digest CONFORM panel adds one blocker: the then-current EC-CONFORM-2 blanket ban on every submitter-supplied serialized value contradicted the plan's necessary normalized-ref and validated-digest channels, so the plan's closed-class tests could pass while the roadmap falsifier still fired. Literal non-projection would destroy required safe output. The roadmap therefore now prohibits forbidden, rejected, raw, or unclassified submitter-controlled values while permitting only explicitly closed and validated projections. The seven-row inventory below is the direct exhaustive binding of amended EC-CONFORM-2, not a waiver: every recursive input/output channel has exact one-of classification, every allowed projection has a positive control, every forbidden or rejected channel has a negative control, both construction and final serialization guards have independent mutations, and byte-equivalent stdout/output-file sinks are scanned. The roadmap rebind invalidates this and every other v10 root-plan digest; this repaired CONFORM digest must clear a fresh exact-digest panel before test work.

The latest immutable local-three panel at
`.phase-loop/reviews/v10-conform-plan-panel.json` has SHA-256
`3ef3139c4e2a4fd6bfe1796a7ab26f441c7c92f337daa0263668b1669cd8ad11`.
It reviewed predecessor plan SHA-256
`cf08ab677f3ee3c0d48b905be902c98ef81758fb884be17e87bf4db93cea821d`,
roadmap SHA-256 `1e8ea70ceae55d326cd84b092e1b9e879180d7b0e774140c3dd00e6ed63b7071`,
bundle SHA-256 `45b6a2b2dd33b6bd34795832619f314e116b2ed8c4ac8cc08841595ba263fa62`,
and instructions SHA-256
`f67adc74dee801094ad0e2dbe71d3a708a33f883780e6967e946862d10072356`;
GPT-5.6 Sol and Gemini returned usable `AGREE`, Grok returned usable `DISAGREE`,
and Fable was absent, so it authorizes nothing. Grok reported that the existing blanket no-copy
node passes before implementation and would reject the intended `_contract/` mirror, and that
wheel-only package-data prose does not prove sdist inclusion. A later live-source review proved
the rejection premise false: the current node scans nonexistent repository-root `src/` and
`tests/fixtures/` paths and therefore passes vacuously. This repair keeps the node as a dual-mode
`MIGRATED_EXISTING` member but requires SL-0 to correct its literal scan root to
`phase-loop-runtime/src/phase_loop_runtime`, prove the scan is nonempty, and plant a
non-enumerated canonical copy under that package tree as a positive control whose exact path must
be reported. It also requires the activated missing-mirror RED and final exact-enumeration
assertion, adds `MANIFEST.in` ownership, and adds one exact wheel/sdist/sdist-derived-wheel archive
node. The predecessor panel remains immutable historical dissent; the changed plan/roadmap pair
requires a fresh full four-seat panel before CONFORM-T0.

The newest immutable local-three review at
`.phase-loop/reviews/v10-conform-plan-panel.json` has file SHA-256
`755fd66590f5ba448c45046e277d9d19417c972f3f09c860bd33e143be3455b9`.
It reviewed predecessor plan SHA-256
`9fcefd70eb3cbffbcc603d8c741fa19d1c66a53815ee974e10ce306800463c2a`,
roadmap SHA-256 `4d652aaff71b484806ea6d1770c9475e0c1e8de90c39e5447c6fadb8d0fa2c6f`,
bundle SHA-256 `73d7c6fa996b7c94abaf79185ec7771ccf9859caabf5636ea0f96c8651ffcece`
(`125308` bytes), and instructions SHA-256
`f67adc74dee801094ad0e2dbe71d3a708a33f883780e6967e946862d10072356`.
Two legs returned usable `AGREE`; GPT-5.6 Sol returned usable `DISAGREE`, finding that the
archive-inclusion singleton digest printed in this plan was only 62 characters and could not
match the manifest. This immutable historical dissent authorizes nothing. The execution notes
now carry the recomputed 64-character LF-terminated nodeid SHA-256 exactly.

The current immutable local-three artifact at that path has SHA-256
`e5570fad8f0e66d808374e28ee707e931ff3d40ab368aec43758453dcf13cfe6`.
It reviewed plan SHA-256
`ca965313ca5829a0956694d70105a2d1c18a152203670ffab3783e7791eedf91`,
roadmap SHA-256 `158c9f28857ef1df02a6b8ca72aef93f3a8a2acc8e591ca6adc70dd53ddb854d`,
bundle SHA-256 `0974bc5de013cffbbdcbc6b203e1102eb4932d32fa75548eabdb8c8329518793`
(`128199` bytes), and instructions SHA-256
`f67adc74dee801094ad0e2dbe71d3a708a33f883780e6967e946862d10072356`;
all three available legs returned usable `AGREE`. It remains immutable
historical evidence only; the roadmap/digest rebind below requires review of
the changed bytes before execution.

At this repair, the overwriteable local panel path contained predecessor evidence SHA-256
`f6a5695f8de3f32aa05f8d037304892c18cd287234d1a966c485be3e427f08f5`. It reviewed CONFORM plan
SHA-256 `2060a7694dd95a441826a0cf77d0a273a6b840a2fc038251c18379117c505f57`, roadmap SHA-256
`a289891a6f6bf27e07e3c1a5260d25f813f90931404c47dd2efc487e0aa268ba`, bundle SHA-256
`9e8f9663b0679132aeabb63f99822b3a52e33356241144b7f2a7f6723c3945b3` (`130557` bytes), and
instructions SHA-256 `f67adc74dee801094ad0e2dbe71d3a708a33f883780e6967e946862d10072356`.
Grok 4.5 and Gemini 3.6 Flash returned usable `AGREE`, GPT-5.6 Sol returned usable `DISAGREE`, and
Fable was deferred, so the artifact authorizes nothing. Sol proved two blockers. First, roadmap
EC-CONFORM-8 named nonexistent canonical `expected_status` and `expected_blocker_codes` fields while
the pinned `Consiliency/spec@v0.2.1` manifest actually supplies `expected_valid` and
`expected_blocker_class`. Second, the closed EC-CONFORM-2 inventory omitted the positional
`submission_file` scalar consumed by both outside-agent CLI commands, including its current
unreadable-input path-derived `input_digest`. This repair corrects the roadmap vocabulary without
changing the pinned contract, makes canonical validity and blocker class the coupled suite oracle,
keeps the live blocker-code map explicitly test-owned and complete, and adds a distinct
`read_locator_only` input class whose raw path never serializes. The roadmap graph and 13-phase
representation remain unchanged; its corrected SHA-256 is
`e291c10d11bb29e612492891b36cc5e7c1d19704da13874f4fd84ec21f2eda8a`. The repaired plan digest is
unreviewed and requires a fresh exact-digest four-seat panel before CONFORM-T0.

The immutable exact-digest panel at
`.phase-loop/reviews/v10-conform-current-20260803T111542Z/panel.json` has SHA-256
`4f5e0c266b885d158274e639103334272dcf882ea128fe0b156bfbc21970667b` and reviewed plan SHA-256
`830077428ae71a31169d021df003b352377440d1727d1a81a02b4f34836946a0` against the current roadmap.
Grok 4.5 and Fable returned usable `AGREE`; Gemini returned transport-limited `PARTIALLY AGREE`;
GPT-5.6 Sol returned usable `DISAGREE`, so the panel authorizes nothing. Sol proved that the plan's
three-node migrated inventory omitted existing public API, CLI, fixture, and vector tests whose
legacy five-field submissions must currently pass but must be rejected by strict canonical
same-version validation. This repair freezes the complete 42-node dialect-migration inventory,
preserves each default legacy branch for the tests-only landing, selects canonical assertions under
activation and the marker, and forbids a same-version compatibility bypass. It also separates
meaningful migrated RED nodes from migrated canonical assertions that already pass, avoiding
artificial failures while retaining exact literal failure and anchor sets.

The next immutable exact-digest panel at
`.phase-loop/reviews/v10-conform-current-20260803T113812Z/panel.json` reviewed predecessor plan
SHA-256 `9d7f2a7250def6814ec351ae7ffe5810109c18494fee8d221bb6636640de9254`.
Grok and Gemini returned usable `AGREE`; Fable returned usable `PARTIALLY AGREE`; GPT-5.6 Sol
returned usable `DISAGREE`, so the panel authorizes nothing. Sol proved that the plan omitted the
roadmap-mandated early Codex `can_probe` evidence step from each unbound review wave. Fable proved
that the adopted no-copy node is vacuous at the reviewed source head because it scans nonexistent
roots. This repair restores the early-prover ordering and exact-bundle binding procedurally without
claiming REVIEWTRUTH's future typed receipts, and freezes the corrected literal scan root plus a
planted-copy positive control before the tests-only candidate is committed.

The exact-digest ratification for plan SHA-256
`13018029d8e3b381cba87c69b627c34bfd3063d610a389e7336eaf0354b5da78` is now historical.
During A2 validation at exact pre-doc candidate
`974593899bbecfbe092ba0aec369e69eee1aabdd`,
the candidate-shaped validation exposed four independent deterministic fixture defects recorded by
`Consiliency/agent-harness#470`: an
ambient `git log --all` walk could select a matching sibling commit outside the candidate's
ancestry; nested cross-mode evidence creation reused a later outer mode's work directory;
criterion-execution replay compared self-reported nested stdout/stderr digests that are expected
to change between executions; and the sdist-derived-wheel fixture passed a tar archive where the
installed `build==1.5.1` frontend requires an extracted source directory.
The amendment below changes the plan digest and is `unreviewed_not_approved` until a fresh ordered
Terra, Grok, Sol, Gemini, Opus, then Fable wave binds the replacement bytes.

CONFORM is no longer an executable root: it starts only from the exact two-parent LEGIBLE
landing, rechecks its owned-path/source anchors on that fetched base before CONFORM-T0, and
repairs/re-panels if LEGIBLE changed an assumed byte. It may execute concurrently with PROOFGATE,
the sole disjoint root-plan writer.

Main already contains the merged `spec@v0.2.1` pin, per-schema digests, and refreshed handoff from agent-harness#377 (merge commit `77edd4635d6c866f2e6f18f68ab45b7b70553c85`). Draft agent-harness#372 at head `53efaf66adb255f868e1abf5fbc57fd82b1e8109` remains open and is evidence only: no file, commit ordering, or claim from that branch is an authority input. Execution must sync current main, record that commit as the exact `<execution-base>`, form the final contract tests and their immutable source fixture as a literal test-paths-only first landing, panel that exact test commit, observe and retain discriminating RED against `<execution-base>`, and only then change packaged data or production code. The chronology verifier must prove the conformance production paths at `<execution-base>` have not changed from the reviewed `1627e3f…` behavior baseline or require another plan review before treating that earlier RED premise as applicable.

Literal default-`main` TDD feasibility is part of the chronology, not a runner convention. SL-0 freezes the test-owned `PHASE_LOOP_TDD_EXPECT_CONFORM=1` activation guard, exact node-ID sets and integer counts, and an exact production marker contract before its tests-only candidate is panelled. With the marker absent and activation unset, ordinary/default CONFORM tests-only CI is GREEN: only the frozen new production-dependent node IDs skip, every pre-existing outside-agent node remains active, and every test-only canonical fixture/oracle integrity node runs green. Within the four stale-documentation nodes, the two planned nodes are `NEW_PRODUCTION`; the two already-present release-surface nodes are `MIGRATED_EXISTING ⊆ PREEXISTING`, run their unchanged legacy assertions by default, and switch to the new pre-doc RED assertions only under explicit activation or the exact production marker. The existing `phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_no_copied_canonical_outside_agent_schema_or_vectors` is another named `MIGRATED_EXISTING` node with the same dual-mode lifecycle. The 42 exact legacy-dialect consumers listed below are also `MIGRATED_EXISTING`: their default branches preserve today's five-field payload and assertions, while activation and the production marker select canonical `v0.2.1` fixtures and assertions. With activation set against unchanged pre-implementation production, every node runs; the failure set is the literal `CONFORM_ACTIVATED_RED_NODE_IDS = CONFORM_NEW_PRODUCTION_NODE_IDS ∪ CONFORM_MIGRATED_RED_NODE_IDS`. Migrated canonical branches whose preserved behavior is already valid may remain green and are not forced into fake RED. There is no xfail and no collection/import failure. SL-1 installs `phase_loop_runtime.conformance.outside_agent_schema.CONFORM_V10_CAPABILITY_MARKER = "spec@v0.2.1:b862f977897a7b87c4419680a3e83735d4ff07b0"`; thereafter the same immutable canonical assertions run automatically with activation unset, including B0's four documentation nodes and the migrated no-copy node in A2. The implementation may not edit the tests, guard, selectors, node IDs, counts, assertion/entry anchors, or frozen command literals.

### Post-freeze evidence-fixture repair (`Consiliency/agent-harness#470`)

The preceding blanket test-edit prohibition has exactly one exception before the CONFORM implementation may merge. The exception owns exactly four paths through exactly seven localized logical hunks. The separately landed repair changes three test paths through six hunks. In `phase-loop-runtime/tests/test_outside_agent_conform_evidence.py`, `_capture_immutable_lifecycle()` replaces ambient, path-simplified `git log --all -- <paths>` selection with the complete unfiltered commit walk from the exact resolved `candidate_commit` (equivalent to `git rev-list <candidate_commit>` with no pathspec). It evaluates the unchanged all-path HEAD-blob equality predicate over that complete walk and every candidate's ordered parents, then requires exactly one matching introduction boundary: the boundary matches all HEAD blobs, has a nonempty parent vector, and none of its parents match all HEAD blobs; every other matching commit must descend from that boundary. Zero or multiple introduction boundaries, a parallel matching history, a missing parent object, or a boundary that is not an ancestor of `candidate_commit` fails closed before archive creation. The selected boundary remains the archive source, and the explicit ancestry assertion remains mandatory. `records_for(mode)` allocates a deterministic per-mode invocation directory through a test-local counter and a leaf such as `root/<mode>/invocation-000` created with parents. `_assert_captured_observable()` preserves every raw command/input/cwd/environment/result/exit/status/anchor and outer stdout/stderr check; only for `criterion-execution` replay comparison, it first requires captured and rerun nested `execution.stdout_sha256` and `execution.stderr_sha256` values to be nonzero 64-hex digests, replaces exactly those four comparison values with sentinels, and compares every other rendered field exactly. Mutation-execution raw digest and full-observable equality remain unchanged. The same existing `test_mutation_definitions_are_frozen_but_not_executed_preimplementation` node gains one opt-in chronology-forgery subcase and no new node ID. In `phase-loop-runtime/tests/test_outside_agent_contract_drift.py`, `test_sdist_and_wheel_include_only_digest_enumerated_contract_mirror` safely extracts the generated sdist using relative/no-`..`/file-or-directory member checks, requires exactly one top-level source directory, and passes that directory to the unchanged derived-wheel build. Direct-wheel, sdist, and derived-wheel member equality, all 15 pinned digests, `VENDOR.json`, and installed API/CLI/vector checks for both wheels remain mandatory. In `phase-loop-runtime/tests/_outside_agent_canonical.py`, the sole added repair hunk rebinds `SEALED_RELEASE_FINAL_PARENT_BLOBS["CHANGELOG.md"]` from `d57713c07468aba942c31744d0a272dd73771f2f` to the post-`Consiliency/agent-harness#468` parent `0da6158723da5324086ed064a83fce97b90a694b`; additions to the existing forged-topology path inventories describe that same hunk and are not new behavior. The four existing non-ancestry repair hunks remain byte-for-byte unchanged; the ancestry-selection hunk is replaced as specified above without changing the six-hunk repair budget. After the test repair lands, the implementation series may add exactly one chronology-only implementation hunk in `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_conform_evidence.py::_validate_chronology()`, with Git helpers local to that surface.

The ancestry-selection hunk must not hardcode a candidate or selection commit, enumerate ambient branches/remotes/reflogs, use a pathspec or `--first-parent`, mutate refs, or fall back when no matching ancestor exists. This selector prohibition does not forbid exact historical provenance OIDs already frozen by the topology/range-diff fixture and plan; those values are evidence inputs, never candidate-selection substitutes. The repair must not use `exist_ok=True`, cleanup/reuse, UUIDs, randomness, timestamps, PIDs, or shared cross-test counters. It must not refresh the captured output digest, drop digest fields, skip replay, reduce replay to exit/classification, broadly erase hashes or paths, weaken mutation replay, pin or downgrade `build`, change Gate prerequisites, rebuild the derived wheel from `runtime_root`, reuse the direct wheel, skip the derived build, extract unsafely, or weaken archive byte/member/installed-behavior assertions. It preserves every mutation definition, schema, record ID, evidence mode, node ID, count, selector outside the exact ancestry-selection replacement, existing command literal, production import, and expected outcome; the exact opt-in chronology command below is the sole added command literal. No production or documentation path may change in the repair candidate.

The REDs bind exact pre-repair commit `974593899bbecfbe092ba0aec369e69eee1aabdd`, tree `f58958d6b85983f9b7b4e337dd801be5fa64fcca`, evidence-helper blob `e5044f381c556226d76586c6fa824f2485590dc3`, evidence-helper SHA-256 `903ab2a0138bffc5a3d534194822476bd3bb3dcb046a5905d06750d0bbe7554a`, contract-drift blob `2c52aea5952662775d3ad2296b930362c0ecee88`, contract-drift SHA-256 `4ccc4df8063d9de639da63094cd7b39723add91e26ba2b32a1ef4e3d01921486`, and test-tree ID `f8907dfdc182ac18c3928d58cab0af24604e2663`. The shared-ref RED proves that ambient history selected sibling `87d64db81d65f73bd538331548391087e001f6a0` and failed its ancestry postcondition; its raw-log and JUnit digests are recorded separately from the isolated single-branch `FileExistsError` RED. Candidate-shaped Gate A v6 at exact composition `465bafec3e5811928fb7686b4a50d1c174a72792`, parent `6c2e8042c23f281691709956bdd0403a7c5a1eb0`, and raw-log SHA-256 `e68b0fbbc51656f511767f4bb3350cea9d5bad4ee59b707f5d57a1b0a5a8c776` separately records the criterion replay digest mismatch and tarball-is-not-a-directory failure. GREEN proves both exact nodes independently and together, the unchanged outside-agent broad suite, the sibling-ref and single-branch topologies, and final Gate A composition with the separate `Consiliency/agent-harness#468` merge commit `ba9407f041acfb0a01689ce88e5059f59bc18c50`. It also replays the marker-absent GREEN and activated pre-implementation RED lifecycle unchanged.

The attempted pre-document replay at exact head `61f73393f5db6591d20b9cf3450eb71af63ddb4a`, tree `482be26224cd1b075e83c8aa2606fd0693ff5392`, is a failed non-authorizing causal-validation attempt. Its exact chronology command failed after `1418.40s` with `CONFORM_RED::partial_sl2_compatibility_transition`, vector `[True,False,False,False]`, and raw-log SHA-256 `8531ba13171f835411ad37501446aca01bd1fdd133bbafda746424e90b0e6009` because only the sealed `CHANGELOG.md` parent had moved. This is neither the product RED nor a successful implementation rebase. After the replacement repair landing, the ordinary chronology command must be GREEN at the pre-document candidate; the opt-in forged-topology command must remain RED only with `CONFORM_RED::chronology_accepts_forged_git_topology` until the sole production chronology hunk turns both commands GREEN.

The original implementation commits through the `9745938` equivalent map `=` and remain ordered. The chronology-verifier commit is the sole insertion before the documentation commit. Original `80d9a14` maps to exactly one final documentation commit and may be the sole `!` range-diff row, only because its `CHANGELOG.md` parent moved from `d57713c07468aba942c31744d0a272dd73771f2f` to `0da6158723da5324086ed064a83fce97b90a694b`. The four-path documentation replay preserves the complete `Consiliency/agent-harness#468` clean-room section and its CI wording, never restores `d57713c…`, and produces final blobs `300127fb4cfddb3e1333d91b3e19b5624be77ab7` for `CHANGELOG.md`, `014423efead1baef42ba3c0c047acf4e03659f10` for `docs/outside-agent-conformance.md`, `6239dd4f9a50378ac32c3ed3ff194c195349e300` for `docs/releases/outside-agent-release-handoff.md`, and `6700a05c82c06fb6960497518dd051bf5b0b1a3b` for `specs/phase-plans-v7.md`.

The chronology-forgery subcase runs only under `PHASE_LOOP_CONFORM_CHRONOLOGY_PROOF=1` with exact command `PHASE_LOOP_CONFORM_CHRONOLOGY_PROOF=1 PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest -q phase-loop-runtime/tests/test_outside_agent_conform_evidence.py::test_mutation_definitions_are_frozen_but_not_executed_preimplementation`. At the rebased equivalent of `974593899bbecfbe092ba0aec369e69eee1aabdd`, before the chronology implementation commit, it supplies resolvable Git identities but aliases `<repair-landing>` to single-parent `<repair-candidate>` while claiming `repair_landing_two_parent=true`, `repair_diff_exact=true`, `single_rebase=true`, and `range_diff_equivalent=true`. The unchanged verifier must accept that forged record and make the test fail with `CONFORM_RED::chronology_accepts_forged_git_topology`. Marker-absent and activated lifecycle outcomes remain unchanged. The same exact command must turn GREEN only after the dedicated production hunk independently rejects the forged topology.

The existing `chronology` fact gains one closed-version `git_proof` object. It contains full lowercase commit/tree OIDs only, never ref names; the exact tests, repair, implementation, and canonical-main landing identities; exact ordered parent vectors; the three test-path/six-hunk repair paths, before/after blobs, zero-context patch, and patch digest; the one verifier-path implementation patch; one pre/post rebase transition; original and rebased commit vectors; and digest-bound RED/GREEN `{path,sha256}` JUnit and raw-log references. `_validate_chronology()` derives the repository from its loaded module, requires that root to equal the resolved Git top-level, and requires a clean worktree whose `HEAD` and tree equal the existing bindings. It recomputes commit/tree existence, ordered ancestry, exact ordered parents `[<test-parent>,<test-candidate>]`, `[<repair-parent>,<repair-candidate>]`, and `[<implementation-parent>,<final-candidate>]`, the repair candidate's sole parent and exact patch, the chronology implementation commit's sole path and authorized verifier-only patch, and the bound RED/GREEN node, command, commit, tree, exit, JUnit, raw-log, and digest facts. It generates `git range-diff` internally and requires the first four original implementation commits to map unchanged and in order, exactly one added chronology-verifier commit between the rebased `9745938` and `80d9a14` equivalents, and only the documentation commit to map `!` under the exact changelog-parent exception above. Replacement objects, grafts, shallow boundaries, ambient `GIT_*` overrides, lazy fetching, missing objects, duplicate transition records, malformed evidence, digest drift, changed/dropped/reordered/extra commits, and caller-supplied topology truth all fail closed.

The amendment lands first, followed by the one-commit, three-test-path, exactly-six-hunk repair candidate and its separate two-parent repair landing. Every materially changed authorization bundle requires the current ordered topology: Terra early `can_probe`; Grok, GPT-5.6 Sol, and Gemini critics; Opus read/write evidence-staging prover; then Fable president. All outputs must be usable, substantive, digest-bound `AGREE`. After the repair landing, the still-unmerged `Consiliency/agent-harness#467` sequence may be rebased exactly once onto canonical main containing the repair while preserving logical order and production semantics. After replaying the original `9745938` equivalent, the executor retains the dedicated forged-topology RED, adds the one-path chronology-verifier implementation commit, and then replays the original `80d9a14` documentation commit under the exact parent-rebind rule above. The internally generated `git range-diff` and closed `git_proof` bind the original and rebased series. Squash, commit reordering, incorporating the repair into an implementation commit, further rebases, or semantic drift remain forbidden. After the repair landing, test paths are immutable through the rebased pre-doc candidate, final candidate, implementation landing, and exact canonical-main audit.

The chronology names `<test-parent>`, pushed `<test-candidate>`, two-parent `<test-landing>`, `<repair-parent>`, pushed `<repair-candidate>`, two-parent `<repair-landing>`, pushed rebased pre-doc `<candidate-commit>`, pushed post-doc `<final-candidate>`, `<implementation-parent>`, two-parent `<implementation-landing>`, and final `<canonical-main-head>`. The tests-only, repair, and implementation landings must each be non-fast-forward two-parent merges whose first parent is the exact canonical-main tip recorded immediately before landing. Because SL-1 modifies `outside_agent_conform_evidence.py` and uses it for same-phase evidence, every candidate, parent, and final proof runs in a fresh process after exact-commit and clean-tree checks; no controlling process may preload the modified verifier/parser/runner surface. Final acceptance is recomputed from a fresh process at exact `<canonical-main-head>`, never from a loaded parent or candidate-authored pass boolean.

The final `<canonical-main-head>` audited after the implementation landing is `M`, and `M` is CONFORM's dependency-completion identity for REVIEWTRUTH. From a fresh clean process at exact `M`, the executor repeats the complete B2 verification and stages the resulting final-audit, final-evidence, exact-source, and fresh early-prover evidence bundle by reference. For this merge/release-class review, the confined Codex `can_probe` leg runs first and its digest-bound evidence is present before any critic starts; Grok, GPT-5.6 Sol, and Gemini then review those exact bytes; Opus runs as the read/write evidence-staging prover after the critics; Fable starts only after the complete four-leg board and acts as the sole binding president. Any finding, contradiction, `PARTIALLY AGREE`, or `DISAGREE` invalidates the affected review attempt and requires a changed evidence bundle and a fresh ordered wave.

The shared ratification policy remains parameterized. The maintainer-ratified defaults are `required_vendors=3`, `required_lens_coverage=3`, the gate-specific configured `required_consensus`, `required_prover=true`, and the gate-specific `on_shortfall`. At reviewed head `M`, the typed runtime does not yet represent `required_prover` or the corrected 3/3 defaults, so the executor must record that the Fable president and current-v10 six-output AGREE requirements come procedurally from the retained `agent-harness#398` and `agent-harness#442` directives rather than claiming they were resolved by shipped code. A valid non-current `required_prover=false` fixture remains supported conceptually and does not weaken this phase's current-v10 completion rule. `PANEL_LEGS` and non-review goldens remain unchanged.

CONFORM does not define or require REVIEWTRUTH's future typed review-wave receipt, per-producer record schemas, isolated worktree executor, external reaper, or manifest-only `FH/F` closeout topology. Those runtime mechanisms belong to downstream REVIEWTRUTH, which depends on CONFORM. Until they land, the coordinator preserves current runner-owned panel artifacts, early-prover artifacts, and source digests and enforces the current-v10 procedure on every unbound review wave. Plan/design and tests-only waves may collect critics first, but a serial Codex `can_probe` leg and its digest-bound evidence must be staged into the same exact bundle before Fable starts; any contradiction invalidates affected earlier verdicts and requires re-review of the updated bundle. Pre-merge and completion waves run the Codex leg first, stage its evidence before any critic, then run Grok/Sol/Gemini, and start Fable last. Every subsequent wave repeats the sequence; no verdict is carried forward. The Codex leg is evidence-only and never satisfies `binding_prover`; Fable alone does. The coordinator records completion through the existing phase-loop lifecycle only after the exact-`M` proof and review gate pass.

The bounded Option-2 early-prover exception is procedural and fail-closed. It may use `--sandbox workspace-write` only in a coordinator-created ephemeral per-leg worktree under `/mnt/workspace/worktrees`, with one serial holder, isolated fixture-only data scope, an external reaper, exact head/tree/bundle digest binding, approval `never`, no network or extra writable roots, and strict ephemeral configuration with user config, rules, MCP, plugins, hooks, browser, remote control, other agents, and dynamic tools disabled. Preflight must prove those facts before launch. If Codex preflight cannot prove them, the wave records a zero-Codex-launch receipt and may use only the roadmap-confined Grok fallback; if that confinement is also unavailable, it records degraded evidence without launching a write-capable prover. Gemini is never write-capable. Neither degraded path authorizes Fable to ignore missing evidence or contradictions.

The only canonical producer input is the merged annotated tag `Consiliency/spec@v0.2.1`, dereferenced commit `b862f977897a7b87c4419680a3e83735d4ff07b0`. The live tag contains exactly two schemas, one 11-row manifest, eleven vector payloads, and the normative test-only oracle `consiliency_spec/outside_agent_router.py`. The corpus is heterogeneous by `schema_target`: three valid and seven invalid rows target `outside_agent_submission.v0.1`, while the sole remaining invalid row, `negative-unsupported-verdict`, targets `outside_agent_route_verdict.v0.1`. Thus the overall role counts remain exactly three valid and eight invalid, partitioned as 3 valid submissions + 7 invalid submissions + 1 invalid route verdict. The packaged `_contract/` surface is a byte-locked consumer mirror; Consiliency/spec retains schema, corpus, verdict, and oracle authority. Governed-pipeline fixture alignment remains owned by governed-pipeline#128 and is not claimed here. Canonical `expected_valid` and `expected_blocker_class` values are consumed directly from every manifest row by the mirrored test-only router oracle. Production must not import or emit that oracle class as an agent-harness routing field; agent-harness#379 remains the bounded production-routing follow-up. CONFORM separately proves fail-closed live pass/block behavior through an exact test-owned eight-case agent-harness blocker-code map whose keys equal the canonical invalid-ID set and whose values are never represented as canonical spec fields.

## Interface Freeze Gates

- [ ] IF-0-CONFORM-1 — `phase_loop_runtime.conformance` preserves the complete read-only `conformance/__init__.py` public export tuple and packages a digest-enumerated mirror of exactly 15 authority files from `Consiliency/spec@v0.2.1` (two schemas, one manifest, eleven vectors, and the test-only oracle `consiliency_spec/outside_agent_router.py`), plus non-self-hashed `VENDOR.json`; `run_outside_agent_vectors()` dispatches every manifest row by `schema_target`; submission API/CLI/vector/package paths consume exactly the three valid and seven invalid `outside_agent_submission.v0.1` rows, while the `negative-unsupported-verdict` row is validated only through the selected `outside_agent_route_verdict.v0.1` schema plus vector-runner/package paths and never through the submission CLI; the exact overall three-valid/eight-invalid set equality and explicit 3+7+1 role partition hold; every manifest row consumes canonical `expected_valid` and `expected_blocker_class`, every `expected_valid=true` row passes with test-oracle blocker class `none`, every `expected_valid=false` row blocks with the exact mirrored-oracle blocker class, and every invalid live result also contains its separately test-owned mapped agent-harness code; route-verdict evidence proves the `schema_target` dispatch anchor was entered, the pinned route-verdict schema identity was selected, and the failure is the `/route` enum violation, so rejection under the wrong submission schema cannot satisfy the gate; amended EC-CONFORM-2 is implemented directly by exact recursive input/output inventory equality and exact one-of assignment to the seven closed classes below, including positional `submission_file` as `read_locator_only`, with forbidden/rejected/raw/free-text/unclassified/sentinel-bearing values absent from final bytes and only normalized safe repository-relative refs, validated fixed-shape digests or immutable Git IDs, closed-vocabulary/boolean/numeric projections, and producer-derived closed-template structural diagnostics permitted; the executable CONFORM evidence verifier independently validates provenance, TDD chronology, producer/consumer mutations, wheel and sdist contents, extracted-package behavior, redaction separation, and the no-cross-repo-overclaim boundary; SL-1/A2 seals only the pre-doc chronology/corpus/package, production, and mutation evidence by running the exact frozen broad-minus-`CONFORM_SL2_STALE_DOC_NODE_IDS` selector green; B0 proves that exact excluded four-node set is the complete stale-doc failure partition on the same candidate tree; and only SL-2/B2, after all owned docs and dispositions are written, may run the broad `-k outside_agent` suite green, require the first zero-mismatch full EC-CONFORM-0…8 `compatibility` matrix, and produce this gate.

The gate also freezes the complete TDD lifecycle: exact literal node IDs and counts; the stale-documentation set's exact two-new/two-migrated partition; exact default skip set/count before the marker, with both migrated stale nodes running unchanged legacy assertions green; exact activation RED failure/anchor set with zero skip/xfail/import/collection failures; automatic marker activation of the new assertions on all four stale nodes; the A2 complement and B0 four-node partition; and final structured JUnit evidence proving every frozen outside-agent node ran exactly once with zero skipped. IF-0-CONFORM-1 is withheld until a fresh exact-canonical-main process validates both two-parent landings, immutable tests, canonical bytes/digests, the full activation/B0/B2 lifecycle, and the final zero-skipped JUnit document.

For the `Consiliency/agent-harness#470` exception, IF-0-CONFORM-1 validates all three two-parent landings and treats "immutable tests" as the exact reviewed three-test-file/six-hunk repair delta followed by byte-for-byte test-path immutability. It also requires the dedicated forged-topology RED before the one production chronology hunk, the exact four-path/seven-hunk total exception, and trusted Git recomputation of every declared topology fact. Phase completion is withheld until IF-0-CONFORM-1 passes at exact `M`, the ordered current-v10 six-output/Fable-president review gate passes over that evidence, and the existing phase-loop lifecycle records completion. This does not move, combine, or weaken the tests-only landing, activated RED, repair landing, B0 partition, implementation landing, or exact-`M` final audit.

## Lane Index & Dependencies

SL-0 — Test-first canonical corpus and falsifier freeze
  Depends on: (none)
  Blocks: SL-1
  Parallel-safe: no

SL-1 — Lane A validator dialect and emission boundaries
  Depends on: SL-0
  Blocks: SL-2
  Parallel-safe: no

SL-2 — Lane B documentation, disposition, and whole-phase verification reducer
  Depends on: SL-0, SL-1
  Blocks: (none)
  Parallel-safe: no

## Lanes

### SL-0 — Test-first canonical corpus and falsifier freeze

- **Scope**: Land only test paths: immutable `spec@v0.2.1` source fixtures and the complete final tests/falsifier definitions for the canonical `expected_valid`/`expected_blocker_class` oracle, separately derived eight-case live blocker-code map, 3+7+1 corpus, route-verdict dispatch, schema selection, `submission_file` locator isolation, redaction, packaging, and evidence verifier; capture pre-implementation RED solely as exact failing test node IDs plus entry into the existing `validate_outside_agent_submission()`, `outside-agent-validate`, and `run_outside_agent_vectors()` outer seams with genuine missing-behavior failures, and panel that exact test commit before packaged data or production changes begin.
- **Owned files**: `phase-loop-runtime/tests/conftest.py`, `phase-loop-runtime/tests/_outside_agent_canonical.py`, `phase-loop-runtime/tests/test_outside_agent*.py`, `phase-loop-runtime/tests/fixtures/outside_agent_advisory/**`, `phase-loop-runtime/tests/fixtures/outside_agent_real/**`, `phase-loop-runtime/tests/fixtures/outside_agent_contract_v0_2_1/**`
- **Interfaces provided**: `CONFORM immutable test commit`, `CONFORM deterministic TDD activation guard`, `canonical v0.2.1 schema-target-partitioned corpus contract`, `canonical v0.2.1 provenance contract`, `closed per-channel redaction/projection contract`, `read-only public-export preservation contract`, `CONFORM evidence-verifier contract`
- **Interfaces consumed**: `EXPECTED_OUTSIDE_AGENT_CONTRACT_PIN` (pre-existing), `Consiliency/spec@v0.2.1` (pre-existing), `agent-harness#372 evidence` (pre-existing, non-authoritative)
- **Parallel-safe**: no; this preamble is the literal panelled RED tests-only landing and must merge before any SL-1 path changes.
- **Tasks**:
  - test: Author the final canonical corpus tests entirely under `phase-loop-runtime/tests/**`: exact overall 3-valid/8-invalid equality; exact 3-valid-submission/7-invalid-submission/1-invalid-route-verdict partitions by `schema_target`; canonical per-row `expected_valid` and `expected_blocker_class` equality; exact invalid-ID equality with a complete separately named test-owned agent-harness blocker-code map; submission API/CLI/vector/package behavior; route-verdict schema/vector/package behavior with no submission-CLI invocation; the seven-class redaction/projection inventory below, including the positional `submission_file` scalar for both CLI commands; exact preservation and importability of every current `phase_loop_runtime.conformance.__all__` name without editing `conformance/__init__.py`; frontmatter-suite collection with `PYTHONPATH=phase-loop-runtime/src` only; the exact `v0.2.1` oracle bytes passing the repo-root Ruff configuration; post-implementation entered dispatcher, selected-schema, and redaction anchors; and every per-criterion mutation. Freeze the exact 42-node `CONFORM_DIALECT_MIGRATED_NODE_IDS` inventory below and give each node an unchanged default legacy branch plus an activated/marker-selected canonical branch; no same-version compatibility bypass may make the legacy five-field dialect valid after the marker. Freeze `phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_no_copied_canonical_outside_agent_schema_or_vectors` as a named dual-mode `MIGRATED_EXISTING` node: its default branch stays byte-for-byte equivalent to today's blanket no-copy assertion; its forced branch requires exactly the 15 `VENDOR.json`-enumerated source/mirror paths and exact tag/commit/raw-byte digests, fails at a named missing-mirror assertion before implementation, and after the marker permits only those exact mirror bytes while rejecting every non-enumerated schema/vector/manifest/oracle copy anywhere under `src/`. Add the new exact node `phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_sdist_and_wheel_include_only_digest_enumerated_contract_mirror`, which requires the direct wheel, direct sdist, and sdist-derived wheel each to contain `VENDOR.json` plus exactly those 15 mirror files and no extra `_contract/` member. Name `M-CONFORM-1-RESTORE-ALLOWLIST` explicitly: assert the frozen candidate source anchor occurs exactly once, replace the anchored Draft 2020-12 submission-validation call with the exact pre-agent-harness#371 allow-list `{submission_schema_version, submission_kind, metadata, provenance_refs, evidence_refs}`, require all three canonical valid submission API/CLI cases to reject under the mutant, and pair it with the unmutated positive control in which all three pass with exit 0 and no blocker. Freeze `phase-loop-runtime/tests/test_outside_agent_redaction_separation.py::test_submission_file_locator_never_serializes_and_digest_tracks_only_captured_bytes` and `phase-loop-runtime/tests/test_outside_agent_redaction_separation.py::test_submission_file_missing_unreadable_paths_fail_closed_without_path_derived_digest` with safe-relative, same-byte absolute-locator, traversal/free-text sentinel, missing, unreadable, stdout/output-file parity, raw-construction-guard mutation, and final-serializer-guard mutation arms for both commands. Freeze the exact RED argv, the exact non-parametrized `CONFORM_SL2_STALE_DOC_NODE_IDS` set below, the exact broad-minus-stale A2 baseline argv, the exact B0 stale-node argv, and every post-implementation mutation argv plus expected exit semantics in the test commit; those declarations are expectations, not evidence that absent production mechanisms ran. Add the test-owned guard in `conftest.py` plus `_outside_agent_canonical.py`: activation is true only when `PHASE_LOOP_TDD_EXPECT_CONFORM=1` or the exact production marker matches; otherwise the collection hook skips only the literal frozen new-production node-ID tuple and attaches each expected node ID as a JUnit property. Migrated existing tests never skip: before marker they retain their exact legacy assertions, under explicit pre-implementation activation they run the canonical assertions, and after marker they select those same canonical assertions automatically.
  - impl: Materialize the test-only source fixture and `PROVENANCE.json` byte-for-byte from merged `Consiliency/spec@v0.2.1`; no file outside `phase-loop-runtime/tests/**` may change in this commit.
  - verify: Before recording RED, run the test-only structural controls that already can pass: collect every outside-agent node using the exact frontmatter `PYTHONPATH=phase-loop-runtime/src`, import the frozen `_outside_agent_canonical.py` helper under pytest's default prepend behavior, compare the fixture to the immutable tag, lint the fixture's exact oracle bytes with repo-root Ruff, and snapshot/import every current public export. Correct the migrated no-copy node's scan root literally to `phase-loop-runtime/src/phase_loop_runtime`, prove the production scan visits at least one file, and exercise a test-owned planted non-enumerated canonical copy under that package tree whose exact repository-relative path must appear in the rejection; neither a zero-file scan nor the old nonexistent root can pass. Capture raw collect-only output and digests for the broad outside-agent selection, the A2 broad-minus-stale selection, and the exact B0 stale-node selection; require `ALL_OUTSIDE_AGENT_NODE_IDS = A2_GREEN_NODE_IDS ⊎ CONFORM_SL2_STALE_DOC_NODE_IDS`, require the B0 collection to equal the four-node stale set, and require every collected node that reads an SL-2-owned repo document to be in that stale set. Then run the frozen RED argv from the test commit against unchanged `<execution-base>` production; retain runner-captured argv, nonzero exit code, stdout/stderr digest, exact failing test node IDs, and entered call-boundary anchors only for the three existing outer seams. Failures must expose genuinely missing canonical behavior, including tagged manifest/vector loading, acceptance of the three canonical valid submissions, route-target support, and the absent package/evidence surface. Do not record dispatch, selected-schema, redaction, or mutation execution in this stage; panel the exact test-commit digest and let the later owned evidence verifier recompute every claim. First run the exact default marker-absent CONFORM selection and require exit 0 with the literal default skip set/count and no other CONFORM skip; then run that same immutable selection with `PHASE_LOOP_TDD_EXPECT_CONFORM=1` and require the literal activated RED failure/anchor set, every expected node collected exactly once, zero skipped/xfail/import/collection errors, and no unexpected pass or failure. Both runs emit raw logs and JUnit XML; every activated failure must resolve to its frozen intended assertion anchor and existing outer-seam or explicit missing-surface probe.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| CONFORM-T0 | test | (none) | all SL-0 test paths | all `test_outside_agent*.py` and `_outside_agent_canonical.py` | `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k "canonical_corpus or corpus_oracle or canonical_oracle_ruff or public_exports or frontmatter_import or conform_evidence or redaction_separation or real_ci"` |
| CONFORM-T1 | impl | CONFORM-T0 | test fixture and provenance paths only | same as CONFORM-T0 | `git diff --name-only <execution-base>..<test-commit>` must contain only `phase-loop-runtime/tests/**` |
| CONFORM-T2 | verify | CONFORM-T1 | runner-owned ordered panel, early-prover evidence, RED log, selector-partition records, and chronology inputs | same as CONFORM-T0 | Have the runner record immutable `.phase-loop/artifacts/CONFORM/pre-implementation-red.json` plus its raw log and `.phase-loop/artifacts/CONFORM/frozen-test-partition.json`: exact base/test commits and digests, exact current-v10 ordered test-panel and early-prover binding, default argv/JUnit with exit 0 and exact skip set/count, activated RED argv/JUnit with nonzero exit, stdout/stderr digests, exact failure/anchor set, each expected node once, zero activated skips/xfail/import/collection errors, entered existing outer-seam or explicit missing-surface anchors only, the corrected nonempty no-copy scan root and planted-copy rejection path, the three exact collection argv/output digests, and exact broad/A2/B0 node sets satisfying the frozen disjoint-union equation; dispatch/schema-selection/redaction and mutation results are forbidden from this stage |

Before `<test-candidate>` is committed, SL-0 writes literal ordered tuples and separate literal integer counts for `ALL_OUTSIDE_AGENT_NODE_IDS`, `CONFORM_PREEXISTING_NODE_IDS`, `CONFORM_TEST_ONLY_INTEGRITY_NODE_IDS`, `CONFORM_NEW_PRODUCTION_NODE_IDS`, `CONFORM_MIGRATED_EXISTING_NODE_IDS`, `CONFORM_DIALECT_MIGRATED_NODE_IDS`, `CONFORM_MIGRATED_RED_NODE_IDS`, `CONFORM_ACTIVATED_RED_NODE_IDS`, `CONFORM_SL2_STALE_DOC_NODE_IDS`, and `A2_GREEN_NODE_IDS`, plus literal `CONFORM_ACTIVATED_RED_ANCHORS`. Expanded parametrized node IDs are listed individually; no glob, marker query, runtime collection result, or implementation-authored value may define a frozen set/count. The immutable integrity tests enforce the disjoint equations `ALL = PREEXISTING ⊎ TEST_ONLY_INTEGRITY ⊎ NEW_PRODUCTION`, `DIALECT_MIGRATED ⊆ MIGRATED_EXISTING ⊆ PREEXISTING`, `MIGRATED_RED ⊆ MIGRATED_EXISTING`, `ACTIVATED_RED = NEW_PRODUCTION ⊎ MIGRATED_RED`, `CONFORM_SL2_STALE_DOC_NODE_IDS ⊆ NEW_PRODUCTION ∪ MIGRATED_EXISTING`, `CONFORM_SL2_STALE_DOC_NODE_IDS = (CONFORM_SL2_STALE_DOC_NODE_IDS ∩ NEW_PRODUCTION) ⊎ (CONFORM_SL2_STALE_DOC_NODE_IDS ∩ MIGRATED_EXISTING)`, and `A2_GREEN = ALL - CONFORM_SL2_STALE_DOC_NODE_IDS`. The dialect-migrated set is exactly the 42-node inventory below, the full migrated-existing set has frozen count 45 after adding the two stale-documentation migrations and no-copy migration, and every tuple length equals its frozen integer count. The stale set's frozen count is four and its new and migrated intersections each have frozen count two. The no-copy node appears exactly once in `PREEXISTING`, `MIGRATED_EXISTING`, `MIGRATED_RED`, `ACTIVATED_RED`, and `A2_GREEN`, never in `DIALECT_MIGRATED`, `NEW_PRODUCTION`, or the stale-doc set; its node ID and activated anchor participate in every corresponding sorted-LF digest. The new archive-inclusion node appears exactly once in `NEW_PRODUCTION`, `ACTIVATED_RED`, and `A2_GREEN`. These memberships add one new node identity while migrating existing identities; no category double-count is permitted.

The guard lives only in SL-0-owned test paths. With the exact marker absent and `PHASE_LOOP_TDD_EXPECT_CONFORM` unset, the default CONFORM selection exits 0, skips exactly `CONFORM_NEW_PRODUCTION_NODE_IDS` once each, and runs every pre-existing, migrated-legacy, and test-only fixture/oracle integrity node once and green; all 45 migrated nodes execute their unchanged legacy branches and pass, and no other CONFORM node skips. With `PHASE_LOOP_TDD_EXPECT_CONFORM=1` against unchanged `<test-parent>` production, every node runs once with zero skipped: every dialect-migrated node selects its canonical branch, the failure set is exactly `CONFORM_ACTIVATED_RED_NODE_IDS`, each failure is its frozen intended assertion rather than import/collection/fixture setup, and all other nodes pass. `CONFORM_MIGRATED_RED_NODE_IDS` is derived during test authorship only by exercising the frozen canonical branches against unchanged production, then written as a literal tuple before the test candidate is committed; it must contain every migrated node whose canonical contract is not already satisfied, while migrated nodes that remain green must retain a meaningful canonical assertion and cannot be weakened to escape the RED set. The no-copy node must reach `CONFORM_RED::digest_enumerated_contract_mirror_missing`, and the archive-inclusion node must reach `CONFORM_RED::sdist_contract_mirror_missing`; neither an import error nor an empty archive probe is RED. `CONFORM_ACTIVATED_RED_ANCHORS` binds each failure to its assertion source anchor and an entered existing API/CLI/vector seam or an explicit missing-file/module/archive-member probe that asserts absence without importing a missing module. No xfail is permitted in either mode. Mutation definitions and exact candidate-only mutation argv remain active test-only integrity data pre-implementation; their production mutations still execute only after SL-1, so `mutation_not_applied` is never misreported as initial RED.

Push and panel exact `<test-candidate>` only after both raw/JUnit proofs and the frontmatter `automation.suite_command` pass. Land it separately as two-parent `<test-landing>` with `<test-parent>` as first parent; verify the landed test-path tree equals the panelled candidate bytes. From fresh clean processes at exact canonical-main `<test-landing>`, rerun the default selection GREEN with the exact skip set/count, rerun the activated selection RED with the exact failure/anchor set, and rerun the full frontmatter suite GREEN. Within the frozen CONFORM node universe the default suite's skip set is exactly `CONFORM_NEW_PRODUCTION_NODE_IDS`; every pre-existing CONFORM node is active and green, and unrelated pre-existing suite skip behavior must be byte-for-byte/baseline-identical rather than widened. No SL-1 branch may start until those landed proofs are sealed.

SL-0 freezes these four non-parametrized node IDs as the complete `CONFORM_SL2_STALE_DOC_NODE_IDS` set, one per SL-2-owned surface:

- `phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_documented_consumer_mirror_policy_allows_only_pinned_contract_bytes` → `docs/outside-agent-conformance.md` (`NEW_PRODUCTION`)
- `phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_v7_disposition_records_merged_contract_and_final_installed_behavior` → `specs/phase-plans-v7.md` (`NEW_PRODUCTION`)
- `phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_release_handoff_records_metadata_only_package_contract_and_dispatch_boundary` → `docs/releases/outside-agent-release-handoff.md` (`MIGRATED_EXISTING`, therefore `PREEXISTING`)
- `phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_public_docs_point_to_handoff_without_claiming_release_dispatch` → `CHANGELOG.md` (`MIGRATED_EXISTING`, therefore `PREEXISTING`)

`CONFORM_DIALECT_MIGRATED_NODE_IDS` has frozen count 42 and contains exactly these current node IDs; no other pre-existing node may switch payload dialect or assertions under the activation guard:

- `test_outside_agent_advisory.py`: `test_builds_clean_advisory_evidence_without_external_access`, `test_serialized_advisory_evidence_is_deterministic_and_metadata_only`, `test_redaction_blocker_maps_to_exit_code_3`, `test_provenance_blocker_maps_to_exit_code_4`
- `test_outside_agent_advisory_cli.py`: `test_cli_clean_pass_outputs_advisory_json`, `test_cli_redaction_violation_returns_exit_3`, `test_cli_provenance_failure_returns_exit_4`, `test_cli_writes_output_file_with_stdout_payload`
- `test_outside_agent_advisory_fixtures.py`: `test_advisory_fixture_outputs_match_stable_summaries`
- `test_outside_agent_authority_boundary.py`: `test_sdk_serialization_never_claims_merge_authority`, `test_cli_stdout_never_claims_merge_authority`, `test_cli_output_file_never_claims_merge_authority`
- `test_outside_agent_core_api.py`: `test_public_core_api_returns_typed_metadata_only_verdict`, `test_core_api_is_deterministic_and_does_not_require_secrets`, `test_validation_does_not_use_network_or_provider_credentials`
- `test_outside_agent_real_ci.py`: `test_fixture_invocations_do_not_run_vectors_for_live_validation`, `test_ci_release_vector_evidence_runs_pinned_vector_runner`
- `test_outside_agent_real_cli.py`: `test_cli_clean_pass_writes_file_and_stdout`, `test_cli_redaction_violation_returns_exit_3`, `test_cli_provenance_failure_returns_exit_4`, `test_cli_contract_pin_failure_returns_exit_5`, `test_cli_other_conformance_blockers_return_exit_6`
- `test_outside_agent_real_output.py`: `test_serializes_clean_governed_pipeline_verdict_shape`, `test_serialized_real_verdict_is_deterministic_json`, `test_serialized_blocked_verdict_has_typed_blockers_and_no_advisory_fields`
- `test_outside_agent_real_runtime.py`: `test_real_validator_wraps_core_once_with_metadata_only_evidence`, `test_real_validator_redaction_violation_maps_to_exit_3`, `test_real_validator_provenance_failure_maps_to_exit_4`, `test_real_validator_contract_pin_failure_maps_to_exit_5`, `test_real_validator_other_conformance_blocker_maps_to_exit_6`, `test_real_validator_rejects_absolute_submitted_refs_without_raw_paths`
- `test_outside_agent_release_surface.py`: `test_real_validator_and_advisory_outputs_share_pinned_metadata_only_contract_evidence`
- `test_outside_agent_schema_validation.py`: `test_accepts_supported_submission_kinds`, `test_unsupported_schema_version_fails_closed`, `test_unsupported_submission_kind_fails_closed`, `test_unknown_top_level_field_fails_closed`, `test_missing_required_metadata_fails_closed`
- `test_outside_agent_vectors.py`: `test_vector_runner_matches_positive_and_negative_expected_outcomes`, `test_unknown_vector_schema_version_fails_closed`, `test_missing_expected_outcome_fails_closed`, `test_manifest_digest_drift_fails_closed`
- `test_outside_agent_redaction.py`: `test_core_verdict_contains_only_metadata_refs_and_digests`

Each abbreviated name above expands to `phase-loop-runtime/tests/<file>::<function>` in the frozen tuple. SL-0 integrity tests assert the expansion has exact count 42, every listed node exists once in the unchanged-base collection, and no collected pre-existing node that requires legacy-dialect validity is outside this set. Default branches preserve the exact current five-field payload/assertion behavior. Activated and marker branches use byte-derived canonical `v0.2.1` fixture builders: positive paths require canonical acceptance, negative paths mutate a canonical-valid base and require the intended typed failure rather than incidental rejection of the old dialect, and manifest/vector nodes consume canonical `schema_target`, `expected_valid`, and `expected_blocker_class`. The final marker-on suite additionally rejects the legacy five-field payload at both public API and CLI seams, preventing an unreviewed same-version compatibility bypass.

The exact frozen A2 post-implementation/pre-doc green argv is `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k "outside_agent and not (test_documented_consumer_mirror_policy_allows_only_pinned_contract_bytes or test_v7_disposition_records_merged_contract_and_final_installed_behavior or test_release_handoff_records_metadata_only_package_contract_and_dispatch_boundary or test_public_docs_point_to_handoff_without_claiming_release_dispatch)"`. The exact frozen B0 argv is `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest -q phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_documented_consumer_mirror_policy_allows_only_pinned_contract_bytes phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_v7_disposition_records_merged_contract_and_final_installed_behavior phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_release_handoff_records_metadata_only_package_contract_and_dispatch_boundary phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_public_docs_point_to_handoff_without_claiming_release_dispatch`. Freeze collect-only forms of the broad `-k outside_agent` argv and both literal argv by adding `--collect-only`; normalize only pytest's summary/order noise, never node IDs. The A2 collection must equal every broad outside-agent node except the four-node stale set and must explicitly include both `test_no_copied_canonical_outside_agent_schema_or_vectors` in its automatic post-marker branch and `test_sdist_and_wheel_include_only_digest_enumerated_contract_mirror`; B0 must collect exactly the four stale nodes once each. The four full function-name tokens in the negative `-k` clause must each map one-to-one to its frozen non-parametrized node ID; no shorter keyword, marker, filename-wide exclusion, future `-k` rewrite, xfail, skip, parameterized suffix, or additional test that reads an SL-2-owned repo document may alter this partition after the test commit. Unit tests of `conform_evidence` may run in A2 only against synthetic artifact/doc roots and may not invoke real full `compatibility` or read the four repo documents. The first two stale nodes are exactly `CONFORM_SL2_STALE_DOC_NODE_IDS ∩ CONFORM_NEW_PRODUCTION_NODE_IDS`; they skip as part of the frozen new-production set only during the marker-absent default-main interval. The last two are exactly `CONFORM_SL2_STALE_DOC_NODE_IDS ∩ CONFORM_MIGRATED_EXISTING_NODE_IDS`; because they are pre-existing, they never skip, preserve and pass their current legacy assertions by default, and expose separately anchored new assertions that fail under explicit pre-implementation activation. The migrated no-copy node is not stale documentation and therefore always remains in A2. Once SL-1 installs the exact marker, activation is automatic with `PHASE_LOOP_TDD_EXPECT_CONFORM` unset: A2 requires its new exact-mirror assertion and the new archive-inclusion node GREEN while excluding only the four stale-doc nodes by selector; B0 selects those four explicitly and requires their activated pre-doc assertions to execute RED with zero skipped.

SL-0 evidence is intentionally limited: current production has no `schema_target` dispatcher, selected-schema identity, or CONFORM evidence verifier, so a test expectation or test-only surrogate cannot count as an entered production anchor. Wrong-schema swap, dispatch-bypass, redaction-guard, and every equivalent per-falsifier mutation must run only after SL-1 is frozen as immutable `<candidate-commit>`; an anchor miss is `mutation_not_applied`, never a kill.

The test fixture is not a new authority. Its `PROVENANCE.json` must name only `Consiliency/spec`, tag `v0.2.1`, dereferenced commit `b862f977897a7b87c4419680a3e83735d4ff07b0`, source path, mirror path, and raw-byte sha256. It must describe the exact 15-file source set: `schemas/outside-agent-submission.schema.json`, `schemas/outside-agent-route-verdict.schema.json`, the normative test-only oracle `consiliency_spec/outside_agent_router.py`, `test-vectors/outside-agent/manifest.json`, and the eleven manifest-referenced vector paths. “Oracle” and `consiliency_spec/outside_agent_router.py` name the same one source/mirror file, not a sixteenth file. Missing files, extra files, duplicate source or mirror paths, a branch ref, a non-`v0.2.1` source, a digest mismatch, or any manifest path absent from provenance must fail closed. Tests must independently compare the fixture, final `_contract/` mirror, `VENDOR.json`, and immutable source-tag bytes; agreement between two repo-authored metadata files alone is self-report and is insufficient.

The exact valid set is also the exact valid-submission set: `{positive-work-request, positive-implementation-submission, positive-ambiguity-report}`. The invalid set is partitioned by target as follows:

| Schema target and role | Invalid case ID | Canonical `expected_blocker_class` | Derived test-owned live agent-harness blocker code | Required execution surfaces |
|---|---|---|---|---|
| `outside_agent_submission.v0.1` invalid submission | `negative-raw-payload` | `unsafe_evidence_reference` | `schema_validation_failed` | submission API, CLI, vector runner, installed package |
| `outside_agent_submission.v0.1` invalid submission | `negative-missing-digest` | `missing_information` | `schema_validation_failed` | submission API, CLI, vector runner, installed package |
| `outside_agent_submission.v0.1` invalid submission | `negative-source-bundle-mismatch` | `unsafe_evidence_reference` | `source_bundle_mismatch` | submission API, CLI, vector runner, installed package |
| `outside_agent_submission.v0.1` invalid submission | `negative-unknown-producer-identity-posture` | `policy_gap` | `schema_validation_failed` | submission API, CLI, vector runner, installed package |
| `outside_agent_submission.v0.1` invalid submission | `negative-path-traversal` | `unsafe_evidence_reference` | `schema_validation_failed` | submission API, CLI, vector runner, installed package |
| `outside_agent_submission.v0.1` invalid submission | `negative-empty-evidence-refs` | `missing_information` | `schema_validation_failed` | submission API, CLI, vector runner, installed package |
| `outside_agent_submission.v0.1` invalid submission | `negative-git-object-id-length` | `unsafe_evidence_reference` | `schema_validation_failed` | submission API, CLI, vector runner, installed package |
| `outside_agent_route_verdict.v0.1` invalid route verdict | `negative-unsupported-verdict` | `policy_gap` | `schema_validation_failed` | direct route-verdict schema validation, vector runner, installed-package validation; never submission CLI |

Tests must assert set equality, not subset membership, for all eleven manifest case IDs; every exact manifest `(case_id, expected_valid, expected_blocker_class)` row; the three `expected_valid=true` IDs; the eight `expected_valid=false` IDs; the 3+7+1 role partitions; the two supported `schema_target` values; provenance paths; each target-specific derived live-code-map key set; and the union of all eight derived map keys. The same suite invokes the mirrored test-only router for every row and requires its blocker class to equal canonical `expected_blocker_class`, including `none` for all three valid rows. The three valid submissions must pass the submission API, CLI, vector runner, and installed-package paths with exit 0 and no blocker. Each of the seven invalid submissions must block those same submission paths, produce a non-pass CLI exit, and include its separately mapped live agent-harness code. `negative-unsupported-verdict` must block under direct Draft 2020-12 validation of the selected pinned route-verdict schema and under vector-runner/installed-package validation, match canonical `policy_gap`, and include its separately mapped live code; it has no submission-CLI exit assertion because the submission CLI must never receive it.

When run post-implementation against immutable `<candidate-commit>`, the route-verdict test is non-vacuous: it must record that manifest `schema_target` dispatch was entered with `outside_agent_route_verdict.v0.1`, identify the selected schema by pinned digest and `$id`, and assert the validation error is the `/route` enum rejection of `accepted_for_merge`. Mutating dispatch to select `outside-agent-submission.schema.json`, bypassing dispatch, or invoking `outside-agent-validate` for that row must fail the test even though the wrong submission schema would also reject the document. Deleting an invalid vector, adding an unclassified vector, changing canonical `expected_valid` or `expected_blocker_class`, changing a row’s role or target, omitting or adding a derived live-code-map key, weakening an invalid result to pass, dropping the mapped code, or flattening execution back to one schema must independently make the same coupled suite fail. Canonical `expected_blocker_class` values remain oracle-owned metadata checked by the mirrored test-only router; the separately named live-code map must not replace them, claim spec authority, enter the mirrored manifest, or surface canonical classes as a new production router.

Before the test commit is frozen, derive the complete submitter-input inventory recursively from both pinned schemas plus the real/advisory builders and CLI arguments, derive the complete output-path inventory from both serializers, and compare both inventories by exact set equality to this closed classification, which directly and exhaustively implements amended EC-CONFORM-2:

| Class | Reachable input and serialized-output channels | Required negative and positive controls |
|---|---|---|
| `read_locator_only` | CLI positional `submission_file` for `outside-agent-preflight` and `outside-agent-validate`; this scalar is consumed only to locate and read candidate input bytes and is not itself a serialized-output channel | A readable safe repo-relative locator and an absolute locator to the same bytes must produce the same `input_digest = sha256(captured_input_bytes)` and byte-identical command payloads, with neither raw locator present. Traversal-shaped and free-text/sentinel-bearing locators may be used only as read attempts and must never reach construction or serialization. Missing and unreadable inputs must block with a closed structural diagnostic, retain zero captured bytes, set `input_digest = sha256(b"")`, and omit the locator. Cover both commands, preflight `--output`, validate required `--output`, stdout/output-file byte parity, and independently anchor-killed mutations that restore path-derived digest construction or append the raw locator at final serialization. |
| `forbidden_free_text` | Canonical submission `submission_id` and `summary`; `work_request.goal` and `constraints[*]`; `implementation_submission.change_summary`; `ambiguity_report.ambiguity_summary` and `questions[*]`; route-verdict `blocker.summary` and `notes`; producer names/versions; evidence repo-owner/repo-name identifiers and source-bundle IDs; legacy input `metadata` keys/values; unknown keys/values; raw payloads, provider bodies, logs, environment values; and any future unclassified free-text scalar | Put a distinct sentinel in each reachable path, prove that path was entered, and require the sentinel absent from both final stdout and `--output` bytes. A clean canonical document must still reach the serializer and retain its non-sensitive typed projections. |
| `normalized_ref_projection` | Canonical input `evidence_refs[*].repo_relative_path`; legacy input `provenance_refs[*].ref` and `evidence_refs[*].ref`; advisory output `provenance_refs[*]`; real/advisory output `evidence_refs[*].ref`; CLI input and real output `submitted_refs[*]` | A canonical safe repo-relative ref may appear only in normalized POSIX form. For each channel inject a sentinel-bearing unsafe, traversal, absolute, or secret-shaped raw ref, prove normalization/rejection ran, and require the raw value absent from final bytes; pair with a safe ref that projects exactly once. |
| `structural_diagnostic_projection` | Real/advisory `blockers[*].ref` and `blockers[*].message` | The producer derives `ref` only from schema-known JSON-pointer tokens, array indices, or a fixed root/unknown-field marker; `message` is producer-derived only from a closed code/keyword template. Inject sentinel-bearing unknown property names and values and require neither to echo; pair with a known schema failure whose stable pointer and message are present. Raw `jsonschema` messages are forbidden. |
| `validated_digest_projection` | Canonical input immutable Git refs, evidence `sha256`, source-bundle manifest hashes, top-level bundle-manifest hash, and implementation head commit; legacy input and real/advisory output `evidence_refs[*].digest`; contract/schema/manifest hashes; top-level vector-manifest hash; derived `input_digest`; any projected immutable Git object ID | A valid lowercase 40/64-hex Git ID or lowercase 64-hex sha256 may project where declared; invalid or sentinel-bearing raw values must block without echo. For a successful read, `input_digest` must equal sha256 of the exact captured input bytes. For missing/unreadable `submission_file`, it must equal sha256 of the exact zero-length captured-byte sequence, never sha256 of the locator string; in all arms it must not equal or contain a raw sentinel. |
| `closed_vocabulary_projection` | Schema/gate/authority/command/version fields; submission kind; claim posture; route; blocker class and code; evidence digest algorithm/kind/source role; path-membership proof type and inclusion; redaction posture; advisory classification; producer-controlled output `metadata` with an exact fixed key/value map; booleans; numeric exit code and status | Values must be constants or members of the pinned enum/code tables, with exact status/authority/exit coupling. Output `metadata` must never copy legacy input metadata. An out-of-vocabulary sentinel must block without echo; each allowed value used by the corpus has a positive control. |
| `serialized_sink` | Real/advisory stdout and `--output` | These are byte sinks, not extra projection channels: require byte-equivalent JSON, scan both for every forbidden/rejected sentinel, and prove the serializer was entered. |

`submission_file`, `provenance_refs`, `evidence_refs`, `submitted_refs`, `blockers[].ref`, and every nested scalar are therefore bound directly to amended EC-CONFORM-2. A path accepted by the local file reader is not thereby safe to serialize: only a separately normalized safe repo-relative reference in a declared ref channel or a producer-derived digest of captured input bytes may reach a sink. A new input field, serializer field, nested scalar, or CLI-controlled value fails exact inventory equality until assigned to exactly one class with both controls; an allowed projection is legal only through its declared read-only use, normalization, fixed-shape validation, closed vocabulary/type, or producer-derived closed template. For post-implementation EC-CONFORM-2 and EC-CONFORM-3 verification against immutable `<candidate-commit>`, execute the frozen per-channel negative cases and per-class positive cases, including both `submission_file` tests, then independently disable the raw-construction guard and final-serializer guard: either anchor-asserted mutation must make its exact test fail, while an enforced path must retain `redaction_posture="metadata_only"`.

The SL-0 no-copy migration preserves the legacy policy, not its defective path expression. The
otherwise general phrase "byte-for-byte equivalent to today's blanket no-copy assertion" is
superseded for path construction only by the literal
`phase-loop-runtime/src/phase_loop_runtime` root, mandatory nonempty scan, and planted-copy
positive control above; freezing the old nonexistent root is forbidden.

### SL-1 — Lane A validator dialect and emission boundaries

- **Scope**: Import the exact tagged contract mirror, replace the hand-authored dialect with packaged Draft 2020-12 validation dispatched by manifest `schema_target`, preserve independent fail-closed redaction, install the exact frozen capability marker, and add the executable machine-evidence verifier without modifying the frozen SL-0 test commit.
- **Owned files**: `phase-loop-runtime/MANIFEST.in`, `phase-loop-runtime/pyproject.toml`, `phase-loop-runtime/src/phase_loop_runtime/cli.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/_contract/**`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_advisory.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_conform_evidence.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_core.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_real.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_real_output.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_redaction.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_schema.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_vectors.py`
- **Interfaces provided**: `packaged canonical validator dialect`, `CONFORM production capability marker`, `metadata-only serialization boundary`, `schema-target-dispatched canonical vector runner`, `executable CONFORM evidence verifier`, `SL-1 implementation commit identity`, `SL-1 pre-doc evidence bundle`
- **Interfaces consumed**: `CONFORM immutable test commit`, `canonical v0.2.1 schema-target-partitioned corpus contract`, `canonical v0.2.1 provenance contract`, `closed per-channel redaction/projection contract`, `read-only public-export preservation contract`, `CONFORM evidence-verifier contract`
- **Parallel-safe**: no; this is the sole production writer and starts only after the immutable test-first landing.
- **Tasks**:
  - test: From a fresh process at exact canonical-main `<test-landing>`, re-run the exact SL-0 default argv GREEN with its frozen skip set/count and the exact activated RED argv with `PHASE_LOOP_TDD_EXPECT_CONFORM=1` at the recorded test commit against unchanged base production and reproduce its exact failing node IDs plus entered existing outer API/CLI/vector anchors before checking out the implementation worktree; do not require or claim any future dispatch, schema-selection, redaction, or mutation anchor in this pre-implementation replay.
  - impl: Import the 15 authority files from the immutable tagged commit, write `VENDOR.json`, add exact package-data inclusion in `pyproject.toml` and exact source-distribution inclusion in `MANIFEST.in`, add `CONFORM_V10_CAPABILITY_MARKER` with the exact frozen value in `outside_agent_schema.py`, implement exact `schema_target` dispatch to the pinned submission or route-verdict schema, keep the submission CLI submission-only, implement all seven closed classes, treating both positional `submission_file` values as read-only locators, deriving readable `input_digest` only from captured bytes and missing/unreadable `input_digest` from zero captured bytes, never from path text, plus target-partitioned vector/package behavior and `outside_agent_conform_evidence.py`; do not edit SL-0 paths, `conformance/__init__.py`, or the already-correct pin/import/provenance modules, and preserve every current public import with its existing name and compatible type/behavior.
  - verify: Freeze SL-1 as immutable `<candidate-commit>` without changing any SL-0 path. Against that exact commit, run only the literal frozen A2 broad-minus-stale baseline argv with exit 0; independently recollect and require `A2_GREEN_NODE_IDS = ALL_OUTSIDE_AGENT_NODE_IDS - CONFORM_SL2_STALE_DOC_NODE_IDS`, so every SL-1 chronology/corpus/package/production node runs and none of the four SL-2-owned stale-documentation nodes does. Run every frozen production/mutation falsifier available before docs—including `M-CONFORM-1-RESTORE-ALLOWLIST`—with its production injection anchor applied and a nonzero killed-mutant exit; have the runner capture immutable `.phase-loop/artifacts/CONFORM/post-implementation-falsifiers.json` plus raw logs containing exact argv, exits, collected-node-set and log/source-anchor digests, entered dispatch/selected-schema/redaction observables, and `mutation_not_applied` for any anchor miss. Then invoke only `chronology`, `corpus`, and `package`; require zero mismatches within those pre-doc claim scopes and seal their outputs as the `SL-1 pre-doc evidence bundle`. Do not run the broad `-k outside_agent` baseline, invoke full `compatibility`, require the EC-CONFORM-0…8 matrix, or produce IF-0-CONFORM-1 in SL-1. Push the exact candidate first, then run A2 from a fresh clean process at that pushed SHA with `PHASE_LOOP_TDD_EXPECT_CONFORM` unset; require the exact marker, exact A2 node count, every selected node once, zero skipped, and every non-final compatibility test included. Separately run a fresh exact `<implementation-parent>` canonical-main process; neither process may inherit an imported `phase_loop_runtime` module from its launcher.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| CONFORM-A0 | test | CONFORM-T2 | frozen SL-0 suite against unchanged production | none; consumes frozen SL-0 tests | Re-run the exact recorded default and activated argv from fresh exact-main processes; require default GREEN with the exact skip set/count, then activated RED; require the same nonzero exit, failing-node set, and entered existing outer API/CLI/vector anchors, and verify each recorded raw-log digest against its own captured bytes, with no future-mechanism anchor claimed |
| CONFORM-A1 | impl | CONFORM-A0 | every SL-1 owned file and no test path | none | Install the exact capability marker; implement tagged-byte import, exact manifest traversal, `schema_target` dispatch and selected-schema instrumentation, submission-only semantic validation and CLI exit codes, route-verdict schema/vector/package validation, canonical per-row oracle comparison plus the separate complete derived live-code map, `submission_file` read-locator isolation with byte-only/zero-captured-byte digest semantics, projection gating, recursive scalar redaction, exact wheel and sdist package inclusion through both `pyproject.toml` and `MANIFEST.in`, and evidence verification |
| CONFORM-A2 | verify | CONFORM-A1 | immutable candidate implementation plus runner-owned pre-doc evidence/build outputs | none | At exact `<candidate-commit>`, run `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k "outside_agent and not (test_documented_consumer_mirror_policy_allows_only_pinned_contract_bytes or test_v7_disposition_records_merged_contract_and_final_installed_behavior or test_release_handoff_records_metadata_only_package_contract_and_dispatch_boundary or test_public_docs_point_to_handoff_without_claiming_release_dispatch)"` with exit 0 and exact collected set `ALL_OUTSIDE_AGENT_NODE_IDS - CONFORM_SL2_STALE_DOC_NODE_IDS`; run every exact pre-doc mutation argv frozen by SL-0 with anchor-applied plus nonzero killed-mutant exit; then run `chronology`, `corpus`, and `package` only and seal their zero-mismatch claim-scoped outputs; the broad `-k outside_agent` green replay is forbidden here. Emit structured A2 JUnit and require every `A2_GREEN_NODE_IDS` member exactly once with zero skipped; this is the broad compatible suite required before implementation panel/merge and includes every compatibility test except the four final-document nodes. |

Use the tagged JSON schemas as the structural source of truth; do not re-encode required fields, additional-properties rules, patterns, verdict vocabulary, or corpus expectations in production Python. Dispatch supports exactly the two manifest values `outside_agent_submission.v0.1` and `outside_agent_route_verdict.v0.1`, loads the corresponding pinned schema, and fails closed on any other target. The only production semantic check beyond schema validation is the schema-inexpressible source-bundle relationship already named by the contract, and it applies only after selecting the submission target. `outside_agent_pin.py`, `outside_agent_imports.py`, `outside_agent_provenance.py`, and `conformance/__init__.py` are read-only: the provenance module remains the existing submission-only semantic helper and must neither receive route-verdict payloads nor be deleted as part of replacing the dialect, and the complete current `conformance.__all__` tuple plus every imported object named by it must remain importable without editing the initializer. Their `v0.2.1` tag, `b862f977897a7b87c4419680a3e83735d4ff07b0` commit, submission-schema sha256 `5670b5001ced0f25010b153fe602db5761f92d69707cf670b6f530a7d689ef4a`, verdict-schema sha256 `86169277d3a0823db1a6c9fa4d20a838b0bc2820818ad00ebd53dcdd03c2b1c2`, manifest sha256 `78858828e9eace93eaf31d90717666ddce54ccb3666113df9d033d67c20cfca0`, literal 15-path digest map, and export snapshot must agree or the phase stops. If implementation requires changing a read-only module/export, or if any frozen production mutation anchor does not match exactly once, report a non-human `contract_bug`, abandon the candidate, and re-plan/re-panel a replacement plan and tests-only commit; never patch the immutable test commit, widen ownership ad hoc, count `mutation_not_applied` as a kill, or edit the pin/canonical bytes to accommodate implementation.

The metadata-only walker remains an independent safety pass over raw input and the CLI `submission_file` locator is separately confined to read-only use before either builder, and both real and advisory serializers route their complete final payload through the shared class-level scalar redactor. The authoritative real sink may downgrade to a redaction violation; the advisory sink remains non-authoritative and may redact in place, but its CLI exit code is always derived from the emitted payload. Readable, missing, unreadable, absolute, traversal-shaped, and free-text locator values may never be copied or path-hashed into output; only captured-byte digest semantics and a closed diagnostic survive. Neither guard may infer safety from schema validity, and neither may set `redaction_posture` merely because the other ran.

`phase_loop_runtime.conformance.outside_agent_conform_evidence` is an owned executable module, invoked as `python3 -m phase_loop_runtime.conformance.outside_agent_conform_evidence <mode>`. The runner owns the immutable pre- and post-implementation capture manifests and raw logs; `chronology` reads but never amends them, writes a separate `.phase-loop/artifacts/CONFORM/chronology-verification.json`, and treats their status/boolean fields as non-authoritative. The module otherwise writes only metadata under `.phase-loop/artifacts/CONFORM/` and exits nonzero on any defect. Its modes must:

- `chronology`: independently validate three disjoint stages. For pre-implementation RED, validate the exact reviewed plan and panel digests; the exact test commit and Fable/Sol/Gemini/Grok panel digests/outcomes; the single recorded author vendor; `<execution-base>` production-path identity against the reviewed `1627e3f…` baseline or a fresh plan review; base → test ancestry; a test-only SL-0 diff; the exact frozen RED argv; nonzero exit; exact failing node IDs; raw-log digest; and entered anchors only at the existing outer API/CLI/vector seams. For post-implementation/pre-doc evidence, validate zero test-path changes after the test commit; the exact immutable candidate source digest; the frozen broad, A2, and B0 collect-only argv/output digests; `ALL_OUTSIDE_AGENT_NODE_IDS = A2_GREEN_NODE_IDS ⊎ CONFORM_SL2_STALE_DOC_NODE_IDS`; the exact literal A2 broad-minus-four-full-function-name `-k` argv and collected node IDs; A2 exit 0; nonzero exit for every anchor-applied mutant; `mutation_not_applied` on any anchor miss; raw-log digests; and entered production dispatch, selected-schema, wrong-schema-swap, dispatch-bypass, redaction, and equivalent per-falsifier anchors. At candidate scope, the absent B0/final records are expected and the mode emits only claim-scoped pre-doc results. For final doc chronology, validate candidate → final ancestry; B0 ran on the same candidate tree with the exact frozen four-node argv, nonzero exit, and failure set exactly `CONFORM_SL2_STALE_DOC_NODE_IDS` with no skip, xfail, collection error, or extra failure; the B1 diff changes only the four SL-2-owned files; and B2, after B1, ran the broad `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k outside_agent` argv with exit 0 and the exact frozen `ALL_OUTSIDE_AGENT_NODE_IDS` set. Recompute commits, diffs, file/log hashes, argv, exits, test IDs, node-set partitions, and anchor application from Git plus runner-captured raw bytes and fresh detached-commit reruns; no checked boolean, verifier-authored summary, test-only surrogate, or prose record may satisfy any stage.
  Chronology additionally proves the exact Git/process topology: `<test-parent>` is the fresh canonical-main head before the test landing; pushed `<test-candidate>` has only SL-0 paths; `<test-landing>` has exactly two parents with first parent `<test-parent>` and second-parent ancestry containing `<test-candidate>`; pushed `<candidate-commit>` descends from `<test-landing>` and changes no SL-0 byte; pushed `<final-candidate>` descends from `<candidate-commit>` and B1 changes only SL-2 files; `<implementation-parent>` is the fresh canonical-main head immediately before landing and contains `<test-landing>`; `<implementation-landing>` has exactly two parents with first parent `<implementation-parent>` and second-parent ancestry containing `<final-candidate>`; and final `<canonical-main-head>` contains `<implementation-landing>` with identical phase-owned bytes. It recomputes the exact marker-absent default JUnit skip set/count; the frozen two-new/two-migrated stale partition; default execution and legacy-assertion success for both migrated stale nodes; the activated pre-implementation JUnit RED failure/anchor set, including the new assertion branches for those two migrated nodes; marker-driven A2 zero-skip complement; marker-driven B0 exact four RED nodes; B2 pre-merge full zero-skip JUnit; and B2 exact-main replay JUnit with the same full zero-skip set. Every candidate/main run is a new OS process in a clean exact-SHA checkout after commit/ref verification and before importing the modified verifier; raw logs and JUnit are authoritative inputs, while candidate-authored status booleans are not.
  For the `Consiliency/agent-harness#470` exception, every prior "zero test-path changes after the test commit" or "changes no SL-0 byte" clause means exactly one allowlisted three-test-path/six-hunk delta containing the candidate-ancestry selector, per-mode invocation-directory, criterion-replay digest-comparison, chronology-forgery RED, safe sdist-extraction, and sealed-changelog-parent-rebind hunks at `<repair-candidate>`/`<repair-landing>`, followed by zero test-path changes through the rebased candidate, final candidate, implementation landing, and canonical main. Chronology also proves that test delta, the later one-path/one-hunk chronology-verifier implementation, all bound RED/GREEN digests and topologies, causal `9745938` composition, separate two-parent repair landing, unchanged lifecycle/node/assertion/archive contract, the one authorized implementation rebase, and the exact range-diff mapping plus single inserted verifier commit and sole authorized documentation `!` row.
- `corpus`: compare the immutable `v0.2.1` source bytes, SL-0 fixture, `_contract/` mirror, `VENDOR.json`, read-only pin, manifest rows, exact overall valid/invalid sets, exact 3+7+1 role partitions by `schema_target`, exact canonical `(case_id, expected_valid, expected_blocker_class)` rows, target-specific derived live-code-map keys and their eight-key union, and mirrored test-only oracle outcomes. For all eleven rows, require live validity to equal canonical `expected_valid` and mirrored-oracle blocker class to equal canonical `expected_blocker_class`. For the ten submission rows, compare live API/CLI/vector-runner results and submission CLI exits and separately require each invalid live result to contain its test-owned mapped agent-harness code. For `negative-unsupported-verdict`, compare direct route-verdict Draft 2020-12 validation and vector-runner results, require entered `schema_target` dispatch plus the pinned route-verdict schema digest/`$id` and `/route` enum error, and prove the row was absent from submission-CLI invocations. It rejects mutable-branch source lookup, missing source bytes, provenance-only claims, any 15-file/set/count mismatch, target flattening, or a wrong-schema rejection.
- `package`: build both wheel and sdist into its runner-owned artifact root; assert the direct wheel and direct sdist each contain `VENDOR.json` and exactly the same 15 `_contract/` authority files with no extra member; build a wheel from the sdist and require its exact mirror inventory/digests to equal the direct wheel; install both wheels into isolated targets; and exercise both installed package variants by `schema_target`. The package evidence must cite the exact immutable archive-inclusion node and the marker-driven migrated no-copy node, and must prove `pyproject.toml` plus `MANIFEST.in` are the two owned inclusion mechanisms. Run the three valid and seven invalid submissions through installed submission API/CLI/vector paths and compare live validity to `expected_valid`, mirrored test-oracle class to `expected_blocker_class`, separately mapped agent-harness codes for invalids, CLI exit codes, identity, manifest hash, and redaction output. Run `negative-unsupported-verdict` only through installed route-verdict schema validation and the installed vector runner, compare its `expected_valid=false` result, canonical `expected_blocker_class=policy_gap`, separately mapped live code, selected pinned schema identity, and `/route` enum error, and assert no submission-CLI record exists for it. Archive listing alone, a source-tree test, a wheel-only member check, or rejecting that row under the submission schema is insufficient.
- `compatibility`: this mode is defined by SL-1 but its first full invocation is forbidden until SL-2/B1 has written every owned disposition/doc surface. At SL-2/B2 it consumes the sealed SL-1 chronology/corpus/package outputs, emits the first complete EC-CONFORM-0…8 result matrix from direct observables, cross-checks v7 disposition, release handoff, CHANGELOG, and conformance docs against runtime/package identity, and rejects any claim that agent-harness owns the schemas/corpus, that governed-pipeline#128 is resolved, that canonical blocker classes are emitted, that advisory output is authoritative, or that publish/production merge enforcement occurred. External consumer alignment remains an explicit unproven assumption, not a pass inferred from prose.

### SL-2 — Lane B documentation, disposition, and whole-phase verification reducer

- **Scope**: Reconcile the already-merged pin with final installed bytes, record only machine-supported v7/release conclusions, and reduce direct producer, consumer, chronology, packaging, redaction, compatibility, and whole-suite evidence into IF-0-CONFORM-1.
- **Owned files**: `CHANGELOG.md`, `docs/outside-agent-conformance.md`, `docs/releases/outside-agent-release-handoff.md`, `specs/phase-plans-v7.md`
- **Interfaces provided**: `merged-contract v7 disposition`, `outside-agent release handoff`, `CONFORM documentation evidence`, `IF-0-CONFORM-1`
- **Interfaces consumed**: `CONFORM immutable test commit`, `canonical v0.2.1 provenance contract`, `closed per-channel redaction/projection contract`, `read-only public-export preservation contract`, `packaged canonical validator dialect`, `metadata-only serialization boundary`, `schema-target-dispatched canonical vector runner`, `executable CONFORM evidence verifier`, `SL-1 implementation commit identity`, `SL-1 pre-doc evidence bundle`
- **Parallel-safe**: no; this is the terminal documentation and verification reducer and depends directly on every producer.
- **Tasks**:
  - test: On the exact immutable `<candidate-commit>` used by A2, run the literal frozen B0 four-node argv before doc edits; require a nonzero exit with failure set exactly `CONFORM_SL2_STALE_DOC_NODE_IDS`, with each node collected once and no pass, skip, xfail, collection error, or additional node. This is the complete stale SL-2-owned disposition/handoff/doc/CHANGELOG partition; every other outside-agent node was already proven green by the disjoint A2 complement. Capture the raw argv/output and digests, do not add or amend tests in this lane, and do not invoke the full `compatibility` evidence mode yet. Run with `PHASE_LOOP_TDD_EXPECT_CONFORM` unset and prove the exact production marker is present, so none of the four is activation-skipped; emit structured B0 JUnit showing each exact node once, four assertion failures, and zero skipped/xfail/error cases.
  - impl: Update only SL-2 docs and the superseded v7 disposition with exact verified identities, explicit authority/consumer boundaries, unresolved assumptions, and no publish or enforcement overclaim. Explicitly reverse the current conformance-doc policy: agent-harness may carry the byte-locked, digest-enumerated `spec@v0.2.1` schemas, manifest, vectors, and test-only oracle as a consumer mirror, while raw provider payloads, secrets, local environment values, and submitter free text remain forbidden output and Consiliency/spec remains authority.
  - verify: Push exact `<final-candidate>`, then from a fresh clean process at that pushed SHA run every extracted command and the resolved frontmatter suite, invoke `compatibility` for the first time, and require its complete EC-CONFORM-0…8 matrix plus structured full outside-agent JUnit to have zero missing/mismatched claims and zero skipped expected nodes. This first B2 proof and its ordered pre-merge review are required before merge and do not yet produce IF-0-CONFORM-1. Land only as two-parent `<implementation-landing>`, then continue B2 from a new clean process at exact `M` by repeating every command, suite, matrix, and structured JUnit comparison; only that final B2 canonical-main replay may produce the gate. Stage those exact final artifacts by reference and require substantive artifact-grounded `AGREE` from the Terra early `can_probe` leg, Grok, GPT-5.6 Sol, Gemini, Opus read/write prover, and temporally last Fable president under the retained procedural `agent-harness#398`/`agent-harness#442` authority. Record completion through the existing phase-loop lifecycle. Withhold the gate or completion if any record is prose-only, self-reported, stale, missing, operationally unproved, inconsistent with the sealed SL-1 evidence, or carried forward from a superseded review attempt.

| Task ID | Type | Depends on | Files in scope | Tests owned | Test command |
|---|---|---|---|---|---|
| CONFORM-B0 | test | CONFORM-A2 | exact frozen stale-documentation nodes against the same immutable pre-doc candidate | none; consumes frozen SL-0 tests | Run `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest -q phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_documented_consumer_mirror_policy_allows_only_pinned_contract_bytes phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_v7_disposition_records_merged_contract_and_final_installed_behavior phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_release_handoff_records_metadata_only_package_contract_and_dispatch_boundary phase-loop-runtime/tests/test_outside_agent_release_surface.py::test_public_docs_point_to_handoff_without_claiming_release_dispatch`; require nonzero exit and failure set exactly `CONFORM_SL2_STALE_DOC_NODE_IDS`, with no pass, skip, xfail, collection error, or extra node; no `compatibility` mode invocation. The exact marker must activate all four with the environment unset; structured JUnit must show each once, four failures, and zero skipped/xfail/error cases. |
| CONFORM-B1 | impl | CONFORM-B0 | every SL-2 owned file and no source/test path | none | Record only values read from the pin, Git, archive inventories, installed submission-CLI results, installed route-schema/vector-runner results, and evidence verifier |
| CONFORM-B2 | verify | CONFORM-B1 | exact pushed final candidate, exact two-parent implementation landing, fresh canonical-main checkout `M`, and runner-owned evidence/build/review outputs | none; consumes frozen SL-0 tests | First, from a fresh exact-pushed-SHA process, run all `## Verification` commands and the frontmatter `automation.suite_command`; invoke full `compatibility` for the first time; require zero command, suite, EC-CONFORM-0…8 matrix, and expected-node JUnit failures/skips; obtain the ordered pre-merge review without producing the gate. Then land the two-parent merge and repeat every command plus the suite in a new clean process at exact `M` with no preloaded phase-loop modules; require chronology to prove the tests-only, repair, and implementation two-parent landings plus candidate/`M` identity, and require final JUnit to contain every `ALL_OUTSIDE_AGENT_NODE_IDS` member exactly once with zero skipped. Stage that exact evidence by reference; require usable substantive artifact-grounded `AGREE` from Terra, Grok, GPT-5.6 Sol, Gemini, Opus, and temporally last Fable; then produce IF-0-CONFORM-1 and use the existing phase-loop lifecycle to mark completion. |

The reducer must re-hash the schemas and manifest and compare them to `outside_agent_pin.py`; compare all 15 mirrored files to immutable producer bytes; and consume the executable verifier output rather than copying its claimed booleans. If bytes disagree, stop with a repairable `contract_bug` instead of hand-editing the pin, producer bytes, test expectations, or docs. Record the exact final implementation commit in the handoff so there is no pre-commit placeholder. Refresh wheel and sdist member inventories after packaging and include the sdist-derived-wheel result. Update the superseded v7 roadmap only to record OACORE-3/OAREAL-2 against the merged contract identity and final installed behavior; do not reactivate it or modify the active v10 roadmap.

Evidence is claim-scoped and fail-closed:

| Claim | Required direct observable | Rejected substitute |
|---|---|---|
| Panelled RED tests-only chronology | exact plan/test-panel digests; literal node-ID sets/counts and exact two-new/two-migrated stale membership; default marker-absent GREEN JUnit with only the exact new-node skip set and both migrated stale nodes executing unchanged legacy assertions; activated `PHASE_LOOP_TDD_EXPECT_CONFORM=1` RED JUnit with every expected node once, zero skipped/xfail/import/collection errors, exact failure/anchor set including the migrated stale nodes' new branches; two-parent test landing; immutable tests | a default-red test commit, xfail, import/collection failure, implementation-edited guard/selectors/nodeids/counts/anchors, prose “tests landed first,” or a checked box |
| Producer provenance and completeness | exact tagged Git bytes and exact 15-file/set equality across source, fixture, mirror, and VENDOR | two repo-authored metadata files agreeing or agent-harness#372 branch contents |
| Canonical consumer behavior | exact canonical manifest equality for every `(case_id, expected_valid, expected_blocker_class)` row; exact 3-valid/8-invalid equality partitioned as 3 valid submissions + 7 invalid submissions + 1 invalid route verdict; submission API/CLI/vector results and both installed-package variants for the 3+7 submission rows; route-verdict schema/vector results from both installed-package variants for the one route row | source-tree unit tests alone, archive listing alone, a homogeneous 3/8 CLI run, or a self-reported pass |
| Canonical valid acceptance | immutable candidate digest; all three valid submission API/CLI positive controls pass with exit 0 and no blocker; `M-CONFORM-1-RESTORE-ALLOWLIST` proves its exact source anchor matched once and makes all three reject | an unnamed catch-all mutation, a mutation with no anchor proof, or a green baseline without restoring the pre-agent-harness#371 allow-list |
| Fail-closed invalids | every `expected_valid=false` row blocks and its mirrored-oracle class equals canonical `expected_blocker_class`; each of seven invalid submissions blocks with a nonzero submission-CLI exit and its separately mapped live agent-harness code; `negative-unsupported-verdict` blocks with canonical class `policy_gap` plus its separately mapped live code under route-verdict schema/vector/package validation and has no submission-CLI invocation | aggregate blocked count, one exit-code rule applied to all eight, a live-code map represented as a canonical field, or “all matched” without per-row canonical validity/class and separate live-code details |
| Schema-target dispatch | immutable post-implementation candidate digest; entered dispatch anchor; manifest target `outside_agent_route_verdict.v0.1`; selected pinned route-verdict schema digest and `$id`; `/route` enum error for `accepted_for_merge`; and anchor-applied swap-schema and dispatch-bypass mutants killed by exact frozen commands | any pre-implementation dispatch claim, rejection by the submission schema, a generic `schema_validation_failed` code, trusting the manifest target, or a mutation summary without source-anchor application and raw command evidence |
| Redaction separation | immutable post-implementation candidate digest; exact input/output inventory equality; every path assigned to the seven-class table; both positional `submission_file` arguments classified `read_locator_only`; readable byte-derived and missing/unreadable zero-captured-byte digests; raw locator absence; entered forbidden and rejected-projection sentinel paths; normalized safe ref/digest controls; stable blocker pointers/messages; final stdout/file bytes; and exact frozen commands that kill each anchor-applied construction/serializer guard mutation | the superseded pre-amendment blanket-ban reading, any projection waiver outside the closed table, pre-implementation redaction claims, scanning only source, testing only one serializer or one command, path-derived unreadable-input digests, raw locator serialization, trusting `redaction_posture`, or mutation self-report |
| Post-implementation falsifier matrix | immutable candidate commit/source digests, exact frozen broad/A2/B0 node-set partition and argv digests, A2 broad-minus-stale baseline exit 0, exact B0 four-node stale failure set on that candidate, anchor-applied mutant exits nonzero, raw-log digests, and fresh exact-pushed-candidate and exact-canonical-main process reproduction with no preloaded modified verifier; marker-driven A2/B0; two-parent implementation landing; B2 pre-merge and canonical-main structured JUnit each proving every selected expected node once with zero skipped; broad `-k outside_agent` exit 0 appears only on the post-B1 final tree | verifier-authored booleans, aggregate kill counts, an anchor that did not match, a pre-doc broad-green claim, a filename/marker exclusion wider than the four frozen nodes, or evidence from a mutable working tree |
| v7 and release disposition | exact merged contract/package/validator/manifest identities plus built/installed evidence | agent-harness#377 landed, a hand-typed hash, or an unexecuted command list |
| Cross-repo compatibility | explicit bounded claims and negative tests against overclaim wording | claiming governed-pipeline alignment, publication, or merge enforcement from this repo’s green suite |

The final evidence JSON must enumerate EC-CONFORM-0 through EC-CONFORM-8 exactly once with status, proving command IDs, artifact digests, and direct-observable references. Missing or duplicate EC IDs, a pass with no direct observable, an operational command merely listed but not run, a prose/self-report source marked authoritative, or any cross-repo assumption promoted to fact makes `compatibility` mode nonzero and withholds IF-0-CONFORM-1. The first B2 candidate evidence remains pre-merge evidence; only B2's repeated proof from a fresh process at exact canonical-main head after the two-parent implementation landing may mark the matrix final or produce IF-0-CONFORM-1.

## Execution Policy

- work-unit defaults: work-unit=`lane_execute`, effort=`high`, unsupported=`inherit_default`, inherit-default=`true`
- SL-2: effort=`high`, work-unit=`phase_reducer`, unsupported=`inherit_default`, inherit-default=`true`, reason=`terminal documentation, release-evidence, and whole-phase verification synthesis`

For both B2 review waves, run the confined Codex `can_probe` leg first and stage its digest-bound
evidence before starting Grok, Sol, or Gemini; run Opus after the critics as the board's read/write
evidence-staging prover; start the separate Fable president only after the complete board finishes.
The exact-`M` wave repeats the entire early-prover/board/president sequence and carries forward no verdict.

## Execution Notes

- Before SL-0, the coordinator reviews the exact plan digest with the roadmap-required early Codex
  `can_probe` evidence, Grok/Sol/Gemini critics, the Opus read/write prover, and the separate Fable
  president. This plan/design wave may collect critics before Codex, but Codex evidence must be staged
  into the same exact bundle before Opus and Fable start; any contradiction invalidates affected critic
  verdicts and requires review of the updated bundle.

- The coordinator must review the exact plan digest with the roadmap-required six-output sequence before SL-0. The panels for `ddf6083004da1de66f58046adbc9945c3618f6b0795fc89a4676eb9b496862d1` and `dadf71fd3d1e757f76b0123c38c2e503d2aa5920b46ab6cc309dc0a5d1798fca` are superseded by this material repair. Both the repaired plan and the exact SL-0 test commit require usable, substantive, exact-digest AGREE outcomes from Terra, Fable, GPT-5.6 Sol, Gemini, Grok, and Opus; any unavailable, errored, empty, capped, refused, timed-out, digest-mismatched, or dissenting output blocks dispatch or promotion. Re-panel every materially changed digest and reconcile every dissent before execution.
- Keep the entire phase under exactly one coordinator-selected author vendor, recorded in chronology and unchanged from SL-0 through final reduction. This plan does not choose that rotation slot; the explicit CLI/operator executor assignment is authoritative. The exact-digest panels still require all four named seats, leaving at least three non-author vendor families. Do not enable runtime lane scheduling or cross-vendor work-unit rotation for this phase.
- Execution is strictly serial: SL-0 then SL-1 then SL-2. No prose `Parallel-safe` marker authorizes fanout, and no lane may share write ownership.
- Draft agent-harness#372 is evidence, not a merge source. Port reviewed behavior as needed, but do not preserve its commit ordering: all final tests, test-only source fixtures, mutation definitions/anchors, expected-case maps, command tables, and positive controls must be consolidated into SL-0 before any SL-1 path changes. Those frozen definitions do not constitute mutation evidence: all dispatch, schema-selection, redaction, and equivalent per-falsifier mutations execute only against immutable `<candidate-commit>` after SL-1. Packaged `_contract/` bytes belong only to SL-1. SL-1 and SL-2 must not modify SL-0-owned files.
- The tests-only change lands independently before the implementation branch: exact pushed/panelled `<test-candidate>` → two-parent `<test-landing>` on canonical main. The `Consiliency/agent-harness#470` repair then lands independently as exact pushed/panelled `<repair-candidate>` → two-parent `<repair-landing>`; its one three-test-file delta contains exactly the candidate-ancestry selector, per-mode invocation-directory, criterion-replay digest-comparison, chronology-forgery RED, safe sdist-extraction, and sealed-changelog-parent-rebind hunks. The implementation branch may take its single authorized rebase onto that repair landing, preserves the logical implementation series and every SL-0 byte except that exact reviewed six-hunk test delta, inserts only the one chronology-verifier hunk after the rebased `9745938` equivalent, freezes pushed `<candidate-commit>` for A2/B0, replays the four SL-2 paths under the exact changelog-parent rule, clears the exact-head board, and lands as two-parent `<implementation-landing>`. Squash, any further rebase, direct-push, single-parent, octopus, test-and-code-in-one-merge, or a first parent other than the recorded canonical-main tip fails chronology.
- Same-phase verifier self-attestation is forbidden. The coordinator checks the remote ref resolves to the expected pushed SHA and the checkout is clean before spawning each candidate process; checks canonical main separately in fresh processes at `<test-parent>`, `<test-landing>`, `<implementation-parent>`, and final `<canonical-main-head>`; and does not import `phase_loop_runtime`, `outside_agent_conform_evidence`, or any modified runner/verifier/parser module in the parent process. Final B2 replays all proof from exact canonical main and treats earlier verifier booleans only as untrusted inputs backed by raw bytes.
- The complete tracked phase-owned write set is the 22-pattern union of SL-0 through SL-2, SHA-256 `285e9ddb9d6f1996e3746f809cbe240053b6efe81504fc8e8c2913fd2660e427` over the sorted LF-joined patterns plus terminal LF. Its six-pattern test subset has SHA-256 `d6c401cf4c8ba48b51df434c463abba3278a4ecac892a418f977bc15367fc145` under the same framing. The exact migrated no-copy node singleton digest is `7755a4d356a9f285e8dd621e74dc14297901b1b5a44bd60198028809eecd73fe`; the exact archive-inclusion node singleton digest is `ccb295db192a045c224dc7f74819f8b83adb6db348d8d8b2d155a5c4f73005c0`, recomputed over its full UTF-8 nodeid plus one terminal LF. `outside_agent_pin.py`, `outside_agent_imports.py`, `outside_agent_provenance.py`, `conformance/__init__.py`, `README.md`, `specs/phase-plans-v10.md`, `ruff.toml`, release workflows, versions, lockfiles, environment examples, migrations, snapshots, governed-pipeline files, legacy `.codex/phase-loop/**`, and canonical `.phase-loop/**` state are read-only. The frozen tests assert exact preservation/importability of the entire current `conformance.__all__` surface. Runner-owned metadata under `.phase-loop/artifacts/CONFORM/**` is allowed but is not phase-owned product output.
- `jsonschema>=4,<5` is already declared in `phase-loop-runtime/pyproject.toml`; no dependency or lockfile change is expected. The only expected pyproject delta is package-data inclusion for `conformance/_contract/**`; the matching `MANIFEST.in` delta is separately required so the same exact mirror enters source distributions and their derived wheels. The exact `spec@v0.2.1` `consiliency_spec/outside_agent_router.py` bytes pass the current repo-root `ruff.toml` (`select=["F"]`, `ignore=["F841"]`); bind that result before the test freeze and re-run Ruff over the final mirrored path. No lint exclusion/config change is owned or allowed.
- Preserve the canonical corpus byte-for-byte from `spec@v0.2.1`. `VENDOR.json` must enumerate exactly the 15 mirrored authority files with source path, mirror path, raw-byte sha256, tag, and dereferenced commit; it cannot self-hash. Preserve exact overall 3-valid/8-invalid set equality and the 3+7+1 `schema_target` role partition. Do not hand-edit canonical bytes to make agent-harness tests pass.
- The one test-only normative oracle is the mirrored `consiliency_spec/outside_agent_router.py`; it may validate declared verdict/class metadata, but production must not import it or claim agent-harness emits canonical routing classes. agent-harness#379 remains the bounded follow-up.
- Documentation impact is explicit: `CHANGELOG.md`, `docs/outside-agent-conformance.md`, the release handoff, and the historical v7 disposition change. `docs/outside-agent-conformance.md` currently says agent-harness must not copy canonical schemas or raw vector bodies; SL-2 must reverse that policy narrowly for the byte-locked, digest-enumerated tagged consumer mirror while preserving the ban on submitter/provider raw content and preserving Consiliency/spec authority. `README.md` is `no_doc_delta` because it already links both outside-agent docs and states the advisory/authoritative split; the reducer verifies those references remain accurate.
- The frontmatter suite intentionally sets only `PYTHONPATH=phase-loop-runtime/src`. Before RED, the frozen tests must prove pytest's default prepend import mode collects every outside-agent node and imports `_outside_agent_canonical.py` without adding `phase-loop-runtime/tests` to `PYTHONPATH`; final B2 repeats that exact suite. The broad `-k outside_agent` exit-0 replay is likewise final-B2-only: pre-doc A2 must use the exact four-full-function-name negative `-k` selector and B0 must run the exact excluded four nodes red. If collection/import or the frozen node-set partition fails, stop and re-plan instead of changing frontmatter or selectors after the test freeze. During the marker-absent tests-only landing, ordinary/default CONFORM tests-only CI is GREEN and only the exact frozen new-production node set skips; migrated existing nodes execute legacy assertions and test-only fixture/oracle integrity nodes stay active. Explicit activation produces assertion RED with zero skips. After the exact marker lands, the same immutable nodes run new assertions by default; B0's four nodes are never activation-skipped.
- This is not a release-dispatch mutation. Do not tag, publish, dispatch a workflow, upgrade governed-pipeline, or claim production merge enforcement. Build artifacts are verification-only and runner-owned.
- This phase does not produce a visible avatar, browser-media, or UI render, so visual evidence is not applicable.
- Policy precedence is CLI/operator override, this phase plan, roadmap policy, `Dispatch Hints`, then registry defaults. Silent executor, model, or effort downgrade is forbidden; only the declared default inheritance is allowed.

## Spec Closeout Plan

- schema: `spec_delta_closeout.v1`
- decision: `canonical_spec_update`
- target surfaces: `phase-loop-runtime/src/phase_loop_runtime/conformance/_contract/**`, `specs/phase-plans-v7.md`, `docs/releases/outside-agent-release-handoff.md`
- evidence paths: `plans/phase-plan-v10-CONFORM.md`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_pin.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/outside_agent_conform_evidence.py`, `phase-loop-runtime/src/phase_loop_runtime/conformance/_contract/VENDOR.json`, `phase-loop-runtime/tests/fixtures/outside_agent_contract_v0_2_1/PROVENANCE.json`, `phase-loop-runtime/tests/test_outside_agent_canonical_corpus.py`, `phase-loop-runtime/tests/test_outside_agent_conform_evidence.py`, `phase-loop-runtime/tests/test_outside_agent_real_ci.py`, `phase-loop-runtime/tests/test_outside_agent_redaction_separation.py`, `phase-loop-runtime/tests/test_outside_agent_release_surface.py`, `docs/releases/outside-agent-release-handoff.md`, `specs/phase-plans-v7.md`
- redaction posture: `metadata_only`
- downstream handling: `mirror cutover` metadata only; it grants no write authority outside this repo

## Verification

- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.cli validate-roadmap specs/phase-plans-v10.md`
- `PYTHONPATH=phase-loop-runtime/src python3 skills-src/claude/claude-plan-phase/scripts/validate_plan_doc.py plans/phase-plan-v10-CONFORM.md`
SL-1/A2 runs this pre-doc selector; B2 repeats it on the final tree:

- `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k "outside_agent and not (test_documented_consumer_mirror_policy_allows_only_pinned_contract_bytes or test_v7_disposition_records_merged_contract_and_final_installed_behavior or test_release_handoff_records_metadata_only_package_contract_and_dispatch_boundary or test_public_docs_point_to_handoff_without_claiming_release_dispatch)"`

Only SL-2/B2, after B1, runs this broad selector green:

- `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k outside_agent`
- `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k "canonical_corpus or corpus_oracle or contract_imports or conform_evidence or real_ci or vectors"`
- `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k "redaction or real_output or advisory or authority_boundary"`
- `PYTHONPATH=phase-loop-runtime/src python3 -m pytest phase-loop-runtime/tests -q --collect-only -k outside_agent`
- `PYTHONPATH=phase-loop-runtime/src:phase-loop-runtime/tests python3 -m pytest phase-loop-runtime/tests -q -k "canonical_oracle_ruff or public_exports or frontmatter_import"`
- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.conformance.outside_agent_conform_evidence chronology`
- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.conformance.outside_agent_conform_evidence corpus`
- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.conformance.outside_agent_conform_evidence package`
- `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.conformance.outside_agent_conform_evidence compatibility`
- `ruff check phase-loop-runtime/src/phase_loop_runtime`
- `git diff --check`

The runner extracts fourteen commands and then the frontmatter suite. Phase timing is part of the command contract: SL-1/A2 runs only the frozen broad-minus-stale selector plus `chronology`, `corpus`, and `package` against the default runner-owned `.phase-loop/artifacts/CONFORM/` root; those modes seal their input/output digests and validate only evidence producible before SL-2, and they do not emit or gate on the full EC matrix. B0 runs only its exact four-node expected-failure argv, which is not a green Verification command. After SL-2/B1 writes the disposition, handoff inventory, conformance docs, and CHANGELOG, SL-2/B2 runs all fourteen green Verification commands and the frontmatter suite in a fresh process at exact pushed `<final-candidate>`, including the first broad `-k outside_agent` green replay with structured JUnit, then runs `compatibility` for the first time and emits the pre-merge EC-CONFORM-0…8 matrix. The ordered pre-merge review gates the two-parent implementation landing. B2 then repeats all fourteen commands and the suite in a fresh process at exact `M`, recomputes the matrix and final JUnit, stages those artifacts by reference for the ordered current-v10 six-output/Fable-president review, and only then produces IF-0-CONFORM-1. Corpus and package evidence must be grouped by `schema_target`: submission API/CLI/vector/package evidence and exit codes for the 3+7 submission rows; route-verdict schema/vector/package evidence, selected-schema identity, and dispatch anchors for the one route row; and exact union equality for the overall 3-valid/8-invalid corpus; exact per-row `expected_valid` and `expected_blocker_class` consumption; and the separately named complete eight-key live-code map. Any pre-B1 broad `-k outside_agent` green requirement, wider selector exclusion, premature full-compatibility invocation, route-row submission-CLI invocation, wrong-schema rejection, missing or stale chronology/panel/log/build/source evidence, nonzero green command or suite result, self-reported substitute, incomplete expected set, package mismatch, `submission_file` inventory/digest/path leak, redaction leak, compatibility overclaim, or carried-forward AGREE is a non-human verification blocker and withholds completion.

## Acceptance Criteria

- [ ] EC-CONFORM-0 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.conformance.outside_agent_conform_evidence chronology`, which independently recomputes exact plan/test/candidate/final identities and panels; one-author-vendor continuity; test-only Git chronology; exact pre-implementation RED argv, nonzero exit, failing node IDs, raw-log digest, and existing outer-seam anchors; the frozen equations `ALL_OUTSIDE_AGENT_NODE_IDS = A2_GREEN_NODE_IDS ⊎ CONFORM_SL2_STALE_DOC_NODE_IDS`, `DIALECT_MIGRATED ⊆ MIGRATED_EXISTING ⊆ PREEXISTING`, `MIGRATED_RED ⊆ MIGRATED_EXISTING`, `ACTIVATED_RED = NEW_PRODUCTION ⊎ MIGRATED_RED`, and `CONFORM_SL2_STALE_DOC_NODE_IDS = (STALE_DOC ∩ NEW_PRODUCTION) ⊎ (STALE_DOC ∩ MIGRATED_EXISTING)` with exact 2+2 membership; exact 42 dialect-migrated and 45 total migrated membership; exact one-time membership of the no-copy node in `PREEXISTING`, `MIGRATED_EXISTING`, `MIGRATED_RED`, `ACTIVATED_RED`, and `A2_GREEN`; exact one-time membership of the archive-inclusion node in `NEW_PRODUCTION`, `ACTIVATED_RED`, and `A2_GREEN`; the exact A2 four-full-function-name negative `-k` argv, complement node set, exit 0, mutation argv/exits, and entered candidate dispatch/schema/redaction anchors; the exact same-candidate B0 four-node argv with failure set equal to the stale set; the B1 docs-only diff; the first broad `-k outside_agent` exit-0 replay only on the final post-B1 B2 tree; and immutable tests after the test commit. It also proves the test-owned activation guard and literal node/count/anchor constants were frozen in `<test-candidate>`; default-main tests-only CI was GREEN with exactly the frozen new-node skip set while all 45 migrated nodes ran their unchanged legacy assertions; explicit activation ran every expected node once, selected canonical branches for all 42 dialect migrations, and failed exactly `CONFORM_ACTIVATED_RED_NODE_IDS` at intended anchors, including every genuinely missing migrated behavior and the new archive node, with zero skips/xfail/import/collection errors; migrated nodes preserved legacy/activated/automatic behavior without a same-version compatibility bypass; A2 ran the no-copy and archive nodes GREEN under marker activation; `<test-landing>`, `<repair-landing>`, and `<implementation-landing>` are exact two-parent landings; candidate and `M` proofs came from fresh exact-SHA processes with no preloaded modified verifier; and B2 final canonical-main JUnit contains every frozen expected outside-agent node exactly once with zero skipped. The ordered current-v10 review then binds those exact artifacts by reference, requires usable substantive artifact-grounded `AGREE` from Terra, Grok, GPT-5.6 Sol, Gemini, Opus, and temporally last Fable, rejects any finding, contradiction, carried-forward AGREE, or incomplete authorization, and records Fable as the sole binding president under retained procedural authority. The fixed seat order, six-output current-v10 result, and Fable binding are effective roadmap facts; `required_prover=false` remains a valid non-current parameterization fixture, and `PANEL_LEGS` plus non-review goldens remain unchanged.
For EC-CONFORM-0 under `Consiliency/agent-harness#470`, the repair landing is the third exact two-parent landing; "immutable tests after the test commit" means the one reviewed three-test-file delta containing exactly six authorized hunks at that landing and zero later test-path changes. The criterion additionally requires the one later production chronology hunk, all bound RED/GREEN pairs and recomputed topologies, unchanged lifecycle/node/assertion/archive evidence, and the one-rebase internally generated `git range-diff` proof with its sole authorized documentation `!` row.
- [ ] EC-CONFORM-1 — proven by `PYTHONPATH=phase-loop-runtime/src python3 -m phase_loop_runtime.conformance.outside_agent_conform_evidence corpus` plus the extracted canonical-focused pytest command; direct submission API, submission CLI, vector-runner, and installed-package results must show the exact three valid submission cases pass with exit 0 and no blocker, while named falsifier `M-CONFORM-1-RESTORE-ALLOWLIST` must prove its candidate source anchor matched exactly once and that restoring `{submission_schema_version, submission_kind, metadata, provenance_refs, evidence_refs}` as the complete pre-agent-harness#371 allow-list rejects all three; the unmutated three-case pass is the positive control.
- [ ] EC-CONFORM-2 — proven by the extracted redaction-focused pytest command and the B2-only `compatibility` evidence mode against immutable `<candidate-commit>`; the recursive input/output inventory must equal the declared seven-class channel inventory exactly with one class per channel, including both CLI positional `submission_file` scalars as the single `read_locator_only` channel identity; every forbidden, rejected, raw, free-text, unclassified, or sentinel-bearing submitter value must be absent from both byte-equivalent stdout and output-file JSON; normalized safe repository-relative refs, validated fixed-shape digests/immutable Git IDs, closed-vocabulary/boolean/numeric projections, and producer-derived closed-template structural diagnostics must retain per-class positive controls; `submission_file` must retain safe-relative and same-byte absolute read positives; traversal/free-text sentinel, missing, and unreadable negatives; readable byte-derived and unreadable zero-captured-byte digest assertions; raw path absence; both-command stdout/output-file parity; every channel must retain its negative control; and independent anchor-asserted raw-construction-guard and final-serializer-guard mutations must each make its exact test fail. The direct machine-observable failure is a named inventory, control, guard, or sink mismatch with nonzero verifier/test exit.
- [ ] EC-CONFORM-3 — proven by the same redaction-focused command and EC matrix against immutable `<candidate-commit>`; exact frozen commands must kill independently anchor-applied construction-enforcement and serialization-enforcement mutations while enforced positive controls retain `redaction_posture="metadata_only"`.
- [ ] EC-CONFORM-4 — proven by `corpus` evidence mode against immutable `<candidate-commit>`; an exact 15-file source/provenance/mirror set and byte digests must pass, while exact frozen missing, extra, duplicate, and fixed-VENDOR byte mutations apply at their anchors and fail with the exact path.
- [ ] EC-CONFORM-5 — proven by the extracted canonical/provenance pytest command against immutable `<candidate-commit>`; exact frozen version-preserving submission-schema and verdict-schema byte mutations must apply at their anchors and independently produce their existing typed digest-mismatch failures against the read-only `v0.2.1` pin.
- [ ] EC-CONFORM-6 — proven by `compatibility` evidence mode; v7 OACORE-3/OAREAL-2 dispositions must resolve to merged `Consiliency/spec@v0.2.1`, `b862f977897a7b87c4419680a3e83735d4ff07b0`, and final installed consumer evidence rather than agent-harness#377 or prose alone.
- [ ] EC-CONFORM-7 — proven by `package` and `compatibility` evidence modes plus `phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_no_copied_canonical_outside_agent_schema_or_vectors` and `phase-loop-runtime/tests/test_outside_agent_contract_drift.py::test_sdist_and_wheel_include_only_digest_enumerated_contract_mirror`; direct wheel, sdist, sdist-derived wheel, installed submission-CLI and route-schema/vector behavior, four identity fields, downstream boundary text, and no-publish/no-enforcement claims must all agree. All three archives contain exactly `VENDOR.json` plus the digest-enumerated 15-file mirror and no extra `_contract/` member; every non-enumerated canonical copy anywhere else under `src/` remains rejected.
- [ ] EC-CONFORM-8 — proven by `corpus` and `package` evidence modes plus the extracted canonical-focused pytest command; exact equality for all eleven canonical `(case_id, expected_valid, expected_blocker_class)` rows, the three-true/eight-false validity partition, the overall eight invalid IDs and separately named eight-key derived live-code map, the seven-invalid-submission/one-invalid-route-verdict partition by `schema_target`, per-case live validity and mirrored-oracle blocker-class equality, per-invalid mapped live codes, nonzero exits only where the submission CLI is applicable, and the three-valid-submission positive control are mandatory. For `negative-unsupported-verdict`, post-implementation evidence at immutable `<candidate-commit>` must prove entered dispatch for `outside_agent_route_verdict.v0.1`, selection of the pinned route-verdict schema digest/`$id`, the `/route` enum failure, and anchor-applied swap-schema/dispatch-bypass mutants killed by their exact frozen commands; a pre-implementation mechanism claim, submission-schema rejection, generic blocker alone, treating the live-code map as canonical, omitting either canonical oracle field, or a submission-CLI run cannot satisfy this criterion.
